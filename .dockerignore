# Dependency artifacts
**/node_modules
**/.next
**/dist
**/build

# VCS
.git
.gitignore
.github

# Test artifacts
coverage
playwright-report
test-results
**/coverage
**/test-results
**/*.test.ts
**/*.test.tsx
**/*.spec.ts
**/*.spec.tsx
**/test
tests

# Local env / secrets (never bake into images)
.env
.env.local
.env.*.local
**/.env
**/.env.local
!**/.env.example

# IDE / OS
.vscode
.idea
.DS_Store
*.swp

# Docs
README.md
CLAUDE.md
docs

# Logs
*.log
npm-debug.log*
yarn-debug.log*

# Docker overrides
docker-compose.override.yml
docker-compose.override.yml.example

# CI / planning
.planning

# Local Postgres bind-mount (commit 1aebb09) — never send to build context
data
data/**
/data
/data/**

# Orchestrator scratch — agent worktrees, planning, caches
.claude
.claude/**
