/**
 * wizard-stepper.test.tsx — Wave 0 RED stub for ONBD-07
 *
 * Covers: numbered 1-5 stepper states (active, completed, upcoming)
 * Filled by Plan 06-06.
 *
 * @wave 0 stub — intentionally skipped until Plan 06-06
 */
import { describe, it } from "vitest";

// TODO: Plan 06-06 — import WizardStepper from "@/components/onboarding/wizard-stepper"

describe.skip("WizardStepper — numbered 1-5 stepper states (ONBD-07)", () => {
  it.todo("renders 5 numbered steps");
  it.todo("active step is visually highlighted");
  it.todo("completed steps show a check indicator");
  it.todo("upcoming steps are visually dimmed");
  it.todo("step 1 is active on initial render");
  it.todo("clicking a completed step navigates back");
});
