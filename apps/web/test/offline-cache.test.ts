/**
 * offline-cache.test.ts — IndexedDB cache layer (PWAX-02)
 *
 * SCAFFOLD: implemented in plan 08-03
 *
 * These tests will be filled out when plan 08-03 (offline cache + queue)
 * creates apps/web/src/lib/offline-cache.ts. Until then, a single sentinel
 * assertion marks the gap so CI tracks the unfilled requirement.
 */
import { describe, it, expect } from "vitest";

describe("offline-cache (PWAX-02)", () => {
  it("SCAFFOLD — openBudgetDB and cache helpers implemented in plan 08-03", () => {
    // SCAFFOLD: implemented in plan 08-03
    expect("SCAFFOLD").toBe("IMPLEMENTED");
  });
});
