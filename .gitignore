node_modules/
.next/
dist/
coverage/
.env.local
# Production secrets live here (never committed). The tracked `.env` is the
# non-secret dev placeholder; prod uses `.env.prod` via `--env-file .env.prod`.
.env.prod
.env.*.local
*.log
.turbo/
playwright-report/
test-results/
.features-gen/

# CI/CD prep additions (2026-05-08)
.claude/
graphify-out/
**/tsconfig.tsbuildinfo

# Bind-mounted persistent data (postgres datadir)
/data/

# Ad-hoc UAT scratch — debug scripts, screenshots, MCP playwright dumps
.playwright-mcp/
/.uat-*.mjs
/*.png
/debug-*.ts
**/e2e/debug-*.spec.ts
apps/api/test/routes/debug-*.test.ts
.superpowers/
