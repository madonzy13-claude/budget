---
phase: quick-260612-g7v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/budgeting/spendings-grid/spendings-grid-client.tsx
  - apps/web/src/app/[locale]/(app)/budgets/[id]/layout.tsx
  - apps/web/src/app/global.css
  - apps/web/src/components/common/viewport-debug.tsx
  - apps/web/test/shell-safe-area.test.ts
  - apps/web/e2e/steps/bdp-shell-geometry.steps.ts
  - apps/web/e2e/features/bdp-tab-frame.feature
autonomous: false
requirements: [SHELL-R14]

must_haves:
  truths:
    - "Spendings tab (browser): NO black dead band between the grid scroller bottom edge and the Safari bottom bar — scroller box bottom sits at the visual-viewport bottom (delta < 4px)"
    - "Spendings tab (standalone): NO dead band below the grid box — entire screen is the scroll surface"
    - "Last transaction row, after full scroll, is reachable ABOVE the bottom bar (in-flow tail spacer is the only clearance)"
    - "Page-scrolling tabs (wallets, reserves, home) keep their bottom clearance — last rows still clear the Safari bar"
    - "SHELL-R14 marker visible in the served bundle + overlay reports page-wrapper padding-bottom, grid-box-bottom-vs-vv delta, spacer height"
  artifacts:
    - path: "apps/web/src/components/budgeting/spendings-grid/spendings-grid-client.tsx"
      provides: "Grid scroller box extends to vv bottom (no BOTTOM_CLEARANCE subtraction); tail spacer carries all clearance"
      contains: "vv - rect.top"
    - path: "apps/web/src/app/[locale]/(app)/budgets/[id]/layout.tsx"
      provides: "Spendings tab opts out of page-level bottom clearance (band/floor dead-strips under non-page-scrolling tab)"
    - path: "apps/web/src/app/global.css"
      provides: "Browser-mode page floor + standalone .pb-shell-safe scoped so they do NOT dead-band the inner-scrolling spendings tab"
    - path: "apps/web/src/components/common/viewport-debug.tsx"
      provides: "SHELL-R14 marker + page-wrapper pad, grid-box-bottom delta, spacer-height metrics"
    - path: "apps/web/test/shell-safe-area.test.ts"
      provides: "Updated Vitest source-guards (Round 4): box==vv math, per-tab clearance opt-out, SHELL-R14"
    - path: "apps/web/e2e/steps/bdp-shell-geometry.steps.ts"
      provides: "Tightened grid geometry thresholds: scroller bottom ≈ vv bottom (delta<4), last-row band"
  key_links:
    - from: "spendings-grid-client.tsx updateMaxH"
      to: "--grid-max-h"
      via: "vv.height - rect.top (no clearance subtraction)"
      pattern: "vh - rect\\.top"
    - from: "bdp/[id]/layout.tsx content wrapper"
      to: ".pb-shell-safe / browser floor"
      via: "per-tab opt-out so spendings inner scroller is not dead-banded"
      pattern: "pb-shell-safe"
---

<objective>
Round 4 iOS shell. Kill the black dead band between the spendings grid scroller's
bottom edge and the Safari bottom bar (browser ≈160px; standalone smaller). The
band is STACKED clearances applied around a NON-page-scrolling tab whose inner grid
scroller owns all vertical scroll.

Purpose: User: "no such black area — entire screen should be available for scrolling."
Output: Grid scroller box reaches the visual-viewport bottom; ALL clearance lives
inside the scrolled content as the in-flow tail spacer; page-level clearance no
longer dead-strips the spendings tab; page-scrolling tabs keep their clearance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
Round 3 record (the regression we are correcting):
@.planning/quick/260612-e82-ios-shell-round-3-sheet-x-button-alignme/260612-e82-SUMMARY.md

## VERIFIED CLEARANCE STACK (each source, file:line, applies-in-mode, keep/remove)

Screenshot is BROWSER mode (Safari URL bar visible). Spendings tab = inner grid
scroller owns vertical scroll; `<main data-shell-scroll>` page barely scrolls.

| #   | Source                                                                         | file:line                                                                 | Applies in         | Effect on spendings                                                                                            | Action                                                               |
| --- | ------------------------------------------------------------------------------ | ------------------------------------------------------------------------- | ------------------ | -------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| 1   | `main[data-shell-scroll]` browser floor `padding-bottom: calc(env+72px)`       | global.css:484-493                                                        | BROWSER            | Page padding below a near-static page → permanent dead strip under grid box (~72px)                            | **REMOVE for spendings** (keep for page-scrolling tabs)              |
| 2   | `BOTTOM_CLEARANCE=88` (browser) subtracted: `maxH = vv.height − rect.top − 88` | spendings-grid-client.tsx:317,325                                         | BROWSER+STANDALONE | iOS `vv.height` ALREADY excludes the bar → subtracting 88 again shrinks box ~88px above the bar → biggest band | **REMOVE subtraction**: `maxH = vv.height − rect.top`                |
| 3   | `.pb-shell-safe { padding-bottom: calc(env+64px) }` on BDP content wrapper     | global.css:518-522 (scope `@media standalone`), applied bdp layout.tsx:94 | STANDALONE only    | Page padding below near-static page → standalone dead band (smaller, matches user "PWA band smaller")          | **REMOVE for spendings** (keep for page-scrolling tabs)              |
| 4   | In-flow tail spacer `h-[calc(env+64px)]` INSIDE scroller                       | spendings-grid-client.tsx:576-580                                         | BOTH               | Correct: extends scrollHeight so last row scrolls above the bar                                                | **KEEP** (re-tune: browser ≈ bar overlap ~80px; standalone = env+64) |

Sum (browser) = #1 72 + #2 88 ≈ 160px dead band → matches decoded ~10.5% screen.
Sum (standalone) = #3 env+64 ≈ 98px below box → smaller band → matches user report.

Correct architecture: box bottom == vv bottom (delta<4px); ALL clearance in the
in-flow tail spacer (iOS honors in-flow block padding; ignores end-of-scroll
container padding — SHELL-R8..R10). Content may slide under the translucent bar
while scrolling — native + desired.

## INTERFACES (executor uses directly — no exploration)

spendings-grid-client.tsx ResizeObserver effect (current, lines ~297-347):

```ts
const BOTTOM_CLEARANCE = isStandalone ? safeBottom + 16 : 88; // line 317 — REMOVE usage
function updateMaxH() {
  const rect = el!.getBoundingClientRect();
  const vh = window.visualViewport
    ? window.visualViewport.height
    : window.innerHeight;
  const maxH = Math.max(160, Math.floor(vh - rect.top - BOTTOM_CLEARANCE)); // line 325 — DROP "- BOTTOM_CLEARANCE"
  el!.style.setProperty("--grid-max-h", maxH + "px");
}
```

Tail spacer (lines 576-580):

```tsx
<div
  aria-hidden
  data-grid-tail-spacer
  className="h-[calc(env(safe-area-inset-bottom,0px)+64px)] shrink-0 w-full pointer-events-none"
/>
```

bdp/[id]/layout.tsx content wrapper (line 94): `<div className="pb-shell-safe">` wraps
ActivePillTaskSlider + {children}. {children} on spendings = the inner-scrolling grid.

global.css browser floor (484-493) `main[data-shell-scroll] { padding-bottom: calc(env+72px) }`
and standalone `.pb-shell-safe` (518-522). Tests grep these blocks — see shell-safe-area.test.ts
`#4` (browser floor >=64px) and standalone `.pb-shell-safe ... +64px`.

viewport-debug.tsx: `BUILD_MARKER = "SHELL-R13"` (line 16); `probeGridMetrics()` (125-154)
returns gridTop/gridClientH/gridScrollH/gridScrollTop/gridMaxH/gridToEnd/gridLastRowGap.

e2e thresholds: bdp-shell-geometry.steps.ts "spendings grid last row clears the bottom
bar by at least 48 pixels" (248-314) — asserts `gap >= 48`. BUG: current gap is ~160
(too much) so it false-passes. New assertion must bound BOTH ends.

@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove stacked clearances — box reaches vv bottom; per-tab page-clearance opt-out; SHELL-R14 overlay</name>
  <files>apps/web/src/components/budgeting/spendings-grid/spendings-grid-client.tsx, apps/web/src/app/[locale]/(app)/budgets/[id]/layout.tsx, apps/web/src/app/global.css, apps/web/src/components/common/viewport-debug.tsx, apps/web/test/shell-safe-area.test.ts</files>
  <behavior>
    Vitest source-guards (new "Round 4 (SHELL-R14)" describe block, strip comments before matching code):
    - Grid maxH math no longer subtracts a bottom clearance: the updateMaxH formula
      matches `vh - rect.top` WITHOUT a trailing `- BOTTOM_CLEARANCE` (assert the
      maxH assignment line has no clearance term; `BOTTOM_CLEARANCE` may remain only
      if used for the tail spacer, but the box formula must be `floor(vh - rect.top)`).
    - In-flow tail spacer still present: `aria-hidden` + `env(safe-area-inset-bottom`
      (clearance moved entirely inside the scrolled content).
    - Spendings tab opts OUT of page-level bottom clearance: the spendings route's
      wrapper does NOT inherit `.pb-shell-safe` AND is excluded from the browser
      `main[data-shell-scroll]` floor — assert the chosen mechanism (e.g. a
      `data-no-page-clearance` attr consumed by a global.css `:has()`/descendant
      rule that zeroes padding, OR spendings page wrapper omits pb-shell-safe).
      Whatever mechanism: assert spendings cannot carry both the inner box AND the
      outer page padding.
    - Page-scrolling tabs retained: home page.tsx still has `pb-shell-safe`; browser
      floor rule still present for non-opted-out pages (assert global.css still
      contains the `main[data-shell-scroll]` browser `padding-bottom: calc(env...+ (6[4-9]|7[0-9]|80)px)` rule).
    - SHELL-R14: viewport-debug BUILD_MARKER is `SHELL-R14`, NOT R11/R12/R13.
    - Overlay new metrics present in source: page-wrapper computed padding-bottom,
      grid-box-bottom-vs-vv delta (gridBoxVvDelta), spacer height (gridSpacerH).
  </behavior>
  <action>
    1. spendings-grid-client.tsx updateMaxH (line ~325): change
       `Math.max(160, Math.floor(vh - rect.top - BOTTOM_CLEARANCE))` →
       `Math.max(160, Math.floor(vh - rect.top))`. The box now reaches vv bottom.
       BOTTOM_CLEARANCE (line 317) is no longer used for the box — either delete it
       or repurpose: set the tail spacer to carry the clearance. Per SHELL-R8..R10
       iOS ignores container end-of-scroll padding, so the IN-FLOW spacer is the only
       reliable clearance. Re-tune spacer: keep standalone `env(safe-area-inset-bottom)+64px`;
       for browser the bar overlaps content (~80px) so the last row must scroll above
       it — keep `env+64px` (env≈0 in browser so 64px breathing room sits the last row
       above the translucent bar; content passing under the bar mid-scroll is desired).
       Update the surrounding comment to state the box==vv-bottom architecture and that
       clearance lives only in the spacer.
    2. global.css + bdp layout.tsx — per-tab page-clearance opt-out so the inner-scrolling
       spendings tab is NOT wrapped in page-level bottom padding (sources #1 + #3 dead-strip
       it). Chosen mechanism (keep it simple + greppable):
       - Add `data-no-page-clearance` to the spendings page's top wrapper (the
         SpendingsGridClient renders a fragment — wrap the MonthNavigator+grid in a
         `<div data-no-page-clearance>` OR add the attr where the spendings route
         content mounts). Prefer adding it in spendings page.tsx around <SpendingsGridClient/>.
       - In global.css, zero the page clearances for that subtree in BOTH modes:
         `@media (display-mode: standalone) { [data-no-page-clearance], [data-no-page-clearance] .pb-shell-safe { padding-bottom: 0 } }`
         and in `@media (display-mode: browser)` add a rule so a `main[data-shell-scroll]`
         containing `[data-no-page-clearance]` drops the 72px floor (use
         `main[data-shell-scroll]:has([data-no-page-clearance]) { padding-bottom: 0 }`).
         NOTE the `.pb-shell-safe` wrapper in bdp layout.tsx wraps ActivePillTaskSlider +
         {children}; do NOT remove pb-shell-safe from the wrapper (the pill slider still
         needs it on page-scrolling tabs) — instead the inner `[data-no-page-clearance]`
         override neutralizes it only for the spendings subtree. Verify `:has()` is
         acceptable (iOS 16.4+ / target Safari supports it; the app already targets
         modern iOS). If `:has()` is risky, fall back to: spendings page.tsx wraps its
         content in its OWN wrapper that sets `padding-bottom: 0 !important` via a utility
         class and the BDP wrapper keeps pb-shell-safe (the inner zero wins on the subtree).
    3. Do NOT touch: standalone base `100lvh`, browser `[data-shell-root] 100dvh`,
       header sticky, BDP tab band offset, PTR, sticky column headers. Round 1-3 fixes
       must not regress.
    4. viewport-debug.tsx: bump `BUILD_MARKER` to `SHELL-R14`. Extend probeGridMetrics()
       to also return: `pageWrapPadBottom` (computed padding-bottom of the nearest
       `.pb-shell-safe` or `[data-no-page-clearance]` ancestor of the grid),
       `gridBoxVvDelta` (vvBottom − grid.getBoundingClientRect().bottom — should be ~0),
       `gridSpacerH` (offsetHeight of `[data-grid-tail-spacer]`). Render them in the
       [grid] overlay block.
    5. shell-safe-area.test.ts: add a `describe("Round 4 — box reaches vv bottom, no stacked clearance (SHELL-R14)")`
       block implementing the behavior assertions above. Update the SHELL marker guard
       test to forbid R13 and require R14 (mirror the existing R11/R12 forbid pattern).
       Keep ALL existing Round 1-3 assertions GREEN (do not delete prior guards).
  </action>
  <verify>
    <automated>cd apps/web && bun run test -- shell-safe-area 2>&1 | tail -25</automated>
  </verify>
  <done>
    Vitest shell-safe-area: ALL prior tests + new Round 4 block GREEN. Grid box math
    has no clearance subtraction; spendings subtree page-clearance zeroed in both modes;
    home keeps pb-shell-safe; browser floor rule still present for non-opted pages;
    BUILD_MARKER=SHELL-R14; overlay reports the 3 new metrics.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tighten e2e geometry thresholds; deploy; live multi-viewport proof</name>
  <files>apps/web/e2e/steps/bdp-shell-geometry.steps.ts, apps/web/e2e/features/bdp-tab-frame.feature</files>
  <action>
    1. bdp-shell-geometry.steps.ts — replace the loose "gap >= 48" assertion in
       "the spendings grid last row clears the bottom bar by at least 48 pixels"
       (lines 248-314) with a BOUNDED proof. Current bug: gap is ~160px (dead band) and
       still passes >=48. New step (rename to match feature): after scrolling grid to
       bottom, measure:
         - `boxVvDelta = vvBottom - grid.getBoundingClientRect().bottom`
           → assert `Math.abs(boxVvDelta) < 4` (scroller BOX bottom ≈ vv bottom; no dead band).
         - deepest interactive row bottom gap to vvBottom → assert it is within the
           clearance band: `gap >= 8 && gap <= 96` (last row sits above the bar via the
           in-flow spacer, NOT 160px away and NOT flush/hidden). Keep the honesty guard
           (scrollTop>50 only when scrollHeight>clientHeight+50).
       Keep logGeometry output for all metrics. Make the assertion messages explicit
       (state boxVvDelta + gap + viewport).
    2. Add a NEW Then step + scenario asserting the grid box bottom hugs vv bottom AT REST
       (before scrolling) too — `Then the spendings grid box bottom reaches the visual viewport bottom`
       asserting `Math.abs(vvBottom - grid box bottom) < 4` at multiple viewports.
       Wire it under `@tasks-geometry` in bdp-tab-frame.feature for the spendings tab.
    3. Do NOT weaken the page-scrolling-tab scenarios (#2/#4/#5 reserves/banner) — those
       must still pass (regression guard for kept clearance). If the existing
       "page bottom clearance is at least 48 pixels" still asserts >=48 on a page-scrolling
       tab, leave it.
    4. Deploy: `docker compose build --no-cache web && make restart-web`. Confirm
       `SHELL-R14` is in the served bundle (grep `/app/apps/web/.next/` chunks inside the
       web container). Verify `docker compose ps` shows web recently restarted + healthy.
    5. Run geometry e2e against the live dev host (timeboxed):
       `PLAYWRIGHT_BASE_URL=https://budget-dev.madonzy.com` (canonical test URL) run
       `--grep @tasks-geometry` from apps/web. All scenarios GREEN across
       geom-320/390/430/1280. If a run exceeds the timebox, report partial results +
       which viewport, do not hang.
  </action>
  <verify>
    <automated>cd apps/web && PLAYWRIGHT_BASE_URL=https://budget-dev.madonzy.com bun run test:e2e -- --grep @tasks-geometry 2>&1 | tail -30</automated>
  </verify>
  <done>
    @tasks-geometry GREEN on geom-320/390/430/1280: spendings grid box bottom delta
    to vv bottom < 4px at rest AND after scroll; last row gap within [8,96]px band;
    page-scrolling-tab clearance scenarios still pass. SHELL-R14 confirmed in served
    bundle; web container healthy.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Removed the stacked bottom clearances: grid scroller box now extends to the visual-viewport bottom; all clearance moved inside the scrolled content (in-flow tail spacer); spendings tab opted out of page-level clearance (browser 72px floor + standalone .pb-shell-safe) while page-scrolling tabs keep theirs. SHELL-R14 overlay reports page-wrapper padding, grid-box-vs-vv delta, and spacer height.</what-built>
  <how-to-verify>
    On the iOS device:
    1. Hard-refresh https://budget-dev.madonzy.com in Safari (browser mode). Add
       `?vpdbg=1`. Confirm the overlay shows `SHELL-R14` (NOT R13 — else stale cache;
       reinstall/clear). Check `gridBoxVvDelta ≈ 0` and `pageWrapPadBottom = 0` on spendings.
    2. Spendings tab, browser: confirm there is NO black dead band between the grid's
       bottom edge and the Safari bottom bar — the scroller reaches the bar. Scroll the
       grid to the bottom: the last transaction row is reachable and sits just above the
       bar (content may slide under the translucent bar while scrolling — that's expected).
    3. Add the PWA to the home screen / open standalone. Repeat: no dead band below the
       grid; entire screen scrollable; last row clears the home indicator.
    4. Regression sweep: Wallets + Home tabs still show their bottom clearance (last rows
       clear the bar/indicator, not flush). Pinned header, sticky column headers, PTR,
       and horizontal column scroll all still work. Sheets (transaction/category) still
       open with X aligned (round 3).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the residual band (include a ?vpdbg=1 screenshot with the SHELL-R14 metrics — gridBoxVvDelta, pageWrapPadBottom, gridSpacerH — per mode)</resume-signal>
</task>

</tasks>

<verification>
- Vitest `shell-safe-area`: all Round 1-3 guards + new Round 4 block GREEN.
- Playwright `@tasks-geometry`: GREEN geom-320/390/430/1280; grid box bottom delta to
  vv bottom < 4px at rest and after scroll; last-row gap in [8,96]; page-scrolling-tab
  clearance scenarios unchanged + passing.
- Served bundle contains `SHELL-R14`; web container healthy after `--no-cache` rebuild.
- Device checkpoint approved for BOTH browser and standalone.
</verification>

<success_criteria>

- Browser spendings: zero black dead band between grid scroller bottom and Safari bar
  (box bottom ≈ vv bottom).
- Standalone spendings: zero dead band below the grid box; entire screen scrollable.
- Last row reachable above the bar via the in-flow tail spacer (only clearance source).
- Page-scrolling tabs (wallets/home/reserves) keep working clearance — no regression.
- Rounds 1-3 (sheet X, pinned header, sticky column headers, PTR, horizontal scroll)
  intact.
  </success_criteria>

<output>
After completion, create `.planning/quick/260612-g7v-spendings-dead-band-remove-stacked-botto/260612-g7v-SUMMARY.md`
</output>
