---
quick_id: 260613-v1p
type: execute
mode: quick
autonomous: false
files_modified:
  # Frontend render
  - apps/web/src/lib/category-colors.ts
  - apps/web/src/lib/category-colors.test.ts
  - apps/web/src/components/budgeting/category-slider.tsx
  - apps/web/src/components/budgeting/spendings-grid/category-column.tsx
  - apps/web/src/components/budgeting/reserves-tab/reserves-table-row.tsx
  - apps/web/src/hooks/use-reserves-summary.ts
  - apps/web/test/components/budgeting/category-slider.test.tsx
  - apps/web/test/components/spendings-grid/category-column.test.tsx
  - apps/web/test/components/reserves-table-row.test.tsx
  # i18n
  - apps/web/messages/en.json
  - apps/web/messages/pl.json
  - apps/web/messages/uk.json
  # Persistence (NEW — color was never stored)
  - drizzle/00XX_category_color_key.sql
  - packages/budgeting/src/adapters/persistence/categories-schema.ts
  - packages/budgeting/src/adapters/persistence/category-repo.ts
  - packages/budgeting/src/adapters/persistence/categories-repo.ts
  - packages/budgeting/src/domain/category.ts
  - packages/budgeting/src/application/create-category.ts
  - packages/budgeting/src/application/rename-category.ts
  - packages/budgeting/src/application/get-spendings-summary.ts
  - packages/budgeting/src/application/get-reserves-summary.ts
  - packages/budgeting/src/application/reserves-summary-builder.ts
  - packages/budgeting/src/ports/categories-repo.ts
  - packages/budgeting/src/contracts/api.ts
  - apps/api/src/routes/categories.ts
  - apps/api/test/routes/categories.test.ts
  - apps/api/test/routes/reserves.test.ts

must_haves:
  truths:
    - "Category color picked in the add/edit popup PERSISTS (survives reload)."
    - "A category with a color shows a 4px left accent bar on its spendings column card."
    - "The same category shows a 4px left accent bar on its reserves row."
    - "colorKey null → NO bar on either surface (looks like today)."
    - "The icon picker is GONE from the popup (no icon-option-* testids render)."
    - "iconKey is no longer written from the form (submit omits/nulls it)."
  artifacts:
    - path: "apps/web/src/lib/category-colors.ts"
      provides: "Single source of truth key→hex map + hexForColorKey() helper"
      exports: ["CATEGORY_COLORS", "hexForColorKey"]
    - path: "drizzle/00XX_category_color_key.sql"
      provides: "ADD COLUMN color_key text (nullable) on budgeting.categories"
      contains: "ADD COLUMN"
  key_links:
    - from: "category-slider.tsx color picker"
      to: "category-colors.ts"
      via: "import CATEGORY_COLORS"
    - from: "POST/PATCH /categories"
      to: "budgeting.categories.color_key"
      via: "createCategorySchema.colorKey → use-case → INSERT/UPDATE"
    - from: "get-reserves-summary → builder → use-reserves-summary row"
      to: "reserves-table-row accent bar"
      via: "colorKey threaded through ReservesSummaryRow"
    - from: "get-spendings-summary colorKey"
      to: "category-column accent bar"
      via: "DTO colorKey (real, not always-null)"
---

<objective>
Two coupled category changes, verified against current code (file:line).

A. REMOVE the category icon picker from the add/edit popup.
B. RENDER the category color as a 4px left accent bar on spendings column cards AND reserves rows.

Purpose: icon adds no value; color becomes a real, native categorization cue.
Output: persisted color, shared color map, two accent-bar renderers, icon picker gone, i18n synced.

⚠ SCOPE EXPANSION — verified, supersedes the brief's assumptions:
The brief assumed iconKey/colorKey are persisted nullable DB columns and only the
reserves DTO lacked colorKey. NOT TRUE. Verified:

- `budgeting.categories` has NO color_key / icon_key column. Migration 0009
  (drizzle/0009_breezy_karen_page.sql) created the table without them; 0012 only
  dropped `scope`; no later ADD COLUMN exists anywhere in drizzle/\*.sql.
- `createCategorySchema` (packages/budgeting/src/contracts/api.ts:93) = `{ name, parentId }`.
  The slider POSTs `colorKey`/`iconKey` but zod safeParse DROPS them — never reach the use-case.
- PATCH /categories/:id (apps/api/src/routes/categories.ts:228) reads ONLY `body.name`.
- create-category.ts INSERT (category-repo.ts:50-60) writes only name/parentId/sort_index.
- get-spendings-summary.ts:185 reads `(c as any).colorKey ?? null` — ALWAYS null today
  (no repo SELECTs the column; the cast is dead).
- There is NO iconKey column to "keep" — removing the icon picker is purely frontend.
  CONSEQUENCE: to show ANY bar, color must FIRST be persisted end-to-end (migration +
  schema + domain + create/rename use-cases + BOTH repo SELECTs + API zod schema + both DTOs).
  This is bigger than 3 tasks; the brief's "reserves DTO is the only gap" verdict is wrong —
  spendings colorKey is ALSO non-functional. Plan reflects verified reality below.
  iconKey: do NOT add a column (none exists, none needed). Just stop sending it.
  </objective>

<context>
@.planning/STATE.md
@./CLAUDE.md
@./DESIGN.md

<verified_facts>
Color column: ABSENT (drizzle/0009 created categories sans color; 0012 dropped scope only).
Create schema: createCategorySchema = { name, parentId } (contracts/api.ts:93-96).
Create route: categories.ts:33-52 spreads parsed.data (no colorKey) into createCategory.
Rename route: categories.ts:220-240 reads only body.name.
Create use-case: create-category.ts persists name/parentId only.
Domain entity: category.ts constructor has name (line 13), no color field.
INSERT: category-repo.ts:50-60 (name, parent_id, sort_index, audit{name,parentId}).
Spendings repo SELECT: category-repo.ts:106/141/149 — selects name/archived/sort_index, NOT color.
Reserves repo SELECT: categories-repo.ts:95 + :136 — `SELECT id, name, reserve_excluded, archived_at, sort_index`. NO color.
Reserves port row: CategoryRow (ports/categories-repo.ts) = { id, name, reserveExcluded, archivedAt, sortIndex? }. NO colorKey.
Reserves DTO: ReservesSummaryRow (use-reserves-summary.ts:29 + get-reserves-summary.ts:25) has NO colorKey. CONFIRMED GAP.
Spendings DTO: SpendingsSummaryCategoryDTO has colorKey (category-column.tsx:24) but it's fed by (c as any).colorKey = always null.
PRESET_COLORS: category-slider.tsx:68-76 (8 keys, exact hexes below).
PRESET_ICONS: category-slider.tsx:57-66 (8 lucide). iconKey zod: :119. defaults: :174 + :206. payloads: :244 + :283.
Icon picker UI: category-slider.tsx ~:484-513. Color picker UI: ~:518-545.
i18n keys (en/pl/uk): grid.catSlider.field.icon (REMOVE), grid.catSlider.field.color (KEEP).
</verified_facts>

<color_map_design>
Shared file `apps/web/src/lib/category-colors.ts`, single source of truth:

export const CATEGORY_COLORS = [
{ key: "yellow", hex: "#F0B90B" },
{ key: "green", hex: "#26A69A" },
{ key: "blue", hex: "#4A90D9" },
{ key: "red", hex: "#EF5350" },
{ key: "orange", hex: "#FF8F00" },
{ key: "purple", hex: "#7C4DFF" },
{ key: "pink", hex: "#EC407A" },
{ key: "gray", hex: "#78909C" },
] as const;

export type CategoryColorKey = (typeof CATEGORY_COLORS)[number]["key"];

const HEX = Object.fromEntries(CATEGORY_COLORS.map((c) => [c.key, c.hex]));

/\*_ key→hex, or null for null/unknown key (→ no bar). _/
export function hexForColorKey(key: string | null | undefined): string | null {
if (!key) return null;
return HEX[key] ?? null;
}

Slider imports CATEGORY_COLORS for the swatch grid (swatch behavior IDENTICAL).
Both renderers import hexForColorKey. Hexes must match the old PRESET_COLORS exactly (they do).
</color_map_design>

<bar_placement>
SPENDINGS (category-column.tsx):
Card root = `w-max min-w-[140px] ... flex flex-col rounded-xl bg-[var(--surface-card-dark)] overflow-clip`.
Inside it: a sticky top backdrop (z-5) then a sticky header band (z-10, rounded-t-xl, border-x/border-t).
Bar = FIRST child of the card root, absolutely positioned:
`absolute left-0 top-0 bottom-0 w-1 z-0 pointer-events-none rounded-l-xl`
style={{ background: hex }}, rendered ONLY when hexForColorKey(summary.colorKey) != null.

- z-0 keeps it BELOW the sticky band (z-10) and backdrop (z-5) → does not break the top mask.
- overflow-clip on the card clips the bar to rounded-xl corners (rounded-l-xl belt-and-suspenders).
- Card root must get `relative` (it has none today) for the absolute bar. Verify `relative` doesn't
  disturb the sticky children — sticky resolves against the scroll container, not this element, so safe.
- w-1 (4px) sits on the card EDGE, outside the w-0/min-w-full content columns → steals ZERO name width.
- Does NOT touch drag/horizontal-scroll/sticky (it's pointer-events-none, z-0, no layout flow).
- Archived columns: HIDE the bar (card already dims via opacity 0.5 + #14181D surface). Decision:
  `!archived && hex && <bar/>` — muted archived look stays clean, no colored accent on dead columns.

RESERVES (reserves-table-row.tsx):
Outer wrapper `relative` holds the absolute swipe button (right) + the inner draggable row.
Inner row (the `rowClass` div) = `flex min-h-[48px] ... rounded-[var(--radius-md)] px-3 bg-[var(--surface-card-dark)]`.
Bar = FIRST child INSIDE the inner draggable row (so it travels with swipe/drag transform):
`absolute left-0 top-0 bottom-0 w-1 z-0 pointer-events-none rounded-l-[var(--radius-md)]`
style={{ background: hex }}, rendered ONLY when hexForColorKey(row.colorKey) != null.

- Inner row needs `relative` + `overflow-hidden` (to clip bar to rounded-md). It currently has neither;
  add both to rowClass. overflow-hidden is safe — InlineEditCell/Input render inside, no overflow content.
- Swipe button is absolute on the WRAPPER (right-2), bar is absolute on the INNER row (left-0) → no clash.
- pointer-events-none → swipe gesture + drag handle unaffected.
- Excluded rows: render the bar too? Decision: render the bar (muted-but-present is fine; the row already
  recolors text to #7A7C7F). The bar reads as a category cue independent of exclusion. STATE this.
  (If UAT dislikes it, gating on `!isExcluded` is trivial.)
  </bar_placement>

<design_note>
DESIGN.md = Binance dark, single yellow accent. The per-category color bar is a DELIBERATE
multi-color EXCEPTION scoped to categorization only (4px, card edge). The yellow `--primary`
stays the sole accent for active/CTA/interactive state. The bar never competes with primary:
it's decorative, secondary, and the category NAME remains the primary identifier (color-alone
is never the only signal → a11y-safe; no contrast requirement on a 4px decorative strip).
</design_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist category color end-to-end (migration → schema → domain → use-cases → both repos → API zod → both DTOs)</name>
  <files>
    drizzle/00XX_category_color_key.sql,
    packages/budgeting/src/adapters/persistence/categories-schema.ts,
    packages/budgeting/src/domain/category.ts,
    packages/budgeting/src/application/create-category.ts,
    packages/budgeting/src/application/rename-category.ts,
    packages/budgeting/src/adapters/persistence/category-repo.ts,
    packages/budgeting/src/adapters/persistence/categories-repo.ts,
    packages/budgeting/src/ports/categories-repo.ts,
    packages/budgeting/src/application/get-spendings-summary.ts,
    packages/budgeting/src/application/get-reserves-summary.ts,
    packages/budgeting/src/application/reserves-summary-builder.ts,
    packages/budgeting/src/contracts/api.ts,
    apps/api/src/routes/categories.ts,
    apps/api/test/routes/categories.test.ts,
    apps/api/test/routes/reserves.test.ts
  </files>
  <behavior>
    - RED first (apps/api/test/routes/categories.test.ts): POST /categories with {name, colorKey:"blue"}
      → GET/spendings summary returns colorKey:"blue" for that category. PATCH with {name, colorKey:"red"}
      → returns "red". POST with no colorKey → null. Unknown colorKey "mauve" → 422 (zod enum) OR stored
      as-is then hexForColorKey→no bar (choose zod enum = stricter; enumerate the 8 keys + nullable).
    - RED (apps/api/test/routes/reserves.test.ts): GET /budgets/:id/reserves → each active+excluded row
      carries colorKey from the category. Tenant-scoped (RLS): a second tenant's category color never leaks.
    - GREEN: implement below.
  </behavior>
  <action>
    Migration (use drizzle-kit if generating, else hand-write into repo-root drizzle/ — real dir per
    project_worktree_base_bug memory): `ALTER TABLE "budgeting"."categories" ADD COLUMN IF NOT EXISTS
    "color_key" text;` (nullable, no default, no destructive change). Name it next in sequence
    (00XX after the current max). Do NOT add icon_key (none needed).

    categories-schema.ts: add `colorKey: text("color_key")` to the `categories` table def.

    domain category.ts: add `public colorKey: string | null = null` to constructor (after name). Add a
    setter or include in rename path so edits propagate. Keep domain Drizzle-free (it is).

    contracts/api.ts createCategorySchema: add `colorKey: z.enum(["yellow","green","blue","red","orange",
    "purple","pink","gray"]).nullable().optional()`. (Mirror the 8 keys; nullable+optional so omit = null.)
    Add an UPDATE schema if rename uses one, or accept colorKey alongside name in the PATCH handler.

    apps/api/src/routes/categories.ts: POST already spreads parsed.data → colorKey flows once schema allows
    it. PATCH (:228): also read body.colorKey (validate via the enum) and pass to renameCategory.

    create-category.ts + rename-category.ts: thread colorKey into the domain entity + persistence call.

    category-repo.ts (spendings/singular): INSERT — add color_key column + value. UPDATE (rename) — set
    color_key. SELECT lists (lines ~106/141/149) — add `color_key` to the projection so spendings reads a
    REAL value (kills the dead `(c as any).colorKey` — make it `c.colorKey`). Audit before/after: include colorKey.

    categories-repo.ts (reserves/plural): both SELECTs (:95 findById, :136 list) — add `color_key`; map
    `colorKey: row.color_key ?? null` into the returned CategoryRow.

    ports/categories-repo.ts: add `colorKey: string | null` to CategoryRow.

    reserves-summary-builder.ts: ReservesSummaryCategory gains colorKey; rowFor + excluded mapper emit
    colorKey; get-reserves-summary.ts maps `colorKey: c.colorKey` in categories.map(...) and adds colorKey
    to ReservesSummaryRow interface.

    get-spendings-summary.ts: change `(c as any).colorKey ?? null` → `c.colorKey ?? null` once the repo
    returns it (keep iconKey as `(c as any).iconKey ?? null` → stays null; harmless, no column).

  </action>
  <verify>
    <automated>make test 2>&1 | tail -30  # bun:test — run categories + reserves route tests + builder; expect green. (heed make test infra debt memory — verify the targeted suites, not the global red count)</automated>
  </verify>
  <done>color_key column exists; POST/PATCH persist + return it; spendings + reserves DTOs both carry a REAL colorKey; RLS-safe; targeted tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared color map + accent bars (spendings + reserves) + reserves hook DTO field</name>
  <files>
    apps/web/src/lib/category-colors.ts,
    apps/web/src/lib/category-colors.test.ts,
    apps/web/src/hooks/use-reserves-summary.ts,
    apps/web/src/components/budgeting/spendings-grid/category-column.tsx,
    apps/web/src/components/budgeting/reserves-tab/reserves-table-row.tsx,
    apps/web/test/components/spendings-grid/category-column.test.tsx,
    apps/web/test/components/reserves-table-row.test.tsx
  </files>
  <behavior>
    - RED (category-colors.test.ts): hexForColorKey("blue")==="#4A90D9"; hexForColorKey(null)===null;
      hexForColorKey("mauve")===null; CATEGORY_COLORS has 8 entries with exact hexes from verified_facts.
    - RED (category-column.test.tsx): summary.colorKey="green" → a w-1 bar element with backgroundColor
      rgb(38,166,154)/#26A69A present; colorKey=null → NO bar element; archived column → NO bar even with color.
    - RED (reserves-table-row.test.tsx): row.colorKey="red" → w-1 bar #EF5350 inside the draggable row;
      colorKey=null → NO bar. Add a data-testid (e.g. `category-accent-bar-{id}`) on the bar for query.
  </behavior>
  <action>
    Create category-colors.ts per <color_map_design>. use-reserves-summary.ts: add
    `colorKey: string | null` to ReservesSummaryRow interface (hook DTO) — backend now sends it (Task 1).
    category-column.tsx: add `relative` to the card root className; render the spendings bar per
    <bar_placement> (first child, z-0, gated on `!archived && hexForColorKey(summary.colorKey)`).
    reserves-table-row.tsx: add `relative overflow-hidden` to rowClass; render the reserves bar per
    <bar_placement> (first child of the inner draggable row, gated on hexForColorKey(row.colorKey)).
    Import hexForColorKey in both renderers. Bar testid `category-accent-bar-{categoryId}`.
  </action>
  <verify>
    <automated>cd apps/web && bun run test -- category-colors category-column reserves-table-row 2>&1 | tail -25</automated>
  </verify>
  <done>shared map unit-tested; bar present with correct hex when colorKey set, absent when null, absent on archived spendings columns; no drag/scroll/sticky/swipe regression in component tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove icon picker + stop writing iconKey + i18n cleanup</name>
  <files>
    apps/web/src/components/budgeting/category-slider.tsx,
    apps/web/test/components/budgeting/category-slider.test.tsx,
    apps/web/messages/en.json,
    apps/web/messages/pl.json,
    apps/web/messages/uk.json
  </files>
  <behavior>
    - RED (category-slider.test.tsx): opening the slider renders NO `icon-option-*` testids; the color
      picker still renders all 8 `color-option-*`; the color picker imports from category-colors.ts
      (swatch behavior identical — selecting a swatch sets colorKey, re-click clears to null).
      Submit payload (create + edit) includes colorKey, and does NOT include iconKey (or sends iconKey:null).
  </behavior>
  <action>
    category-slider.tsx: delete the icon picker FormField (~:484-513) + PRESET_ICONS (:57-66) + the now-unused
    lucide imports (ShoppingCart, Home, Car, Utensils, Heart, Briefcase, Music, BookOpen — keep Loader2).
    Remove iconKey from: zod schema (:119), CategorySliderProps.initial (keep field optional or drop — drop
    the form field), defaultValues (:174), reset() (:206), and BOTH submit payloads (:244 create, :283 edit)
    → omit iconKey (or send null). Replace PRESET_COLORS local const with import { CATEGORY_COLORS } from
    "@/lib/category-colors" and map over it in the color picker (swatch markup UNCHANGED).
    i18n: remove `grid.catSlider.field.icon` from en/pl/uk (KEEP grid.catSlider.field.color). Remove any
    icon-option labels if present (none found — icons are glyph-only).
  </action>
  <verify>
    <automated>cd apps/web && bun run test -- category-slider 2>&1 | tail -20 && bun run check:i18n 2>&1 | tail -15</automated>
  </verify>
  <done>icon picker + PRESET_ICONS + unused lucide imports gone; iconKey not written; color picker uses shared map; en/pl/uk in sync (check:i18n green); slider tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Persisted category color + 4px left accent bar on spendings columns AND reserves rows; icon picker removed.
    Claude ran: make test (backend), apps/web bun run test (component), check:i18n, then rebuilt + restarted
    web (+ api + worker, since the reserves/categories DTO is a backend change) and verified the SERVED bundle.
  </what-built>
  <how-to-verify>
    Pre-checkpoint, Claude MUST do (per memories — verify yourself first; always rebuild web; docker on):
      1. Docker up. `make test` + `make ci-gate` green (ci-gate = tenant-leak, must stay green — colorKey is tenant-scoped).
      2. `cd apps/web && bun run test` green; `bun run check:i18n` green.
      3. Run the DB migration against dev (color_key column).
      4. Rebuild: `docker compose build web api worker` then `make restart-web restart-api restart-worker`
         (api+worker because reserves/categories summary DTO changed). Verify `docker compose ps` shows
         recent restart + healthy; confirm SERVED web bundle is fresh (memory: build cache ships stale images
         — if a change won't appear, --no-cache rebuild + grep the served .next).
      5. Playwright/BDD smoke for the live flow if a quick reuse exists.
    Then human verifies on https://budget-dev.madonzy.com (canonical test URL):
      1. Open add-category popup → icon picker is GONE; color swatches present.
      2. Create a category, pick (e.g.) blue → save. Reload. The blue persists (open edit → blue still selected).
      3. Spendings grid: that category's column card shows a 4px BLUE bar on its left edge, inside the rounded
         corners, NOT overlapping the sticky header mask; horizontal scroll + column drag still smooth; name full width.
      4. Reserves tab: same category row shows the 4px blue left bar; swipe-left still reveals Exclude/Restore; drag works.
      5. A colorless category → NO bar on either surface (neutral, like before). Archived spendings column → no bar.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- make test + make ci-gate green (Docker on). cd apps/web && bun run test green. bun run check:i18n green.
- DB has color_key column; POST/PATCH /categories persist + return it; both summary DTOs carry real colorKey.
- Bar renders with correct hex when colorKey set, absent when null/archived(spendings), on both surfaces.
- Icon picker + PRESET_ICONS + unused lucide imports removed; iconKey no longer written.
- No regression: spendings drag/h-scroll/sticky-mask/name-width; reserves swipe+drag; archived styling; ci-gate.
</verification>

<success_criteria>
Color persists end-to-end and shows as a 4px left accent bar on spendings columns + reserves rows (null→no bar,
archived spendings→no bar); icon picker gone; shared color map is the single source of truth; i18n synced; all
targeted suites + ci-gate green; served web/api/worker verified fresh on budget-dev.madonzy.com.
</success_criteria>

<output>
After completion, create `.planning/quick/260613-v1p-remove-category-icon-picker-render-categ/260613-v1p-SUMMARY.md`
</output>
