---
phase: quick-260612-e82
plan: 01
subsystem: shell-safe-area
tags: [ios, pwa, shell, sheet, spendings-grid, viewport, geometry-e2e]
key-files:
  modified:
    - apps/web/src/components/ui/sheet.tsx
    - apps/web/src/components/budgeting/tasks/pill-task-slider.tsx
    - apps/web/src/components/budgeting/spendings-grid/spendings-grid-client.tsx
    - apps/web/src/components/common/viewport-debug.tsx
    - apps/web/test/shell-safe-area.test.ts
    - apps/web/e2e/features/bdp-tab-frame.feature
    - apps/web/e2e/steps/bdp-shell-geometry.steps.ts
decisions:
  - "Architecture (a) for grid scroller: ResizeObserver + visualViewport listeners write --grid-max-h CSS var (measured bound) — viewport-unit constants forbidden"
  - "SheetClose offset: calc(env(safe-area-inset-top,0px)+22px) centers X on px-6 py-4 / text-xl title row in all display modes"
  - "Geometry honesty guard: scrollTop>50 check only applies when scrollHeight>clientHeight+50 (tall viewports may fit content without scrolling)"
metrics:
  duration: "~22min"
  completed: "2026-06-12T10:41:49Z"
  tasks_completed: 3
  files_modified: 7
---

# Quick Task 260612-e82: iOS Shell Round 3 Summary

One-liner: Runtime-measured grid scroller (ResizeObserver), X-button env-offset alignment, banner gutter trim, SHELL-R13 overlay — kills the constant-rot class of clearance bug.

## Tasks Completed

### T1: Sheet X alignment + banner gutter trim (e79c033)

- `SheetClose` className changed from bare `top-4` to `top-[calc(env(safe-area-inset-top,0px)+22px)]`
  — browser: env→0 → top:22px centers on the `py-4`/text-xl header row; standalone: adds same inset as R2 top spacer → X drops to title level
- `PillTaskSlider` wrapper: `mb-3` → `mb-1.5` (12px → 6px, root cause #3)
- Vitest tests A+B added to Round 3 describe block; 25 total GREEN

### T2: ResizeObserver grid bound + SHELL-R13 overlay (bc7b63d)

- Replaced `max-h-[calc(100dvh-128px)]` with `max-h-[var(--grid-max-h,80vh)]`
- ResizeObserver effect on `gridRef` measures `getBoundingClientRect().top` per callback:
  `maxH = Math.max(160, floor(visualViewport.height − rect.top − BOTTOM_CLEARANCE))`
  BOTTOM_CLEARANCE: standalone=safeBottom+16, browser=88px (Safari bar+slack)
- Listens: ResizeObserver + window resize + visualViewport resize/scroll → disconnects cleanly
- tail spacer retained with `env(safe-area-inset-bottom,0px)+64px`
- `viewport-debug.tsx`: SHELL-R12 → SHELL-R13, added `[grid]` metrics block (gridTop, gridClientH, gridScrollH, gridMaxH, gridToEnd, gridLastRowGap)
- Vitest tests C/D/E + SHELL-R12→R13 marker assertion; 28 total GREEN

### T3: Geometry E2E + deploy (5b047bd, 6af56cc)

- New `@tasks-geometry` scenario: "spendings grid last row clears the bottom bar in browser mode"
  scrolls grid to bottom, asserts `vpHeight − deepestRowBottom >= 48px`
- Transaction seeding: `expense_ledger` table, 6 rows per category, so columns have vertical scroll room
- Honesty guard: `scrollTop > 50` only when `scrollHeight > clientHeight + 50` (tall viewports that don't need to scroll still pass)
- `docker compose build --no-cache web && make restart-web` — SHELL-R13 confirmed in `/app/apps/web/.next/` chunks
- 16/16 `@tasks-geometry` scenarios GREEN across geom-320/390/430/1280

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] viewportDebug not in scope for Test E**

- Found during: T2 RED phase
- Issue: Round 3 describe block didn't have its own `readFileSync` for viewport-debug.tsx
- Fix: Added `viewportDebugR3` local read inside the Round 3 describe block
- Files modified: shell-safe-area.test.ts

**2. [Rule 1 - Bug] `100dvh-128px` string present in comment**

- Found during: T2 GREEN phase
- Issue: New comment explaining the old approach contained the exact string the test matched against
- Fix: Reworded comment to "dvh-constant" (no numeric)
- Files modified: spendings-grid-client.tsx

**3. [Rule 1 - Bug] Wrong DB table name for transaction seeding**

- Found during: T3 E2E run
- Issue: Schema is `budgeting.expense_ledger`, not `budgeting.transactions`
- Fix: Corrected table + columns (budget_id, amount_original_cents, currency_original, amount_converted_cents, fx_rate, fx_as_of, kind)
- Files modified: bdp-shell-geometry.steps.ts

**4. [Rule 1 - Bug] Honesty guard too strict for tall viewports**

- Found during: T3 geom-430 failure (430×932)
- Issue: scrollTop=0 at 430×932 because 639px content fits in 639px clientHeight exactly — no scroll room needed; guard asserted scrollTop>50 unconditionally
- Fix: Guard only activates when scrollHeight > clientHeight+50; gap assertion still runs
- Files modified: bdp-shell-geometry.steps.ts

## Known Stubs

None.

## Self-Check

### Files exist

- apps/web/src/components/ui/sheet.tsx — contains `top-[calc(env(safe-area-inset-top,0px)+22px)]`
- apps/web/src/components/budgeting/tasks/pill-task-slider.tsx — contains `mb-1.5`
- apps/web/src/components/budgeting/spendings-grid/spendings-grid-client.tsx — contains `ResizeObserver`, `--grid-max-h`, `BOTTOM_CLEARANCE`
- apps/web/src/components/common/viewport-debug.tsx — contains `SHELL-R13`, `gridLastRowGap`
- apps/web/test/shell-safe-area.test.ts — 28 tests GREEN
- apps/web/e2e/features/bdp-tab-frame.feature — new spendings grid scenario
- apps/web/e2e/steps/bdp-shell-geometry.steps.ts — new step + transaction seeding

### Commits exist

- e79c033: T1 sheet X + banner trim
- bc7b63d: T2 ResizeObserver + SHELL-R13
- 5b047bd: T3 E2E scenario (initial)
- 6af56cc: T3 transaction seeding fix + honesty guard

### Test results

- Vitest shell-safe-area: 28/28 GREEN
- Playwright @tasks-geometry: 16/16 GREEN (geom-320/390/430/1280)
- SHELL-R13 confirmed in served bundle at /app/apps/web/.next/

## Self-Check: PASSED
