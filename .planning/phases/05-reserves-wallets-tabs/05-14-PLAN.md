---
phase: 05-reserves-wallets-tabs
plan: 14
type: execute
wave: 5
depends_on: [13]
files_modified:
  - packages/budgeting/src/contracts/factory.ts
  - apps/api/src/boot.ts
  - apps/api/src/routes/budgets.ts
  - apps/api/src/routes/spendings-summary.ts
  - apps/api/test/routes/reserves.test.ts
  - apps/api/test/routes/spendings-summary.test.ts
autonomous: true
requirements: [RSRV-REWRITE-API]
must_haves:
  truths:
    - "The factory + boot wire the event-loader → orchestrator → summary/spendings/recompute with the new deps and the package compiles + boots."
    - "GET /budgets/:id/reserves returns the new shape: per-category {reserveCents, usedCents, overspentCents} + totals {internalCents, userDefinedCents, surplusCents, direction, disabled, budgetCurrency}; NO expected/actual/walletShare%/mismatch keys."
    - "GET /budgets/:id/spendings-summary returns per-category used + overspent from the engine; the reserveAvailable/funded concept is gone."
    - "Every changed/affected route has ≥1 integration test (project rule) asserting the new JSON shape."
  artifacts:
    - path: "apps/api/test/routes/reserves.test.ts"
      provides: "Integration test for GET /reserves + POST /reserves/:catId/adjust asserting the new shape end-to-end against real Postgres."
      min_lines: 60
  key_links:
    - from: "packages/budgeting/src/contracts/factory.ts"
      to: "reserve-event-loader-repo + getReservePositions"
      via: "getReservePositions({ eventLoader })"
      pattern: "eventLoader"
    - from: "apps/api/src/routes/budgets.ts"
      to: "deps.budgeting.getReservesSummary"
      via: "GET /:id/reserves returns the new DTO"
      pattern: "getReservesSummary"
---

<objective>
Wire the new reserve stack through the composition root (factory + boot) and reshape the HTTP surface. `ReservesSummaryDto` (and the `/reserves` JSON) becomes per-category `{reserveCents, usedCents, overspentCents}` + totals `{internalCents, userDefinedCents, surplusCents, direction, disabled, budgetCurrency}`. The spendings-summary route exposes used + overspent per category. Drop every expected/actual/walletShare%/mismatch field. Every touched route gets an integration test.

Purpose: 05-12/05-13 changed the application layer's types + deps; the factory still constructs `getReservePositions` with the OLD repos, so the package will not build until rewired. This plan reconnects the graph (event-loader → orchestrator → summary/spendings/topup-recompute) and locks the external JSON contract with real-Postgres integration tests (CLAUDE.md: every route ≥1 test).
Output: updated factory + boot wiring, reshaped `/reserves` + `/spendings-summary` routes, new/updated route integration tests. The Zod `reserveAdjustmentSchema` (target cents) is unchanged; only the response shape changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reserves-wallets-tabs/05-REWRITE-SPEC.md

<read_first>

- packages/budgeting/src/contracts/factory.ts — the DI root. Lines 188-202 build `reservePositions = getReservePositions({...OLD repos...})`; lines 257-386 wire setWalletBalance/updateWallet/archiveCategory/adjustCategoryReserve/getReservesSummary/recomputeReserveTopup with OLD deps. REWIRE all to the new deps (event-loader, orchestrator, surplus recompute).
- apps/api/src/boot.ts — lines 225-268 construct reserveBalanceRepo/spendingsSummaryRepo + `getSpendingsSummaryService = getSpendingsSummary({...})` with OLD deps incl. `reservePositions: baseBudgeting.reservePositions`. Update to the new getSpendingsSummary deps (reservePositions required, drop reserveBalanceRepo/reservesSummaryRepo/categoriesRepo if removed).
- apps/api/src/routes/budgets.ts — GET /:id/reserves (lines 388-405) + POST /:id/reserves/:categoryId/adjust (lines 410-451). The handlers just forward to the use-cases; no shape logic to change EXCEPT the fallback initial object is in the RSC (05-15), not here. Confirm error mapping still valid.
- apps/api/src/routes/spendings-summary.ts — GET / returns SpendingsSummaryDTO; no field logic here (the DTO is built in the use-case). Confirm it still compiles with the reshaped DTO.
- packages/budgeting/src/adapters/persistence/reserve-event-loader-repo.ts (05-11) — the adapter the factory must instantiate (with its injected repos: transactionRepo, limitRepo, reservesSummaryRepo).
- apps/api/test/routes/ — existing route integration tests for the setup/teardown pattern (seed budget via Better Auth org, auth cookie, real Postgres). Mirror one for the new reserves.test.ts.
- CLAUDE.md — every API route ≥1 integration test in apps/api/test/routes/; no DB mocking.
  </read_first>

<interfaces>
<!-- The /reserves JSON contract (response of getReservesSummary, consumed by the web hook in 05-15). -->
```jsonc
{
  "rows": [ { "categoryId": "...", "name": "Grocery", "reserveCents": "130000", "usedCents": "160000", "overspentCents": "0" } ],
  "excludedRows": [ { "categoryId": "...", "name": "Fun", "reserveCents": "0", "usedCents": "0", "overspentCents": "0" } ],
  "totals": {
    "internalCents": "210000",
    "userDefinedCents": "300000",
    "surplusCents": "90000",
    "direction": "WITHDRAW",   // surplus<0 TOPUP, >0 WITHDRAW, 0 NONE
    "disabled": false,
    "budgetCurrency": "EUR"
  }
}
```
adjust response: `{ "categoryId": "...", "reserveCents": "130000", "deltaCents": "30000", "summary": <the /reserves DTO above> }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Rewire factory.ts + boot.ts to the event-loader → orchestrator → summary/spendings/topup graph</name>
  <files>packages/budgeting/src/contracts/factory.ts, apps/api/src/boot.ts</files>
  <action>
1. `factory.ts`:
   - Import the event-loader adapter from `../adapters/persistence/reserve-event-loader-repo` and construct it with its injected repos: `const eventLoader = createReserveEventLoaderRepo({ transactionRepo, categoryLimitRepo: limitRepo, reservesSummaryRepo });` (match the adapter's factory signature from 05-11).
   - `const reservePositions = getReservePositions({ eventLoader });` (drop the 6 OLD repo deps).
   - `recomputeReserveTopup`: pass `{ taskRepo: createTaskRepo(), reservePositions, budgetCurrencyOf: getWorkspaceDefaultCurrency, isReservesEnabled }` (drop categoriesRepo/reserveBalanceRepo/reservesSummaryRepo).
   - `setWalletBalance`, `updateWallet`: pass the trimmed deps (repo, budgetCurrencyOf, reservePositions, taskRepo, isReservesEnabled, fxProvider) — remove categoriesRepo/reserveBalanceRepo/reservesSummaryRepo allocation deps.
   - `archiveCategory`: now `archiveCategory({ repo: categoryRepo })` (+ optional taskRepo/reservePositions/budgetCurrencyOf/isReservesEnabled if you wired the post-archive recompute in 05-13).
   - `archiveWallet`: keep repo + taskRepo + fxProvider + (reservePositions/budgetCurrencyOf/isReservesEnabled for the RESERVE_TOPUP recompute); drop the actual-recalc deps.
   - `adjustCategoryReserve`: `{ adjustmentsRepo, categoriesRepo, reservePositions, isReservesEnabled, budgetCurrencyOf, taskRepo }` (drop reserveBalanceRepo/reservesSummaryRepo).
   - `getReservesSummary`: `{ reservePositions, categoriesRepo, budgetCurrencyOf, isReservesEnabled }` (drop reserveBalanceRepo/reservesSummaryRepo).
   - `toggleCategoryReserveExcluded`: `{ repo: categoriesRepo }` (+ topup recompute deps if wired).
   - Keep `reserveBalanceRepo: createReserveBalanceRepo()` in the module ONLY if other consumers still need it; otherwise remove it from `BudgetingModule` (grep first — `getReservePositions`/`getReservesSummary`/`getSpendingsSummary` no longer use it; if nothing else does, delete the field + the `createReserveBalanceRepo` import). Adjust `BudgetingModule` interface types accordingly.
2. `boot.ts`:
   - `getSpendingsSummaryService = getSpendingsSummary({ categoryRepo, categoryLimitRepo, transactionRepo, summaryRepo: spendingsSummaryRepo, reservePositions: baseBudgeting.reservePositions })` — drop reserveBalanceRepo/reservesSummaryRepo/categoriesRepo if 05-12 removed them from the deps. If the reshaped `getSpendingsSummary` made `reservePositions` required, this already passes it.
   - Remove now-unused local `reserveBalanceRepo`/`reservesSummaryRepo` constructions in boot ONLY if nothing else references them (grep `reserveBalanceRepo`/`reservesSummaryRepo` in boot.ts first).
3. `bun run typecheck` (root) MUST pass — the whole monorepo compiles with the new wiring. Fix any remaining type mismatches at call sites.
   Commit: `feat(05-14): wire event-loader → orchestrator → summary/spendings/topup in factory + boot`.
  </action>
  <verify>
    <automated>cd /home/claude/budget && grep -q "eventLoader" packages/budgeting/src/contracts/factory.ts && grep -q "getReservePositions({ eventLoader" packages/budgeting/src/contracts/factory.ts && bun run typecheck 2>&1 | tail -5 | grep -qiE "error" && echo "TYPECHECK-FAILED" || echo "typecheck-clean"</automated>
  </verify>
  <done>factory builds reservePositions from the event-loader; all reserve use-cases receive the trimmed/new deps; boot's getSpendingsSummary passes reservePositions; `bun run typecheck` is clean across the monorepo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Confirm /reserves + /spendings-summary routes emit the new shape + add integration tests</name>
  <files>apps/api/src/routes/budgets.ts, apps/api/src/routes/spendings-summary.ts, apps/api/test/routes/reserves.test.ts, apps/api/test/routes/spendings-summary.test.ts</files>
  <behavior>
    - GET /budgets/:id/reserves → 200 with rows[].{reserveCents,usedCents,overspentCents}, excludedRows[], totals.{internalCents,userDefinedCents,surplusCents,direction,disabled,budgetCurrency}; NO walletSharePercent/walletShareAmountCents/mismatchCents/reserveBalanceCents keys anywhere.
    - POST /budgets/:id/reserves/:catId/adjust with {expectedCents:N} → 200, response.reserveCents === "N" (after the delta lands) and response.summary is the new /reserves DTO; appends exactly one adjustment row when N≠currentR.
    - reserves_enabled=false → /reserves totals.disabled === true, rows [].
    - GET /spendings-summary?month=YYYY-MM → categories[].{reserveUsedCents,overspentCents,balanceCents}; no reserveAvailableCents key.
  </behavior>
  <action>
1. Routes: the handlers forward to the use-cases, so no field logic changes — but VERIFY the error mapping + that `c.json(result.value)` now serializes the new DTO. If `budgets.ts` references any removed dep (it does not — it only calls `deps.budgeting.getReservesSummary`/`adjustCategoryReserve`), leave as-is. Same for `spendings-summary.ts`.
2. Create `apps/api/test/routes/reserves.test.ts` (bun:test, real Postgres, mirror an existing route test's auth + seed harness):
   - Seed a budget + 2 categories with limits + a RESERVE wallet (balance 3000.00) + a couple of transactions producing a known reserve state.
   - `GET /budgets/:id/reserves`: assert 200; assert `rows[0]` has keys `reserveCents,usedCents,overspentCents,categoryId,name` and assert it does NOT have `walletSharePercent`,`walletShareAmountCents`,`reserveBalanceCents`; assert `totals` has `internalCents,userDefinedCents,surplusCents,direction,disabled,budgetCurrency` and NOT `mismatchCents`,`totalCategoryReservesCents`. Assert `totals.userDefinedCents === "300000"` and `direction` ∈ {TOPUP,WITHDRAW,NONE}.
   - `POST /budgets/:id/reserves/:catId/adjust { expectedCents: 50000 }`: assert 200, `body.reserveCents === "50000"`, `body.deltaCents` present, `body.summary.totals.internalCents` reflects the new reserve; then query `category_reserve_adjustments` and assert exactly one row with delta = 50000 − priorR.
   - Disable path: PATCH the budget reserves_enabled=false (via the budget-identity route) then GET /reserves → `totals.disabled === true`, `rows.length === 0`.
3. UPDATE `apps/api/test/routes/spendings-summary.test.ts` (create if absent): assert a category carries `reserveUsedCents` + `overspentCents` + `balanceCents` for the queried month and NO `reserveAvailableCents`.
4. Run `make test` (API integration subset). Green.
   Commit: `test(05-14): /reserves + /spendings-summary new-shape integration tests`.
  </action>
  <verify>
    <automated>cd /home/claude/budget && grep -q "reserveCents" apps/api/test/routes/reserves.test.ts && grep -q "surplusCents" apps/api/test/routes/reserves.test.ts && grep -qE "not\\.toHaveProperty\\(\"walletShare|mismatchCents" apps/api/test/routes/reserves.test.ts; echo "test-asserts:$?"</automated>
  </verify>
  <done>/reserves + adjust + /spendings-summary integration tests assert the new keys are PRESENT and the old keys (walletShare%, mismatch, reserveBalanceCents, reserveAvailableCents) are ABSENT; the adjust test verifies the ledger delta; disable test verifies disabled:true; all green against real Postgres.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                            | Description                                                                                                                                                                            |
| --------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| client→API (/reserves, /adjust, /spendings-summary) | Untrusted HTTP input crosses here. tenantId derived from the authenticated session's tenantIds gate (budgetId must be in the verified set → 404 otherwise). adjust body Zod-validated. |
| API→application                                     | Validated inputs forwarded to RLS-scoped use-cases.                                                                                                                                    |

## STRIDE Threat Register

| Threat ID | Category               | Component                  | Disposition | Mitigation Plan                                                                                                                                                                                                                         |
| --------- | ---------------------- | -------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-0514-01 | Elevation of Privilege | GET /reserves cross-budget | mitigate    | `tenantIds.includes(budgetId)` gate returns 404 for non-member budgets (existing pattern, retained). RLS is the second layer.                                                                                                           |
| T-0514-02 | Tampering              | adjust expectedCents       | mitigate    | `reserveAdjustmentSchema = z.object({ expectedCents: z.number().int().min(0), note: max 280 }).strict()` rejects negatives, non-integers, and unknown keys (mass-assignment defense). Server computes the delta — client cannot set it. |
| T-0514-03 | Information Disclosure | reserves JSON leakage      | mitigate    | DTO contains only the caller's budget categories + aggregate surplus; no other tenant's data is reachable (RLS-scoped orchestrator). Integration test asserts only expected keys serialize.                                             |
| T-0514-04 | Tampering              | reserves_enabled toggle    | accept      | Toggle lives on the budget-identity PATCH (owner-gated, existing); disabling reserves only hides/zeroes the read — underlying adjustments are untouched (decision K), reversible by re-enabling.                                        |

</threat_model>

<verification>
- `bun run typecheck` (monorepo) clean.
- `make test` API subset green: `apps/api/test/routes/reserves.test.ts` + `spendings-summary.test.ts`.
- `curl`-equivalent in the integration test: GET /reserves body has NO `walletSharePercent|walletShareAmountCents|mismatchCents|reserveBalanceCents`; spendings-summary has NO `reserveAvailableCents`.
- `grep -rn "mismatchCents\|walletShare" apps/api/src` → only comments, no live serialization.
</verification>

<success_criteria>

- Composition root rewired; monorepo type-checks and boots.
- /reserves + adjust + /spendings-summary emit the new shape; old fields gone.
- Each touched route has an integration test on real Postgres asserting the new shape + key absence + the adjust ledger delta + the disabled path.
  </success_criteria>

<output>
After completion, create `.planning/phases/05-reserves-wallets-tabs/05-14-SUMMARY.md`
</output>
