---
phase: 05-reserves-wallets-tabs
plan: 15
type: execute
wave: 6
depends_on: [14]
files_modified:
  - apps/web/src/hooks/use-reserves-summary.ts
  - apps/web/src/hooks/use-spendings-summary.ts
  - apps/web/src/hooks/use-update-reserve-adjustment.ts
  - apps/web/src/components/budgeting/reserves-tab/reserves-table-client.tsx
  - apps/web/src/components/budgeting/reserves-tab/reserves-table-row.tsx
  - apps/web/src/components/budgeting/reserves-tab/reserves-totals-footer.tsx
  - apps/web/src/app/[locale]/(app)/budgets/[id]/reserves/page.tsx
  - apps/web/src/components/budgeting/spendings-grid/category-column.tsx
  - apps/web/messages/en.json
  - apps/web/e2e/features/reserves.feature
  - apps/web/e2e/steps/reserves.steps.ts
  - apps/web/e2e/page-objects/ReservesPo.ts
autonomous: false
requirements: [RSRV-REWRITE-UI]
must_haves:
  truths:
    - "The Reserves tab shows a SINGLE reserve value + used per category + a surplus banner (top-up/withdraw) — no expected/actual/share% columns, no MismatchChip."
    - "The spendings grid shows used + overspent per category per month from the new summary shape."
    - "The web reserve mirror lib apps/web/src/lib/reserve-allocator.ts and the mismatch-chip component are DELETED; the optimistic-update hook no longer imports them."
    - "An E2E feature tagged @tasks-redesign drives the reserves flow (adjust reserve → reserve+used update → surplus banner) against the rebuilt web image."
  artifacts:
    - path: "apps/web/e2e/features/reserves.feature"
      provides: "playwright-bdd Gherkin feature for the rebuilt Reserves tab, tagged @tasks-redesign."
      min_lines: 25
    - path: "apps/web/src/components/budgeting/reserves-tab/reserves-table-row.tsx"
      provides: "Row rendering single reserve value + used; no expected/actual/share columns."
      min_lines: 40
  key_links:
    - from: "apps/web/src/components/budgeting/reserves-tab/reserves-table-client.tsx"
      to: "useReservesSummary new shape"
      via: "rows[].reserveCents / usedCents + totals.surplusCents/direction"
      pattern: "surplusCents|reserveCents"
    - from: "apps/web/e2e/features/reserves.feature"
      to: "@tasks-redesign tag"
      via: "feature/scenario tag"
      pattern: "@tasks-redesign"
---

<objective>
Reshape the Reserves tab + spendings grid to the new model and delete the dead UI. The Reserves tab renders ONE reserve value + used per category, with a surplus banner (top-up/withdraw) sourced from `totals.surplusCents`/`direction`. The spendings grid shows used + overspent per category. Delete `apps/web/src/lib/reserve-allocator.ts` (the optimistic mirror of the dead greedy domain) and `mismatch-chip.tsx`. Add a `@tasks-redesign`-tagged playwright-bdd E2E that drives the rebuilt flow. Web is bundled at build time → rebuild + restart web before verifying.

Purpose: The API now returns reserve/used/overspent/internal/surplus (05-14). The current UI still renders expected/actual/share% + the MismatchChip off the old DTO and runs a client-side greedy optimistic mirror — all dead. This plan aligns the UI with the new contract, removes the dead pieces, and proves the flow E2E.
Output: updated hooks + reserves components + spendings column + i18n + a tagged E2E feature/steps/page-object. Includes a [BLOCKING] rebuild+restart-web checkpoint, then a human-verify checkpoint for the visual reserves flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reserves-wallets-tabs/05-REWRITE-SPEC.md

<read_first>

- apps/web/src/hooks/use-reserves-summary.ts — reshape `ReservesSummaryRow` → {categoryId,name,reserveCents,usedCents,overspentCents} and `totals` → {internalCents,userDefinedCents,surplusCents,direction,disabled,budgetCurrency}. Drop walletShare%/mismatch.
- apps/web/src/hooks/use-spendings-summary.ts — drop `reserveAvailableCents`; keep reserveUsedCents/overspentCents/balanceCents.
- apps/web/src/hooks/use-update-reserve-adjustment.ts — optimistic adjust hook; it imports `applyExpectedChange` from apps/web/src/lib/reserve-allocator.ts (DEAD). Rewrite optimistic update to the trivial new model: on adjust(cat, X) set that row's reserveCents=X locally; recompute totals.internalCents = Σ rows.reserveCents (active), surplusCents = userDefinedCents − internal, direction from sign. Remove the reserve-allocator import.
- apps/web/src/components/budgeting/reserves-tab/reserves-table-client.tsx — Active section currently renders expected/actual/share column headers + ReservesTotalsFooter with mismatch. Reshape: single "Reserve" column + "Used" column; surplus banner.
- apps/web/src/components/budgeting/reserves-tab/reserves-table-row.tsx — renders reserveBalanceCents (expected) + walletShareAmountCents + sharePct. Reshape to render reserveCents (editable) + usedCents (read-only); remove share/actual.
- apps/web/src/components/budgeting/reserves-tab/reserves-totals-footer.tsx — currently Σcategories/Σwallets; add the surplus banner (top-up/withdraw direction). It already imports nothing from mismatch-chip; the MismatchChip lives in mismatch-chip.tsx (DELETE that file).
- apps/web/src/components/budgeting/reserves-tab/mismatch-chip.tsx — DELETE (dead).
- apps/web/src/lib/reserve-allocator.ts — DELETE (dead optimistic mirror of the greedy domain).
- apps/web/src/app/[locale]/(app)/budgets/[id]/reserves/page.tsx — the RSC fallback `initial` object uses the OLD totals keys (mismatchCents/totalCategoryReservesCents). Update the fallback to the new shape.
- apps/web/src/components/budgeting/spendings-grid/category-column.tsx — uses reserveUsedCents/overspentCents (lines 30-31); confirm it still works after dropping reserveAvailableCents (it likely never read it). Update the SpendingsSummaryCategoryDTO type import if needed.
- apps/web/e2e/page-objects/ReservesPo.ts — extend with selectors for the new reserve value + used cells + surplus banner.
- apps/web/e2e/features/tasks.feature — the ONLY current @tasks-redesign feature; mirror its Gherkin + fixture style (playwright-bdd, fresh-user-per-scenario, Page Objects per MEMORY.md e2e conventions).
- MEMORY.md notes: E2E via playwright-bdd .feature + Page Objects + fresh-user fixture; @tasks-redesign tag; run `--grep @tasks-redesign`; PLAYWRIGHT_BASE_URL from .env.local APP_URL; ALWAYS rebuild web after frontend edits (make restart-web) before verifying; SWs need secure context (localhost not HTTP tailscale).
  </read_first>

<interfaces>
<!-- New web hook types (mirror the 05-14 JSON). -->
```typescript
export interface ReservesSummaryRow { categoryId: string; name: string; reserveCents: string; usedCents: string; overspentCents: string; }
export interface ReservesSummaryDto {
  rows: ReservesSummaryRow[];
  excludedRows: ReservesSummaryRow[];
  totals: { internalCents: string; userDefinedCents: string; surplusCents: string; direction: "TOPUP"|"WITHDRAW"|"NONE"; disabled: boolean; budgetCurrency: string; };
}
```
Surplus banner copy: direction TOPUP → "Top up reserve wallet by {amount}"; WITHDRAW → "Withdraw {amount} from reserve wallet"; NONE → "Reserve reconciled". (Add i18n keys under bdp.tab.reserves.surplus.*.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Reshape hooks + components + RSC fallback; delete dead web reserve-allocator + mismatch-chip</name>
  <files>apps/web/src/hooks/use-reserves-summary.ts, apps/web/src/hooks/use-spendings-summary.ts, apps/web/src/hooks/use-update-reserve-adjustment.ts, apps/web/src/components/budgeting/reserves-tab/reserves-table-client.tsx, apps/web/src/components/budgeting/reserves-tab/reserves-table-row.tsx, apps/web/src/components/budgeting/reserves-tab/reserves-totals-footer.tsx, apps/web/src/app/[locale]/(app)/budgets/[id]/reserves/page.tsx, apps/web/src/components/budgeting/spendings-grid/category-column.tsx, apps/web/messages/en.json</files>
  <action>
1. `use-reserves-summary.ts`: replace `ReservesSummaryRow` + `ReservesSummaryDto` with the <interfaces> shapes. queryKey unchanged.
2. `use-spendings-summary.ts`: remove `reserveAvailableCents` from the `categories[]` item type.
3. `use-update-reserve-adjustment.ts`: REMOVE `import { applyExpectedChange } from "@/lib/reserve-allocator"`. Rewrite the optimistic `onMutate` to the new trivial model: clone the cached `ReservesSummaryDto`; set the target row's `reserveCents = String(expectedCents)`; recompute `totals.internalCents = String(Σ active rows.reserveCents)`, `totals.surplusCents = String(BigInt(userDefinedCents) − internal)`, `totals.direction = surplus<0?"TOPUP":surplus>0?"WITHDRAW":"NONE"`. Keep rollback-on-error + invalidate-on-settle. The mutation body still POSTs `{ expectedCents }` to `/budgets/:id/reserves/:catId/adjust` and on success replaces the cache with `response.summary`.
4. `reserves-table-client.tsx`: replace the Active column-header block (Category / Expected / Actual / Share) with (Category / Reserve / Used). For each active row pass `reserveCents`+`usedCents` to the row; the inline-edit onUpdate still calls `updateAdjustment.mutateAsync({ categoryId, expectedCents: Number(newReserveCents) })` but compares against `BigInt(r.reserveCents)`. Excluded section stays name-only. Replace the `ReservesTotalsFooter` props with the surplus banner props (surplusCents, direction, userDefinedCents, internalCents).
5. `reserves-table-row.tsx`: render the editable `reserveCents` cell (was reserveBalanceCents) + a read-only `usedCents` cell. DELETE the `walletShareAmountCents`/`sharePct`/`underfunded` share-column logic. Excluded rows still name-only.
6. `reserves-totals-footer.tsx`: keep Σ rows.reserve / Σ wallets if desired, and ADD the surplus banner: when direction!=="NONE" render an inline banner with the localized copy + formatted |surplus|; reconciled state when NONE. (You may inline a small banner here instead of resurrecting MismatchChip.)
7. `reserves/page.tsx`: update the RSC fallback `initial` object's `totals` to `{ internalCents:"0", userDefinedCents:"0", surplusCents:"0", direction:"NONE", disabled:false, budgetCurrency:"EUR" }` and rows/excludedRows [].
8. `category-column.tsx`: ensure its `SpendingsSummaryCategoryDTO` import/type no longer references `reserveAvailableCents`; it already renders reserveUsed + overspent — leave that intact.
9. `messages/en.json`: add `bdp.tab.reserves.column.reserve`, `bdp.tab.reserves.column.used`, and `bdp.tab.reserves.surplus.topup` / `.withdraw` / `.reconciled` keys. Remove now-unused `column.expected`/`column.actual`/`column.share` + `mismatch.*` keys if no longer referenced (grep first). (PL/UK files are 05-16/Phase-8 i18n scope; add EN here, leave PL/UK keys present to avoid missing-key crashes — or copy EN values as placeholders.)
10. DELETE `apps/web/src/lib/reserve-allocator.ts` and `apps/web/src/components/budgeting/reserves-tab/mismatch-chip.tsx` (`git rm`).
11. Update the corresponding component unit tests that referenced the deleted pieces (apps/web/test/components/mismatch-chip.test.tsx → delete; reserves-table-row.test.tsx / reserves-totals-footer.test.tsx / reserves-table-client-excluded.test.tsx → update to new props or delete the share/mismatch assertions). Run `cd apps/web && bun run test` for the reserves components.
   Commit: `feat(05-15): reshape reserves+spendings UI to new model; delete dead allocator+mismatch-chip`.
  </action>
  <verify>
    <automated>cd /home/claude/budget && ! test -f apps/web/src/lib/reserve-allocator.ts && ! test -f apps/web/src/components/budgeting/reserves-tab/mismatch-chip.tsx && ! grep -rqE "walletSharePercent|walletShareAmountCents|mismatchCents|reserveBalanceCents|reserveAvailableCents" apps/web/src/hooks/use-reserves-summary.ts apps/web/src/hooks/use-spendings-summary.ts && grep -q "surplusCents" apps/web/src/hooks/use-reserves-summary.ts; echo "ok:$?"</automated>
  </verify>
  <done>Hooks expose the new shape; reserves row/client render reserve+used (+surplus banner) with no expected/actual/share; web reserve-allocator + mismatch-chip files deleted; optimistic hook no longer imports the dead lib; reserves component tests pass under Vitest.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Rebuild + restart the web image so the reshaped UI is served</name>
  <what-built>Frontend edits (apps/web/** + messages/en.json) are bundled at BUILD time — the running web container serves the OLD bundle until rebuilt. The E2E in Task 2 + the human visual check MUST run against the rebuilt image.</what-built>
  <action>
Per CLAUDE.md "Local Development" + MEMORY.md (always rebuild web after frontend edits):
```
docker compose build web && make restart-web
```
Then confirm the container restarted recently + healthy:
```
docker compose ps web
```
Claude runs this itself; only escalate if the build/secrets step fails.
  </action>
  <how-to-verify>
1. `docker compose build web` exits 0.
2. `make restart-web` exits 0 (wraps `infisical run` for secrets).
3. `docker compose ps web` shows a recent restart + healthy.
  </how-to-verify>
  <resume-signal>Type "web-rebuilt" once `docker compose ps web` shows the web service freshly restarted + healthy.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the @tasks-redesign reserves E2E (feature + steps + page-object)</name>
  <files>apps/web/e2e/features/reserves.feature, apps/web/e2e/steps/reserves.steps.ts, apps/web/e2e/page-objects/ReservesPo.ts</files>
  <behavior>
    - Scenario (tagged @tasks-redesign): a fresh user with a budget + a category + a RESERVE wallet opens the Reserves tab, sees the single Reserve column + Used column, edits a category's reserve to a value, and sees the reserve value update + the surplus banner reflect top-up/withdraw direction.
    - Golden-path + the disable case if cheap (toggle reserves off → tab shows disabled notice).
  </behavior>
  <action>
1. Extend `ReservesPo.ts` with selectors: `reserveCell(categoryName)` (the editable reserve value), `usedCell(categoryName)`, `surplusBanner()` (data-testid on the new banner), `setReserve(categoryName, value)` (click cell → type → blur/enter), `disabledNotice()` (data-testid="reserves-disabled-notice" already exists).
2. Create `apps/web/e2e/features/reserves.feature` tagged `@tasks-redesign`, mirroring tasks.feature structure (Background with fresh user + budget via the existing fixtures). Scenarios:
   - "Reserves tab shows reserve and used per category" → open tab, assert reserve + used cells visible for the seeded category, NO share column header.
   - "Adjusting a category reserve updates reserve and surplus banner" → set reserve to a known value, assert the reserve cell shows it, assert the surplus banner shows the expected direction (top-up or withdraw) given the wallet balance vs internal.
3. Create `apps/web/e2e/steps/reserves.steps.ts` binding the Gherkin to ReservesPo (playwright-bdd `Given/When/Then`). Use `PLAYWRIGHT_BASE_URL` from .env.local APP_URL (do NOT hardcode localhost:3000) per MEMORY.md; use localhost (secure context) not HTTP tailscale.
4. Run `cd apps/web && PLAYWRIGHT_BASE_URL=$(grep APP_URL .env.local | cut -d= -f2) bun run test:e2e --grep @tasks-redesign` (or the documented `make test-e2e` with the grep). Claude runs it itself before asking for human confirmation (MEMORY.md: verify yourself first). Green.
   Commit: `test(05-15): @tasks-redesign reserves E2E (reserve/used + surplus banner)`.
  </action>
  <verify>
    <automated>cd /home/claude/budget && grep -q "@tasks-redesign" apps/web/e2e/features/reserves.feature && grep -qE "surplus|reserve" apps/web/e2e/features/reserves.feature && grep -q "surplusBanner\|reserveCell" apps/web/e2e/page-objects/ReservesPo.ts; echo "e2e-authored:$?"</automated>
  </verify>
  <done>A @tasks-redesign reserves.feature + steps + extended ReservesPo exist; the adjust-reserve scenario passes against the rebuilt web image driving reserve+used+surplus-banner; run via --grep @tasks-redesign.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual verification of the rebuilt Reserves tab</name>
  <what-built>The Reserves tab now renders a single Reserve value + Used per category + a surplus banner (top-up/withdraw), with the expected/actual/share columns and the MismatchChip removed. The spendings grid shows used + overspent. The @tasks-redesign E2E passed against the rebuilt image.</what-built>
  <how-to-verify>
1. Open the app at the APP_URL from .env.local (use localhost for a secure context — SWs/Serwist need it), sign in, open a budget → Reserves tab.
2. Confirm each category shows exactly ONE Reserve value (editable) + a Used value. No "Expected"/"Actual"/"Share %" columns. No mismatch chip.
3. Edit a category's reserve; confirm the value sticks and the surplus banner updates to "Top up…" or "Withdraw…" matching whether internal exceeds the reserve-wallet balance.
4. Open the Spendings grid; confirm a category with overspend shows Used + Overspent.
5. (Optional) Settings → toggle reserves off → Reserves tab shows the disabled notice; toggle back on → numbers return identical (idempotent re-enable).
  </how-to-verify>
  <resume-signal>Type "approved" if the Reserves tab matches the new model (single reserve + used + surplus banner, no expected/actual/share/mismatch), or describe what looks wrong.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary    | Description                                                                                                                                                                                        |
| ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| browser→API | The reserves UI calls the same authenticated `/reserves` + `/adjust` endpoints (gated + Zod-validated in 05-14). The optimistic update is presentation-only; the server response is authoritative. |

## STRIDE Threat Register

| Threat ID | Category               | Component                            | Disposition | Mitigation Plan                                                                                                                                                                          |
| --------- | ---------------------- | ------------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-0515-01 | Tampering              | optimistic reserve/surplus recompute | accept      | Client-side optimistic numbers are cosmetic; on settle the cache is replaced by the server's authoritative `summary`/refetch. A wrong optimistic value self-corrects and cannot persist. |
| T-0515-02 | Information Disclosure | reserves rendered client-side        | accept      | Only the authenticated user's budget data (already returned by the gated API) is rendered; no new data path.                                                                             |

</threat_model>

<verification>
- `cd apps/web && bun run test` reserves component tests green; no import of `@/lib/reserve-allocator` anywhere (`grep -rn "lib/reserve-allocator" apps/web/src` → nothing).
- Files deleted: `apps/web/src/lib/reserve-allocator.ts`, `apps/web/src/components/budgeting/reserves-tab/mismatch-chip.tsx`.
- `--grep @tasks-redesign` E2E (reserves) passes against the rebuilt web image.
- `grep -rnE "walletSharePercent|mismatchCents|reserveBalanceCents|reserveAvailableCents" apps/web/src` → nothing.
</verification>

<success_criteria>

- Reserves tab: single reserve + used + surplus banner; spendings grid: used + overspent.
- Dead web pieces (reserve-allocator mirror, mismatch-chip) deleted; optimistic hook rewritten.
- @tasks-redesign reserves E2E green against the rebuilt image; human visual check approved.
  </success_criteria>

<output>
After completion, create `.planning/phases/05-reserves-wallets-tabs/05-15-SUMMARY.md`
</output>
