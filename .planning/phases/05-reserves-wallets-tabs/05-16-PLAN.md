---
phase: 05-reserves-wallets-tabs
plan: 16
type: execute
wave: 7
depends_on: [9, 10, 11, 12, 13, 14, 15]
files_modified:
  - packages/budgeting/src/domain/reserve-allocator.ts
  - packages/budgeting/test/domain/reserve-allocator.test.ts
  - packages/budgeting/src/adapters/persistence/reserve-balance-repo.ts
  - packages/budgeting/src/ports/reserve-balance-repo.ts
  - apps/web/messages/pl.json
  - apps/web/messages/uk.json
autonomous: false
requirements: [RSRV-REWRITE-VERIFY]
must_haves:
  truths:
    - "No dead code remains: the greedy reserve-allocator (domain + web mirror), the expected VIEW, reserve_actual_cents, setReserveActualMany, mismatch/walletShare contracts + UI are GONE — repo-wide grep returns nothing live."
    - "The golden fixture still reproduces exactly and the disable→re-enable round-trip is idempotent (keystone + 05-10 tests green)."
    - "make test (budgeting + api) green; monorepo type-check clean; @tasks-redesign reserves E2E green; tenant-leak ci-gate green."
    - "graphify is updated so the knowledge graph reflects the deleted/added modules."
  artifacts:
    - path: "packages/budgeting/src/domain/reserve-allocator.ts"
      provides: "DELETED — greedy allocator removed (replaced by reserve-engine.ts)."
  key_links:
    - from: "repo-wide grep"
      to: "dead reserve symbols"
      via: "no live references remain"
      pattern: "applyExpectedChange|refillUnderfunded|deductFromBottom|setReserveActualMany"
---

<objective>
Close the rewrite: remove the genuinely-dead reserve code as a FIRST-CLASS deliverable (not a side effect), prove the whole system green against the golden table + the disable round-trip + the full test/CI suite, reconcile any straggler dead references discovered by grep, and refresh the knowledge graph. Also remove/replace the dead reserve `reserve-balance-repo` VIEW reads if no consumer remains, and clear orphaned PL/UK i18n keys.

Purpose: 05-09..05-15 evolved the system in place and left the OLD greedy domain (`reserve-allocator.ts` + its test + the web mirror, already deleted in 05-15), the VIEW-reading `reserve-balance-repo`, and the `reserve_actual_cents`/`mismatch`/`walletShare` contracts behind. Decision: dead-code removal is an acceptance criterion. This plan makes the grep clean, runs the full gates, and updates graphify.
Output: deleted `reserve-allocator.ts` (+ test) and (if unused) `reserve-balance-repo` VIEW methods + port; cleaned PL/UK keys; a green full-suite run; `graphify update .`. Final human sign-off that the rewrite is complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reserves-wallets-tabs/05-REWRITE-SPEC.md

<read_first>

- packages/budgeting/src/domain/reserve-allocator.ts + test/domain/reserve-allocator.test.ts — the greedy domain + its test. DELETE both (the engine + its tests replaced them).
- packages/budgeting/src/adapters/persistence/reserve-balance-repo.ts + ports/reserve-balance-repo.ts — the VIEW-based balance reader. After 05-12/05-13 the orchestrator uses the event-loader instead. GREP for live consumers; if none remain, DELETE the adapter + port + its factory/boot construction. If a consumer remains (e.g. getForCategory used elsewhere), leave only that method and drop the VIEW-dependent ones (the VIEW was dropped in 05-11, so any method SELECTing category_reserve_balance WILL fail at runtime and MUST be removed).
- packages/budgeting/src/contracts/factory.ts + apps/api/src/boot.ts — remove any leftover `createReserveBalanceRepo()` / `reserveBalanceRepo` wiring that is now unused.
- apps/web/messages/pl.json + uk.json — remove orphaned reserve keys (column.expected/actual/share, mismatch.\*) to match the EN cleanup from 05-15; add the new reserve/used/surplus keys (placeholder = EN value is acceptable; full PL/UK translation is Phase 8 i18n scope, but keys MUST exist to avoid missing-key runtime errors).
- bunfig.toml — domain coverage threshold 80% (the engine must keep it satisfied after the allocator is deleted).
- CLAUDE.md — `make test`, `make ci-gate` (tenant-leak), `make test-e2e`; MEMORY.md: Docker ON before declaring verified; run real runners not the red `make test` sweep count.
- graphify SKILL + CLAUDE.md graphify section — `graphify update .` (AST-only, no API cost) after code changes.
  </read_first>
  </context>

<tasks>

<task type="auto">
  <name>Task 0: Delete the greedy allocator + dead VIEW reader; reconcile straggler references</name>
  <files>packages/budgeting/src/domain/reserve-allocator.ts, packages/budgeting/test/domain/reserve-allocator.test.ts, packages/budgeting/src/adapters/persistence/reserve-balance-repo.ts, packages/budgeting/src/ports/reserve-balance-repo.ts</files>
  <action>
1. `git rm packages/budgeting/src/domain/reserve-allocator.ts packages/budgeting/test/domain/reserve-allocator.test.ts`.
2. GREP the whole repo for remaining references to the greedy API + dead symbols and remove each live use:
   ```
   grep -rnE "reserve-allocator|applyExpectedChange|refillUnderfunded|deductFromBottom|applyWalletDelta|applyExclude|setReserveActualMany|reserve_actual_cents|reserveActualCents|category_reserve_balance|reserveBalanceRepo|getDiscardedForBudget|expectedReserveCents|realReserveCents|fundedCents|accruedCents|walletSharePercent|walletShareAmountCents|mismatchCents|MismatchChip" --include=*.ts --include=*.tsx apps packages | grep -v node_modules
   ```
   Each hit must be either (a) deleted, or (b) a comment/historical doc note. NO live import/call/JSON-key may remain. Expected residual files to clean if any slipped past earlier plans: archive-wallet.ts, update-wallet.ts, set-wallet-balance.ts, toggle-category-reserve-excluded.ts, any test still importing the allocator.
3. `reserve-balance-repo.ts` + `ports/reserve-balance-repo.ts`: every method here SELECTs the now-DROPPED `category_reserve_balance` VIEW (getForBudget/getExcludedForBudget/getForCategory/getDiscardedForBudget) → all are runtime-broken. GREP for consumers:
   ```
   grep -rn "reserveBalanceRepo\|ReserveBalanceRepo" --include=*.ts apps packages | grep -v node_modules | grep -v "reserve-balance-repo"
   ```
   If NO consumer remains → `git rm` the adapter + port and remove `createReserveBalanceRepo` imports/usages from factory.ts + boot.ts. If a consumer remains, remove that consumer's dependency on it first (it should already read the orchestrator), then delete.
4. Remove the leftover `reserveBalanceRepo` field from `BudgetingModule` (factory.ts) + any boot.ts local if now unused.
5. `bun run typecheck` clean.
   Commit: `refactor(05-16): delete greedy reserve-allocator + dead VIEW reader; grep-clean dead reserve code`.
  </action>
  <verify>
    <automated>cd /home/claude/budget && ! test -f packages/budgeting/src/domain/reserve-allocator.ts && ! grep -rnE "applyExpectedChange|refillUnderfunded|deductFromBottom|setReserveActualMany|reserve_actual_cents|reserveActualCents|category_reserve_balance|walletSharePercent|mismatchCents|MismatchChip" --include=*.ts --include=*.tsx apps packages | grep -v node_modules | grep -vE "^\s*//|^\s*\*|\.planning/"; echo "grep-clean-exit:$?"</automated>
  </verify>
  <done>reserve-allocator.ts + test deleted; the VIEW-reading reserve-balance-repo removed (or reduced to a still-used, non-VIEW method) with no live consumers of the dropped VIEW; the dead-symbol grep returns only comments/docs (the grep above prints NOTHING for live code); monorepo type-checks.</done>
</task>

<task type="auto">
  <name>Task 1: Clean + backfill PL/UK i18n reserve keys</name>
  <files>apps/web/messages/pl.json, apps/web/messages/uk.json</files>
  <action>
1. In `pl.json` + `uk.json`, remove the orphaned `bdp.tab.reserves` keys that EN dropped in 05-15 (`column.expected`, `column.actual`, `column.share`, and `mismatch.*` if present).
2. Add the new keys present in EN: `bdp.tab.reserves.column.reserve`, `column.used`, `surplus.topup`, `surplus.withdraw`, `surplus.reconciled`. Use the EN string as a placeholder value (full PL/UK translation is Phase 8 scope; the goal here is no missing-key runtime errors). Keep JSON valid + key-sorted if the repo lints that.
3. `cd apps/web && bun run test` (or the i18n key-parity check if one exists) to confirm no missing-key warnings for the reserves namespace.
   Commit: `chore(05-16): backfill PL/UK reserve i18n keys for the new model`.
  </action>
  <verify>
    <automated>cd /home/claude/budget && python3 -c "import json; [json.load(open(f)) for f in ['apps/web/messages/pl.json','apps/web/messages/uk.json','apps/web/messages/en.json']]; print('json-valid')" && grep -q "surplus" apps/web/messages/pl.json && grep -q "surplus" apps/web/messages/uk.json; echo "ok:$?"</automated>
  </verify>
  <done>PL + UK JSON are valid; orphaned expected/actual/share/mismatch keys removed; new reserve/used/surplus keys present (EN placeholder ok); reserves namespace has no missing keys across the three locales.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Run the full gates with Docker ON (golden + round-trip + make test + ci-gate + E2E)</name>
  <what-built>The rewrite is code-complete and grep-clean. Now prove it end-to-end with the real stack — per MEMORY.md, Docker MUST be ON and the correct runners used (not the red `make test` sweep count). Frontend was already rebuilt in 05-15; rebuild api if any backend changes landed since.</what-built>
  <action>
Claude runs ALL of these itself (MEMORY.md: verify yourself first; Docker always on):
1. Ensure Docker up; rebuild api if backend changed since 05-15: `docker compose build api && make restart-api`.
2. Domain keystone + round-trip:
   `cd packages/budgeting && bun test test/domain/reserve-engine.test.ts test/domain/reserve-engine-multimonth.test.ts test/domain/reserve-engine-disable.test.ts` → all green (golden 29 rows + accrual/retroactive + disable idempotency).
3. Backend unit + integration: `make test` — verify the BUDGETING + API reserve tests pass (filter to the relevant suites; the global red count includes ~292 pre-existing infra-debt failures per MEMORY.md `project_make_test_infra_debt` — assert the reserve/engine/reserves-summary/spendings/reserve-topup + routes/reserves + routes/spendings-summary suites are green, not the aggregate count).
4. Tenant-leak gate: `make ci-gate` → green (6 security tests).
5. E2E: `PLAYWRIGHT_BASE_URL=$(grep APP_URL apps/web/.env.local | cut -d= -f2) make test-e2e` (or `--grep @tasks-redesign` for the reserves subset) → reserves scenarios green. Use localhost (secure context).
Only escalate to the user if Docker/Infisical/auth blocks a run (then this is the human gate).
  </action>
  <how-to-verify>
1. reserve-engine + multimonth + disable domain tests: 0 fail.
2. Targeted `make test` reserve suites (engine, reserves-summary, get-reserve-positions, get-spendings-summary, reserve-topup, routes/reserves, routes/spendings-summary): 0 fail.
3. `make ci-gate`: 6/6 pass.
4. `make test-e2e --grep @tasks-redesign`: reserves scenarios pass.
  </how-to-verify>
  <resume-signal>Type "gates-green" once the domain golden+round-trip, the targeted reserve suites, ci-gate, and the @tasks-redesign reserves E2E all pass; or paste the first failing output.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update the knowledge graph</name>
  <files>graphify-out/GRAPH_REPORT.md</files>
  <action>
Run `graphify update .` (AST-only, no API cost per CLAUDE.md) so the graph reflects the new `reserve-engine.ts` + `reserve-event-loader-repo` and the deleted `reserve-allocator.ts` + `reserve-balance-repo`. Do NOT hand-edit graph files.
  </action>
  <verify>
    <automated>cd /home/claude/budget && ls graphify-out/GRAPH_REPORT.md >/dev/null 2>&1 && echo "graph-present:0" || echo "graph-missing:1"</automated>
  </verify>
  <done>`graphify update .` ran successfully; the graph no longer references reserve-allocator/reserve-balance-repo and includes reserve-engine/reserve-event-loader.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Final sign-off — rewrite complete</name>
  <what-built>The reserves model rewrite is complete: pure engine reproduces the 29-row golden fixture; disable→re-enable is idempotent; persistence reset (no VIEW, no reserve_actual_cents); replay orchestrator + delta-only adjustments + userDefined-only wallet edits + both deletion modes + surplus RESERVE_TOPUP; reshaped contracts/API/UI; all dead greedy code removed; full gates green; graph updated.</what-built>
  <how-to-verify>
1. Confirm the targeted reserve test suites + the three engine tests + ci-gate + @tasks-redesign reserves E2E are green (from Task 2).
2. Spot-check the app (Reserves tab single reserve+used+surplus banner; spendings used+overspent; toggle reserves off→on returns identical numbers).
3. Confirm `grep -rnE "applyExpectedChange|setReserveActualMany|category_reserve_balance|mismatchCents|walletShare|MismatchChip" --include=*.ts --include=*.tsx apps packages | grep -v node_modules` shows only comments/historical notes, no live code.
  </how-to-verify>
  <resume-signal>Type "rewrite-complete" to close Phase 05 reserves rewrite, or list anything outstanding.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                                                                                                                          |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none new) | Deletion + verification plan. The only runtime change is REMOVING dead code; trust boundaries were established in 05-11/05-13/05-14 and are exercised by the gates run here (ci-gate = tenant-leak). |

## STRIDE Threat Register

| Threat ID | Category               | Component                     | Disposition | Mitigation Plan                                                                                                                                                                                        |
| --------- | ---------------------- | ----------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-0516-01 | Information Disclosure | tenant isolation post-rewrite | mitigate    | `make ci-gate` (6 tenant-leak tests) is a BLOCKING gate in Task 2 — proves the rewritten reserve reads/writes did not open a cross-tenant path.                                                        |
| T-0516-02 | Denial of Service      | dropped-VIEW runtime errors   | mitigate    | Task 0 removes every method that SELECTs the dropped `category_reserve_balance` VIEW; the grep gate + typecheck + `make test` confirm no code path hits the missing VIEW (which would 500 at runtime). |

</threat_model>

<verification>
- `cd packages/budgeting && bun test test/domain/reserve-engine*.test.ts` → 0 fail (golden + multimonth + disable).
- Targeted reserve suites under `make test` + `make ci-gate` (6/6) + `make test-e2e --grep @tasks-redesign` green (run with Docker ON).
- `grep -rnE "applyExpectedChange|refillUnderfunded|deductFromBottom|setReserveActualMany|reserve_actual_cents|category_reserve_balance|walletSharePercent|mismatchCents|MismatchChip" --include=*.ts --include=*.tsx apps packages | grep -v node_modules` → only comment/doc lines.
- `bun run typecheck` clean; `graphify update .` ran.
</verification>

<success_criteria>

- Greedy reserve-allocator (domain + web mirror) + the expected VIEW + reserve_actual_cents + setReserveActualMany + mismatch/walletShare contracts & UI are all GONE (grep-clean for live code).
- Golden fixture reproduces exactly; disable→re-enable idempotent.
- Targeted reserve test suites + ci-gate + @tasks-redesign reserves E2E green with Docker on; type-check clean.
- PL/UK i18n reserve keys backfilled; knowledge graph updated.
  </success_criteria>

<output>
After completion, create `.planning/phases/05-reserves-wallets-tabs/05-16-SUMMARY.md`
</output>
