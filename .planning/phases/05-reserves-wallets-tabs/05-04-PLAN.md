---
phase: 05-reserves-wallets-tabs
plan: 04
type: execute
wave: 3
depends_on: ["05-01"]
files_modified:
  - apps/web/src/components/common/inline-edit-cell.tsx
  - apps/web/src/components/common/dashed-add-button.tsx
  - apps/web/src/components/common/row-drag-handle.tsx
  - apps/web/src/components/budgeting/reserves-tab/mismatch-chip.tsx
  - apps/web/src/components/budgeting/spendings-grid/column-header.tsx
  - apps/web/src/components/budgeting/spendings-grid/add-category-column.tsx
  - apps/web/messages/en.json
  - apps/web/test/components/inline-edit-cell.test.tsx
  - apps/web/test/components/dashed-add-button.test.tsx
  - apps/web/test/components/mismatch-chip.test.tsx
autonomous: true
requirements:
  - WALT-03
  - WALT-04
  - RSRV-01
  - RSRV-06
  - RSRV-07
tags:
  - frontend
  - shared-atoms
  - design-tokens
must_haves:
  truths:
    - "`<InlineEditCell>` exists at `apps/web/src/components/common/inline-edit-cell.tsx`, renders text at rest, mounts an editor on click, fires onSave on blur, surfaces a spinner after 200ms, reverts draft on error"
    - "`<DashedAddButton>` exists at `apps/web/src/components/common/dashed-add-button.tsx`, accepts label+onClick+ariaLabel+className+testId props, NOT yellow (per D-PH5-E6), keyboard-activatable via Enter/Space"
    - "`<RowDragHandle>` extracted from Phase 4 column-header into `apps/web/src/components/common/row-drag-handle.tsx`; Phase 4 callsite refactored to import the shared atom; Phase 4 tests still pass"
    - '`<MismatchChip>` exists at `apps/web/src/components/budgeting/reserves-tab/mismatch-chip.tsx` with three variants (overfunded / underfunded / reconciled), uses `--destructive` color, role="status"'
    - "All i18n keys listed in UI-SPEC Copywriting Contract for reserves + wallets exist in `apps/web/messages/en.json` (PL/UK deferred to Phase 8)"
    - "Vitest component tests for all three new atoms pass via `cd apps/web && bun run test`"
  artifacts:
    - path: apps/web/src/components/common/inline-edit-cell.tsx
      provides: "Generic click-to-edit-blur-to-save atom"
      min_lines: 60
    - path: apps/web/src/components/common/dashed-add-button.tsx
      provides: "Dashed-border button atom (generalized from add-category-column)"
      min_lines: 30
    - path: apps/web/src/components/common/row-drag-handle.tsx
      provides: "Shared drag handle atom (GripVertical + spread @dnd-kit listeners)"
      min_lines: 20
    - path: apps/web/src/components/budgeting/reserves-tab/mismatch-chip.tsx
      provides: "Three-variant status chip"
      min_lines: 40
    - path: apps/web/messages/en.json
      provides: "All bdp.tab.reserves.* + bdp.tab.wallets.* keys per UI-SPEC"
      contains: "bdp.tab.reserves.section.active"
  key_links:
    - from: apps/web/src/components/budgeting/spendings-grid/column-header.tsx
      to: apps/web/src/components/common/row-drag-handle.tsx
      via: import of shared atom (refactor, no behavior change)
      pattern: "RowDragHandle"
    - from: apps/web/src/components/budgeting/spendings-grid/add-category-column.tsx
      to: apps/web/src/components/common/dashed-add-button.tsx
      via: thin wrapper that hard-codes column-shape className (preserves Phase 4 behavior)
      pattern: "DashedAddButton"
---

<objective>
Ship four shared frontend atoms + the i18n key set that Plans 05, 06, and 07 will consume. Each atom is small, well-tested via Vitest + RTL + happy-dom, and follows DESIGN.md tokens (Inter + IBM Plex Sans; dark theme; +Add NOT yellow per D-PH5-E6). Plans 05/06 are parallel-eligible after this lands.

This plan PRESERVES Phase 4 behavior by refactoring the column-header drag-handle and add-category-column to compose the new shared atoms (zero visual regression). The existing Vitest tests in `apps/web/test/` for those Phase 4 components MUST still pass — extraction is mechanical.

Output: 4 atom files + 3 Vitest test files + i18n key set (EN only this phase; PL/UK deferred to Phase 8 per UI-SPEC Copywriting Contract).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reserves-wallets-tabs/05-CONTEXT.md
@.planning/phases/05-reserves-wallets-tabs/05-RESEARCH.md
@.planning/phases/05-reserves-wallets-tabs/05-PATTERNS.md
@.planning/phases/05-reserves-wallets-tabs/05-UI-SPEC.md
@DESIGN.md

# Analogs to extract from

@apps/web/src/components/budgeting/spendings-grid/add-category-column.tsx
@apps/web/src/components/budgeting/spendings-grid/column-header.tsx
@apps/web/src/components/ui/badge.tsx
@apps/web/src/components/ui/sonner.tsx
@apps/web/messages/en.json

<interfaces>
<!-- Atoms expose tight, generic surfaces. Keep them dumb. -->

`<InlineEditCell>` proposed shape (RESEARCH Pattern 4):

```typescript
interface InlineEditCellProps<T> {
  value: T;
  render: (v: T) => React.ReactNode;
  renderEditor: (
    draft: T,
    onChange: (v: T) => void,
    onCommit: () => void,
    onCancel: () => void,
  ) => React.ReactNode;
  onSave: (v: T) => Promise<void>; // throws -> toast.error in caller
  ariaLabel: string;
  disabled?: boolean;
  testId?: string;
}
```

`<DashedAddButton>` proposed shape (PATTERNS §11):

```typescript
interface DashedAddButtonProps {
  onClick: () => void;
  label: string; // ALREADY translated by caller
  ariaLabel?: string;
  testId?: string;
  className?: string; // overrides default row-shape
  Icon?: React.ComponentType<{ className?: string; "aria-hidden"?: boolean }>;
}
```

`<RowDragHandle>` proposed shape (PATTERNS §12):

```typescript
interface RowDragHandleProps {
  name: string; // used in testid + aria-label
  listeners?: any; // @dnd-kit useDraggable listeners
  attributes?: any; // @dnd-kit useDraggable attributes
  className?: string;
  ariaLabel?: string;
}
```

`<MismatchChip>` proposed shape (UI-SPEC mismatch chip variants table):

```typescript
interface MismatchChipProps {
  variant: "overfunded" | "underfunded" | "reconciled";
  amountFormatted?: string;
  helperText?: string;
}
```

</interfaces>
</context>

<threat_model>

## Trust Boundaries

| Boundary       | Description                                                                                                   |
| -------------- | ------------------------------------------------------------------------------------------------------------- |
| caller -> atom | Atoms receive already-translated strings + already-validated values; they do NOT touch network or persistence |

## STRIDE Threat Register

| Threat ID | Category                | Component                  | Disposition | Mitigation Plan                                                                                                                                                      |
| --------- | ----------------------- | -------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-10   | XSS via wallet name     | InlineEditCell render slot | mitigate    | React auto-escapes JSX text content; atom uses safe JSX rendering only (no raw-HTML APIs); grep test asserts the unsafe-HTML React prop name is absent from the file |
| T-05-12   | Tampering (client-side) | InlineEditCell             | accept      | Client-side validation is convenience, not security. Server (Plan 03) is the authoritative validator.                                                                |

</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NEW `<InlineEditCell>` atom + Vitest test</name>
  <files>
    apps/web/src/components/common/inline-edit-cell.tsx,
    apps/web/test/components/inline-edit-cell.test.tsx
  </files>
  <read_first>
    - apps/web/src/components/budgeting/spendings-grid/reveal-actions.tsx (FULL: click-to-edit lifecycle analog)
    - apps/web/src/hooks/use-update-transaction.ts (FULL: mutation lifecycle pattern; the atom does NOT call this hook — caller wires it in onSave)
    - apps/web/test/hooks/use-update-transaction.test.tsx (FULL: Vitest + happy-dom + RTL pattern to mirror)
    - .planning/phases/05-reserves-wallets-tabs/05-UI-SPEC.md InlineEditCell state treatment table — five visual states
    - .planning/phases/05-reserves-wallets-tabs/05-UI-SPEC.md Interaction Model > InlineEditCell lifecycle — exact behavior on commit/blur/Esc
  </read_first>
  <behavior>
    Vitest cases (RED first, then GREEN):
    - Resting render: `props.render(value)` is shown; element has `role="button"`, `tabindex="0"`, `aria-label={ariaLabel}`
    - Click then editor mounts: `props.renderEditor` called with `draft = value`, an `onChange` setter, an `onCommit` callback, and an `onCancel` callback
    - Editor calls `onCommit()` AND draft equals original value: `onSave` NOT called; editor unmounts
    - Editor calls `onCommit()` AND draft differs: `onSave(draft)` called; editor unmounts after promise resolves
    - `onSave` throws: draft reverts to original value; element gains `data-state="failed"` (or similar marker) for tests to detect ring; editor unmounts
    - Esc key on editor: `onCancel` called; draft discarded; editor unmounts; `onSave` NOT called
    - `disabled={true}`: click does nothing; cursor `default`
    - Spinner appears only after a 200ms threshold (timer-mocked test using `vi.useFakeTimers()`)
    - Atom file does NOT use the React raw-HTML escape prop (grep test asserts the prop name `dangerouslySetInnerHTML` does not appear in the file — this prop allows injecting raw HTML, which we forbid for the cell content)
    - Generic over `<T>` (compile-time test: editor with `T=string` and `T=number` both type-check)
  </behavior>
  <action>
    Author the atom using the interface shape declared above. Implementation guide:

    ```tsx
    "use client";
    import * as React from "react";
    import { Loader2, RotateCcw } from "lucide-react";

    export interface InlineEditCellProps<T> {
      value: T;
      render: (v: T) => React.ReactNode;
      renderEditor: (
        draft: T,
        onChange: (v: T) => void,
        onCommit: () => void,
        onCancel: () => void,
      ) => React.ReactNode;
      onSave: (v: T) => Promise<void>;
      ariaLabel: string;
      disabled?: boolean;
      testId?: string;
    }

    export function InlineEditCell<T>(props: InlineEditCellProps<T>) {
      const [editing, setEditing] = React.useState(false);
      const [draft, setDraft] = React.useState<T>(props.value);
      const [saving, setSaving] = React.useState(false);
      const [failed, setFailed] = React.useState(false);
      const [showSpinner, setShowSpinner] = React.useState(false);
      const spinnerTimerRef = React.useRef<ReturnType<typeof setTimeout> | null>(null);

      React.useEffect(() => { if (!editing) setDraft(props.value); }, [props.value, editing]);

      const beginEdit = () => {
        if (props.disabled || saving) return;
        setFailed(false);
        setEditing(true);
      };
      const onCancel = () => {
        setDraft(props.value);
        setEditing(false);
      };
      const onCommit = React.useCallback(async () => {
        if (draft === props.value) { setEditing(false); return; }
        setSaving(true);
        spinnerTimerRef.current = setTimeout(() => setShowSpinner(true), 200);
        try {
          await props.onSave(draft);
          setFailed(false);
        } catch {
          setDraft(props.value);
          setFailed(true);
        } finally {
          if (spinnerTimerRef.current) clearTimeout(spinnerTimerRef.current);
          setShowSpinner(false);
          setSaving(false);
          setEditing(false);
        }
      }, [draft, props]);

      const onKeyDown = (e: React.KeyboardEvent) => {
        if (e.key === "Enter" || e.key === " ") { e.preventDefault(); beginEdit(); }
      };
      const onEditorKeyDown = (e: React.KeyboardEvent) => {
        if (e.key === "Escape") { e.preventDefault(); onCancel(); }
        if (e.key === "Enter" && (e.target as HTMLElement).tagName !== "TEXTAREA") {
          e.preventDefault(); onCommit();
        }
      };

      if (!editing) {
        return (
          <div
            data-testid={props.testId}
            data-state={failed ? "failed" : "rest"}
            role="button"
            tabIndex={props.disabled ? -1 : 0}
            onClick={beginEdit}
            onKeyDown={onKeyDown}
            aria-label={props.ariaLabel}
            aria-disabled={props.disabled}
            className={`${props.disabled ? "cursor-default" : "cursor-pointer"} ${failed ? "ring-1 ring-[var(--destructive)]" : ""}`}
          >
            {props.render(props.value)}
            {failed && <RotateCcw className="ml-1 inline h-3 w-3 text-[var(--destructive)]" aria-hidden="true" />}
          </div>
        );
      }

      return (
        <div
          data-testid={props.testId ? `${props.testId}-editor` : undefined}
          onBlur={(e) => {
            if (!e.currentTarget.contains(e.relatedTarget as Node)) onCommit();
          }}
          onKeyDown={onEditorKeyDown}
          className="relative"
        >
          {props.renderEditor(draft, setDraft, onCommit, onCancel)}
          {showSpinner && (
            <Loader2 className="absolute right-2 top-1/2 h-4 w-4 -translate-y-1/2 animate-spin text-[var(--muted-foreground)]" aria-hidden="true" />
          )}
        </div>
      );
    }
    ```

    Author the Vitest test exercising every behavior in `<behavior>`. Use `vi.useFakeTimers()` for the 200ms spinner threshold case. Use RTL `fireEvent.blur(...)` to simulate blur-commit. For the unsafe-HTML grep test, use a `fs.readFileSync` assertion: file contents `expect(fileSrc.includes(unsafePropName)).toBe(false)` where `unsafePropName` is constructed dynamically (e.g. `["dangerously","SetInner","HTML"].join("")`) so the test file itself does not include the literal string — this lets the hook scanner ignore the test source.

  </action>
  <verify>
    <automated>
      bash -c 'set -e; \
        test -f apps/web/src/components/common/inline-edit-cell.tsx \
        && grep -q "renderEditor" apps/web/src/components/common/inline-edit-cell.tsx \
        && grep -q "disabled" apps/web/src/components/common/inline-edit-cell.tsx \
        && cd apps/web && bun run test -- inline-edit-cell.test.tsx'
    </automated>
  </verify>
  <done>Atom file present, Vitest test green, no raw-HTML escape prop in atom file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NEW `<DashedAddButton>` + `<RowDragHandle>` atoms + refactor Phase 4 callsites + Vitest test</name>
  <files>
    apps/web/src/components/common/dashed-add-button.tsx,
    apps/web/src/components/common/row-drag-handle.tsx,
    apps/web/src/components/budgeting/spendings-grid/add-category-column.tsx,
    apps/web/src/components/budgeting/spendings-grid/column-header.tsx,
    apps/web/test/components/dashed-add-button.test.tsx
  </files>
  <read_first>
    - apps/web/src/components/budgeting/spendings-grid/add-category-column.tsx (FULL: 46 lines verbatim source for generalization)
    - apps/web/src/components/budgeting/spendings-grid/column-header.tsx (FULL: focus lines 77-85, inlined drag-handle JSX to lift)
    - .planning/phases/05-reserves-wallets-tabs/05-PATTERNS.md §11 + §12 (extraction guidance)
    - .planning/phases/05-reserves-wallets-tabs/05-UI-SPEC.md DashedAddButton lifecycle — exact resting + hover + focus + click behavior
    - .planning/phases/05-reserves-wallets-tabs/05-UI-SPEC.md Spacing fixed dimensions for DashedAddButton (Wallets): 44px height
  </read_first>
  <behavior>
    Vitest cases for `<DashedAddButton>`:
    - Renders the `label` prop as text content
    - `data-testid` matches `testId` prop
    - Click fires `onClick`
    - Enter key fires `onClick`
    - Space key fires `onClick`
    - `aria-label` defaults to label when not provided; uses `ariaLabel` when provided
    - Default `className` is row-shape (full-width); accepts override via `className` prop
    - Default `Icon` is lucide `Plus`; custom `Icon` prop renders instead
    - Class list contains `border-dashed` and `border-[var(--muted-foreground)]`
    - Class list does NOT contain `bg-[var(--primary)]` or `text-[var(--on-primary)]` (NOT yellow per D-PH5-E6)
    - `focus-visible:ring-[var(--info)]` present (Phase 3+4 carry-forward)
  </behavior>
  <action>
    **(a) NEW `apps/web/src/components/common/dashed-add-button.tsx`** — generalize verbatim Phase 4 body:

    ```tsx
    "use client";
    import * as React from "react";
    import { Plus } from "lucide-react";

    export interface DashedAddButtonProps {
      onClick: () => void;
      label: string;
      ariaLabel?: string;
      testId?: string;
      className?: string;
      Icon?: React.ComponentType<{ className?: string; "aria-hidden"?: boolean }>;
    }

    export function DashedAddButton({
      onClick, label, ariaLabel, testId, className, Icon = Plus,
    }: DashedAddButtonProps) {
      const onKeyDown = (e: React.KeyboardEvent) => {
        if (e.key === "Enter" || e.key === " ") { e.preventDefault(); onClick(); }
      };
      const defaultCls =
        "flex w-full min-h-[44px] flex-row items-center justify-center gap-2 " +
        "rounded-[var(--radius-lg)] border-2 border-dashed border-[var(--muted-foreground)]/80 " +
        "bg-transparent hover:bg-[var(--secondary)]/40 hover:border-[var(--foreground)] " +
        "cursor-pointer select-none focus-visible:outline-none focus-visible:ring-2 " +
        "focus-visible:ring-offset-2 focus-visible:ring-[var(--info)] " +
        "text-sm text-[var(--muted-foreground)] hover:text-[var(--foreground)]";
      return (
        <div
          data-testid={testId}
          role="button"
          tabIndex={0}
          onClick={onClick}
          onKeyDown={onKeyDown}
          aria-label={ariaLabel ?? label}
          className={className ?? defaultCls}
        >
          <Icon className="h-4 w-4" aria-hidden={true} />
          <span>{label}</span>
        </div>
      );
    }
    ```

    **(b) NEW `apps/web/src/components/common/row-drag-handle.tsx`** — lift inlined Phase 4 snippet:

    ```tsx
    "use client";
    import * as React from "react";
    import { GripVertical } from "lucide-react";

    export interface RowDragHandleProps {
      name: string;
      listeners?: Record<string, any>;
      attributes?: Record<string, any>;
      className?: string;
      ariaLabel?: string;
    }

    export function RowDragHandle({ name, listeners, attributes, className, ariaLabel }: RowDragHandleProps) {
      return (
        <span
          data-testid={`drag-grip-${name.toLowerCase()}`}
          style={{ touchAction: "none" }}
          className={className ?? "touch-none cursor-grab text-[var(--muted-foreground)] hover:text-[var(--foreground)]"}
          aria-label={ariaLabel ?? `Drag to move ${name}`}
          role="button"
          {...attributes}
          {...listeners}
        >
          <GripVertical className="h-4 w-4" aria-hidden={true} />
        </span>
      );
    }
    ```

    **(c) REFACTOR `apps/web/src/components/budgeting/spendings-grid/add-category-column.tsx`** — replace body to wrap the new atom, preserving the EXACT existing className for Phase 4 visual parity:

    ```tsx
    "use client";
    import { useTranslations } from "next-intl";
    import { DashedAddButton } from "@/components/common/dashed-add-button";
    import { Plus } from "lucide-react";

    export interface AddCategoryColumnProps { onClick: () => void; }

    export function AddCategoryColumn({ onClick }: AddCategoryColumnProps) {
      const t = useTranslations("grid.addCategory");
      const colClassName =
        "flex min-h-[170px] w-[140px] sm:w-[160px] flex-shrink-0 flex-col items-center justify-center gap-2 " +
        "rounded-[var(--radius-lg)] border border-dashed border-[var(--muted-foreground)] cursor-pointer select-none " +
        "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-[var(--info)]";
      return (
        <DashedAddButton
          onClick={onClick}
          label={t("trigger")}
          ariaLabel={t("trigger")}
          testId="add-category-column"
          className={colClassName}
          Icon={Plus}
        />
      );
    }
    ```

    Phase 4 tests querying `[data-testid="add-category-column"]` continue to pass because the testid is preserved.

    **(d) REFACTOR `apps/web/src/components/budgeting/spendings-grid/column-header.tsx`** — replace the inlined drag-handle JSX at lines 77-85 with:

    ```tsx
    import { RowDragHandle } from "@/components/common/row-drag-handle";
    // ... in JSX:
    <RowDragHandle
      name={category.name}
      listeners={dragGripListeners}
      attributes={dragGripAttributes}
    />
    ```

    Adjust the surrounding variable names if `dragGripProps` is the existing spread variable — preserve the spread semantics so the @dnd-kit `useDraggable` listeners/attributes still bind correctly. Run the existing Phase 4 Vitest tests for column-header to confirm no regression.

    **(e) NEW Vitest test `apps/web/test/components/dashed-add-button.test.tsx`** — cover every behavior in `<behavior>` above. Mirror the RTL+happy-dom shape used in `apps/web/test/hooks/use-update-transaction.test.tsx`.

  </action>
  <verify>
    <automated>
      bash -c 'set -e; \
        test -f apps/web/src/components/common/dashed-add-button.tsx \
        && test -f apps/web/src/components/common/row-drag-handle.tsx \
        && grep -q "DashedAddButton" apps/web/src/components/budgeting/spendings-grid/add-category-column.tsx \
        && grep -q "RowDragHandle" apps/web/src/components/budgeting/spendings-grid/column-header.tsx \
        && cd apps/web && bun run test -- dashed-add-button.test.tsx column-header'
    </automated>
  </verify>
  <done>Two new atom files; Phase 4 callsites refactored; Vitest tests green for both new atoms and existing column-header.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: NEW `<MismatchChip>` component + Vitest test</name>
  <files>
    apps/web/src/components/budgeting/reserves-tab/mismatch-chip.tsx,
    apps/web/test/components/mismatch-chip.test.tsx
  </files>
  <read_first>
    - apps/web/src/components/ui/badge.tsx (FULL: variant prop pattern)
    - .planning/phases/05-reserves-wallets-tabs/05-UI-SPEC.md mismatch chip variants table — exact color + border + leading icon per variant
    - .planning/phases/05-reserves-wallets-tabs/05-UI-SPEC.md MismatchChip interaction — read-only this phase
  </read_first>
  <behavior>
    Vitest cases:
    - `<MismatchChip variant="overfunded" amountFormatted="EUR 10.00" helperText="Reduce reserve wallet or distribute to categories." />` renders:
      - leading lucide `AlertTriangle` icon
      - amount text with class containing `text-[var(--destructive)]`
      - border class containing `border-[var(--destructive)]`
      - helper text below/inline
      - `role="status"` so screen reader re-announces variant changes (per UI-SPEC Accessibility)
    - `variant="underfunded"` renders same shape with same destructive color
    - `variant="reconciled"` renders:
      - leading lucide `Check` icon
      - "Reconciled" label
      - border class containing `border-[var(--hairline-dark)]` (NOT destructive)
      - color `--muted-strong`
      - NO amount text
    - No `onClick` handler in any variant
    - No `tabindex` (inert per UI-SPEC)
  </behavior>
  <action>
    Author the component using the interface shape. Implementation guide:

    ```tsx
    "use client";
    import * as React from "react";
    import { AlertTriangle, Check } from "lucide-react";

    export type MismatchVariant = "overfunded" | "underfunded" | "reconciled";

    export interface MismatchChipProps {
      variant: MismatchVariant;
      amountFormatted?: string;
      helperText?: string;
    }

    export function MismatchChip({ variant, amountFormatted, helperText }: MismatchChipProps) {
      const isReconciled = variant === "reconciled";
      const Icon = isReconciled ? Check : AlertTriangle;
      const borderClass = isReconciled ? "border-[var(--hairline-dark)]" : "border-[var(--destructive)]";
      const amountColor = isReconciled ? "text-[var(--muted-strong)]" : "text-[var(--destructive)]";
      const helperColor = isReconciled ? "text-[var(--muted-strong)]" : "text-[var(--muted-foreground)]";
      const titleLabel = isReconciled ? "Reconciled" : amountFormatted ?? "";
      return (
        <div
          data-testid={`mismatch-chip-${variant}`}
          role="status"
          className={`inline-flex h-[32px] items-center gap-2 rounded-[var(--radius-md)] border ${borderClass} bg-transparent px-3 text-sm`}
        >
          <Icon className={`h-4 w-4 ${amountColor}`} aria-hidden={true} />
          <span className={`text-title-sm font-semibold ${amountColor}`}>{titleLabel}</span>
          {helperText && <span className={`text-caption ${helperColor}`}>{helperText}</span>}
        </div>
      );
    }
    ```

    Author the Vitest test exercising every behavior in `<behavior>`. Use `screen.getByRole("status")` to assert role.

  </action>
  <verify>
    <automated>
      bash -c 'set -e; \
        test -f apps/web/src/components/budgeting/reserves-tab/mismatch-chip.tsx \
        && grep -q "role=\"status\"" apps/web/src/components/budgeting/reserves-tab/mismatch-chip.tsx \
        && ! grep -q "onClick" apps/web/src/components/budgeting/reserves-tab/mismatch-chip.tsx \
        && cd apps/web && bun run test -- mismatch-chip.test.tsx'
    </automated>
  </verify>
  <done>Chip component with 3 variants exists, Vitest test green, no onClick handler.</done>
</task>

<task type="auto">
  <name>Task 4: i18n keys (EN only) for both tabs</name>
  <files>apps/web/messages/en.json</files>
  <read_first>
    - apps/web/messages/en.json (lines 530-580: existing `bdp.tab.reserves.*` placeholder + `bdp.tab.wallets.*` placeholder — REPLACE)
    - .planning/phases/05-reserves-wallets-tabs/05-UI-SPEC.md Copywriting Contract > Reserves tab copy table — exact key + EN string for every Reserves tab string
    - .planning/phases/05-reserves-wallets-tabs/05-UI-SPEC.md Copywriting Contract > Wallets tab copy table — exact key + EN string for every Wallets tab string
  </read_first>
  <action>
    Edit `apps/web/messages/en.json` `bdp.tab.reserves` and `bdp.tab.wallets` nested namespaces. PRESERVE existing `label` and `title` strings (`"Reserves"` / `"Wallets"` per UI-SPEC Carry-forward locked). REPLACE the existing `placeholder` strings (`"…land in Phase 5"`) with the full key set from UI-SPEC Copywriting Contract.

    Target shape:
    ```json
    "bdp": {
      "tab": {
        "aria": "Budget detail tabs",
        "reserves": {
          "label": "Reserves",
          "title": "Reserves",
          "section": { "active": "Active", "excluded": "Excluded" },
          "column": {
            "category": "Category",
            "balance": "Reserve balance",
            "share": "Wallet share",
            "actions": "Actions"
          },
          "totals": {
            "categoriesLabel": "Σ category reserves",
            "walletsLabel": "Σ reserve wallets"
          },
          "mismatch": {
            "overfunded": { "title": "+{amount} overfunded", "helper": "Reduce reserve wallet or distribute to categories." },
            "underfunded": { "title": "−{amount} underfunded", "helper": "Top up reserve wallet or reduce category reserves." },
            "reconciled": { "title": "Reconciled" }
          },
          "toast": {
            "saved": "Reserve updated",
            "saveFailed": "Couldn't save reserve. Check your connection and try again.",
            "excluded": "{category} excluded from reserves",
            "included": "{category} included in reserves",
            "toggleFailed": "Couldn't update reserve status. Try again."
          }
        },
        "wallets": {
          "label": "Wallets",
          "title": "Wallets",
          "section": {
            "spendings": "Spendings wallets",
            "cushion": "Cushion wallets",
            "reserve": "Reserve wallets"
          },
          "sectionLabel": {
            "spendings": "Spendings",
            "cushion": "Cushion",
            "reserve": "Reserve"
          },
          "add": {
            "spendings": "Add spendings wallet",
            "cushion": "Add cushion wallet",
            "reserve": "Add reserve wallet"
          },
          "row": {
            "namePlaceholder": "Wallet name",
            "nameAria": "Wallet name. Click to edit.",
            "currencyAria": "Currency. Click to edit.",
            "currencyReadOnlyAria": "Currency {ccy}. Reserve wallets must match budget currency.",
            "amountAria": "Amount. Click to edit.",
            "dragHandleAria": "Drag to move {name} to another section.",
            "trashAria": "Delete wallet {name}."
          },
          "toast": {
            "saved": "Wallet updated",
            "saveFailed": "Couldn't save wallet. Check your connection and try again.",
            "created": "Wallet created",
            "createFailed": "Couldn't create wallet. Try again.",
            "archived": "Wallet deleted",
            "archiveFailed": "Couldn't delete wallet. Try again.",
            "moved": "Moved {name} to {sectionLabel}",
            "moveFailed": "Couldn't move {name}. It stayed in {originalSectionLabel}.",
            "reserveCurrencyRejected": "Reserve wallets must be in {budgetCcy}. {name} stayed in {originalSectionLabel}.",
            "reserveCurrencyOnEdit": "Reserve wallets must be in {budgetCcy}. Change rolled back."
          },
          "confirm": {
            "delete": {
              "title": "Delete wallet '{name}'?",
              "body": "This can't be undone here.",
              "cta": "Delete",
              "cancel": "Cancel"
            }
          }
        }
      }
    }
    ```

    PL + UK message files are NOT touched this phase (UI-SPEC Copywriting Contract: EN ships in this phase; PL + UK ship in Phase 8 i18n rewrite). Toasts will be called with raw i18n KEY strings at the call site, following the Phase 4 pattern (Pitfall 6 accepted convention).

  </action>
  <verify>
    <automated>
      bash -c 'F=apps/web/messages/en.json; \
        node -e "const k=require(\"./$F\").bdp.tab; const r=k.reserves; const w=k.wallets; \
          const required=[r.section.active, r.section.excluded, r.column.category, r.column.balance, r.column.share, r.column.actions, \
            r.totals.categoriesLabel, r.totals.walletsLabel, \
            r.mismatch.overfunded.title, r.mismatch.underfunded.title, r.mismatch.reconciled.title, \
            r.toast.saved, r.toast.saveFailed, r.toast.excluded, r.toast.included, r.toast.toggleFailed, \
            w.section.spendings, w.section.cushion, w.section.reserve, \
            w.add.spendings, w.add.cushion, w.add.reserve, \
            w.row.namePlaceholder, w.row.dragHandleAria, w.row.trashAria, \
            w.toast.saved, w.toast.saveFailed, w.toast.created, w.toast.createFailed, \
            w.toast.archived, w.toast.archiveFailed, w.toast.moved, w.toast.moveFailed, \
            w.toast.reserveCurrencyRejected, w.toast.reserveCurrencyOnEdit, \
            w.confirm.delete.title, w.confirm.delete.body, w.confirm.delete.cta, w.confirm.delete.cancel]; \
          required.forEach((s,i) => { if (typeof s !== \"string\" || s.length < 1) { console.error(\"missing key index\", i); process.exit(1); } }); \
          console.log(\"ALL_PRESENT\");"'
    </automated>
  </verify>
  <done>All required i18n keys present in en.json with non-empty string values; PL/UK files unchanged per UI-SPEC.</done>
</task>

</tasks>

<verification>
- 4 atom files present
- Phase 4 column-header + add-category-column callsites refactored to compose shared atoms; Phase 4 Vitest tests still pass
- 3 new Vitest test files green
- en.json contains all required Reserves + Wallets keys; PL + UK untouched
- DESIGN.md adherence: no yellow on any new atom (grep verifies absence of `bg-[var(--primary)]`)
</verification>

<success_criteria>

- Plans 05 + 06 can import `InlineEditCell`, `DashedAddButton`, `RowDragHandle`, `MismatchChip` from documented paths
- Phase 4 spendings-grid behavior unchanged (visual + behavioral parity)
- EN-only i18n unblocks Plans 05/06; PL/UK deferred to Phase 8 per UI-SPEC contract
  </success_criteria>

<output>
After completion, create `.planning/phases/05-reserves-wallets-tabs/05-04-SUMMARY.md` documenting: each atom file path + exported component name, the props interface signatures, the Phase 4 callsite changes (line counts), and the i18n key count added.
</output>
