---
phase: 05-reserves-wallets-tabs
plan: 10
type: tdd
wave: 2
depends_on: [9]
files_modified:
  - packages/budgeting/test/domain/reserve-engine-multimonth.test.ts
  - packages/budgeting/test/domain/reserve-engine-disable.test.ts
  - packages/budgeting/src/domain/reserve-engine.ts
autonomous: true
requirements: [RSRV-REWRITE-ENGINE]
must_haves:
  truths:
    - "Closed-month underspend accrues into the running reserve and later months can draw it (decision G)."
    - "Raising reserve (adjust or accrual) covers OUTSTANDING overspent across ALL closed months oldest-first (decision I)."
    - "Disabling reserves makes every category report used→overspent and internal hidden, WITHOUT mutating underlying state (decision K)."
    - "Re-enabling replays the full history and reproduces R/U/overspent identical to the pre-disable values (idempotent round-trip)."
  artifacts:
    - path: "packages/budgeting/test/domain/reserve-engine-multimonth.test.ts"
      provides: "Multi-month accrual + retroactive-coverage dedicated fixtures (decisions G, I)."
      min_lines: 80
    - path: "packages/budgeting/test/domain/reserve-engine-disable.test.ts"
      provides: "Disable→re-enable idempotency round-trip over a multi-month, multi-adjustment history (decision K)."
      min_lines: 70
  key_links:
    - from: "packages/budgeting/test/domain/reserve-engine-disable.test.ts"
      to: "packages/budgeting/src/domain/reserve-engine.ts"
      via: "reserveEngine({ reservesEnabled: false }) vs true comparison"
      pattern: "reservesEnabled:\\s*false"
---

<objective>
Lock the parts of the model the single-open-month golden table does NOT exercise: multi-month accrual carry, retroactive overspent coverage (oldest-first), and the disable→re-enable idempotency round-trip. These are dedicated domain tests against the keystone `reserve-engine.ts` from 05-09; any engine gaps they expose are fixed here.

Purpose: Decisions G (accrual at closed-month close), I (retroactive coverage oldest-first), and K (disable = read-transform, re-enable replays identically) are the model's hardest invariants and are untested by the golden fixture. This plan proves them with their own fixtures before the engine is wired into persistence.
Output: Two new domain test files + any engine adjustments needed to pass them. RED → GREEN per behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reserves-wallets-tabs/05-REWRITE-SPEC.md

<read_first>

- packages/budgeting/src/domain/reserve-engine.ts (from 05-09 — the engine under test; may need edits for the disable transform + accrual ordering)
- packages/budgeting/test/domain/reserve-engine.test.ts (from 05-09 — golden + op-unit style to mirror)
- .planning/phases/05-reserves-wallets-tabs/05-REWRITE-SPEC.md — decisions G/H/I/K (lines 80-84) + "Beyond the golden table" (lines 132-141). Copy decision text into assertions as comments.
  </read_first>

<interfaces>
<!-- Uses the 05-09 ReserveEngineEvent / ReserveEngineResult contracts. Key events for this plan: -->
- `{ type: "accrual"; categoryId; month }` — emitted by the test for each CLOSED month, in chronological order, to fold that month's `left` into R (op 4).
- `{ type: "setUserDefined"; cents }` — for surplus assertions.
- `reservesEnabled: false` in the `reserveEngine(...)` call — triggers the decision-K output transform.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 0: Multi-month accrual + retroactive-coverage tests (RED→GREEN)</name>
  <files>packages/budgeting/test/domain/reserve-engine-multimonth.test.ts, packages/budgeting/src/domain/reserve-engine.ts</files>
  <behavior>
    - Accrual (G): Grocery limit 300/mo, spend 100 in 2026-01 (left 200) → at 2026-01 close, accrual makes R = 200. In 2026-02 spend 500 (overage 200) → op1 draws 200 from R → used 200, R 0, overspent 0.
    - Multi-month carry: a second closed month's `left` adds to the SAME running R (R accumulates across months).
    - Retroactive coverage (I): two closed months each overspent (2026-01 overage 100, 2026-02 overage 100) with R=0 → overspent total 200 (used 0). Then `adjust` reserve to 150 (d=150) → cover min(150, overspent 200)=150 → U=150; per-month split OLDEST-FIRST: 2026-01 used 100 (fully covered), 2026-02 used 50, overspent 50. R ends 0. Assert each month cell.
  </behavior>
  <action>
1. Create `packages/budgeting/test/domain/reserve-engine-multimonth.test.ts` (bun:test, mirror 05-09 style). Build `events` arrays directly (no CSV).
2. Test "closed-month underspend accrues then a later month draws it" (decision G — quote at top of test):
   - Events (chronological): setLimit Grocery 2026-01 normal 300/cushion 300; spendDelta Grocery 2026-01 +10000 (i.e. 100.00); accrual Grocery 2026-01; setLimit Grocery 2026-02 normal 300/cushion 300; spendDelta Grocery 2026-02 +50000.
   - Call `reserveEngine({ events, openMonth: "2026-02" })`.
   - Assert: 2026-01 cell left=20000, overspent 0, used 0; 2026-02 cell overage 20000, used 20000, overspent 0; final R=0, U=20000.
3. Test "accrual accumulates across two closed months":
   - Grocery 300/mo; spend 100 in 2026-01 and 200 in 2026-02; accrual after EACH. openMonth 2026-03 (both closed).
   - Assert R after both accruals = (300−100)+(300−200) = 200+100 = 30000 cents; U=0.
4. Test "retroactive coverage covers outstanding overspent oldest-first" (decision I — quote it):
   - Grocery 300/mo; spend 400 in 2026-01 (overage 100) and 400 in 2026-02 (overage 100); accrual after each (left 0 both, so R stays 0, overspent accumulates). openMonth 2026-03.
   - Pre-adjust assert: 2026-01 overspent 10000, 2026-02 overspent 10000, U=0, R=0.
   - Append `adjust Grocery delta 15000` (set reserve to 150 while R=0 → d=15000). Re-run engine.
   - Assert: U=15000, R=0; 2026-01 cell used 10000 / overspent 0; 2026-02 cell used 5000 / overspent 5000 (oldest-first split). Internal = R = 0.
5. Run `cd packages/budgeting && bun test test/domain/reserve-engine-multimonth.test.ts`. Fix `reserve-engine.ts` ONLY where these reveal a gap (most likely: oldest-first per-month `used` split, and op-4 accrual ordering). Re-run to green.
   Commit: `test(05-10): multi-month accrual + retroactive coverage (RED)` then `feat(05-10): engine accrual + oldest-first retroactive split (GREEN)` (squash to one commit if the engine needed no change).
  </action>
  <verify>
    <automated>cd packages/budgeting && bun test test/domain/reserve-engine-multimonth.test.ts 2>&1 | tail -4 | grep -qE "0 fail|pass"; echo "green:$?"</automated>
  </verify>
  <done>Accrual, multi-month carry, and oldest-first retroactive-coverage tests all pass; engine still passes the 05-09 golden test (run it to confirm no regression).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 1: Disable→re-enable idempotency round-trip (RED→GREEN)</name>
  <files>packages/budgeting/test/domain/reserve-engine-disable.test.ts, packages/budgeting/src/domain/reserve-engine.ts</files>
  <behavior>
    - Build a non-trivial multi-month history (2+ closed months with accrual, ≥1 adjustment, ≥1 category overspending, userDefined set).
    - ENABLED result: capture per-category R, U, and per-month overspent/used.
    - DISABLED result (`reservesEnabled:false`, SAME events): every category's per-month `used` === 0, `overspent` === that month's `overage` (the full overage), and `internalCents` === 0. Underlying states are NOT mutated (re-running enabled after disabled yields identical numbers).
    - RE-ENABLED result (`reservesEnabled:true` again, same events) === the original ENABLED result exactly (R, U, every cell). Idempotent.
  </behavior>
  <action>
1. Create `packages/budgeting/test/domain/reserve-engine-disable.test.ts` (bun:test). Quote decision K at the top.
2. Build a shared `events` fixture: two categories (Grocery, Housing); months 2026-01..2026-02 closed with limits, varied spend (some under, some over), accrual after each; one `adjust` mid-stream; one `setUserDefined 300000`; openMonth 2026-03.
3. `const enabled = reserveEngine({ events, openMonth: "2026-03", reservesEnabled: true });`
   `const disabled = reserveEngine({ events, openMonth: "2026-03", reservesEnabled: false });`
   `const reEnabled = reserveEngine({ events, openMonth: "2026-03", reservesEnabled: true });`
4. Assertions:
   - Disabled transform: for every cell in `disabled.cells`, `expect(cell.usedCents).toBe(0n)` and `expect(cell.overspentCents).toBe(cell.overageCents)`. `expect(disabled.internalCents).toBe(0n)`.
   - No mutation / idempotency: deep-compare `reEnabled` to `enabled` — same `internalCents`, `surplusCents`, same `states` (R,U per category), and same per-cell `usedCents`/`overspentCents`/`overageCents`/`leftCents`. Write a helper `expectResultsEqual(a, b)` that iterates states + sorts cells by (categoryId,month) and asserts each field.
   - Sanity: `enabled` has at least one cell with `usedCents > 0n` (proves the disable transform actually changed something).
5. Run `cd packages/budgeting && bun test test/domain/reserve-engine-disable.test.ts`. If the engine mutates shared state across calls (it should be pure per-call), fix so each `reserveEngine(...)` call folds from scratch. Re-run to green.
   Commit: `test(05-10): disable→re-enable idempotency round-trip (RED)` then `feat(05-10): decision-K disable read-transform, idempotent re-enable (GREEN)`.
  </action>
  <verify>
    <automated>cd packages/budgeting && bun test test/domain/reserve-engine-disable.test.ts 2>&1 | tail -4 | grep -qE "0 fail|pass"; echo "green:$?"</automated>
  </verify>
  <done>Disabled output zeroes used + reports full overage as overspent + internal 0; re-enabled output is byte-for-byte equal to the original enabled output; engine remains a pure per-call fold. The 05-09 golden test still passes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                           |
| -------- | --------------------------------------------------------------------- |
| (none)   | Domain-only test plan. No IO, no tenant data, no new runtime surface. |

## STRIDE Threat Register

| Threat ID | Category  | Component              | Disposition | Mitigation Plan                                                                                                                                                                                                          |
| --------- | --------- | ---------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-0510-01 | Tampering | disable read-transform | mitigate    | Decision-K transform must NOT mutate stored state (else disabling would silently corrupt reserves). The idempotency round-trip test (re-enable === enable) is the mitigation — it fails loudly if the transform mutates. |

</threat_model>

<verification>
- `cd packages/budgeting && bun test test/domain/reserve-engine-multimonth.test.ts test/domain/reserve-engine-disable.test.ts` → all green.
- Regression: `cd packages/budgeting && bun test test/domain/reserve-engine.test.ts` → still green (engine edits did not break the golden fixture).
- `grep -nE "drizzle|@budget/platform|persistence" packages/budgeting/src/domain/reserve-engine.ts` → still nothing (engine stayed pure).
</verification>

<success_criteria>

- Multi-month accrual + carry + retroactive-coverage (oldest-first) tests pass.
- Disable zeroes used and surfaces full overage as overspent; internal hidden (0).
- Re-enable round-trip is provably idempotent vs the enabled run.
- No regression in the 05-09 golden fixture; engine remains pure.
  </success_criteria>

<output>
After completion, create `.planning/phases/05-reserves-wallets-tabs/05-10-SUMMARY.md`
</output>
