---
phase: 09-investments-wallet
plan: 06
type: execute
wave: 3
depends_on: [09-01, 09-02, 09-03, 09-05]
autonomous: true
requirements:
  [INV-01, INV-03, INV-04, INV-05, INV-07, INV-08, INV-09, INV-10, INV-14]
# NOTE: 18 files / 3 tasks is INTENTIONAL — this plan is one cohesive API surface
# (contracts + 7 use-cases + factory in Task 1; route + boot/app wiring in Task 2;
# flag plumbing + un-skip tests in Task 3). The plan-checker blessed proceed-as-is
# for architectural coherence; a split would fracture the DI factory across waves
# and risk INV coverage. Each task stays bounded (~6-10 files).
files_modified:
  - packages/investments/src/contracts/api.ts
  - packages/investments/src/contracts/factory.ts
  - packages/investments/src/application/create-holding.ts
  - packages/investments/src/application/update-holding.ts
  - packages/investments/src/application/archive-holding.ts
  - packages/investments/src/application/list-holdings.ts
  - packages/investments/src/application/reorder-holdings.ts
  - packages/investments/src/application/search-instruments.ts
  - packages/investments/src/application/fetch-instrument-price.ts
  - packages/investments/src/index.ts
  - apps/api/src/routes/investments.ts
  - apps/api/src/boot.ts
  - apps/api/src/app.ts
  - apps/api/src/routes/budget-identity.ts
  - packages/tenancy/src/adapters/persistence/workspace-repo.ts
  - packages/tenancy/src/ports/budget-repo.ts
  - packages/tenancy/src/contracts/api.ts
  - apps/api/test/routes/investments.test.ts

must_haves:
  truths:
    - "investments_enabled round-trips: PATCH /budgets/:id {investments_enabled} persists and GET /budgets/:id DTO returns investmentsEnabled"
    - "POST/GET/PATCH/POST-archive/POST-reorder /budgets/:id/investments exist; GET enriches each holding with value, P/L %, and weight % (budget-ccy denominator)"
    - "hourly price refresh surfaces in GET /investments (list reads latest instrument_price_cache for tracked holdings via JOIN, NOT the add-time holding-row price; custom holdings keep their holding-row price)"
    - "GET /budgets/:id/investments/search?q= returns local trigram matches; never calls a price provider"
    - "POST add with an uncached tracked instrument triggers an instant price fetch rate-limited to 10/user/min via budgeting.api_rate_limits (shipped in 09-01 migration 0038, no 0039); the 11th in a minute blocks the save (A2) with a clear error"
    - "Holdings reorder rejects ids not in the requesting tenant's investments (holding_id_not_in_section), mirroring the wallet guard"
    - "The P05 investments route tests are un-skipped and green (real Postgres)"
  artifacts:
    - path: "apps/api/src/routes/investments.ts"
      provides: "Hono investments route factory (CRUD + search + reorder + on-add fetch)"
      contains: "createInvestmentsRoute"
    - path: "packages/investments/src/contracts/api.ts"
      provides: "Zod schemas for create/update/reorder/search/holding-type enum"
      contains: "createHoldingSchema"
    - path: "packages/investments/src/contracts/factory.ts"
      provides: "InvestmentsModule DI factory"
      contains: "createInvestmentsModule"
  key_links:
    - from: "apps/api/src/routes/budget-identity.ts"
      to: "packages/tenancy workspace-repo update patch"
      via: "investments_enabled body field -> investmentsEnabled patch (mirrors cushion_enabled)"
      pattern: "investments_enabled"
    - from: "packages/investments/src/application/list-holdings.ts"
      to: "budgeting.instrument_price_cache"
      via: "read-time JOIN on instrument_id overrides current_price_cents for tracked holdings (hourly cron surfaces here)"
      pattern: "instrument_price_cache"
    - from: "packages/investments/src/application/fetch-instrument-price.ts"
      to: "budgeting.api_rate_limits"
      via: "atomic per-user/minute upsert+read of the 10/user/min counter (table from 09-01 migration 0038)"
      pattern: "api_rate_limits"
    - from: "apps/api/src/routes/investments.ts"
      to: "packages/investments factory use-cases"
      via: "deps.investments.createHolding / listHoldings / reorderHoldings / searchInstruments / fetchInstrumentPrice"
      pattern: "deps.investments"
    - from: "apps/api/src/app.ts"
      to: "createInvestmentsRoute"
      via: "app.route('/budgets/:budgetId/investments', createInvestmentsRoute(deps))"
      pattern: "createInvestmentsRoute"

threat_model:
  - id: T-9-14
    stride: Elevation of Privilege (authz)
    surface: investments routes
    mitigation: "Every handler calls pickTenant(c) + membership is enforced by the same middleware gating /budgets/:budgetId/* (existing pattern); RLS is the second layer. No route trusts a client-supplied tenant."
  - id: T-9-15
    stride: Tampering (cross-section reorder)
    surface: POST /investments/reorder
    mitigation: "reorderHoldings validates all orderedIds belong to the requesting tenant's investments; returns holding_id_not_in_section (422) otherwise — mirrors wallet_id_not_in_section (Pitfall 8)."
  - id: T-9-16
    stride: Elevation of Privilege (rate-limit bypass / denial-of-wallet)
    surface: on-add instant price fetch (10/user/min)
    mitigation: "DB-side atomic counter budgeting.api_rate_limits(user_id, window_min, count) — table shipped in 09-01 migration 0038 (no 0039). fetch-instrument-price upserts (date_trunc('minute', now())) ON CONFLICT DO UPDATE SET count = count+1 RETURNING count, server-side, not client-controlled (Pitfall 4). The 11th defers — and per A2 the save BLOCKS (no pending holding) rather than fetching."
  - id: T-9-17
    stride: Input Validation
    surface: POST/PATCH bodies
    mitigation: "zValidator/safeParse on every state-changing endpoint; holding_type constrained to the 9-value Zod enum; numeric fields parsed via big.js at the boundary; symbol for search is a bound parameter (T-9-08)."
---

<objective>
Build the Investments API surface: Zod contracts, application use-cases (create/update/archive/list/reorder/search/fetch-price + portfolio enrichment), the `InvestmentsModule` factory, the Hono `investments.ts` route file mounted under `/budgets/:budgetId/investments`, boot wiring, and the `investments_enabled` flag plumbed through the budgets DTO + PATCH + tenancy repo/port/contracts (mirroring `cushion_enabled`). Implement the on-add rate-limited (10/user/min) instant price fetch that BLOCKS the save when no price can be obtained (A2), backed by the `budgeting.api_rate_limits` table shipped in 09-01 (no migration is authored here). Un-skip the P05 route integration tests.

Purpose: This is the server contract every web component (P07) consumes (INV-01 flag, INV-03/04 CRUD, INV-05 group autocomplete via existing-groups, INV-07 search, INV-08/09/10 enriched read, INV-14 rate-limited add). It composes the P03 repos + P02 domain math + existing FxProvider. Holdings live under the budget route namespace exactly like tasks/categories. NOTE: 18 files across 3 tasks is intentional and architecturally cohesive (the checker approved proceed-as-is); the plan is NOT split because the DI factory + its consumers must land together within wave 3 and a split would fracture INV coverage across waves with no scope benefit. Each task stays bounded (~6-10 files).
Output: contracts + 7 use-cases + factory + route + boot/app wiring + flag plumbing + green route tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-investments-wallet/09-SPEC.md
@.planning/phases/09-investments-wallet/09-RESEARCH.md
@.planning/phases/09-investments-wallet/09-PATTERNS.md
@apps/api/src/routes/wallets.ts
@packages/investments/src/adapters/persistence/holding-repo.ts
@packages/investments/src/adapters/persistence/instrument-repo.ts
@packages/investments/src/domain/portfolio-metrics.ts

INTERFACES — exact analogs; use directly:

Route factory pattern (apps/api/src/routes/wallets.ts — copy verbatim structure):
export function createInvestmentsRoute(deps: BootedDeps) { const app = new Hono<{Variables: Record<string,any>}>(); function pickTenant(c){ const ids = c.get("tenantIds") as string[]|undefined; return ids?.[0] ?? ""; } ... return app; }
Endpoints to mount (mirror the wallet handlers; tenant from pickTenant, userId from c.get('userId') ?? session.user.id):
POST / -> deps.investments.createHolding({ ...parsed, tenantId, budgetId, actorUserId })
GET / -> deps.investments.listHoldings({ tenantId, budgetId }) (enriched: value/PL%/weight%)
GET /search?q= -> deps.investments.searchInstruments(q) (local trigram; min 2 chars)
POST /price/:instrumentId -> deps.investments.fetchInstrumentPrice({ instrumentId, userId }) (rate-limited; 429-or-block on overflow)
PATCH /:id -> deps.investments.updateHolding({ ...parsed, tenantId, budgetId, holdingId, actorUserId })
POST /:id/archive -> deps.investments.archiveHolding({ tenantId, budgetId, holdingId, actorUserId })
POST /reorder -> deps.investments.reorderHoldings({ tenantId, actorUserId, orderedIds }) ; on holding_id_not_in_section -> 422 (mirror wallets.ts:241-246)

Reorder guard analog (apps/api/src/routes/wallets.ts:213-248): reorderWalletsSchema -> reorderWallets; reject wallet_id_not_in_section. Mirror as reorderHoldingsSchema -> reorderHoldings rejecting holding_id_not_in_section.

App mount (apps/api/src/app.ts:105 tasks pattern): app.route("/budgets/:budgetId/investments", createInvestmentsRoute(deps)); add the import. Mount alongside the other /budgets/:budgetId/\* sub-routes.

Boot wiring (apps/api/src/boot.ts:42-82 BootedDeps + :160 createBudgetingModule): create the investments module via createInvestmentsModule({ fxCache or fxProvider, holdingRepo, instrumentRepo, priceCacheRepo, priceProvider }) and add `investments: ReturnType<typeof createInvestmentsModule>` to BootedDeps (intersection like budgeting). Reuse the existing fxProvider (baseBudgeting.fxProvider) for enrichment conversion — do NOT build a second FX path (SPEC constraint).

Flag plumbing (mirror cushion_enabled EXACTLY):

- packages/tenancy/src/adapters/persistence/workspace-repo.ts: add investments_enabled to the SELECT list (line ~42), to the DTO map (`investmentsEnabled: row.investments_enabled ?? false`, line ~60), and to the update patch block (line ~267: if patch.investmentsEnabled !== undefined -> UPDATE tenancy.budgets SET investments_enabled = ...).
- packages/tenancy/src/ports/budget-repo.ts: add investmentsEnabled?: boolean to the patch type (line ~15).
- packages/tenancy/src/contracts/api.ts: add investmentsEnabled?: boolean to the budget DTO type (line ~14, beside reservesEnabled).
- apps/api/src/routes/budget-identity.ts: patchBudgetSchema (line 16) add investments_enabled: z.boolean().optional(); in the patch-apply block (line ~157) add ...(body.investments_enabled !== undefined ? { investmentsEnabled: body.investments_enabled } : {}).
- apps/api/src/routes/budgets.ts GET DTO (line ~155): the DTO already spreads from workspace-repo; ensure investmentsEnabled is surfaced (add `investmentsEnabled: budget.investmentsEnabled ?? false` next to reservesEnabled/cushionEnabled at line 155-156).
  (Onboarding wizard create payload: the create path already passes feature flags; ensure investments_enabled is accepted in the create schema if the wizard sends it — mirror reserves_enabled. The wizard UI toggle itself is P07.)

Zod contracts (packages/investments/src/contracts/api.ts) — mirror packages/budgeting/src/contracts/api.ts:8-39 (walletTypeSchema/createWalletSchema/reorderWalletsSchema):
holdingTypeSchema = z.enum(["equities","etf","bond","crypto","reit","commodity","cash_fx","real_estate","other"])
createHoldingSchema = z.object({ name, holdingType: holdingTypeSchema, group: z.string().nullish(), instrumentId: z.string().uuid().nullish(), buyPriceCents: <bigint-as-string or number>, buyCurrency: z.string().length(3), quantity: z.string(), currentPriceCents, currentPriceCurrency }) (cash_fx variant: amount + currency only — accept via refine or a discriminated shape; value-only, no P/L per D-31)
updateHoldingSchema = createHoldingSchema.partial()
reorderHoldingsSchema = z.object({ orderedIds: z.array(z.string().uuid()) })
Export inferred types. Numeric inputs accept comma+dot — normalize to dot before big.js (D-15) at the use-case boundary.

Rate-limit (INV-14, Pitfall 4): fetch-instrument-price.ts checks the DB counter budgeting.api_rate_limits(user_id, window_min timestamptz, count int) — table ALREADY EXISTS from 09-01 migration 0038, do NOT author any migration here and do NOT reuse another table. Atomic upsert per user per minute:
INSERT INTO budgeting.api_rate_limits (user_id, window_min, count) VALUES (<userId>::uuid, date_trunc('minute', now()), 1) ON CONFLICT (user_id, window_min) DO UPDATE SET count = budgeting.api_rate_limits.count + 1 RETURNING count;
if the returned count >= 10 (i.e. this is the 11th in the minute), return a typed RateLimited result -> route returns 429; the create flow treats no-price as a BLOCKED save (A2) and surfaces the price-blocked error (P07 renders the banner). On count <= 10, call priceProvider.currentPrice + upsert instrument_price_cache + return the price.

list-holdings enrichment (INV-08/09/10) + read-time price JOIN (B4 — the hourly cron must surface here):
For each active holding compute via P02 portfolio-metrics: totalValue (qty x current), profitLossPct, weight%. CRITICAL price source per holding:

- TRACKED holding (instrument_id IS NOT NULL): JOIN budgeting.instrument_price_cache c ON c.instrument_id = inv.instrument_id and use c.price / c.currency as the CURRENT price, OVERRIDING the holding row's current_price_cents/current_price_currency. This is what makes the hourly cron (which upserts instrument_price_cache) visible in GET /investments — the add-time holding-row price is the optimistic fallback only (used when no cache row exists yet). (RESEARCH A5: holding-row current_price_cents stays the simple optimistic-add write target; the read prefers the cache via JOIN.)
- CUSTOM holding (instrument_id IS NULL): use the holding row's current_price_cents/current_price_currency as-is (no cache row exists for custom holdings).
  Convert each holding's current price to the budget default currency via the existing fxProvider rate map (convert current->buy ccy for P/L when currencies differ; cash_fx returns null P/L; weight denominator in budget ccy). Join budgeting.instruments to expose isDelisted (instruments.active=false) so the row can render the delisted chrome. Batch unique currency pairs (one rate lookup per pair, like wallets.ts:98-122).
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zod contracts + 7 use-cases + factory (with read-time price JOIN enrichment + api_rate_limits-backed fetch)</name>
  <read_first>
    - packages/budgeting/src/contracts/api.ts:8-39 (walletTypeSchema/createWalletSchema/reorderWalletsSchema — exact Zod shapes to mirror)
    - packages/budgeting/src/contracts/factory.ts:1-60 (DI factory wiring shape — createBudgetingModule)
    - packages/investments/src/domain/portfolio-metrics.ts (P02 — the enrichment math to call)
    - packages/investments/src/adapters/persistence/holding-repo.ts + instrument-repo.ts + price-cache-repo.ts (P03 — the repos to inject; price-cache-repo is the read side of the list JOIN)
    - apps/api/src/routes/wallets.ts:98-138 (batch FX enrichment pattern for the list read)
    - 09-RESEARCH.md Pitfall 4 (rate-limit counter) + Open Questions RESOLVED Q2 (api_rate_limits is in 0038) + A5 (holding-row price is the optimistic write; read prefers cache) + Architectural Responsibility Map
  </read_first>
  <behavior>
    - createHoldingSchema rejects a holding_type outside the 9-value enum (422).
    - listHoldings enrichment + read-time JOIN: for a TRACKED holding whose instrument_price_cache row was refreshed to a NEW price AFTER the holding was added, listHoldings returns the cache price (not the stale add-time holding-row current_price_cents) — assert a refreshed cache price appears in the list response while the holding row's current_price_cents is unchanged. A CUSTOM holding (instrument_id null) returns its holding-row price unchanged. An EUR-bought USD-priced holding returns value + P/L computed after USD->EUR conversion; a cash_fx holding returns null P/L and a value equal to its amount; weights within a group sum ~100%.
    - fetchInstrumentPrice (api_rate_limits-backed): 1st-10th call in a minute for a user returns a live price; the 11th returns RateLimited (no provider call) — assert the count is read from budgeting.api_rate_limits against a real test Postgres.
    - reorderHoldings returns holding_id_not_in_section when an id is not in the tenant's investments.
  </behavior>
  <action>
    1. contracts/api.ts: author holdingTypeSchema, createHoldingSchema, updateHoldingSchema, reorderHoldingsSchema, searchQuerySchema + inferred types (mirror budgeting/contracts/api.ts). Normalize comma->dot for numeric strings before big.js.
    2. application/: create-holding.ts, update-holding.ts, archive-holding.ts (set archived_at), list-holdings.ts (calls HoldingRepo.listForBudget + JOINs instrument_price_cache to OVERRIDE current price for tracked holdings / keeps holding-row price for custom + enriches via portfolio-metrics + fxProvider rate map + instruments.active for delisted), reorder-holdings.ts (validates ids in tenant, holding_id_not_in_section), search-instruments.ts (InstrumentRepo.search, min 2 chars), fetch-instrument-price.ts (atomic upsert of budgeting.api_rate_limits per user/minute + RateLimited result when count>=10 + priceProvider.currentPrice + cache upsert; NO migration authored — table is from 0038). Use Result/neverthrow shape consistent with budgeting use-cases.
    3. contracts/factory.ts: createInvestmentsModule({ fxProvider, holdingRepo, instrumentRepo, priceCacheRepo, priceProvider }) returning { createHolding, updateHolding, archiveHolding, listHoldings, reorderHoldings, searchInstruments, fetchInstrumentPrice }. Export from packages/investments/src/index.ts.
    Add tests to apps/api/test or packages/investments/test for the read-time-JOIN + enrichment + rate-limit + reorder-guard behaviors (real Postgres where a repo is involved).
  </action>
  <verify>
    <automated>cd /home/claude/budget && bun run typecheck 2>&1 | tail -8 && bun test packages/investments/test/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - contracts/api.ts contains createHoldingSchema and a 9-value holdingTypeSchema enum
    - factory.ts exports createInvestmentsModule with all 7 use-cases
    - list-holdings.ts references instrument_price_cache (read-time JOIN) and overrides current_price for tracked holdings
    - fetch-instrument-price.ts references budgeting.api_rate_limits (reads/writes the counter; no migration authored in this plan)
    - a test proves a refreshed instrument_price_cache price appears in listHoldings while the holding row's current_price_cents is unchanged (cache wins for tracked holdings)
    - bun run typecheck exits 0; packages/investments tests green
  </acceptance_criteria>
  <done>Contracts + use-cases + factory exist; read-time price JOIN (cache overrides tracked, holding-row for custom), api_rate_limits-backed rate-limit, and reorder-guard behaviors tested.</done>
</task>

<task type="auto">
  <name>Task 2: Hono investments route + boot/app wiring</name>
  <read_first>
    - apps/api/src/routes/wallets.ts (full file — the route factory + reorder guard to mirror)
    - apps/api/src/boot.ts:42-82 (BootedDeps) + :160 (createBudgetingModule wiring — add investments module beside it)
    - apps/api/src/app.ts:105 (tasks route mount pattern under /budgets/:budgetId/*)
    - packages/investments/src/contracts/factory.ts (Task 1 output — the module to wire)
  </read_first>
  <action>
    1. apps/api/src/routes/investments.ts: createInvestmentsRoute(deps) with the 7 endpoints from the INTERFACES block (POST/GET/GET-search/POST-price/PATCH/POST-archive/POST-reorder). zValidator/safeParse on POST + PATCH + reorder bodies; pickTenant on every handler; error mapping (not_found -> 404, holding_id_not_in_section -> 422, RateLimited -> 429). The price-blocked-on-add (A2) path returns a 4xx with a body the UI maps to the price-blocked banner.
    2. boot.ts: instantiate the investments repos (DrizzleHoldingRepo via appPool, DrizzleInstrumentRepo + DrizzlePriceCacheRepo) + the composite priceProvider (keys from deps.env), call createInvestmentsModule, add `investments:` to BootedDeps. Reuse baseBudgeting.fxProvider for enrichment.
    3. app.ts: import createInvestmentsRoute; app.route("/budgets/:budgetId/investments", createInvestmentsRoute(deps)).
  </action>
  <verify>
    <automated>cd /home/claude/budget && grep -q "createInvestmentsRoute" apps/api/src/app.ts && grep -q "holding_id_not_in_section" apps/api/src/routes/investments.ts && bun run typecheck 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/routes/investments.ts contains createInvestmentsRoute with POST/GET/PATCH/archive/reorder/search/price endpoints
    - app.ts mounts /budgets/:budgetId/investments; boot.ts adds investments to BootedDeps
    - reorder rejects with holding_id_not_in_section
    - bun run typecheck exits 0
  </acceptance_criteria>
  <done>Investments route mounted and wired into boot; reorder guard + rate-limit error mapping in place.</done>
</task>

<task type="auto">
  <name>Task 3: Surface investments_enabled flag + un-skip P05 route tests</name>
  <read_first>
    - packages/tenancy/src/adapters/persistence/workspace-repo.ts:36-61,237-273 (SELECT list + DTO map + update patch — add investments_enabled mirroring cushion_enabled)
    - packages/tenancy/src/ports/budget-repo.ts:15-16 + packages/tenancy/src/contracts/api.ts:14 (patch type + DTO type)
    - apps/api/src/routes/budget-identity.ts:16,145-161 (patchBudgetSchema + patch-apply block)
    - apps/api/src/routes/budgets.ts:155-156 (GET DTO surfacing reservesEnabled/cushionEnabled)
    - apps/api/test/routes/investments.test.ts (P05 scaffold — flip it.skip -> it for the round-trip + RLS + rate-limit tests)
  </read_first>
  <action>
    1. Plumb investments_enabled end-to-end mirroring cushion_enabled: workspace-repo SELECT + DTO map (`investmentsEnabled: row.investments_enabled ?? false`) + update patch block; budget-repo port patch type; tenancy contracts DTO type; budget-identity patchBudgetSchema (`investments_enabled: z.boolean().optional()`) + patch-apply mapping; budgets.ts GET DTO (`investmentsEnabled: budget.investmentsEnabled ?? false`). Also accept investments_enabled in the budget CREATE schema if the onboarding create passes feature flags (mirror reserves_enabled).
    2. Un-skip the three investments.test.ts tests (round-trip INV-03, cross-tenant RLS, rate-limit INV-14) now that the route + flag exist; make them pass against real Postgres. The rate-limit test exercises the budgeting.api_rate_limits counter (table from 09-01 0038).
  </action>
  <verify>
    <automated>cd /home/claude/budget && grep -q "investmentsEnabled" packages/tenancy/src/adapters/persistence/workspace-repo.ts && grep -q "investments_enabled" apps/api/src/routes/budget-identity.ts && DATABASE_URL_APP="$DATABASE_URL_APP" bun test apps/api/test/routes/investments.test.ts 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - workspace-repo.ts SELECT + DTO map + update patch all reference investments_enabled / investmentsEnabled
    - budget-identity.ts patchBudgetSchema includes investments_enabled
    - apps/api/test/routes/investments.test.ts has its core tests un-skipped (no it.skip on the round-trip/RLS/rate-limit cases) and passes
  </acceptance_criteria>
  <done>investments_enabled round-trips through PATCH + GET DTO; investments route integration tests are green (real Postgres).</done>
</task>

</tasks>

<verification>
- bun run typecheck exits 0
- bun test apps/api/test/routes/investments.test.ts green (round-trip + RLS + rate-limit via api_rate_limits)
- a list-holdings test proves the instrument_price_cache value (hourly cron output) wins over the add-time holding-row price for tracked holdings
- make ci-gate passes with budgeting.investments in the tenant-leak set
</verification>

<success_criteria>

- investments_enabled persists via PATCH and surfaces in GET /budgets/:id (INV-01)
- Full holdings CRUD + search + reorder + on-add fetch routes mounted (INV-03/04/05/07/14)
- GET enriches each holding with value/P-L%/weight% in budget ccy; tracked holdings read CURRENT price from instrument_price_cache (hourly cron surfaces), custom holdings use holding-row price (INV-08/09/10 + B4)
- On-add fetch is rate-limited via budgeting.api_rate_limits (from 0038, no 0039); the 11th blocks the add
- Reorder rejects cross-tenant ids
- P05 route tests un-skipped and green
  </success_criteria>

<output>
After completion, create `.planning/phases/09-investments-wallet/09-06-SUMMARY.md`
</output>
