---
phase: 09-investments-wallet
plan: 07
type: execute
wave: 4
depends_on: [09-06]
autonomous: false
requirements: [INV-01, INV-02, INV-05, INV-06, INV-09, INV-10, INV-11, INV-16]
files_modified:
  - apps/web/src/hooks/use-investments.ts
  - apps/web/src/hooks/use-create-holding.ts
  - apps/web/src/hooks/use-update-holding.ts
  - apps/web/src/hooks/use-archive-holding.ts
  - apps/web/src/hooks/use-reorder-holdings.ts
  - apps/web/src/components/budgeting/wallets-tab/investments-section.tsx
  - apps/web/src/components/budgeting/wallets-tab/investment-group-header.tsx
  - apps/web/src/components/budgeting/wallets-tab/investment-row.tsx
  - apps/web/src/components/budgeting/wallets-tab/investment-row-sheet.tsx
  - apps/web/src/components/budgeting/wallets-tab/holding-sheet.tsx
  - apps/web/src/components/budgeting/wallets-tab/holding-delete-confirm.tsx
  - apps/web/src/components/budgeting/wallets-tab/instrument-search-input.tsx
  - apps/web/src/components/budgeting/wallets-tab/asset-class-chip.tsx
  - apps/web/src/components/budgeting/wallets-tab/type-dropdown.tsx
  - apps/web/src/components/budgeting/wallets-tab/group-combobox.tsx
  - apps/web/src/components/budgeting/wallets-tab/price-blocked-banner.tsx
  - apps/web/src/components/budgeting/wallets-tab/wallets-sectioned-list.tsx
  - apps/web/src/components/budgeting/wallets-tab/wallet-row.tsx
  - apps/web/src/components/settings/investments-section.tsx
  - apps/web/src/components/onboarding/steps/step-features.tsx
  - apps/web/messages/en.json
  - apps/web/messages/pl.json
  - apps/web/messages/uk.json
  - apps/web/test/investments/investment-row.test.tsx
  - apps/web/test/investments/holding-sheet.test.tsx
  - apps/web/test/investments/group-combobox.test.tsx

must_haves:
  truths:
    - "With investments_enabled off the wallets DOM has no Investments section; with it on the section renders LAST"
    - "Holdings are created/edited only via the right-side HoldingSheet; rows expose no inline input"
    - "Each row renders name, currency, total value, P/L % (green/red text), and weight %; mobile collapses to name+currency+value with tap-to-expand"
    - "Instrument search debounces 2s/blur, shows symbol+name+asset-class-chip suggestions + a Custom option, and never blocks on the price provider for search"
    - "Drag reorders within the section, drag into/out of a group reassigns it, whole groups reorder, cross-wallet-section drop is rejected"
    - "investments_enabled toggles in Settings and the onboarding wizard features step using the existing Switch/FeatureRow"
    - "All investments copy is wired through budget.investments.* in EN/PL/UK"
    - "Create/edit/reorder are optimistic via clientApiWrite; offline writes degrade with toast/rollback"
  artifacts:
    - path: "apps/web/src/components/budgeting/wallets-tab/investments-section.tsx"
      provides: "Client island: DndContext + groups + rows + dashed add"
      contains: "investmentsEnabled"
    - path: "apps/web/src/components/budgeting/wallets-tab/holding-sheet.tsx"
      provides: "Add/edit Sheet (single scrolling form, tracked/custom/cash variants)"
      contains: "Sheet"
    - path: "apps/web/src/hooks/use-investments.ts"
      provides: "React-Query hook on [budget, id, investments]"
      contains: "investments"
  key_links:
    - from: "apps/web/src/components/budgeting/wallets-tab/wallets-sectioned-list.tsx"
      to: "InvestmentsSection"
      via: "render LAST when investmentsEnabled (mirrors reservesEnabled gate)"
      pattern: "InvestmentsSection"
    - from: "apps/web/src/hooks/use-create-holding.ts"
      to: "clientApiWrite POST /investments"
      via: "optimistic onMutate/onError/onSettled on [budget,id,investments]"
      pattern: "clientApiWrite"
    - from: "apps/web/src/components/settings/investments-section.tsx"
      to: "PATCH /budgets/:id {investments_enabled}"
      via: "Switch handleEnabledChange (mirrors cushion-section)"
      pattern: "investments_enabled"

threat_model:
  - id: T-9-18
    stride: Tampering (client-side flag bypass)
    surface: investments_enabled section render
    mitigation: "Flag is read from useBudget (server DTO); even if the client renders the section, the API enforces RLS + membership (P06 T-9-14) — the UI gate is convenience, not the security boundary."
  - id: T-9-19
    stride: Information Disclosure (XSS via instrument/group names)
    surface: holding name + group + instrument display_name rendering
    mitigation: "All user/instrument strings render as React text nodes (auto-escaped); no dangerouslySetInnerHTML anywhere in the investments components."
---

<objective>
Build the entire Investments web surface: the React-Query hooks (`use-investments` + 4 optimistic mutation hooks), the `InvestmentsSection` client island (DndContext + collapsible groups + rows + dashed add), the `HoldingSheet` add/edit form (tracked/custom/cash variants with debounced instrument search, type dropdown, group combobox), the swipe-row extension (Delete-only -> Edit+Delete), the Settings + onboarding flag toggles, and EN/PL/UK i18n — all honoring `09-UI-SPEC.md` exactly. Un-skip the `@investments-wallet` E2E scenarios. This plan has a human-verify checkpoint (visual/interaction) per the UI contract.

Purpose: Closes the user-facing requirements (INV-01/02 visibility, INV-05 group autocomplete, INV-06 sheet-only edit, INV-09/10 row+group render, INV-11 DnD, INV-16 optimistic cache). Reuses every primitive mapped in 09-PATTERNS.md (Sheet, @dnd-kit sensors, clientApiWrite, wallet-row swipe, Switch/FeatureRow). DESIGN.md is authority; P/L green/red is the documented exception; dashed add is NOT yellow.
Output: 5 hooks, 13 components (3 new-no-analog), 2 flag toggles, 3 i18n files, 3 component tests, green E2E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-investments-wallet/09-SPEC.md
@.planning/phases/09-investments-wallet/09-UI-SPEC.md
@.planning/phases/09-investments-wallet/09-PATTERNS.md
@apps/web/src/components/budgeting/wallets-tab/wallets-sectioned-list.tsx
@apps/web/src/components/budgeting/wallets-tab/wallet-row.tsx
@apps/web/src/components/budgeting/category-form-sheet.tsx
@apps/web/src/components/settings/cushion-section.tsx
@apps/web/src/components/onboarding/steps/step-features.tsx
@apps/web/src/hooks/use-wallets.ts
@apps/web/src/hooks/use-create-wallet.ts

INTERFACES — exact analogs; use directly (09-PATTERNS.md has the full excerpts):

Hooks (apps/web/src/hooks/use-wallets.ts + use-create-wallet.ts): copy verbatim, swap key to ["budget", id, "investments"], path to /investments, json.holdings as HoldingDto[]. use-create/update/archive/reorder-holding.ts mirror the wallet equivalents with clientApiWrite("POST"|"PATCH", `/investments`...) optimistic onMutate/onError/onSettled.

Section render gate (wallets-sectioned-list.tsx:65-77,355-434): const investmentsEnabled = budgetMeta?.investmentsEnabled ?? false; render {investmentsEnabled && <InvestmentsSection budgetId budgetCurrency />} AFTER the last WalletSection (RESERVE) in document order.

DnD sensors (wallets-sectioned-list.tsx:152-158 — already exactly calibrated): PointerSensor {distance:4}, TouchSensor {delay:300, tolerance:5}, KeyboardSensor. Apply useSortable to the RowDragHandle element ONLY (Pitfall 2 — prevents tap-expand/long-press-drag conflict). Group headers register as droppables with id "group-<name>" (Pitfall 7); onDragEnd: if over.id startsWith "group-" -> set group; if it is a wallet-section id -> reject with toast crossSectionRejected; else intra-section reorder.

HoldingSheet (analog category-form-sheet.tsx): <Sheet side="right"> controlled by open/onOpenChange (NO SheetTrigger — trigger is the dashed add button or row pen). Field order per UI-SPEC Sheet Form Layout: tracked = Name(search) -> Type -> Group -> Buy price -> Buy currency -> Quantity -> Current price(read-only + "Last updated"); custom = same but Current price editable, prefilled to buy price; cash_fx = Currency + Amount + Group ONLY (no buy/current/quantity). Footer: Save (Button variant="default" = the ONE yellow CTA), Cancel (ghost, discard-confirm if dirty), Archive (edit mode, ghost + Trash2 destructive). Optimistic save via the create/update hook; discard-confirm AlertDialog on dirty close (D-18).

Swipe-row extension (wallet-row.tsx — extend the existing swipe-to-reveal from Delete-only to Edit+Delete per D-19): pen icon LEFT of trash in the revealed panel; both 44x44 tap zones; pen opens the Sheet, trash opens HoldingDeleteConfirm. Keep wallet usage unchanged (the new actions are additive/parameterized).

Settings toggle (cushion-section.tsx:121-146,265-285): investments-section.tsx mirrors it — useState(initial) + optimistic flip, api.budgets[":id"].$patch({ json: { investments*enabled: checked } }), rollback setEnabled(!checked) + toast on error, Switch + label/help from budget.investments.feature*\*.

Onboarding (step-features.tsx:97-142 FeatureRow pattern): add an investments FeatureRow (checked={investmentsEnabled}) beside reserves/cushion; thread investmentsEnabled through the wizard props + the create-budget payload (investments_enabled).

UI-SPEC hard rules (09-UI-SPEC.md — honor exactly):

- Numbers in .text-num-md/.text-num-sm (IBM Plex Sans tabular-nums), NEVER Inter.
- P/L: --trading-up (#0ecb81) / --trading-down (#f6465d) TEXT color only + signed % + TrendingUp/Down 12px icon; cash = "—" in --muted-strong.
- Delisted row: opacity-50, all text --muted-strong, "Delisted" chip, drag handle stays full opacity.
- DashedAddButton NOT yellow; no empty-state helper copy.
- Group headers collapsible, localStorage key inv-group-{budgetId}-{groupSlug}, default expanded; ungrouped render flat + always visible.
- PriceBlockedBanner role="alert", left border --destructive 4px, save disabled + inline Retry.
- Type dropdown icons per the UI-SPEC Type-enum table (equities->TrendingUp, etf->BarChart2, bond->Landmark, crypto->Bitcoin, reit->Building2, commodity->Package, cash_fx->Banknote, real_estate->Home, other->MoreHorizontal).
- No emojis — lucide only.

i18n: namespace budget.investments.\* — add EVERY key from the UI-SPEC Copywriting Contract to en.json, pl.json, uk.json (PL/UK copy quality not gated this phase but keys must exist + check:i18n completeness gate must pass).

Three no-analog components (09-PATTERNS.md No Analog Found) — build per shadcn docs: instrument-search-input.tsx (Popover + Command + useEffect 2s debounce timer; min 2 chars; Custom row always last), asset-class-chip.tsx (<span> --surface-elevated-dark bg + .text-caption), group-combobox.tsx (Popover + Command filtering existing groups + free-type new).

REBUILD REQUIREMENT (MEMORY: Always rebuild web after frontend edits): apps/web/\*_ + messages/_.json are bundled at build time. After edits run `make restart-web` (docker compose build web && restart) BEFORE the human-verify checkpoint and before E2E; verify the served bundle reflects the change.
</context>

<tasks>

<task type="auto">
  <name>Task 1: React-Query hooks + InvestmentsSection island + group/row/DnD render</name>
  <read_first>
    - apps/web/src/hooks/use-wallets.ts + use-create-wallet.ts (+ use-update/archive/reorder-wallet.ts) — copy the optimistic hook shapes
    - apps/web/src/components/budgeting/wallets-tab/wallets-sectioned-list.tsx (full — section gate, sensors, onDragEnd, DragOverlay)
    - apps/web/src/components/budgeting/wallets-tab/wallet-row.tsx + wallet-section.tsx (row + section header analogs)
    - apps/web/src/components/common/row-drag-handle.tsx + dashed-add-button.tsx (reused atoms)
    - 09-UI-SPEC.md Section Layout + Drag-and-Drop Model + Group Headers + Color (P/L + delisted)
  </read_first>
  <action>
    1. Hooks: use-investments.ts (useQuery key ["budget", id, "investments"], clientApiFetch /investments, json.holdings as HoldingDto[]); use-create/update/archive/reorder-holding.ts mirroring the wallet hooks with clientApiWrite + optimistic onMutate/onError/onSettled on the investments key (INV-16).
    2. investments-section.tsx: client island. useBudget for investmentsEnabled + budgetCurrency; useInvestments for rows. DndContext with the calibrated sensors; group rows by group_name (ungrouped flat); render InvestmentGroupHeader per group + InvestmentRow per holding + a trailing DashedAddButton ("+ Add investment", NOT yellow). onDragEnd handles intra-reorder / group-reassign (over.id startsWith "group-") / cross-section reject (toast crossSectionRejected) per Pitfall 7. DragOverlay clone styled per UI-SPEC.
    3. investment-group-header.tsx: collapsible (ChevronDown/Right), group name + group-% "of portfolio"; localStorage inv-group-{budgetId}-{slug}, default expanded; registered as a droppable id "group-<name>"; role="button" aria-expanded.
    4. investment-row.tsx + investment-row-sheet.tsx: read-only cells (name/currency/value/P-L%/weight%); desktop single line + hover pen+trash (28x28); mobile collapsed (name+currency+value) with tap-to-expand (P-L%+weight% second line, role=button aria-expanded); useSortable on the RowDragHandle ONLY; data-delisted -> opacity-50 + --muted-strong + Delisted chip; P/L via --trading-up/down text + sign + 12px icon; cash P/L = "—". Numbers in .text-num-*.
    5. Wire InvestmentsSection into wallets-sectioned-list.tsx: render LAST when investmentsEnabled (mirror the reservesEnabled gate) — append after the RESERVE WalletSection.
    Add apps/web/test/investments/investment-row.test.tsx (Vitest RTL): asserts the 5 fields render, total value = qty x price, P/L sign/color class, cash "—", delisted opacity class, and NO inline <input> on the row (INV-06/09).
  </action>
  <verify>
    <automated>cd /home/claude/budget/apps/web && bun run test investments/investment-row 2>&1 | tail -12 && cd /home/claude/budget && bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - use-investments.ts uses queryKey ["budget", budgetId, "investments"]
    - wallets-sectioned-list.tsx renders <InvestmentsSection ... /> gated on investmentsEnabled, after the RESERVE section
    - investment-row.test.tsx passes and asserts no inline input + correct P/L color + cash "—"
    - bun run typecheck exits 0
  </acceptance_criteria>
  <done>Hooks + section island + group/row render + DnD wired; section gates on the flag and renders last; row test green.</done>
</task>

<task type="auto">
  <name>Task 2: HoldingSheet form (search/type/group/cash variants) + swipe-row Edit+Delete + delete confirm + price-blocked banner</name>
  <read_first>
    - apps/web/src/components/budgeting/category-form-sheet.tsx (Sheet chrome + form submit via clientApiWrite)
    - apps/web/src/components/budgeting/wallets-tab/wallet-row.tsx (the swipe-to-reveal primitive to extend Delete-only -> Edit+Delete)
    - apps/web/src/components/budgeting/wallets-tab/wallet-delete-confirm.tsx (AlertDialog wrapper analog)
    - apps/web/src/components/ui/{sheet,command,popover,select,alert-dialog}.tsx (shadcn primitives — all already installed)
    - the existing amount-input / currency-select components (extend for comma+dot decimals per D-15)
    - 09-UI-SPEC.md Sheet Form Layout + Type enum icons + Price States + Copywriting Contract
  </read_first>
  <action>
    1. holding-sheet.tsx: <Sheet side="right"> (controlled open/onOpenChange, no trigger). Single scrolling form with the tracked/custom/cash field sets per UI-SPEC. Footer Save (variant="default" yellow), Cancel (ghost + discard-confirm on dirty), Archive (edit mode, ghost Trash2 destructive). Submits via use-create-holding/use-update-holding (optimistic). On an on-add price-fetch failure (P06 returns the blocked error), render <PriceBlockedBanner> and disable Save (A2).
    2. instrument-search-input.tsx: Popover + Command, useEffect 2s debounce (or blur) -> GET /investments/search?q= (min 2 chars); suggestion rows = symbol + name + <AssetClassChip>; a Custom row always last; selecting autofills type (from chip) + triggers the on-add price fetch (prefills current price read-only); Custom clears autofill.
    3. asset-class-chip.tsx + type-dropdown.tsx + group-combobox.tsx: per the no-analog specs (chip span; Select with the 9 lucide icons+labels; Popover+Command combobox filtering existing groups + free-type new — INV-05).
    4. holding-delete-confirm.tsx: AlertDialog, body literally "This can't be undone here." (D-03), confirm = archive (variant="destructive").
    5. price-blocked-banner.tsx: role="alert", left border --destructive 4px, body copy + inline Retry (Button variant="link") that re-triggers the fetch.
    6. Extend wallet-row.tsx swipe-to-reveal: add an Edit action (pen) left of Delete; parameterize so wallet usage stays Delete-only and investment-row-sheet gets Edit+Delete. Pen opens HoldingSheet; trash opens holding-delete-confirm.
    Add apps/web/test/investments/holding-sheet.test.tsx (cash variant shows only currency+amount+group; tracked current-price is read-only; dirty close fires discard-confirm) + group-combobox.test.tsx (existing groups filter + free-type creates new) — INV-05/06.
  </action>
  <verify>
    <automated>cd /home/claude/budget/apps/web && bun run test investments/holding-sheet investments/group-combobox 2>&1 | tail -14 && cd /home/claude/budget && bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - holding-sheet.tsx renders the cash variant with ONLY currency+amount+group (no buy/current/quantity)
    - tracked current-price field is read-only; custom is editable + prefills buy price
    - wallet-row.tsx swipe now exposes Edit + Delete for investment rows (pen left of trash) while wallet usage stays Delete-only
    - holding-delete-confirm body is exactly "This can't be undone here."
    - holding-sheet + group-combobox tests pass; bun run typecheck exits 0
  </acceptance_criteria>
  <done>Sheet form (3 variants) + search/type/group/chip + delete-confirm + price-blocked banner built; swipe extended to Edit+Delete; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Settings + onboarding flag toggles + EN/PL/UK i18n + un-skip E2E + web rebuild</name>
  <read_first>
    - apps/web/src/components/settings/cushion-section.tsx:121-146,265-285 (Switch toggle + PATCH pattern)
    - apps/web/src/components/onboarding/steps/step-features.tsx:97-142 (FeatureRow + wizard prop threading + create payload)
    - apps/web/messages/en.json (budget namespace location ~line 367) + pl.json + uk.json
    - apps/web/e2e/investments-wallet.feature + pages/investments-po.ts + steps/investments.steps.ts (P05 scaffold to un-skip)
    - 09-UI-SPEC.md Copywriting Contract (every key) + Feature Flag Toggle section
    - MEMORY: Always rebuild web after frontend edits (make restart-web) + check:i18n must pass
  </read_first>
  <action>
    1. settings/investments-section.tsx: mirror cushion-section — Switch + label/help (budget.investments.feature_label / feature_help_text), optimistic flip, api.budgets[":id"].$patch({ json: { investments_enabled: checked } }), rollback + toast on error. Mount it in the Settings tab beside the reserves/cushion sections.
    2. step-features.tsx: add an investments FeatureRow (checked + onChange threaded through the wizard props); include investments_enabled in the create-budget payload.
    3. i18n: add ALL budget.investments.* keys from the UI-SPEC Copywriting Contract to en.json (real copy), pl.json + uk.json (translated keys — quality not gated but keys must exist). Run the check:i18n completeness gate.
    4. make restart-web (docker compose build web && restart) so the bundle includes the new components + messages; verify docker compose ps shows web recently restarted + healthy and the served bundle reflects the section.
    5. Un-skip the @investments-wallet E2E scenarios (flag visibility, add-custom-holding, drag-into-group, optimistic-create); run make test-e2e --grep @investments-wallet against the running stack (canonical URL https://budget-dev.madonzy.com) until green.
  </action>
  <verify>
    <automated>cd /home/claude/budget && bun run check:i18n 2>&1 | tail -8 && grep -q "investments_enabled" apps/web/src/components/settings/investments-section.tsx && grep -rq "investments" apps/web/messages/pl.json apps/web/messages/uk.json && cd apps/web && bunx bddgen 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - settings/investments-section.tsx PATCHes investments_enabled and is mounted in Settings
    - step-features.tsx has an investments FeatureRow + passes investments_enabled in the create payload
    - check:i18n passes (all budget.investments.* keys present in en/pl/uk)
    - docker compose ps shows web restarted + healthy after make restart-web
    - the @investments-wallet E2E scenarios are un-skipped
  </acceptance_criteria>
  <done>Flag toggles in Settings + onboarding; full EN/PL/UK keys; web rebuilt; E2E scenarios un-skipped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full Investments section on the live stack: feature-flag toggle (Settings + onboarding), the section as the last panel on the Wallets tab, the add/edit Sheet with debounced instrument search, grouped collapsible rows with value/P-L%/weight%, drag reorder + group reassignment, swipe Edit/Delete, and the delisted/price-blocked chrome. Automated checks (Vitest + ci-gate + route tests + @investments-wallet E2E) are green and the web image is rebuilt.</what-built>
  <how-to-verify>
    1. Visit https://budget-dev.madonzy.com, open a budget, go to Settings -> toggle Investments ON. Confirm the wallets page now shows an INVESTMENTS section as the LAST section (no hard reload needed).
    2. Tap "+ Add investment". Type a known ticker (e.g. "AAPL"), wait ~2s -> a suggestion list shows symbol + name + an asset-class chip. Select it -> type preselects, current price prefills (read-only). Fill buy price + quantity -> Save. The row appears optimistically with value + a colored P/L % + weight %.
    3. Add a second holding with the same Group name -> both render under one collapsible group header showing "% of portfolio". Collapse/expand the group; reload -> state persists.
    4. Add a Custom holding (pick "Custom — enter manually") -> current price is editable, prefilled to buy price. Add a Cash holding (type Cash) -> only currency + amount + group; the row P/L shows "—".
    5. Drag a holding into the other group -> its group changes. Drag to reorder within a group -> order persists. On mobile width: tap a row to expand P/L%+weight%; swipe-left -> Edit + Delete; long-press the handle -> drag (three gestures don't collide).
    6. Confirm P/L green/red is text-only (not a filled badge), the dashed add button is NOT yellow, and all numbers use the tabular numeric font. Toggle the flag OFF -> the section disappears.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/interaction issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- apps/web Vitest investments/* tests green
- check:i18n passes (EN/PL/UK complete)
- make test-e2e --grep @investments-wallet green against the running stack
- web image rebuilt + healthy (docker compose ps)
</verification>

<success_criteria>

- Section renders last only when flag on; flag toggles in Settings + onboarding (INV-01/02)
- Sheet-only editing, no inline inputs (INV-06); group autocomplete (INV-05)
- Rows show value/P-L%/weight%, grouped with group-% (INV-09/10); DnD reorder + group reassign + cross-section reject (INV-11)
- Optimistic create/edit/reorder via clientApiWrite; offline toast/rollback (INV-16)
- EN/PL/UK keys complete; UI-SPEC visual contract honored; web rebuilt
  </success_criteria>

<output>
After completion, create `.planning/phases/09-investments-wallet/09-07-SUMMARY.md`
</output>
