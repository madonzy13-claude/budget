---
phase: 09-investments-wallet
plan: 05
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [INV-01, INV-02, INV-03, INV-06, INV-11, INV-14, INV-16]
files_modified:
  - apps/api/test/routes/investments.test.ts
  - apps/web/e2e/investments-wallet.feature
  - apps/web/e2e/pages/investments-po.ts
  - apps/web/e2e/steps/investments.steps.ts
  - bunfig.toml
  - apps/api/test/ci-gate/tenant-leak.config.ts

must_haves:
  truths:
    - "A Nyquist test scaffold exists for every Phase-9 requirement that lacks one, so no executing task is left with a MISSING automated verify"
    - "An investments route integration test scaffold exists (real Postgres) wired for the holdings round-trip + rate-limit scenario"
    - "An investments-wallet Gherkin .feature + Page Object + step bindings exist under the @investments-wallet tag (fresh-user fixture)"
    - "budgeting.investments is registered in the ci-gate tenant-leak file list"
  artifacts:
    - path: "apps/web/e2e/investments-wallet.feature"
      provides: "E2E coverage scaffold for INV-01/02/06/11/16"
      contains: "@investments-wallet"
    - path: "apps/api/test/routes/investments.test.ts"
      provides: "Route + RLS + rate-limit integration scaffold (INV-03/14)"
      contains: "investments"
  key_links:
    - from: "apps/api/test/ci-gate/tenant-leak.config.ts"
      to: "budgeting.investments"
      via: "tenant-leak gate file list registration"
      pattern: "investments"

threat_model:
  - id: T-9-13
    stride: Information Disclosure (test gap)
    surface: tenant-leak CI gate coverage
    mitigation: "Registering budgeting.investments in the ci-gate list makes a cross-tenant holding leak a CI failure; the route integration scaffold asserts a holding from tenant B is invisible to tenant A (RLS) before P06 fills the bodies."
---

<objective>
Create the Wave-0 test scaffolding that satisfies the Nyquist sampling contract (`09-VALIDATION.md`): a failing/pending integration scaffold for the investments routes (real Postgres, RLS + rate-limit), an `@investments-wallet` Gherkin feature + Page Object + step bindings (fresh-user fixture), and the ci-gate tenant-leak registration for `budgeting.investments`. This unblocks the `<automated>` verify commands referenced by P06 (API) and P07 (web) so no executing task ships with a MISSING verify.

Purpose: TDD-mode + Nyquist require that every requirement maps to an automated check before implementation. The pure-domain (P02) and adapter (P03/P04) tests are authored inside those TDD plans; this plan covers the cross-cutting route/E2E/ci-gate scaffolds that span P06+P07 and must exist up front. Scaffolds are written as real test files that compile and run; bodies that depend on not-yet-built routes/UI use `.skip` or `.todo` with a clear "filled by 09-06 / 09-07" comment, so the suites stay green while reserving the assertions.
Output: 1 route integration scaffold, 1 .feature + PO + steps, ci-gate + bunfig registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-investments-wallet/09-SPEC.md
@.planning/phases/09-investments-wallet/09-RESEARCH.md
@.planning/phases/09-investments-wallet/09-VALIDATION.md
@.planning/phases/09-investments-wallet/09-UI-SPEC.md

INTERFACES — exact analogs; use directly:

ci-gate config: the tenant-leak file list is config-driven (Makefile ci-gate runs `bun run test:ci-gate`). Read the existing config (search for the array listing budgeting.wallets / budgeting.tasks tables or the per-endpoint test file list) and append budgeting.investments following the exact entry shape used for budgeting.wallets. Existing tenant-leak test files live in apps/api/test/routes/\*.test.ts (e.g. wallet-patch.test.ts, reserves.test.ts) — pattern the new investments entry on wallet-patch.test.ts.

Route integration test analog: apps/api/test/routes/wallet-patch.test.ts + reserves.test.ts — real Postgres (DATABASE_URL_APP), the harness that boots the app + seeds a tenant + asserts cross-tenant invisibility. The investments scaffold mirrors this: a describe('Investments routes') with (a) a holding create -> list round-trip test (INV-03), (b) a cross-tenant RLS invisibility test, (c) a rate-limit test placeholder for 10/user/min on the on-add fetch (INV-14). Tests that need the P06 route can be it.skip with a // 09-06 fills this comment; the RLS + round-trip ones should be authored to run once P06 lands (mark it.skip now, P06 un-skips).

E2E analog: existing apps/web/e2e/_.feature + apps/web/e2e/pages/_-po.ts + apps/web/e2e/steps/\*.steps.ts (playwright-bdd, Gherkin, Page Objects, fresh-user-per-scenario fixture — see MEMORY: E2E Tests Always Use Gherkin). The Phase-7 tasks e2e tag convention is @tasks-redesign; this phase uses @investments-wallet. Scenarios to scaffold (Background: a fresh user with a budget + investments_enabled on):

- Flag off hides the section; flag on shows it as the last section (INV-01/02).
- Add a custom holding via the Sheet; row appears; no inline input on the row (INV-06).
- Drag a holding into a group; group assignment persists (INV-11).
- Optimistic create reflects without reload (INV-16).
  Steps may be marked pending/skip where they need P07 UI; the .feature + PO selectors + step stubs must exist and bddgen must succeed.

bunfig.toml: confirm packages/investments/test/\*\* is swept by bun:test (the 80% domain threshold already applies project-wide; ensure the new package's test dir is covered — add a coverage path entry if the config enumerates packages).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investments route integration scaffold + ci-gate tenant-leak registration</name>
  <read_first>
    - apps/api/test/routes/wallet-patch.test.ts (real-Postgres route test harness: app boot + tenant seed + cross-tenant assertion)
    - apps/api/test/routes/reserves.test.ts (key-presence + cross-tenant invisibility pattern)
    - apps/api/test/ci-gate/tenant-leak.config.ts (or wherever the gate file list lives — search for "wallets" to find it) — the array to append budgeting.investments to
    - 09-VALIDATION.md Per-Task Verification Map + 09-RESEARCH.md Wave 0 Gaps list
  </read_first>
  <action>
    1. Create apps/api/test/routes/investments.test.ts mirroring wallet-patch.test.ts: describe('Investments routes') with three tests — (a) POST /budgets/:id/investments then GET lists the holding with all fields round-tripped (INV-03); (b) a holding created under tenant B is NOT visible in tenant A's GET (RLS, INV-03); (c) on-add instant fetch rate-limit: the 11th fetch within a minute for one user defers/blocks (INV-14). Mark (a)/(b)/(c) it.skip with a "// 09-06 un-skips once /budgets/:id/investments routes land" comment so the suite stays green now and P06 flips them on.
    2. Register budgeting.investments in the ci-gate tenant-leak config (append the entry following the budgeting.wallets shape). If the gate enumerates a per-endpoint test-file list, add apps/api/test/routes/investments.test.ts to it.
    3. If bunfig.toml enumerates coverage packages, add packages/investments so its domain tests count toward the 80% threshold.
  </action>
  <verify>
    <automated>cd /home/claude/budget && grep -q "investments" apps/api/test/ci-gate/tenant-leak.config.ts 2>/dev/null || grep -rq "budgeting.investments" apps/api/test 2>/dev/null; test -f apps/api/test/routes/investments.test.ts && bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/test/routes/investments.test.ts exists and contains a describe('Investments routes')
    - the ci-gate tenant-leak file list contains an investments entry (grep finds budgeting.investments or investments.test.ts in the config)
    - bun run typecheck exits 0
  </acceptance_criteria>
  <done>Route integration scaffold exists (skipped pending P06); budgeting.investments is in the ci-gate list.</done>
</task>

<task type="auto">
  <name>Task 2: @investments-wallet Gherkin feature + Page Object + step bindings</name>
  <read_first>
    - apps/web/e2e/ (an existing .feature using the fresh-user fixture — copy its Background + tag structure)
    - apps/web/e2e/pages/ (an existing Page Object, e.g. a wallets/spendings PO — copy the selector + method shape)
    - apps/web/e2e/steps/ (existing step bindings; note playwright-bdd binds steps to a single test extender — see MEMORY)
    - 09-UI-SPEC.md Copywriting Contract (exact i18n copy for selectors: "Add investment", section "INVESTMENTS", "Custom — enter manually") + Hand-Off Checklist
  </read_first>
  <action>
    Create apps/web/e2e/investments-wallet.feature tagged @investments-wallet with a Background (fresh user + a budget + investments_enabled on) and the four scenarios listed in the context (flag visibility INV-01/02; add-custom-holding INV-06; drag-into-group INV-11; optimistic-create INV-16). Create apps/web/e2e/pages/investments-po.ts with selectors for the section header, the dashed "+ Add investment" button, the Sheet form fields, a holding row, and the group header (use accessible roles + the exact EN copy from the Copywriting Contract). Create apps/web/e2e/steps/investments.steps.ts binding the scenario steps to the PO. Where a step needs P07 UI not yet built, mark it pending (Given/When that throws "pending — 09-07") OR tag those scenarios @skip with a "// un-skip after 09-07" note — but the .feature, PO, and steps MUST exist and `bunx bddgen` MUST succeed.
  </action>
  <verify>
    <automated>cd /home/claude/budget/apps/web && bunx bddgen 2>&1 | tail -10 && test -f e2e/investments-wallet.feature && test -f e2e/pages/investments-po.ts && grep -q "@investments-wallet" e2e/investments-wallet.feature && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/e2e/investments-wallet.feature exists and contains @investments-wallet
    - apps/web/e2e/pages/investments-po.ts and apps/web/e2e/steps/investments.steps.ts exist
    - bunx bddgen succeeds (the feature compiles to test files)
  </acceptance_criteria>
  <done>E2E feature + Page Object + steps scaffolded under @investments-wallet; bddgen green.</done>
</task>

</tasks>

<verification>
- bun run typecheck exits 0
- bunx bddgen succeeds
- ci-gate config lists budgeting.investments
</verification>

<success_criteria>

- Every Phase-9 requirement has a home for an automated check (no MISSING verify downstream)
- Route integration scaffold (skipped) + E2E feature/PO/steps exist
- budgeting.investments registered in the tenant-leak gate
  </success_criteria>

<output>
After completion, create `.planning/phases/09-investments-wallet/09-05-SUMMARY.md`
</output>
