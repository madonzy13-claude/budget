---
phase: 06-settings-onboarding-share-ui
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/api/src/routes/budget-members.ts
  - apps/api/src/app.ts
  - apps/api/test/routes/budget-members.test.ts
autonomous: true
requirements: [SETT-05, SETT-06, SETT-07]
must_haves:
  truths:
    - "GET /budgets/:id/members returns the member list with roles for a SHARED budget"
    - "POST /budgets/:id/members/:memberId/revoke removes a member when called by the owner"
    - "A non-owner calling revoke gets 403"
    - "D-15: POST /budgets/:id/share still mints a share token whose TTL is a fixed 7 days at the API level (SHRD-03 'configurable' is satisfied API-side only; no TTL picker in the UI) — existing endpoint, regression-verified"
    - "D-12: POST /budgets/:id/leave still enforces last-owner protection — a SHARED budget's last owner is blocked from leaving (409) and must Delete instead; no ownership-transfer feature — existing endpoint, regression-verified"
  artifacts:
    - path: "apps/api/src/routes/budget-members.ts"
      provides: "GET members list + POST revoke-member endpoints"
      contains: "revoke"
  key_links:
    - from: "apps/api/src/app.ts"
      to: "budget-members.ts"
      via: "app.route mount under /budgets"
      pattern: "createBudgetMembersRoute"
    - from: "apps/api/src/routes/budget-members.ts revoke"
      to: "withBootstrapUserContext owner lookup"
      via: "owner-only authorization gate"
      pattern: 'role !== "owner"'
---

<objective>
Add the Members-section backend: a `GET /budgets/:id/members` read endpoint and a
`POST /budgets/:id/members/:memberId/revoke` owner-only mutation. Generate-share-link
(`POST /budgets/:id/share`) and leave-budget (`POST /budgets/:id/leave`) already exist
from Phase 2 — this plan adds an integration test that regression-guards them but does
NOT rebuild them.

Purpose: SETT-05 (member list), SETT-06 (share link — verify existing), SETT-07 (revoke
member + leave with last-owner protection). RESEARCH Q4 RESOLVED: no `GET /budgets/:id/members`
route exists; `workspaceRepo.listMembers` is in the repo interface — surface it.

Output: budget-members.ts route file mounted under /budgets, budget-members.test.ts green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-settings-onboarding-share-ui/06-PATTERNS.md
@apps/api/src/routes/budgets.ts

<interfaces>
<!-- Existing share-link endpoint (budgets.ts:403) — DO NOT rebuild, only consume/regression-test. -->
POST /budgets/:id/share  { ttlDays?: number = 7 } → 201 { url, ... }  (owner-gated inside createShareLink)

<!-- Existing leave endpoint (budgets.ts:241) — DO NOT rebuild. Last-owner → 409. -->

POST /budgets/:id/leave → 200 | 409 "Cannot leave as last owner"

<!-- workspaceRepo.listMembers exists in the repo interface (mocked at budgets.test.ts:29). -->

workspaceRepo.listMembers(budgetId): Promise<{ userId, role, name?, email? }[]>

<!-- Owner-only gate pattern (budgets.ts:160-186 — copy verbatim for revoke). -->

const lookup = await withBootstrapUserContext(UserId(session.user.id), async (tx) => {
const result = await tx.execute(sql`
    SELECT bm.role::text AS role, b.kind::text AS kind, b.name AS name
      FROM tenancy.budget_members bm
      JOIN tenancy.budgets b ON b.id = bm.budget_id
     WHERE bm.budget_id = ${budgetId}::uuid AND bm.user_id = ${session.user.id}::uuid
     LIMIT 1`);
return result.rows[0] as { role: string; kind: string; name: string } | undefined;
});
if (lookup.isErr()) return c.json({ error: "internal" }, 500);
if (!lookup.value) return c.json({ error: "Member not found" }, 404);
if (lookup.value.role !== "owner") return c.json({ error: "forbidden" }, 403);

<!-- Better Auth org member removal: auth.api.removeMember / removeMemberFromOrganization
     — leaveOrganization is already used at budgets.ts:250. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create budget-members.ts route — GET members + POST revoke; mount in app.ts</name>
  <read_first>
    - apps/api/src/routes/budgets.ts (tenant gate 117-126; owner-only gate 160-186; leaveOrganization usage ~241-260 — the Better Auth API call shape)
    - apps/api/src/app.ts (mount block ~63-78 — createShareJoinRoute and createTasksRoute show the sub-route mounting pattern under /budgets)
    - apps/api/test/routes/budget-members.test.ts (RED Wave 0 test — its assertions are the contract)
    - .planning/phases/06-settings-onboarding-share-ui/06-PATTERNS.md (Pattern 1 + owner-only gate)
  </read_first>
  <behavior>
    - `GET /budgets/:id/members` → 200 with `{ members: [{ userId, role, name?, email? }] }`; 401 unauth; 404 non-member
    - `POST /budgets/:id/members/:memberId/revoke` as owner → 200, member removed; 403 non-owner; 401 unauth; 404 non-member budget
    - revoke does not allow removing the last owner (mirror the leave-budget last-owner guard) → 409 if attempted
  </behavior>
  <action>
    Create `apps/api/src/routes/budget-members.ts` exporting a `createBudgetMembersRoute(deps)`
    Hono factory (mirror `createBudgetSettingsRoute` / `createShareJoinRoute` shape).

    `GET /:id/members`:
    - Tenant gate (session → 401; `tenantIds.includes(budgetId)` → 404).
    - `const members = await deps.tenancy.workspaceRepo.listMembers(budgetId);`
    - `return c.json({ members }, 200);`

    `POST /:id/members/:memberId/revoke`:
    - Tenant gate.
    - Owner-only gate: copy the `withBootstrapUserContext` lookup verbatim from
      budgets.ts:160-186; `role !== "owner"` → 403.
    - Reject self-revoke of the last owner: if `memberId` resolves to an owner and that
      owner is the only owner → `return c.json({ error: "last_owner" }, 409)`. Determine
      owner count from the member list or a `SELECT count(*) ... WHERE role='owner'`.
    - Remove the member via Better Auth: `await auth.api.removeMember({ body: {
      organizationId: budgetId, memberIdOrEmail: memberId }, headers: c.req.raw.headers })`
      — match the exact Better Auth method/param names used by the existing
      `leaveOrganization` call at budgets.ts:250 (read that call to get the precise API
      surface; if `removeMember` is not the right name use `removeMemberFromOrganization`).
    - `return c.json({ ok: true }, 200);`

    Register the routes as sub-paths of `:id` (`GET /:id/members`, `POST /:id/members/:memberId/revoke`)
    — no collision with `DELETE /share/:linkId`.

    In `apps/api/src/app.ts`, add a mount line in the `/budgets` block. Mount it BEFORE
    `app.route("/budgets", budgetsRoutesFactory(deps))` is fine since the paths are
    `/:id/members*` and do not collide with `budgetsRoutesFactory` static paths — but to
    avoid `/:id` swallowing, mount budget-members under its own prefix:
    ```typescript
    app.route("/budgets", createBudgetMembersRoute(deps));
    ```
    placed adjacent to the other `/budgets` mounts. If Hono route precedence causes
    `:id` in `budgetsRoutesFactory` to swallow `/:id/members`, mount `createBudgetMembersRoute`
    BEFORE `budgetsRoutesFactory` in the mount order (sub-path specificity wins when
    registered first).

  </action>
  <verify>
    <automated>cd apps/api && bun test test/routes/budget-members.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "createBudgetMembersRoute" apps/api/src/routes/budget-members.ts` >= 1
    - `grep "listMembers" apps/api/src/routes/budget-members.ts` matches
    - `grep "role !== \"owner\"" apps/api/src/routes/budget-members.ts` matches
    - `grep -v '^//' apps/api/src/routes/budget-members.ts | grep -c "last_owner"` >= 1
    - `grep "createBudgetMembersRoute" apps/api/src/app.ts` matches
    - `cd apps/api && bun test test/routes/budget-members.test.ts` exits 0
  </acceptance_criteria>
  <done>budget-members.ts provides GET members + owner-only revoke (with last-owner guard), mounted in app.ts; budget-members.test.ts is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Regression test — share-link generation + last-owner leave still work</name>
  <read_first>
    - apps/api/src/routes/budgets.ts (POST /:id/share at 403-438; POST /:id/leave at 241-260)
    - apps/api/test/routes/budgets.test.ts (buildApp factory; existing route-ordering regression test ~167)
    - apps/api/test/routes/share-links.test.ts (existing share-link test — extend rather than duplicate if it covers the generate path)
  </read_first>
  <action>
    Append to `apps/api/test/routes/budget-members.test.ts` (or extend `share-links.test.ts`
    if it already owns the share-generation assertions — prefer no duplication) two
    regression tests using the `buildApp` factory:

    1. `POST /budgets/:id/share` with `{}` → 201 and the response body has a `url` field.
       Use a fake `deps` whose share-link path resolves successfully. Assert the default
       TTL is 7 days at the API level (SHRD-03 satisfied at API; D-15 fixes the UI to 7d).

    2. `POST /budgets/:id/leave` as the last owner of a SHARED budget → 409. Stub the
       Better Auth `leaveOrganization` deps so it throws the "Cannot leave as last owner"
       error and assert the route maps it to 409.

    Also add a route-ordering regression test (mirror budgets.test.ts:167): assert
    `GET /budgets/:id/members` resolves to the members handler and is NOT swallowed by
    `GET /budgets/:id` — e.g. the members response shape `{ members: [...] }` differs
    from the GET /:id shape.

  </action>
  <verify>
    <automated>cd apps/api && bun test test/routes/budget-members.test.ts test/routes/share-links.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts `POST /budgets/:id/share` returns 201 with a `url` field
    - A test asserts last-owner `POST /budgets/:id/leave` returns 409
    - A route-ordering test asserts `GET /:id/members` is not swallowed by `GET /:id`
    - `cd apps/api && bun test test/routes/budget-members.test.ts` exits 0
  </acceptance_criteria>
  <done>Existing share-link and leave-budget endpoints are regression-guarded; route ordering for /:id/members verified.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                    |
| -------------------------------------- | ---------------------------------------------- |
| client → revoke endpoint               | Untrusted memberId param; owner-gated mutation |
| route handler → Better Auth org plugin | Membership removal delegated to Better Auth    |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                              | Disposition | Mitigation Plan                                                                                                 |
| ---------- | ---------------------- | ------------------------------------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------- |
| T-06-03-01 | Elevation of Privilege | Non-owner revokes another member                       | mitigate    | Owner-only gate (`withBootstrapUserContext` role lookup → 403) before the removeMember call                     |
| T-06-03-02 | Denial of Service      | Last owner revoked, budget becomes orphaned            | mitigate    | Last-owner guard returns 409; no ownership-transfer feature in v1.1 (last owner must Delete)                    |
| T-06-03-03 | Information Disclosure | Member list of a non-member budget leaked              | mitigate    | Tenant gate (`tenantIds.includes(budgetId)` → 404) before listMembers                                           |
| T-06-03-04 | Information Disclosure | Member list exposes more than role/name (e.g. raw PII) | accept      | listMembers returns userId/role and display name only; emails are already member-visible within a shared budget |

</threat_model>

<verification>
- `cd apps/api && bun test test/routes/budget-members.test.ts` green
- `make test` passes (no regression in budgets.test.ts / share-links.test.ts)
</verification>

<success_criteria>

- GET /budgets/:id/members lists members with roles
- POST revoke-member is owner-only with last-owner protection
- Existing share-link + leave endpoints regression-verified
- budget-members.test.ts is GREEN
  </success_criteria>

<output>
After completion, create `.planning/phases/06-settings-onboarding-share-ui/06-03-SUMMARY.md`
</output>
</content>
</invoke>
