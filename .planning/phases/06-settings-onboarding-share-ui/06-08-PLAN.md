---
phase: 06-settings-onboarding-share-ui
plan: 08
type: execute
wave: 4
depends_on: [05, 06, 07]
autonomous: false
files_modified:
  - apps/web/messages/pl.json
  - apps/web/messages/uk.json
  - tests/e2e/features/settings/budget-settings.feature
  - tests/e2e/features/onboarding/onboarding-wizard.feature
  - tests/e2e/features/share/join.feature
  - tests/e2e/pages/BudgetSettingsPage.ts
  - tests/e2e/pages/OnboardingPage.ts
  - tests/e2e/pages/JoinPage.ts
  - tests/e2e/steps/budget-settings.steps.ts
  - tests/e2e/steps/onboarding.steps.ts
  - tests/e2e/steps/join.steps.ts
requirements:
  [
    SETT-01,
    SETT-02,
    SETT-03,
    SETT-04,
    SETT-05,
    SETT-06,
    SETT-07,
    SETT-08,
    SETT-09,
    ONBD-01,
    ONBD-02,
    ONBD-03,
    ONBD-04,
    ONBD-05,
    ONBD-06,
    ONBD-07,
    ONBD-08,
    ONBD-09,
    SHRD-04,
  ]
must_haves:
  truths:
    - "Every Phase 6 EN i18n string has a PL and UK translation; no message lookup fails at app boot"
    - "playwright-bdd .feature files cover settings danger zone, cushion toggle, the onboarding wizard end-to-end, and share-link join"
    - "make test, bun run test, make ci-gate, and make test-e2e are all green"
    - "An impeccable DESIGN.md sweep confirms yellow-accent discipline across the three new surfaces"
  artifacts:
    - path: "tests/e2e/features/onboarding/onboarding-wizard.feature"
      provides: "Gherkin coverage of the 5-step wizard incl. resume"
      contains: "Scenario"
    - path: "tests/e2e/features/share/join.feature"
      provides: "Gherkin coverage of recipient join + error states"
      contains: "Scenario"
  key_links:
    - from: "tests/e2e/steps/onboarding.steps.ts"
      to: "OnboardingPage.ts"
      via: "createBdd page object binding"
      pattern: "OnboardingPage"
    - from: "apps/web/messages/pl.json"
      to: "apps/web/messages/en.json"
      via: "settings/onboarding/share namespace parity"
      pattern: "onboarding"
---

<objective>
Close Phase 6: deliver PL + UK translations for every new string, write the
playwright-bdd E2E coverage for the three flows, run the full CI gate suite green, and
end with a human UAT checkpoint.

Purpose: SETT/ONBD/SHRD verification end-to-end + the project rule that all new UI
strings ship in EN/PL/UK from day one. Phase 8 owns the full i18n rewrite; Phase 6 still
delivers its own new strings in all three locales.

Output: pl.json + uk.json parity, 3 .feature files + page objects + step bindings, full
gate green, DESIGN.md sweep, human UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-settings-onboarding-share-ui/06-UI-SPEC.md
@.planning/phases/06-settings-onboarding-share-ui/06-VALIDATION.md
@apps/web/messages/en.json

<interfaces>
<!-- New EN namespaces added across Plans 06-05/06/07: settings, onboarding, share. -->
<!-- pl.json + uk.json must mirror the exact key structure of en.json for these 3 namespaces. -->

<!-- E2E stack: playwright-bdd ^8. .feature files + Page Objects + steps via createBdd(test). -->
<!-- Analog set: tests/e2e/features/wallets/*.feature + pages/WalletsPage.ts + steps/wallets.steps.ts -->
<!-- Fresh-user-per-scenario fixture: tests/e2e/fixtures/index.js -->
<!-- Existing OnboardingPage.ts is pre-Phase-6 (/onboarding, currency-only) — REWRITE for the 5-step wizard. -->
<!-- Existing SettingsPage.ts is the GLOBAL account settings page — do NOT reuse; create BudgetSettingsPage.ts. -->
<!-- E2E base URL from PLAYWRIGHT_BASE_URL (.env.local APP_URL); DB-seeding steps need DATABASE_URL_APP. -->

<!-- ICU apostrophe rule: every apostrophe in pl/uk values must be doubled. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PL + UK translations for the settings/onboarding/share namespaces</name>
  <read_first>
    - apps/web/messages/en.json (source-of-truth EN keys for settings, onboarding, share)
    - apps/web/messages/pl.json (existing structure — add the 3 new namespaces with the same key shape)
    - apps/web/messages/uk.json (existing structure)
    - .planning/phases/06-settings-onboarding-share-ui/06-UI-SPEC.md (Copywriting Contract — semantic intent for accurate translation)
  </read_first>
  <action>
    For BOTH `apps/web/messages/pl.json` and `apps/web/messages/uk.json`, add the
    `settings`, `onboarding`, and `share` top-level namespaces with the SAME nested key
    structure as `apps/web/messages/en.json`. Translate every leaf value: pl.json into
    Polish, uk.json into Ukrainian.

    Preserve every ICU placeholder verbatim (`{name}`, `{budgetName}`, `{amount}`).
    Every apostrophe in a value must be doubled for ICU MessageFormat. Do NOT alter the
    EN file. Do NOT touch other namespaces. The three new namespaces in all three files
    must have identical KEY sets (only values differ).

  </action>
  <verify>
    <automated>cd apps/web && node scripts/check-i18n-parity.mjs settings onboarding share || node -e "const en=require('./messages/en.json'),pl=require('./messages/pl.json'),uk=require('./messages/uk.json');const flat=(o,p='')=>Object.entries(o||{}).flatMap(([k,v])=>typeof v==='object'?flat(v,p+k+'.'):[p+k]);for(const ns of ['settings','onboarding','share']){const e=JSON.stringify(flat(en[ns]).sort());if(JSON.stringify(flat(pl[ns]).sort())!==e)throw new Error('pl '+ns+' mismatch');if(JSON.stringify(flat(uk[ns]).sort())!==e)throw new Error('uk '+ns+' mismatch');}console.log('i18n parity OK')"</automated>
  </verify>
  <acceptance_criteria>
    - the parity check command exits 0 (pl and uk have the same flattened key set as en for all 3 namespaces)
    - `grep -c '"onboarding"' apps/web/messages/pl.json` returns 1
    - `grep -c '"onboarding"' apps/web/messages/uk.json` returns 1
    - `cd apps/web && node -e "require('./messages/pl.json');require('./messages/uk.json')"` exits 0 (valid JSON)
  </acceptance_criteria>
  <done>pl.json and uk.json carry full Polish and Ukrainian translations for the settings, onboarding, and share namespaces with key parity to en.json.</done>
</task>

<task type="auto">
  <name>Task 2: playwright-bdd E2E — settings, onboarding wizard, share-link join</name>
  <read_first>
    - tests/e2e/features/wallets/add-edit-drag-delete.feature (analog .feature file)
    - tests/e2e/pages/WalletsPage.ts (analog Page Object — constructor + typed locators)
    - tests/e2e/steps/wallets.steps.ts (analog steps — createBdd + fixture import + API seeding via page.request)
    - tests/e2e/pages/OnboardingPage.ts (pre-Phase-6 — REWRITE for the 5-step /budgets/new wizard)
    - tests/e2e/fixtures/index.js (fresh-user-per-scenario fixture)
    - apps/web/playwright.config.ts (E2E config source of truth)
  </read_first>
  <action>
    Create three `.feature` files (tag `@phase6`), three Page Objects, and three step
    files bound via `createBdd(test)` with the fresh-user fixture. Seed data via
    `page.request.post("/api/budgets", ...)` — no DB mocking.

    `tests/e2e/features/settings/budget-settings.feature` + `pages/BudgetSettingsPage.ts`
    + `steps/budget-settings.steps.ts` — scenarios:
    - Owner renames the budget via inline autosave; the new name persists after reload.
    - Owner toggles cushion mode on; the toast appears; the state persists after reload.
    - Owner generates a share link; the URL field appears with a Copy button.
    - Owner archives the budget; it disappears from the home grid.
    - Owner deletes the budget — the "Delete forever" button is disabled until the typed
      name matches exactly, then the budget is gone.
    - A PRIVATE budget shows no Members section.
    `BudgetSettingsPage.ts`: `open(locale, budgetId)`, `openSection(name)` (accordion
    trigger by role=button), and locators for the identity input, cushion switch, share
    button, archive/delete dialogs.

    `tests/e2e/features/onboarding/onboarding-wizard.feature` +
    `pages/OnboardingPage.ts` (rewritten) + `steps/onboarding.steps.ts` — scenarios:
    - A fresh user walks all 5 steps and lands on `/budgets/[id]/spendings`.
    - The wizard is resumable: refresh mid-wizard returns to the saved step.
    - Selecting starter categories creates them in the new budget.
    `OnboardingPage.ts`: `open(locale)`, `fillName`, `pickCurrency`, `pickType`,
    `toggleCategory`, `clickNext`, `clickBack`, `clickSkip`, `clickCreate`.

    `tests/e2e/features/share/join.feature` + `pages/JoinPage.ts` +
    `steps/join.steps.ts` — scenarios:
    - A recipient with a valid link sees the "Join {budget name}" card and joins,
      landing on `/budgets/[id]/spendings`.
    - An unauthenticated recipient can SEE the join page (not bounced to sign-in).
    - A revoked/expired link shows the error state.
    `JoinPage.ts`: `open(locale, token)`, locators for the join CTA and the error card.

    Register the new step files / feature globs per `apps/web/playwright.config.ts`
    (the BDD config that compiles `.feature` → spec). If the config enumerates feature
    dirs or step files explicitly, add the new paths.

  </action>
  <verify>
    <automated>cd apps/web && bunx bddgen 2>&1 | tail -5 && ls tests/e2e/features/onboarding/onboarding-wizard.feature tests/e2e/features/share/join.feature tests/e2e/features/settings/budget-settings.feature</automated>
  </verify>
  <acceptance_criteria>
    - 3 `.feature` files exist, each containing `@phase6` and at least 2 `Scenario:` blocks
    - `grep -c "Scenario" tests/e2e/features/onboarding/onboarding-wizard.feature` >= 2
    - 3 Page Object files exist (`BudgetSettingsPage.ts`, `OnboardingPage.ts`, `JoinPage.ts`)
    - each step file contains `createBdd` and imports the fixture
    - `grep "OnboardingPage" tests/e2e/steps/onboarding.steps.ts` matches
    - `bunx bddgen` (or the project's BDD codegen) exits 0
  </acceptance_criteria>
  <done>Three @phase6 .feature files with Page Objects and step bindings cover settings, the onboarding wizard, and share-link join.</done>
</task>

<task type="auto">
  <name>Task 3: Full gate — make test, bun run test, ci-gate, test-e2e + DESIGN.md sweep</name>
  <read_first>
    - Makefile (test, ci-gate, test-e2e, dev-build targets)
    - DESIGN.md (yellow-accent discipline, dark canvas, font rules — the sweep checklist)
    - .planning/phases/06-settings-onboarding-share-ui/06-UI-SPEC.md (accent reserved list — the 5 yellow-allowed elements)
  </read_first>
  <action>
    Step 1 — Rebuild the Docker images so the running stack reflects all Phase 6 source
    edits (`web`/`api` run from prebuilt images — source does NOT hot-reload):
    ```bash
    make dev-build
    ```

    Step 2 — Run the full gate in order, fixing any red before moving on:
    ```bash
    make test
    cd apps/web && bun run test
    make ci-gate
    make test-e2e
    ```
    `make ci-gate` must stay 5/5 (onboarding_progress is a USER-SCOPED INCLUDED table
    from Plan 06-01). `make test-e2e` runs the new `@phase6` features against the running
    stack with `PLAYWRIGHT_BASE_URL` from `.env.local`.

    Step 3 — Impeccable DESIGN.md sweep over the three new surfaces (Settings tab,
    onboarding wizard, share-join page). Verify against the UI-SPEC accent-reserved list:
    yellow appears ONLY on (1) wizard primary CTA, (2) active stepper segment,
    (3) cushion Switch thumb when ON, (4) join-page text link, (5) share-field Copy icon.
    Back / Skip / +Add / Cancel / section labels / accordion triggers / share-field
    border / member role badge must NOT be yellow. Fix any violation.

    Step 4 — Do not declare the phase verified until every gate command exits 0 with
    Docker running. "Infra unavailable" is not an acceptable skip.

  </action>
  <verify>
    <automated>make test && cd apps/web && bun run test && cd "$OLDPWD" && make ci-gate && make test-e2e</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0
    - `cd apps/web && bun run test` exits 0
    - `make ci-gate` exits 0 (5/5 backend tenant-leak tests, onboarding_progress included)
    - `make test-e2e` exits 0 (all `@phase6` features pass)
    - DESIGN.md sweep notes recorded in the SUMMARY with each accent-rule item checked
  </acceptance_criteria>
  <done>Full CI gate green with Docker running; DESIGN.md accent sweep clean across all three Phase 6 surfaces.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human UAT — Settings, Onboarding, Share-link join</name>
  <what-built>
    The complete Phase 6 surface: the 5-section Settings tab (identity inline-autosave,
    cushion toggle, recurring rules in a Sheet, members + share link, danger zone), the
    5-step onboarding wizard at /budgets/new with resume, and the public share-link join
    page. All EN/PL/UK strings, all CI gates green.
  </what-built>
  <how-to-verify>
    With the stack running (`make dev-build` done), at `PLAYWRIGHT_BASE_URL` from
    `.env.local`:
    1. Open a budget's Settings tab. Confirm 5 accordion sections (or 4 for a PRIVATE
       budget). Rename the budget inline — confirm the toast and that the name persists
       after reload.
    2. Toggle Cushion mode on; switch to the Spendings tab and confirm headers reflect
       cushion limits; toggle back.
    3. On a SHARED budget, click "Generate share link", copy the URL, open it in a
       private window — confirm the join page renders WITHOUT a sign-in bounce.
    4. Sign in in that window and click "Join" — confirm you land on the budget's
       Spendings tab as a member.
    5. Sign up as a brand-new user — confirm the 5-step wizard appears, walk all 5 steps,
       and confirm you land on /budgets/[id]/spendings with the starter categories.
    6. Mid-wizard, refresh the page — confirm it resumes at the same step.
    7. In Danger Zone, archive a budget — confirm it leaves the home grid. Delete a
       budget — confirm the "Delete forever" button stays disabled until the typed name
       matches exactly.
    8. Switch the UI locale to PL and UK — confirm no raw message keys appear on any of
       the three surfaces.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issues found.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                          |
| ----------------------------------- | ---------------------------------------------------- |
| E2E test runner → running stack     | Tests exercise the real auth + API + DB path         |
| i18n message catalogs → rendered UI | Untrusted-free, but key drift breaks the app at boot |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                   | Disposition | Mitigation Plan                                                                                                                  |
| ---------- | ---------------------- | ----------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------- |
| T-06-08-01 | Tampering              | i18n key drift between en/pl/uk crashes the app at runtime  | mitigate    | The Task 1 parity check fails closed on any key-set mismatch across the 3 locales                                                |
| T-06-08-02 | Repudiation            | E2E gate skipped ("infra unavailable") hides real failures  | mitigate    | Task 3 requires Docker up + all four gate commands exit 0; no `continue-on-error`, no whole-job skip (memory: E2E must block CI) |
| T-06-08-03 | Information Disclosure | `onboarding_progress` regresses out of the tenant-leak gate | mitigate    | `make ci-gate` re-runs Test 4 which parses USER-DATA-TABLES.txt — onboarding_progress stays an INCLUDED USER-SCOPED table        |

</threat_model>

<verification>
- i18n parity check green for settings/onboarding/share across en/pl/uk
- `make test && cd apps/web && bun run test && make ci-gate && make test-e2e` all green
- DESIGN.md accent sweep clean
- Human UAT approved
</verification>

<success_criteria>

- PL + UK translations complete with key parity
- 3 @phase6 .feature files + Page Objects + steps covering settings, onboarding, join
- Full CI gate green with Docker running
- DESIGN.md sweep clean
- Human UAT approved
  </success_criteria>

<output>
After completion, create `.planning/phases/06-settings-onboarding-share-ui/06-08-SUMMARY.md`
</output>
