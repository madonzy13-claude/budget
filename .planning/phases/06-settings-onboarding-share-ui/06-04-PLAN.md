---
phase: 06-settings-onboarding-share-ui
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - apps/api/src/routes/budget-danger-zone.ts
  - apps/api/src/routes/onboarding.ts
  - apps/api/src/app.ts
  - packages/tenancy/src/ports/onboarding-progress-repo.ts
  - packages/tenancy/src/adapters/persistence/onboarding-progress-repo.ts
  - packages/tenancy/src/ports/workspace-repo.ts
  - packages/tenancy/src/adapters/persistence/workspace-repo.ts
  - apps/api/test/routes/budget-archive.test.ts
  - apps/api/test/routes/onboarding.test.ts
autonomous: true
requirements: [SETT-08, ONBD-07]
must_haves:
  truths:
    - "POST /budgets/:id/archive sets budgets.archived_at and returns 200 for the owner"
    - "POST /budgets/:id/delete hard-deletes the budget only when the typed name matches, server-side"
    - "Archived budgets no longer appear in GET /budgets/active"
    - "GET /onboarding/progress returns the session user's saved step"
    - "PUT /onboarding/progress upserts step and is scoped to the session user (request-supplied user_id ignored)"
  artifacts:
    - path: "apps/api/src/routes/onboarding.ts"
      provides: "GET + PUT /onboarding/progress endpoints"
      contains: "onboarding/progress"
    - path: "apps/api/src/routes/budget-danger-zone.ts"
      provides: "POST /:id/archive + POST /:id/delete sub-path endpoints (own route file — no budgets.ts overlap)"
      contains: "/:id/archive"
    - path: "packages/tenancy/src/adapters/persistence/onboarding-progress-repo.ts"
      provides: "Drizzle repo for onboarding_progress upsert/read"
      contains: "onboardingProgress"
  key_links:
    - from: "apps/api/src/routes/budget-danger-zone.ts archive"
      to: "budgets.archived_at"
      via: "owner-gated UPDATE"
      pattern: "archived_at"
    - from: "GET /budgets/active"
      to: "archived_at IS NULL filter"
      via: "archived budgets hidden from listing"
      pattern: "archived_at IS NULL"
---

<objective>
Add the danger-zone backend (archive soft-delete + typed-name hard-delete) and the
onboarding-progress backend (GET/PUT keyed by the session user). Archive must hide the
budget from the active-budgets listing so D-09 (hidden from home + switcher) holds at
the source.

Purpose: SETT-08 (Archive + Delete) and ONBD-07 (resumable wizard state). RESEARCH
Pitfall 1 RESOLVED: use sub-path mutations `POST /:id/archive` + `POST /:id/delete` — a
bare `DELETE /:id` would collide with the existing `DELETE /share/:linkId`.

MERGE-HAZARD RESOLUTION (checker BLOCKER 2): the archive/delete handlers do NOT go into
`apps/api/src/routes/budgets.ts` (Plan 06-02 owns that file). They live in a NEW
dedicated file `apps/api/src/routes/budget-danger-zone.ts`, mounted under `/budgets`
exactly like Plan 06-03's `budget-members.ts`. The active-listing filter
(`archived_at IS NULL`) is made in `packages/tenancy/src/adapters/persistence/workspace-repo.ts`
— the repo method behind `GET /budgets/active`, NOT in `budgets.ts`.

WAVE PLACEMENT: this plan ALSO adds methods to `workspace-repo.ts` (`archive`,
`hardDelete`, the active-listing filter) and mounts a route in `apps/api/src/app.ts`.
Plan 06-02 also edits `workspace-repo.ts` (Wave 2) and Plan 06-03 also edits `app.ts`
(Wave 2). To keep same-wave plans file-disjoint, this plan is serialized AFTER 06-02 and
06-03 — `depends_on: [01, 02, 03]`, `wave: 3`. By Wave 3 the `workspace-repo.ts` and
`app.ts` edits from 06-02/06-03 are already merged, so this plan's additions to those
files are conflict-free appends.

Output: archive/delete endpoints on a new budget-danger-zone.ts route, onboarding.ts
route mounted, budget-archive.test.ts + onboarding.test.ts green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-settings-onboarding-share-ui/06-PATTERNS.md
@apps/api/src/routes/budgets.ts

<interfaces>
<!-- onboarding_progress table (created by Plan 06-01). Keyed by user_id. -->
tenancy.onboarding_progress { user_id uuid PK, step integer, completed_at timestamptz NULL }

<!-- New onboarding endpoints — mounted at /onboarding (NOT under /budgets). -->

GET /onboarding/progress → 200 { step: number, completedAt: string|null } | 404 if no row
PUT /onboarding/progress { step: number (1..5), completedAt?: string } → 200 { ok: true }

<!-- New danger-zone endpoints — sub-paths of /:id, in their OWN route file
     (budget-danger-zone.ts) mounted under /budgets. Avoids bare DELETE /:id collision
     AND avoids editing budgets.ts (Plan 06-02 owns that file). -->

POST /budgets/:id/archive → 200 (owner-only; sets archived_at = now())
POST /budgets/:id/delete { confirmName: string } → 200 | 422 name_mismatch (owner-only; hard delete)

<!-- Owner-only gate: copy budgets.ts:160-186 (withBootstrapUserContext role lookup). -->
<!-- GET /budgets/active is at budgets.ts:89 — backed by workspaceRepo.listForUser (line ~59);
     the archived_at IS NULL filter goes in that repo method, NOT in budgets.ts. -->
<!-- workspace-repo.ts already carries 06-02's updateIdentity + hasTransactions by the time
     this plan runs (Wave 2 < Wave 3) — add archive/hardDelete alongside them. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: onboarding-progress repo + GET/PUT /onboarding/progress route</name>
  <read_first>
    - packages/tenancy/src/adapters/persistence/onboarding-progress-schema.ts (created by Plan 06-01 — the table shape)
    - packages/tenancy/src/adapters/persistence/workspace-repo.ts (analog: a tenancy Drizzle repo using withTenantTx-style helpers)
    - apps/api/src/routes/budget-settings.ts (analog: a small standalone Hono route factory)
    - apps/api/src/app.ts (mount block — where /budget-settings is mounted at line ~128)
    - apps/api/test/routes/onboarding.test.ts (RED Wave 0 test — the contract)
  </read_first>
  <behavior>
    - `GET /onboarding/progress` for a user with no row → 404 (or 200 `{step:1,completedAt:null}` — pick one and make the Wave 0 test match)
    - `GET /onboarding/progress` for a user with a saved step → 200 `{ step, completedAt }`
    - `PUT /onboarding/progress {step:3}` → 200, upserts `(user_id, step)`
    - `PUT /onboarding/progress {step:5, completedAt}` → 200, marks complete
    - All reads/writes use the SESSION user id — any `user_id` in the request body is ignored
  </behavior>
  <action>
    Step 1 — `packages/tenancy/src/ports/onboarding-progress-repo.ts`: define
    ```typescript
    export interface OnboardingProgressRepo {
      get(userId: string): Promise<{ step: number; completedAt: string | null } | null>;
      upsert(userId: string, step: number, completedAt?: string | null): Promise<void>;
    }
    ```

    Step 2 — `packages/tenancy/src/adapters/persistence/onboarding-progress-repo.ts`:
    implement `DrizzleOnboardingProgressRepo`. Because the table's RLS predicate keys on
    `app.current_user_id`, every query must run in a transaction that sets that GUC.
    Use the platform helper that sets `app.current_user_id` (the same primitive
    `identity` repos use for user-scoped RLS — read an existing user-scoped repo for the
    exact helper name, e.g. `withUserTx` / `withBootstrapUserContext`). Implement:
    - `get`: `SELECT step, completed_at FROM tenancy.onboarding_progress WHERE user_id = ${userId}::uuid`
    - `upsert`: `INSERT INTO tenancy.onboarding_progress (user_id, step, completed_at)
      VALUES (${userId}::uuid, ${step}, ${completedAt ?? null})
      ON CONFLICT (user_id) DO UPDATE SET step = EXCLUDED.step, completed_at = EXCLUDED.completed_at`

    Step 3 — `apps/api/src/routes/onboarding.ts`: export `createOnboardingRoute(deps)`
    Hono factory (mirror `createBudgetSettingsRoute`). Two handlers:
    - `GET /progress`: `const session = c.get("session"); if (!session) return 401;`
      → `deps.tenancy.onboardingProgressRepo.get(session.user.id)`.
    - `PUT /progress`: zValidator on `z.object({ step: z.number().int().min(1).max(5),
      completedAt: z.string().datetime().optional() })`. Use `session.user.id` as the
      key — NEVER read a user_id from the body. → `repo.upsert(...)` → `c.json({ ok: true })`.

    Step 4 — In `apps/api/src/app.ts`, mount: `app.route("/onboarding", createOnboardingRoute(deps));`
    Wire `onboardingProgressRepo` into the `deps.tenancy` bag in `apps/api/src/boot.ts`
    (or wherever the tenancy repos are assembled — find it by grepping for `workspaceRepo:`).

  </action>
  <verify>
    <automated>cd apps/api && bun test test/routes/onboarding.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "OnboardingProgressRepo" packages/tenancy/src/ports/onboarding-progress-repo.ts` >= 1
    - `grep "ON CONFLICT (user_id)" packages/tenancy/src/adapters/persistence/onboarding-progress-repo.ts` matches
    - `grep "createOnboardingRoute" apps/api/src/routes/onboarding.ts` matches
    - `grep "session.user.id" apps/api/src/routes/onboarding.ts` matches AND no `c.req` body field named user_id is read
    - `grep "app.route(\"/onboarding\"" apps/api/src/app.ts` matches
    - `cd apps/api && bun test test/routes/onboarding.test.ts` exits 0
  </acceptance_criteria>
  <done>onboarding-progress repo + GET/PUT /onboarding/progress route exist, scoped to the session user; onboarding.test.ts is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: New budget-danger-zone.ts route — POST /:id/archive + POST /:id/delete; filter archived from GET /budgets/active</name>
  <read_first>
    - apps/api/src/routes/budgets.ts (owner-only gate 160-186; GET /active at 89-99; DELETE /share/:linkId at 441 — the collision to avoid; READ-ONLY here — do NOT edit this file)
    - apps/api/src/routes/budget-members.ts (Plan 06-03 — the sibling "own route file under /budgets" pattern: createBudgetMembersRoute factory + app.ts mount)
    - apps/api/test/routes/budget-archive.test.ts (RED Wave 0 test — the contract incl. name_mismatch → 422)
    - apps/api/src/app.ts (mount block — add the new route mount adjacent to createBudgetMembersRoute; this plan runs after 06-03 so that mount already exists)
    - packages/tenancy/src/adapters/persistence/workspace-repo.ts (listForUser at line ~59 — the active-budgets query; add the archived_at IS NULL filter here; 06-02's updateIdentity/hasTransactions are already present)
    - packages/tenancy/src/ports/workspace-repo.ts (add archive + hardDelete method signatures alongside 06-02's updateIdentity/hasTransactions)
    - .planning/phases/06-settings-onboarding-share-ui/06-PATTERNS.md (Pattern 2 route ordering; owner-only gate)
  </read_first>
  <behavior>
    - `POST /budgets/:id/archive` as owner → 200, `archived_at` set; as non-owner → 403; unauth → 401
    - `POST /budgets/:id/delete {confirmName}` matching the budget name, as owner → 200, budget row hard-deleted
    - `POST /budgets/:id/delete` with a non-matching `confirmName` → 422 `{error:"name_mismatch"}` (server re-validates, never trusts the client)
    - `POST /budgets/:id/delete` as non-owner → 403
    - `GET /budgets/active` no longer returns budgets whose `archived_at IS NOT NULL`
  </behavior>
  <action>
    DO NOT EDIT `apps/api/src/routes/budgets.ts` — Plan 06-02 owns that file. Read
    `budgets.ts` for the gate patterns only; copy them into the new file.

    Step 1 — Create `apps/api/src/routes/budget-danger-zone.ts` exporting a
    `createBudgetDangerZoneRoute(deps)` Hono factory (mirror `createBudgetMembersRoute`
    in `budget-members.ts` — same shape: a Hono instance, sub-paths of `/:id`, mounted
    under `/budgets`).

    `POST /:id/archive`: Tenant gate (session → 401; `tenantIds.includes(budgetId)` → 404);
    then the owner-only gate (copy budgets.ts:160-186 — the `withBootstrapUserContext`
    lookup also returns the budget `name`, reused by delete). `role !== "owner"` → 403.
    Then update via a repo method: add `workspaceRepo.archive(budgetId, actorUserId)` →
    `UPDATE tenancy.budgets SET archived_at = now() WHERE id = ${budgetId}::uuid`
    inside `withTenantTx`. `return c.json({ ok: true }, 200);`

    `POST /:id/delete` (`zValidator("json", z.object({ confirmName: z.string().min(1) }))`):
    Tenant gate; owner-only gate (the lookup returns `name` — the actual budget name).
    Server-side typed-name check: `if (c.req.valid("json").confirmName !== lookup.value.name)
    return c.json({ error: "name_mismatch" }, 422);` — this is the security control;
    the client's disabled-button is cosmetic only. Then hard-delete via
    `workspaceRepo.hardDelete(budgetId, actorUserId)` — `DELETE FROM tenancy.budgets
    WHERE id = ${budgetId}::uuid` inside `withTenantTx`. The `tasks` / `category_*` /
    `transactions` FKs are `ON DELETE CASCADE` per Phase 1/5 schema; if any FK is
    RESTRICT, delete children first in the same tx. `return c.json({ ok: true }, 200);`

    Both routes are sub-paths of `/:id` — they do NOT collide with `DELETE /share/:linkId`.

    Step 2 — In `apps/api/src/app.ts`, add `app.route("/budgets", createBudgetDangerZoneRoute(deps));`
    adjacent to the `createBudgetMembersRoute` mount (already present from Plan 06-03).
    Mount it BEFORE `budgetsRoutesFactory(deps)` if Hono route precedence would let the
    budgets-factory `/:id` swallow `/:id/archive` — sub-path specificity wins when
    registered first (same rule Plan 06-03 applies to budget-members).

    Step 3 — Add `archive` + `hardDelete` to the `WorkspaceRepo` interface in
    `packages/tenancy/src/ports/workspace-repo.ts` (alongside 06-02's `updateIdentity` /
    `hasTransactions`) and implement them in
    `packages/tenancy/src/adapters/persistence/workspace-repo.ts` (the `withTenantTx`
    UPDATE / DELETE described in Step 1).

    Step 4 — In `packages/tenancy/src/adapters/persistence/workspace-repo.ts`, in the
    `listForUser` method (line ~59 — the query behind `GET /budgets/active`), add
    `AND archived_at IS NULL` to the WHERE clause so archived budgets disappear from the
    listing that feeds the home grid and the switcher dropdown (D-09). Edit the repo
    method only — do not duplicate the filter, and do not touch `budgets.ts`.

  </action>
  <verify>
    <automated>cd apps/api && bun test test/routes/budget-archive.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "createBudgetDangerZoneRoute" apps/api/src/routes/budget-danger-zone.ts` >= 1
    - `grep -c 'r.post("/:id/archive"' apps/api/src/routes/budget-danger-zone.ts` returns 1 (or the factory-local var name for the Hono instance)
    - `grep -c 'r.post("/:id/delete"' apps/api/src/routes/budget-danger-zone.ts` returns 1
    - `grep "name_mismatch" apps/api/src/routes/budget-danger-zone.ts` matches
    - `grep "createBudgetDangerZoneRoute" apps/api/src/app.ts` matches
    - `grep "archived_at IS NULL" packages/tenancy/src/adapters/persistence/workspace-repo.ts` matches
    - `git diff --name-only apps/api/src/routes/budgets.ts` is EMPTY — budgets.ts must be untouched by this plan
    - `cd apps/api && bun test test/routes/budget-archive.test.ts` exits 0
  </acceptance_criteria>
  <done>Archive (soft) + Delete (typed-name hard) endpoints exist in a NEW budget-danger-zone.ts route file (budgets.ts untouched); archived budgets are filtered from GET /budgets/active via workspace-repo.ts; budget-archive.test.ts is GREEN.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                    |
| --------------------------------- | -------------------------------------------------------------- |
| client → archive/delete endpoints | Untrusted, destructive; owner-gated + typed-name confirmed     |
| client → onboarding endpoints     | Untrusted body; user_id must come from session, never the body |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                           | Disposition | Mitigation Plan                                                                                                        |
| ---------- | ---------------------- | ------------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------- |
| T-06-04-01 | Elevation of Privilege | Non-owner archives or deletes a shared budget                       | mitigate    | Owner-only gate (`withBootstrapUserContext` role lookup → 403) on both archive and delete                              |
| T-06-04-02 | Tampering              | Typed-name delete confirmation bypassed by hitting the API directly | mitigate    | Server re-validates `confirmName === budget.name` → 422 `name_mismatch`; client disable is cosmetic only               |
| T-06-04-03 | Information Disclosure | One user reads/writes another user's onboarding_progress            | mitigate    | Endpoints key on `session.user.id`; body never carries a user_id; table RLS keys on `app.current_user_id` (Plan 06-01) |
| T-06-04-04 | Tampering              | SQL injection in archive/delete/onboarding queries                  | mitigate    | Drizzle `sql` template tags with bind params only                                                                      |
| T-06-04-05 | Denial of Service      | Hard delete leaves orphaned child rows / FK errors                  | mitigate    | Rely on `ON DELETE CASCADE` FKs; if any FK is RESTRICT, delete children in the same tx                                 |

</threat_model>

<verification>
- `cd apps/api && bun test test/routes/budget-archive.test.ts test/routes/onboarding.test.ts` green
- `make test` passes (no regression in budgets.test.ts / active-listing)
- `budgets.ts` is NOT modified by this plan (06-02 owns it)
</verification>

<success_criteria>

- Archive sets archived_at and hides the budget from GET /budgets/active
- Delete hard-deletes only on a server-validated typed-name match
- Archive/delete live in their own route file — no budgets.ts merge conflict with Plan 06-02
- Serialized after 06-02/06-03 so the shared workspace-repo.ts + app.ts edits are conflict-free
- Onboarding progress GET/PUT scoped to the session user
- budget-archive.test.ts + onboarding.test.ts are GREEN
  </success_criteria>

<output>
After completion, create `.planning/phases/06-settings-onboarding-share-ui/06-04-SUMMARY.md`
</output>
</content>
</invoke>
