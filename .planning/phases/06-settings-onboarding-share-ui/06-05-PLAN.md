---
phase: 06-settings-onboarding-share-ui
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - apps/web/src/app/[locale]/(app)/budgets/[id]/settings/page.tsx
  - apps/web/src/components/settings/settings-accordion.tsx
  - apps/web/src/components/settings/budget-identity-section.tsx
  - apps/web/src/components/settings/cushion-mode-section.tsx
  - apps/web/src/components/settings/members-section.tsx
  - apps/web/src/components/settings/danger-zone-section.tsx
  - apps/web/src/components/settings/share-url-field.tsx
  - apps/web/src/app/[locale]/(app)/recurring/page.tsx
  - apps/web/messages/en.json
  - apps/web/test/settings/settings-accordion.test.tsx
  - apps/web/test/settings/danger-zone-section.test.tsx
autonomous: true
requirements:
  [
    SETT-01,
    SETT-02,
    SETT-03,
    SETT-04,
    SETT-05,
    SETT-06,
    SETT-07,
    SETT-08,
    SETT-09,
  ]
must_haves:
  truths:
    - "The Settings tab renders a 5-section accordion: Identity, Cushion, Recurring, Members (SHARED only), Danger Zone"
    - "D-01: Budget identity fields (name, currency) autosave per field on blur via PATCH /budgets/:id with a toast — no Save button (reuses the Phase 4 grid / Phase 5 Wallets inline-autosave pattern)"
    - "The currency field is locked with a tooltip when hasTransactions is true"
    - "D-02: the cushion-mode Switch persists instantly (PATCH cushion_mode_enabled) and shows a toast — no confirm dialog, because the toggle is fully reversible"
    - "D-03: the Recurring section reuses the existing Phase 4 recurring-rules-list / recurring-rule-form components inline, with add/edit opening in the Sheet primitive; the standalone /recurring route is retired"
    - "D-11: the Danger zone shows Archive + Delete to owners only; non-owner members see only Leave budget"
    - "D-13: deleting a budget requires a typed-name confirmation (type the exact budget name to enable hard-delete) inside an alert-dialog"
    - "D-14: Generate share link reveals an ephemeral read-only URL field with a Copy button — the link is shown for the session only and is gone on reload; no persistent outstanding-links list"
    - "D-16: revoking a member requires a confirm alert-dialog before the revoke POST fires"
  artifacts:
    - path: "apps/web/src/components/settings/settings-accordion.tsx"
      provides: "5-section accordion shell, default open = budget-identity"
      contains: "Accordion"
    - path: "apps/web/src/components/settings/danger-zone-section.tsx"
      provides: "Archive / Delete / Leave with AlertDialog confirms"
      contains: "AlertDialog"
  key_links:
    - from: "budget-identity-section.tsx"
      to: "PATCH /budgets/:id"
      via: "InlineEditCell onSave"
      pattern: "\\$patch"
    - from: "members-section.tsx"
      to: "POST /budgets/:id/share"
      via: "ShareUrlField generate"
      pattern: "share"
    - from: "danger-zone-section.tsx"
      to: "POST /budgets/:id/delete"
      via: "typed-name AlertDialog confirm"
      pattern: "delete"
---

<objective>
Fill the placeholder Settings tab with the 5-section accordion: Budget Identity (inline
autosave), Cushion Mode (Switch toggle), Recurring Rules (reuse Phase 4 components in a
Sheet), Members (SHARED-only, share link + revoke), and Danger Zone (archive / delete /
leave). Retire the standalone `/recurring` route.

Purpose: SETT-01..09. All backend endpoints land in Waves 1-2. This plan is the UI
composition layer. Per D-03 the recurring CRUD is reused, not rebuilt; per D-04 the
sections are an accordion (single-open); per D-09–D-16 the danger zone and member
actions use AlertDialog confirms.

i18n NOTE (checker WARNING 5): this plan CREATES the `share` namespace in en.json
(share-field copy keys). Plan 06-07 EXTENDS the same namespace with join-page keys and
therefore declares `depends_on: [01, 05]` so the `share` namespace exists before 06-07
adds to it. The two plans never write the same `share` keys.

Output: working Settings tab; `/recurring` retired; component tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-settings-onboarding-share-ui/06-UI-SPEC.md
@.planning/phases/06-settings-onboarding-share-ui/06-PATTERNS.md
@apps/web/src/components/common/inline-edit-cell.tsx
@apps/web/src/components/budgeting/recurring-rules-list.tsx

<interfaces>
<!-- Backend endpoints available after Waves 1-2: -->
GET   /budgets/:id          → { id, name, kind, defaultCurrency, cushionModeEnabled, reservesEnabled, hasTransactions, ... }
PATCH /budgets/:id          { name?, default_currency?, cushion_mode_enabled? } → 200 | 409 currency_locked
GET   /budgets/:id/members  → { members: [{ userId, role, name?, email? }] }
POST  /budgets/:id/members/:memberId/revoke → 200 | 403 | 409 last_owner
POST  /budgets/:id/share    { ttlDays?: 7 } → 201 { url }
POST  /budgets/:id/leave    → 200 | 409 last-owner
POST  /budgets/:id/archive  → 200
POST  /budgets/:id/delete   { confirmName } → 200 | 422 name_mismatch

<!-- Recurring-rules CRUD endpoint (Phase 4, unchanged — SETT-04 reuse): -->
<!--   the Phase 4 recurring-rules-list.tsx calls the budget-scoped recurring-rules route. -->
<!--   That route is /budgets/:id/recurring-rules/* — it is NOT the retired /recurring web route. -->

<!-- Reused primitives (no modification): -->
<!--   accordion.tsx, switch.tsx — Plan 06-01 -->
<!--   sheet.tsx, alert-dialog.tsx, sonner.tsx, separator.tsx, badge.tsx, avatar.tsx, tooltip.tsx -->
<!--   InlineEditCell — apps/web/src/components/common/inline-edit-cell.tsx (double-commit guard built in) -->
<!--   recurring-rules-list.tsx + recurring-rule-form.tsx — Phase 4, reuse directly -->
<!-- api-client call shape: api.budgets[":id"].$patch({ param, json }) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Identity + Cushion + Recurring sections + the accordion shell + i18n keys</name>
  <read_first>
    - apps/web/src/app/[locale]/(app)/budgets/[id]/settings/page.tsx (current placeholder — RSC shell that renders the client accordion)
    - apps/web/src/app/[locale]/(app)/budgets/[id]/wallets/page.tsx (analog RSC tab page — params, budget fetch)
    - apps/web/src/components/common/inline-edit-cell.tsx (InlineEditCell props — value/render/renderEditor/onSave; has its own commit guard)
    - apps/web/src/components/settings/display-currency-picker.tsx (currency select + useTranslations + toast + api pattern)
    - apps/web/src/components/budgeting/recurring-rules-list.tsx + recurring-rule-form.tsx (reuse verbatim in the Recurring section; note the API path they call)
    - apps/web/src/components/ui/accordion.tsx + switch.tsx (Plan 06-01 primitives)
    - .planning/phases/06-settings-onboarding-share-ui/06-UI-SPEC.md (Settings interaction model + Copywriting Contract — exact copy strings)
  </read_first>
  <behavior>
    - settings-accordion renders 5 `AccordionItem`s in order identity/cushion/recurring/members/danger; `type="single"`, `defaultValue="budget-identity"`, `collapsible`
    - the Members AccordionItem is rendered ONLY when `budget.kind === "SHARED"`
    - budget name InlineEditCell: blur → `api.budgets[":id"].$patch({ json:{ name } })` → toast `t("identity.name_saved")`
    - currency field: editable select when `hasTransactions===false`; static text + Tooltip (`t("identity.currency_locked_tooltip")`) when true
    - cushion Switch: checked = `budget.cushionModeEnabled`; onCheckedChange → `$patch({ json:{ cushion_mode_enabled } })` → toast (on/off variants)
    - Recurring section reuses the Phase 4 recurring-rules CRUD, which still calls the budget-scoped `/budgets/:id/recurring-rules/*` endpoint — SETT-04 stays verified after the `/recurring` web route is retired
  </behavior>
  <action>
    Step 1 — Rewrite `apps/web/src/app/[locale]/(app)/budgets/[id]/settings/page.tsx`:
    RSC that reads `params.id` + `params.locale`, fetches `GET /budgets/:id` server-side
    (use the existing server budget-fetch helper — grep `budget-fetch.server`), renders
    `<h1>` with `t("settings.page_heading")` ("Budget Settings"), and renders
    `<SettingsAccordion budget={budget} />` passing `{ id, name, kind, defaultCurrency,
    cushionModeEnabled, hasTransactions }`.

    Step 2 — `apps/web/src/components/settings/settings-accordion.tsx` (client component):
    `<Accordion type="single" defaultValue="budget-identity" collapsible>` with 5
    `AccordionItem`s. `AccordionTrigger` text from `t("settings.sections.*")`. Members
    item conditionally rendered (`budget.kind === "SHARED"`). Each `AccordionContent`
    renders the matching section component.

    Step 3 — `budget-identity-section.tsx` (D-01): name via `<InlineEditCell>`
    (renderEditor = `<Input maxLength={80}>`; onSave does the PATCH + toast). Currency:
    when `hasTransactions` false → currency `<Select>` (reuse `display-currency-picker`
    pattern) with onChange → PATCH; when true → static `<span>` + a lock icon wrapped in
    `<Tooltip>`. NO Save button — every field commits on blur with a toast (D-01).

    Step 4 — `cushion-mode-section.tsx` (D-02): a `<Switch>` bound to `cushionModeEnabled`,
    label "Cushion mode" + help text from UI-SPEC; `onCheckedChange` → PATCH
    `cushion_mode_enabled` → toast `t("cushion.on_toast")` / `t("cushion.off_toast")`.
    No confirm dialog (D-02) — the toggle persists instantly because it is fully
    reversible.

    Step 5 — Recurring section (D-03): render `<RecurringRulesList>` inline; the
    "+Add rule" dashed button (neutral, NOT yellow) opens `<Sheet>` containing
    `<RecurringRuleForm>` (D-03). Reuse both Phase 4 components verbatim — do not fork
    them. CONFIRM the reused `recurring-rules-list.tsx` still issues its CRUD calls
    against the budget-scoped `/budgets/:id/recurring-rules/*` route (NOT the retired
    standalone `/recurring/*` route) — if any reused component imports a path starting
    `/recurring` that is NOT `/budgets/:id/recurring-rules`, fix it to the budget-scoped
    route. The `/recurring` retirement in Step 6 is a WEB-route retirement only; the API
    recurring-rules endpoint is untouched.

    Step 6 — Retire `/recurring` (D-03): rewrite
    `apps/web/src/app/[locale]/(app)/recurring/page.tsx` to a `redirect()` to
    `/${locale}/budgets/[id]/settings` (per D-03; if no budget id is in context, redirect
    to `/`). Delete `recurring-page-client.tsx` / `actions.ts` if they are now
    unreferenced (grep first).

    Step 7 — Add the EN i18n keys to `apps/web/messages/en.json` under a new top-level
    `"settings"` namespace using the exact strings from the UI-SPEC Copywriting Contract
    (page_heading, sections.*, identity.*, cushion.*, members.*, danger.*). Apostrophes
    in ICU strings MUST be doubled (`You''re`). PL/UK translations are added in Plan 06-08.

    Step 8 — Component test `apps/web/test/settings/settings-accordion.test.tsx` (Vitest
    + RTL): renders 5 sections for a SHARED budget; renders only 4 (no Members) for a
    PRIVATE budget; default-open section is Budget Identity. Replace the Plan 06-01 stub.

    Step 9 — After the redirect replacement in Step 6, run `make test` to exercise the
    recurring-rules API integration tests and confirm SETT-04's backend endpoint is still
    reachable and green post-retirement (the web route retirement must not regress the
    recurring-rules route).

  </action>
  <verify>
    <automated>cd apps/web && bun run test -- settings/settings-accordion 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'defaultValue="budget-identity"' apps/web/src/components/settings/settings-accordion.tsx` matches
    - `grep -E 'kind === "SHARED"|kind !== "SHARED"' apps/web/src/components/settings/settings-accordion.tsx` matches (Members conditional)
    - `grep "InlineEditCell" apps/web/src/components/settings/budget-identity-section.tsx` matches
    - `grep -E "Switch" apps/web/src/components/settings/cushion-mode-section.tsx` matches
    - `grep "RecurringRulesList" apps/web/src/components/settings/settings-accordion.tsx` OR a recurring-section file matches
    - SETT-04 path check: `grep -rEn "recurring-rules|/recurring" apps/web/src/components/budgeting/recurring-rules-list.tsx` shows the API call path resolves to a `/budgets/:id/recurring-rules/*` route and NOT a bare `/recurring/*` API route
    - `grep -E "redirect" apps/web/src/app/[locale]/(app)/recurring/page.tsx` matches
    - `grep '"settings"' apps/web/messages/en.json` matches and the namespace has `sections`, `identity`, `cushion`, `danger` keys
    - `make test` exits 0 after the redirect replacement (recurring-rules API integration tests still green — SETT-04 not regressed)
    - `cd apps/web && bun run test -- settings/settings-accordion` exits 0
  </acceptance_criteria>
  <done>Settings tab renders the 5-section accordion; identity autosaves, cushion toggles, recurring reuses Phase 4 against the budget-scoped recurring-rules API; /recurring web route retired without regressing SETT-04; EN keys added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Members section (share link + revoke) + Danger Zone (archive/delete/leave)</name>
  <read_first>
    - apps/web/src/components/ui/alert-dialog.tsx (confirm dialog primitive)
    - apps/web/src/components/settings/display-currency-picker.tsx (client mutation + toast pattern)
    - .planning/phases/06-settings-onboarding-share-ui/06-PATTERNS.md (Pattern 6 ShareUrlField; section 7 inline-autosave)
    - .planning/phases/06-settings-onboarding-share-ui/06-UI-SPEC.md (Members + Danger Zone interaction model + exact dialog copy + accent-color rules)
    - apps/web/src/components/ui/badge.tsx + avatar.tsx (member row primitives)
  </read_first>
  <behavior>
    - members-section lists members (avatar + name + role badge + Revoke ghost button); empty state copy when no non-owner members
    - "Generate share link" → POST /budgets/:id/share → reveals `<ShareUrlField>` (read-only input + Copy icon button, yellow icon only); ephemeral (unmounts on reload)
    - Copy → `navigator.clipboard.writeText` → toast `t("share.copied")`; copy failure → toast `t("share.copy_failed")` (the catch branch MUST be implemented, not just described)
    - Revoke → AlertDialog confirm → POST revoke → member removed + toast
    - Danger Zone: owner sees Archive + Delete; non-owner sees Leave only
    - Archive → AlertDialog → POST archive → redirect to `/` (budget now hidden)
    - Delete → AlertDialog with typed-name input; confirm button enabled ONLY when input === budget.name; → POST /delete; 422 name_mismatch surfaces an error
    - Leave → AlertDialog → POST /leave; last-owner → Leave button disabled + tooltip
  </behavior>
  <action>
    Step 1 — `share-url-field.tsx` (D-14): implement per PATTERNS Pattern 6 — `useState`
    for the generated url + generating flag; `generate()` calls
    `api.budgets[":id"].share.$post({ param:{id}, json:{ttlDays:7} })`; the revealed URL
    is EPHEMERAL — held in component state only, no persistence, gone on reload, and no
    outstanding-links list is rendered (D-14). `copy()` uses
    `navigator.clipboard.writeText` wrapped in `try/catch` — the catch branch MUST call
    `toast.error(t("share.copy_failed"))` so a clipboard-permission failure surfaces to
    the user (not a silent no-op). Read-only `<Input>` + a `<Button>` with a yellow
    `Copy` lucide icon (icon only, not a filled button — accent discipline). Caption
    below: `t("share.expires_in_7_days")`.

    Step 2 — `members-section.tsx` (client): fetch members via
    `api.budgets[":id"].members.$get` (React Query). Render a row per member:
    `<Avatar>` + name + role `<Badge>` + a destructive ghost "Revoke" button
    (`min-h-[44px]`, red text — never yellow). Revoke (D-16) opens an `<AlertDialog>` with
    copy from the UI-SPEC ("Remove [name]?" / body / "Remove" / "Cancel") — the revoke
    POST fires ONLY after the user confirms the dialog; confirm → POST revoke →
    invalidate the members query + toast. Empty state (`"No other members yet."`)
    + the "Generate share link" button which mounts `<ShareUrlField>` on click.

    Step 3 — `danger-zone-section.tsx` (client, D-11 / D-13): section heading in
    destructive red. Props include `isOwner` and `isLastOwner` (derive `isOwner` from
    comparing `budget.ownerUserId`/the current member role; pass from the accordion).
    - Owner (D-11): "Archive budget" ghost button (red border) + "Delete budget" filled
      destructive button.
      - Archive → `<AlertDialog>` ("Archive this budget?" copy) → POST
        `api.budgets[":id"].archive.$post` → on success `router.push("/" )` and toast.
      - Delete (D-13) → `<AlertDialog>` with a typed-name `<Input>`; the "Delete forever"
        confirm button `disabled` unless the input value `=== budget.name` (type the
        exact budget name to enable hard-delete); on confirm POST
        `api.budgets[":id"].delete.$post({ json:{ confirmName } })`; on 422 show an
        inline error; on 200 `router.push("/")`.
    - Non-owner (D-11): "Leave budget" ghost button (red border) — no Archive/Delete —
      → `<AlertDialog>` → POST `leave`. If `isLastOwner` (a non-owner can't be last
      owner, but a sole-owner-shared case) the Leave button is `disabled` with a
      `<Tooltip>` (`t("danger.last_owner_tooltip")`).
    Use the exact dialog copy strings from the UI-SPEC Copywriting Contract.

    Step 4 — Add the `members`, `danger`, and `share` i18n keys to
    `apps/web/messages/en.json`. This plan CREATES the `share` namespace (share-field
    keys: `copied`, `copy_failed`, `expires_in_7_days`, the generate-button label).
    Plan 06-07 extends the SAME `share` namespace with join-page keys — declare no
    join-page keys here. Double all ICU apostrophes.

    Step 5 — Component test `apps/web/test/settings/danger-zone-section.test.tsx`:
    owner sees Archive + Delete, not Leave; non-owner sees Leave only; the Delete confirm
    button is disabled until the typed name matches exactly. Replace the Plan 06-01 stub.

  </action>
  <verify>
    <automated>cd apps/web && bun run test -- settings/danger-zone-section 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep "navigator.clipboard.writeText" apps/web/src/components/settings/share-url-field.tsx` matches
    - `grep "copy_failed" apps/web/src/components/settings/share-url-field.tsx` matches (the copy-failure toast is implemented, not just described)
    - `grep "AlertDialog" apps/web/src/components/settings/danger-zone-section.tsx` matches
    - `grep -E "disabled" apps/web/src/components/settings/danger-zone-section.tsx` matches (typed-name gate)
    - `grep -E "confirmName" apps/web/src/components/settings/danger-zone-section.tsx` matches
    - `grep -E "members" apps/web/src/components/settings/members-section.tsx` matches
    - `grep "AlertDialog" apps/web/src/components/settings/members-section.tsx` matches (revoke confirm dialog — D-16)
    - `grep -E '"members"|"danger"|"share"' apps/web/messages/en.json` matches all three
    - `cd apps/web && bun run test -- settings/danger-zone-section` exits 0
  </acceptance_criteria>
  <done>Members section (share link + revoke) and Danger Zone (archive/delete/leave with typed-name confirm) work; copy-failure toast implemented; the share namespace is created in en.json; component tests green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary        | Description                                                                |
| --------------- | -------------------------------------------------------------------------- |
| browser → API   | All section mutations cross to authenticated, owner/tenant-gated endpoints |
| clipboard write | Share URL written to the OS clipboard (requires secure context)            |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                                        | Disposition | Mitigation Plan                                                                                                                      |
| ---------- | ---------------------- | -------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| T-06-05-01 | Elevation of Privilege | Non-owner sees/uses Archive/Delete controls                                      | mitigate    | Danger zone renders Archive/Delete only when `isOwner`; the server owner-gate (Plan 06-04) is the real authority — UI is convenience |
| T-06-05-02 | Tampering              | Typed-name delete confirm bypassed in the UI                                     | accept      | UI disable is cosmetic; the server re-validates `confirmName` (Plan 06-04 T-06-04-02) — defense is server-side                       |
| T-06-05-03 | Information Disclosure | Members section shown on a PRIVATE budget exposes nothing useful but is wrong UX | mitigate    | Members `AccordionItem` is not rendered when `kind !== "SHARED"`                                                                     |
| T-06-05-04 | Information Disclosure | Generated share URL persists in DOM/history after use                            | mitigate    | `ShareUrlField` is ephemeral — unmounts on reload; no link-history list (D-14)                                                       |

</threat_model>

<verification>
- `cd apps/web && bun run test -- settings/` green
- `cd apps/web && bun run build` compiles
- `/recurring` redirects (no standalone page)
- `make test` green after the /recurring retirement — SETT-04 recurring-rules API not regressed
</verification>

<success_criteria>

- 5-section accordion Settings tab works (4 sections for PRIVATE)
- Identity autosave, cushion toggle, recurring Sheet, member share/revoke, archive/delete/leave all wired
- /recurring retired without breaking the recurring-rules API (SETT-04 verified post-retirement)
- The share namespace is created in en.json for Plan 06-07 to extend
- Settings component tests green
  </success_criteria>

<output>
After completion, create `.planning/phases/06-settings-onboarding-share-ui/06-05-SUMMARY.md`
</output>
</content>
</invoke>
