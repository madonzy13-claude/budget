---
phase: 10-user-settings-redesign
plan: 05
type: execute
wave: 2
depends_on: []
autonomous: true
requirements: [USET-07]
files_modified:
  - apps/web/src/app/[locale]/forgot-password/page.tsx
  - apps/web/src/app/[locale]/reset-password/page.tsx
  - apps/web/src/app/[locale]/sign-in/page.tsx
  - apps/web/test/auth/forgot-password.test.tsx
  - apps/web/test/auth/reset-password.test.tsx
  - apps/web/e2e/features/forgot-password.feature
  - apps/web/messages/en.json
  - apps/web/messages/pl.json
  - apps/web/messages/uk.json

must_haves:
  truths:
    - "A /[locale]/forgot-password page lets a logged-out user request a reset link via authClient.requestPasswordReset; it always shows a neutral success (no account enumeration)"
    - "A /[locale]/reset-password page reads ?token=, takes a new password (min length 10), calls authClient.resetPassword({ newPassword, token }), and redirects to sign-in on success; a missing/expired token shows an error linking back to forgot-password"
    - "The sign-in 'Forgot your password?' link points at /[locale]/forgot-password (was a dead /reset-password link)"
  artifacts:
    - path: "apps/web/src/app/[locale]/forgot-password/page.tsx"
      provides: "Request-reset page"
      contains: "requestPasswordReset"
    - path: "apps/web/src/app/[locale]/reset-password/page.tsx"
      provides: "Consume-token set-new-password page"
      contains: "resetPassword"

threat_model:
  - id: T-10-07
    stride: Information Disclosure (account enumeration)
    surface: forgot-password request form
    mitigation: "The forgot page shows the SAME neutral 'if an account exists, we sent a link' result whether or not the email is registered — no enumeration. Reset/sign-in endpoints keep the global 100/60s IP rate limit (anti-brute-force); E2E uses AUTH_RATE_LIMIT_DISABLED=true."
  - id: T-10-08
    stride: Tampering (token replay / weak password)
    surface: reset-password consume page
    mitigation: "Token is single-use + 1800s TTL (resetPasswordTokenExpiresIn), validated server-side by Better Auth; the page enforces minPasswordLength 10 client-side AND Better Auth re-validates server-side. Expired/used token → error + link to request a fresh one (no silent success)."
---

<objective>
Build the two MISSING logged-out password pages and fix the dead sign-in link.
`/[locale]/forgot-password` requests a reset link (`authClient.requestPasswordReset`), `/[locale]/reset-password`
consumes `?token=` and sets a new password (`authClient.resetPassword`). Repoint sign-in's "Forgot your password?"
link from the dead `/reset-password` target to `/forgot-password`.

Purpose: CONTEXT decision 7 + RESEARCH §4. These pages also back the logged-in email-gated password change in plan
10-04 (same `/reset-password` consume page). No backend change — the reset email + token TTL are already wired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-user-settings-redesign/10-CONTEXT.md
@.planning/phases/10-user-settings-redesign/10-RESEARCH.md

CLIENT METHODS (RESEARCH §4 — already wired server-side):

- forgot-password: `authClient.requestPasswordReset({ email, redirectTo: "/<locale>/reset-password" })` (alias
  `forgetPassword`). Fires the existing sendResetPassword (reset-password template, 1800s TTL). Always render the
  neutral success state afterward.
- reset-password: read `token` from the URL query; new-password form (min length **10**, mirror
  minPasswordLength); `authClient.resetPassword({ newPassword, token })` → on success redirect to
  `/<locale>/sign-in`; on Better Auth error (missing/expired/used token) show an error + a link back to
  /forgot-password.

EXISTING: sign-in/page.tsx:110 has `href={`/${locale}/reset-password`}` for the "Forgot?" link — change to
`/${locale}/forgot-password`. Existing `reset._` i18n keys may already exist (RESEARCH "Exists"); reuse them and add
only what's missing (forgot._ request-page copy, neutral-success line, expired-token error). Mirror sign-in/page.tsx
structure + DESIGN.md tokens (centered auth card, yellow accent CTA, Inter/IBM Plex).
</context>

<feature>
  <name>forgot-password + reset-password pages + sign-in link fix</name>
  <files>
    apps/web/src/app/[locale]/forgot-password/page.tsx,
    apps/web/src/app/[locale]/reset-password/page.tsx,
    apps/web/src/app/[locale]/sign-in/page.tsx,
    apps/web/test/auth/forgot-password.test.tsx,
    apps/web/test/auth/reset-password.test.tsx,
    apps/web/e2e/features/forgot-password.feature,
    apps/web/messages/en.json, apps/web/messages/pl.json, apps/web/messages/uk.json
  </files>
  <read_first>
    - apps/web/src/app/[locale]/sign-in/page.tsx (auth-card structure to mirror + the link at ~110 to fix)
    - apps/web/src/lib/auth-client.ts (requestPasswordReset / resetPassword availability + exact arg shape)
    - apps/web/messages/en.json (reset.* / signin.* existing keys to reuse)
    - 10-RESEARCH.md §4
  </read_first>
  <behavior>
    forgot-password.test.tsx (Vitest+RTL):
      - submit with an email → calls authClient.requestPasswordReset({ email, redirectTo }) (mock) → neutral
        success message renders (same copy regardless of whether the email exists).
    reset-password.test.tsx:
      - with ?token=abc, a valid new password (>=10) → calls authClient.resetPassword({ newPassword, token:"abc" });
        a password < 10 chars shows the min-length validation and does NOT call resetPassword;
        with no token (or a rejected token) the page shows the expired/invalid error + a link to /forgot-password.
    forgot-password.feature (Gherkin, fresh user): golden — request reset (intercept email), open the reset link
      with the token, set a new password, sign in with it; plus an expired-token scenario shows the error.
      EN/PL/UK + 375px. The sign-in "Forgot?" link navigates to /forgot-password.
  </behavior>
  <implementation>
    RED: write both component tests (mock authClient, mock useSearchParams for the token) → fail.
      Commit: test(10-05): failing forgot/reset page tests.
    GREEN:
      1. forgot-password/page.tsx (email form → requestPasswordReset → neutral success).
      2. reset-password/page.tsx (token from query, min-10 new-password form → resetPassword → redirect to sign-in;
         error state for bad/expired token).
      3. sign-in/page.tsx: repoint the Forgot link to /forgot-password.
      4. i18n: add forgot.* (+ any missing reset.* leaf) in en/pl/uk.
      `cd apps/web && bun run test`; `make restart-web`. Commit: feat(10-05): forgot/reset-password pages + sign-in link fix.
    E2E: forgot-password.feature + steps; `make test-e2e --grep @forgot-password`.
      Commit: test(10-05): forgot-password e2e (golden + expired token).
  </implementation>
  <acceptance_criteria>
    - apps/web/src/app/[locale]/forgot-password/page.tsx exists and calls authClient.requestPasswordReset (grep)
    - apps/web/src/app/[locale]/reset-password/page.tsx exists and calls authClient.resetPassword with a token from the query (grep)
    - sign-in/page.tsx links to `/${locale}/forgot-password` (not /reset-password) — grep confirms the new target, old gone
    - reset page rejects passwords shorter than 10 chars (test asserts no resetPassword call)
    - `cd apps/web && bun run test` passes both auth page tests
    - `make test-e2e --grep @forgot-password` green (golden + expired-token)
  </acceptance_criteria>
</feature>

<verification>
- cd apps/web && bun run test (forgot + reset page tests green)
- make test-e2e --grep @forgot-password green
- grep: sign-in links to /forgot-password; reset page reads ?token + enforces min 10
- make restart-web then manually load /forgot-password + /reset-password?token=x
</verification>

<success_criteria>

- forgot-password + reset-password pages built; sign-in dead link fixed (USET-07)
- Reset page enforces min length 10 + handles expired/invalid token
- Backs the logged-in password change (plan 10-04) via the shared consume page
  </success_criteria>

<output>
After completion, create `.planning/phases/10-user-settings-redesign/10-05-SUMMARY.md`
</output>
