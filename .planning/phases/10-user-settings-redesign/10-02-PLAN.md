---
phase: 10-user-settings-redesign
plan: 02
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [USET-01, USET-02, USET-03]
files_modified:
  - apps/web/src/lib/settings-tabs.ts
  - apps/web/src/app/[locale]/(app)/settings/[[...tab]]/page.tsx
  - apps/web/src/app/[locale]/(app)/settings/[[...tab]]/loading.tsx
  - apps/web/src/app/[locale]/(app)/settings/layout.tsx
  - apps/web/src/components/settings/user-settings-shell.tsx
  - apps/web/src/components/settings/general-pill.tsx
  - apps/web/src/components/settings/user-pill.tsx
  - apps/web/test/settings/user-settings-shell.test.tsx
  - apps/web/messages/en.json
  - apps/web/messages/pl.json
  - apps/web/messages/uk.json

must_haves:
  truths:
    - "The legacy 4-tab (app)/settings page is replaced by a 2-pill client carousel (General · User) mirroring the BDP pill pattern: catch-all [[...tab]] route + pushState carousel + per-route loading.tsx"
    - "Switching pills updates the URL (/settings, /settings/user) via pushState with ZERO per-nav RSC roundtrip"
    - "General pill renders the existing LocaleSelect + DisplayCurrencyPicker restyled into the new shell — no backend change; locale change still full-reloads to swap [locale]"
    - "User pill renders the section accordion shell (Profile · Security · Danger Zone) mirroring settings-accordion.tsx; section bodies are mounted as slots filled by plans 10-03/04/06"
    - "settings.providers.* i18n keys are removed; new settings.general / settings.user / settings.profile / settings.security / settings.danger key trees exist in en/pl/uk"
  artifacts:
    - path: "apps/web/src/lib/settings-tabs.ts"
      provides: "Shared (non-client) pill consts/types — parallel to lib/bdp-tabs.ts"
      contains: "isSettingsTab"
    - path: "apps/web/src/components/settings/user-settings-shell.tsx"
      provides: "Client pushState carousel for the 2 pills"
      contains: "use client"
    - path: "apps/web/src/components/settings/user-pill.tsx"
      provides: "Profile/Security/Danger accordion shell with section slots"
      contains: "Accordion"

threat_model:
  - id: T-10-02
    stride: Spoofing / auth boundary
    surface: (app)/settings route (authenticated area)
    mitigation: "Route stays under the existing (app) segment whose layout + middleware enforce the session (RLS + getServerSession). No new auth surface introduced; the shell is a client view over the already-authenticated user's own session data (locale/currency from getServerSession). No tenant data is read here."
---

<objective>
Replace the legacy 4-tab `(app)/settings` page with a **2-pill client carousel** (General · User) that reuses the
BDP pill pattern (catch-all `[[...tab]]` route, pushState carousel, per-route `loading.tsx` for instant soft-nav).
The **General** pill restyles the existing language + display-currency controls (no backend change). The **User**
pill renders the section accordion shell (Profile · Security · Danger Zone) mirroring `settings-accordion.tsx`,
with section bodies as slots that plans 10-03 / 10-04 / 10-06 fill. Restructure the `settings.*` i18n tree
(remove `settings.providers.*`).

Purpose: CONTEXT decisions 1-3. This is the structural scaffold every User-pill section plugs into — like Phase-3's
BDP frame was for the tab phases. Output: the shell, both pills, the accordion slots, loading.tsx, i18n tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-user-settings-redesign/10-CONTEXT.md
@.planning/phases/10-user-settings-redesign/10-RESEARCH.md
@DESIGN.md

ANALOGS TO MIRROR (read_first has the exact files):

- Client pill carousel: the BDP client tabs (`apps/web/src/lib/bdp-tabs.ts` is the non-client shared consts;
  the catch-all `[[...tab]]` route + the `budget-detail.tsx` client carousel under `budgets/[id]/`). Mirror the
  structure but LIGHTER — user settings has only 2 pills and NO per-budget RSC data tabs, so no tiered data
  prefetch is needed (General reads session; User mounts client components). Pills push `/settings` and
  `/settings/user`.
- Section accordion: `apps/web/src/components/settings/settings-accordion.tsx` (Accordion + AccordionItem per
  section). Build the User-pill accordion the same way with items Profile / Security / Danger Zone.
- Existing controls to reuse VERBATIM in General: `apps/web/src/components/settings/locale-select.tsx`
  (window.location.assign full reload — KEEP) + `display-currency-picker.tsx`.
- Design tokens: DESIGN.md (Binance dark, single yellow accent #fcd535, surface-card #1e2329, hairline #2b3139,
  Inter / IBM Plex). Reuse `components/ui/*` primitives — no new visual vocabulary.

SHARED CONSTS (lib/settings-tabs.ts — parallel to bdp-tabs.ts, NO "use client" so the server page can call it):

```ts
export const SETTINGS_TAB_ORDER = ["general", "user"] as const;
export type SettingsTab = (typeof SETTINGS_TAB_ORDER)[number];
export function isSettingsTab(s: string | undefined | null): s is SettingsTab {
  return !!s && (SETTINGS_TAB_ORDER as readonly string[]).includes(s);
}
```

Server page (`[[...tab]]/page.tsx`) reads the optional `tab` segment, validates with `isSettingsTab` (default
"general"), reads `getServerSession({ disableCookieCache: true })` for initial locale/currency (mirror the OLD
page's fresh-read comment), and renders `<UserSettingsShell initialTab activeUser=...>`.

SECTION SLOTS: user-pill.tsx renders three AccordionItems whose bodies import the section components that plans
10-03/04/06 create (`<ProfileSection/>`, `<SecuritySection/>`, `<AccountDangerZone/>`). In THIS plan those imports
point at thin placeholder components (or are wired with a TODO comment) so the shell compiles and renders the
accordion structure; the real bodies land in 10-03/04/06. Default-open: Profile.

i18n: remove the `settings.providers` subtree from en/pl/uk. Add key trees (with PL/UK translations, not English
placeholders): `settings.general.{title}`, `settings.user.{title}`, plus the pill labels
`settings.pills.{general,user}`. Reserve `settings.profile.*`, `settings.security.*`, `settings.danger.*` namespaces
(the section plans add their own leaf keys — add only the section titles here).
</context>

<feature>
  <name>2-pill settings carousel + General pill + User accordion shell + i18n restructure</name>
  <files>
    apps/web/src/lib/settings-tabs.ts,
    apps/web/src/app/[locale]/(app)/settings/[[...tab]]/page.tsx,
    apps/web/src/app/[locale]/(app)/settings/[[...tab]]/loading.tsx,
    apps/web/src/app/[locale]/(app)/settings/layout.tsx,
    apps/web/src/components/settings/user-settings-shell.tsx,
    apps/web/src/components/settings/general-pill.tsx,
    apps/web/src/components/settings/user-pill.tsx,
    apps/web/test/settings/user-settings-shell.test.tsx,
    apps/web/messages/en.json, apps/web/messages/pl.json, apps/web/messages/uk.json
  </files>
  <read_first>
    - apps/web/src/lib/bdp-tabs.ts (exact shared-consts pattern to parallel)
    - the BDP catch-all route + budget-detail client carousel under apps/web/src/app/[locale]/(app)/budgets/[id]/ (pushState carousel + loading.tsx pattern)
    - apps/web/src/components/settings/settings-accordion.tsx (accordion section shell)
    - apps/web/src/app/[locale]/(app)/settings/page.tsx (the page being REPLACED — copy the fresh-session read + header)
    - apps/web/src/components/settings/{locale-select,display-currency-picker}.tsx (reuse verbatim in General)
    - apps/web/messages/en.json (settings.* subtree — see what providers.* keys to remove + structure)
    - DESIGN.md (tokens)
  </read_first>
  <behavior>
    - Visiting /[locale]/settings renders the General pill active (language + currency); /[locale]/settings/user
      renders the User pill (Profile/Security/Danger accordion). Both pills are buttons that pushState the URL.
    - Clicking a pill swaps the visible pane WITHOUT a full navigation (assert no document reload / RSC fetch in
      the component test — the carousel switches the active index in client state and pushState updates the URL).
    - General: LocaleSelect + DisplayCurrencyPicker render with the user's current values; changing locale triggers
      the existing full reload (unchanged).
    - User accordion: three sections render with Profile open by default; section bodies are the placeholder/slot
      components (real bodies arrive in 10-03/04/06).
    - `grep "settings.providers" apps/web/messages/*.json` returns nothing; the new pill/section title keys exist
      in all three locales.
  </behavior>
  <implementation>
    1. lib/settings-tabs.ts (consts above). Commit: feat(10-02): settings-tabs shared consts.
    2. Replace the route: delete the old `(app)/settings/page.tsx`; create `[[...tab]]/page.tsx` (server: validate
       tab, fresh session read, render shell) + `[[...tab]]/loading.tsx` (skeleton, mirror a BDP tab loading.tsx)
       + `settings/layout.tsx` if the BDP analog uses one for the sticky pill bar.
    3. user-settings-shell.tsx ("use client" pushState carousel over the 2 pills, sticky pill bar styled per
       DESIGN.md yellow accent). general-pill.tsx (reuse LocaleSelect + DisplayCurrencyPicker). user-pill.tsx
       (Accordion with Profile/Security/Danger items + slot components).
    4. i18n: remove settings.providers.* from en/pl/uk; add pill + section title keys (PL/UK translated).
    5. Component test user-settings-shell.test.tsx (Vitest+RTL): renders both pills, clicking "User" shows the
       accordion + calls pushState (mock history), General shows locale+currency. Write test FIRST (red), then green.
       Run `cd apps/web && bun run test`.
    6. `make restart-web` (FE bundled at build) and load /settings to confirm the carousel renders.
       Commit: feat(10-02): 2-pill settings carousel + General + User accordion shell + i18n.
  </implementation>
  <acceptance_criteria>
    - apps/web/src/lib/settings-tabs.ts contains `export function isSettingsTab` and `SETTINGS_TAB_ORDER`
    - the old apps/web/src/app/[locale]/(app)/settings/page.tsx no longer exists; `[[...tab]]/page.tsx` and `[[...tab]]/loading.tsx` exist
    - user-settings-shell.tsx contains "use client" and uses history pushState (grep "pushState")
    - user-pill.tsx renders an Accordion with three items (grep "Profile"/"Security"/"Danger" or their i18n keys)
    - `grep -r "settings.providers" apps/web/messages` exits 1 (removed)
    - `cd apps/web && bun run test` passes user-settings-shell.test.tsx
  </acceptance_criteria>
</feature>

<verification>
- cd apps/web && bun run test (shell component test green)
- grep settings.providers apps/web/messages → exit 1
- make restart-web then load /settings + /settings/user → carousel + accordion render (no full reload on pill switch)
- next build (apps/web) compiles — no server-only-in-client boundary error (memory: pass server components as children)
</verification>

<success_criteria>

- Legacy 4-tab page replaced by the 2-pill carousel (USET-01)
- User pill accordion shell (Profile/Security/Danger slots) ready for section plans (USET-02)
- General pill restyles existing locale + currency, no backend change (USET-03)
- Provider i18n removed; new key trees in all three locales
  </success_criteria>

<output>
After completion, create `.planning/phases/10-user-settings-redesign/10-02-SUMMARY.md`
</output>
