---
phase: 10-user-settings-redesign
plan: 04
type: tdd
wave: 2
depends_on: [10-02]
autonomous: true
requirements: [USET-05]
files_modified:
  - apps/web/src/components/settings/security-section.tsx
  - apps/web/src/components/settings/sessions-list.tsx
  - apps/web/test/settings/security-section.test.tsx
  - apps/web/test/settings/sessions-list.test.tsx
  - apps/web/e2e/features/settings-security.feature
  - apps/web/messages/en.json
  - apps/web/messages/pl.json
  - apps/web/messages/uk.json

must_haves:
  truths:
    - "The Security section offers an email-gated password change that REUSES the reset flow: a button calls authClient.requestPasswordReset({ email: <self>, redirectTo: /<locale>/reset-password }) — no up-front new-password entry, no custom pending table"
    - "The active-sessions list (existing sessions-list.tsx) is restyled into the section and gains a 'Sign out all other devices' action calling authClient.revokeOtherSessions()"
    - "revokeSession still removes a single row; revokeOtherSessions leaves only the current session"
  artifacts:
    - path: "apps/web/src/components/settings/security-section.tsx"
      provides: "Security accordion body: password-change button + sessions list + sign-out-others"
      contains: "requestPasswordReset"
    - path: "apps/web/src/components/settings/sessions-list.tsx"
      provides: "Sessions list with sign-out-others action"
      contains: "revokeOtherSessions"

threat_model:
  - id: T-10-05
    stride: Spoofing / privilege (password change)
    surface: in-app password change
    mitigation: "Password change is email-gated: the button only triggers a reset EMAIL to the account's own verified address; the actual password is set only after clicking the emailed token link (reuses sendResetPassword + /reset-password consume, resetPasswordTokenExpiresIn 1800s). A hijacked session cannot set a new password without access to the inbox. Reset/sign-in endpoints keep the global 100/60s rate limit (anti-brute-force)."
  - id: T-10-06
    stride: Session management
    surface: revokeOtherSessions
    mitigation: "Better Auth scopes revokeOtherSessions to the authenticated user's own sessions only (server-enforced); it cannot revoke another user's sessions. Current session is preserved so the user is not locked out."
---

<objective>
Build the **Security** section of the User pill: (1) an **email-gated password change** that reuses the existing
reset flow — a button fires `authClient.requestPasswordReset` to the user's own address, and the password is set on
the shared `/reset-password` consume page (no new-password entry in settings, no custom pending table); (2) the
existing **active-sessions list** restyled into the section with **revoke** (existing) plus a new **"Sign out all
other devices"** action (`authClient.revokeOtherSessions()`).

Purpose: CONTEXT decision 5 + RESEARCH §3/§4. Mounts into the User-pill Security slot from plan 10-02. No backend
change — pure reuse of already-wired Better Auth client methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-user-settings-redesign/10-CONTEXT.md
@.planning/phases/10-user-settings-redesign/10-RESEARCH.md

CLIENT METHODS (RESEARCH §3, §4 — already available, no config change):

- Password change (logged in): `authClient.requestPasswordReset({ email: session.user.email, redirectTo:
"/<locale>/reset-password" })` → existing sendResetPassword fires the reset-password email → user clicks → lands
  on the /reset-password page built by plan 10-05. Show a toast "We emailed you a link to set a new password."
- Sessions: `authClient.revokeSession({ token })` (EXISTING in sessions-list.tsx) + NEW
  `authClient.revokeOtherSessions()` for sign-out-others → toast + drop all rows except the current one.

UI: security-section.tsx ("use client") is the Security accordion body. It renders a "Change password" button
(→ requestPasswordReset, with a confirm/explanatory line that a link will be emailed) and embeds <SessionsList>.
Add the sign-out-others button to sessions-list.tsx itself (above or below the table), guarded by a confirm
AlertDialog (mirror the existing single-revoke AlertDialog). Reuse ui/button + ui/alert-dialog + toast — DESIGN.md
tokens, no new visual vocabulary.

i18n: add settings.security.\* leaf keys (change_password label + emailed-link toast + sign_out_others label +
its confirm copy + success/error toasts) in en/pl/uk.
</context>

<feature>
  <name>Security section: email-gated password change + sessions restyle + sign-out-others</name>
  <files>
    apps/web/src/components/settings/security-section.tsx,
    apps/web/src/components/settings/sessions-list.tsx,
    apps/web/test/settings/security-section.test.tsx,
    apps/web/test/settings/sessions-list.test.tsx,
    apps/web/e2e/features/settings-security.feature,
    apps/web/messages/en.json, apps/web/messages/pl.json, apps/web/messages/uk.json
  </files>
  <read_first>
    - apps/web/src/components/settings/sessions-list.tsx (existing revokeSession + AlertDialog pattern to extend)
    - apps/web/src/components/settings/user-pill.tsx (the Security slot this mounts into — from 10-02)
    - apps/web/src/lib/auth-client.ts (requestPasswordReset / revokeOtherSessions availability)
    - apps/web/src/app/[locale]/sign-in/page.tsx (forgetPassword/requestPasswordReset call idiom if present)
    - 10-RESEARCH.md §3, §4
  </read_first>
  <behavior>
    security-section.test.tsx (Vitest+RTL):
      - "Change password" button calls authClient.requestPasswordReset({ email, redirectTo }) (mock) + success toast.
    sessions-list.test.tsx:
      - existing single-revoke still removes its row;
      - "Sign out all other devices" → confirm → calls authClient.revokeOtherSessions() (mock) → only the current
        session row remains.
    settings-security.feature (Gherkin, fresh user): trigger password-change email (intercept the send), revoke a
      session, sign out other devices. EN/PL/UK + 375px.
  </behavior>
  <implementation>
    RED: write the two component tests (mock authClient) → fail. Commit: test(10-04): failing security section + sign-out-others tests.
    GREEN:
      1. sessions-list.tsx: add the sign-out-others button + AlertDialog + revokeOtherSessions handler (mirror the
         existing revoke handler/state update).
      2. security-section.tsx: change-password button → requestPasswordReset({ email, redirectTo }) + embed
         <SessionsList>.
      3. i18n settings.security.* en/pl/uk.
      `cd apps/web && bun run test` until green. Commit: feat(10-04): security section + sessions sign-out-others.
    REFACTOR: dedupe the sign-out-others AlertDialog + handler against the existing single-revoke dialog into one
      reusable confirm-action shape if it reads cleanly; keep the session-row state update in one place. Tests green.
      Commit: refactor(10-04): share revoke confirm-dialog shape.
    E2E: settings-security.feature + steps; `make test-e2e --grep @settings-security`.
      Commit: test(10-04): security e2e.
  </implementation>
  <acceptance_criteria>
    - security-section.tsx calls authClient.requestPasswordReset with a redirectTo ending /reset-password (grep)
    - sessions-list.tsx calls authClient.revokeOtherSessions (grep) behind a confirm dialog
    - `cd apps/web && bun run test` passes security-section.test.tsx + sessions-list.test.tsx
    - settings.security.* keys exist in en/pl/uk
    - `make test-e2e --grep @settings-security` green
  </acceptance_criteria>
</feature>

<verification>
- cd apps/web && bun run test (both component tests green)
- make test-e2e --grep @settings-security green
- grep: requestPasswordReset (redirectTo /reset-password) + revokeOtherSessions present
</verification>

<success_criteria>

- Email-gated password change reusing the reset flow (USET-05)
- Active-sessions list restyled + revoke + sign-out-others
  </success_criteria>

<output>
After completion, create `.planning/phases/10-user-settings-redesign/10-04-SUMMARY.md`
</output>
