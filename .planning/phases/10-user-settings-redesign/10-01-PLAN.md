---
phase: 10-user-settings-redesign
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [USET-08]
files_modified:
  - apps/api/src/routes/settings.ts
  - apps/api/test/routes/settings.test.ts
  - packages/identity/src/adapters/persistence/better-auth.ts
  - packages/identity/src/adapters/persistence/schema.ts
  - packages/identity/src/contracts/api.ts
  - packages/identity/src/ports/user-repo.ts
  - packages/identity/src/adapters/persistence/user-repo.ts
  - packages/identity/src/application/update-provider-prefs.ts
  - packages/identity/test/provider-prefs.test.ts
  - apps/web/src/lib/server-session.ts
  - drizzle/0045_phase10_drop_provider_prefs.sql

must_haves:
  truths:
    - "The AI/voice Provider feature is removed end-to-end: no preferredLlmProvider/preferredSttProvider in identity schema, contracts, repo, Better Auth additionalFields, the API route, or the web session normalizer"
    - "Migration 0045 drops identity.users.preferred_llm_provider and preferred_stt_provider and applies cleanly (make migrate exits 0 after migrator rebuild)"
    - "PUT /settings/provider-prefs no longer exists; a request to it returns 404"
    - "make ci-gate stays green (6/6 tenant-leak) after the column drop"
    - "make test (identity + api) is green with the provider-prefs tests removed; no dangling import of LLMProviderName/STTProviderName/updateProviderPrefs remains (grep clean)"
  artifacts:
    - path: "drizzle/0045_phase10_drop_provider_prefs.sql"
      provides: "Hand-authored migration dropping both provider-pref columns"
      contains: "DROP COLUMN"
    - path: "apps/api/src/routes/settings.ts"
      provides: "Settings route with the provider-prefs handler + schema + imports removed"
      contains: "display-currency"
    - path: "packages/identity/src/adapters/persistence/schema.ts"
      provides: "users table without the two provider columns"
      contains: "export const users"

threat_model:
  - id: T-10-01
    stride: Tampering (destructive migration)
    surface: drizzle/0045 DROP COLUMN on identity.users
    mitigation: "Columns are nullable text holding only a non-sensitive provider preference enum — no other data depends on them (no FK, no index beyond the column itself). DROP COLUMN is irreversible but loses only the discarded feature's preference. Migration is the LAST step (after all code stops referencing the columns) so a half-applied state never leaves code reading a dropped column. Verify on a fresh dev DB before prod."
---

<objective>
Remove the AI/voice **Provider** feature (LLM + STT preferences) end-to-end — API route, identity package
(contracts, port, repo, application service, Better Auth additionalFields), the web session normalizer, and
finally the two DB columns via migration 0045. The legacy settings page's Provider tab is removed separately by
Plan 10-02 (which replaces the whole page); this plan owns everything BELOW the FE shell.

Purpose: CONTEXT decision 8 — the Provider feature is dead and must leave no schema, type, or route residue.
Doing the column DROP last (after all code stops referencing it) avoids a window where live code reads a dropped
column. Output: provider code deleted across api + identity + web-session, one migration, tests updated, ci-gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-user-settings-redesign/10-CONTEXT.md
@.planning/phases/10-user-settings-redesign/10-RESEARCH.md

REMOVAL SURFACE (exact, from CONTEXT "Provider-removal surface"):

- apps/api/src/routes/settings.ts — delete `providerPrefsSchema` (~30-33), the `PUT /provider-prefs` handler
  (~73-96), and the `LLMProviderName`/`STTProviderName` import (~13-17). Leave /locale, /display-currency,
  /sessions untouched.
- apps/api/test/routes/settings.test.ts — delete the provider-prefs test block (~119-149).
- packages/identity/src/adapters/persistence/better-auth.ts — delete the `preferredLlmProvider` +
  `preferredSttProvider` entries from `user.additionalFields` (~149-158). Leave `locale`/`displayCurrency`.
- packages/identity/src/adapters/persistence/schema.ts — delete the `preferredLlmProvider`/`preferredSttProvider`
  column defs from `users`. Update the doc comment that lists additionalFields.
- packages/identity/src/contracts/api.ts — delete `LLMProviderName`, `STTProviderName`, and the two UserDTO fields.
- packages/identity/src/ports/user-repo.ts — delete the `updateProviderPrefs` signature.
- packages/identity/src/adapters/persistence/user-repo.ts — delete the `updateProviderPrefs` method + its SQL.
- packages/identity/src/application/update-provider-prefs.ts — delete the whole file.
- packages/identity/test/provider-prefs.test.ts — delete the whole file.
- apps/web/src/lib/server-session.ts — delete the `preferredLlmProvider`/`preferredSttProvider` camelCase
  normalization (~196-201).
- drizzle/0045_phase10_drop_provider_prefs.sql — NEW migration (0045 is the next free number; verify nothing
  newer landed: `ls drizzle/*.sql | tail`).

GOTCHA (memory: migrator rebuild): `make migrate` silently no-ops unless the migrator image is rebuilt (it bakes
`drizzle/`). After authoring the migration: `docker compose build migrator && make migrate`, then verify with
`\d identity.users` that neither column remains. ci-gate downs the dev stack — restart after.

MIGRATION BODY (hand-authored, mirror existing drizzle/\*.sql idempotent style):

```sql
-- 0045_phase10_drop_provider_prefs.sql — Phase 10: remove AI/voice Provider feature (USET-08)
ALTER TABLE identity.users DROP COLUMN IF EXISTS preferred_llm_provider;
ALTER TABLE identity.users DROP COLUMN IF EXISTS preferred_stt_provider;
```

Check whether a `post-migration.sql` references these columns (grep) — if so, remove those references in lockstep.
</context>

<feature>
  <name>Provider feature removal (api + identity + web-session) + column-drop migration</name>
  <files>
    apps/api/src/routes/settings.ts,
    apps/api/test/routes/settings.test.ts,
    packages/identity/src/adapters/persistence/better-auth.ts,
    packages/identity/src/adapters/persistence/schema.ts,
    packages/identity/src/contracts/api.ts,
    packages/identity/src/ports/user-repo.ts,
    packages/identity/src/adapters/persistence/user-repo.ts,
    packages/identity/src/application/update-provider-prefs.ts,
    packages/identity/test/provider-prefs.test.ts,
    apps/web/src/lib/server-session.ts,
    drizzle/0045_phase10_drop_provider_prefs.sql
  </files>
  <read_first>
    - apps/api/src/routes/settings.ts (current handlers — remove only the provider-prefs ones)
    - packages/identity/src/adapters/persistence/better-auth.ts (additionalFields block ~149-158)
    - packages/identity/src/adapters/persistence/schema.ts (users table column defs)
    - packages/identity/src/contracts/api.ts (the two type exports + UserDTO)
    - packages/identity/src/ports/user-repo.ts + adapters/persistence/user-repo.ts (the method to remove)
    - apps/web/src/lib/server-session.ts (normalization block ~196-201)
    - one existing drizzle/*.sql (idempotent ALTER style) + post-migration.sql (grep for the two column names)
  </read_first>
  <behavior>
    - After removal: `grep -rn "preferredLlmProvider\|preferredSttProvider\|updateProviderPrefs\|LLMProviderName\|STTProviderName\|provider-prefs" packages apps` returns ZERO hits.
    - apps/api/test/routes/settings.test.ts still passes for /locale, /display-currency, /sessions; the provider-prefs case is gone (not skipped).
    - An integration request `PUT /settings/provider-prefs` returns 404 (route no longer mounted).
    - After `docker compose build migrator && make migrate`: `\d identity.users` shows neither provider column.
    - `make ci-gate` → 6/6 tenant-leak pass on the new shape.
  </behavior>
  <implementation>
    1. CODE FIRST (so nothing reads the column once it's dropped): delete the removal-surface entries above in this
       order — web-session normalizer → api route + its test → identity contracts/port/repo/application/test →
       better-auth additionalFields → schema column defs. After each package, `bun test` that package.
       Commit: refactor(10-01): remove Provider feature from api + identity + web session.
    2. MIGRATION LAST: author drizzle/0045_phase10_drop_provider_prefs.sql (body above). Remove any
       post-migration.sql references in lockstep. `docker compose build migrator && make migrate`; verify
       `\d identity.users`. Commit: feat(10-01): drop provider-pref columns (migration 0045).
    3. GATE: `make test` (identity + api green), then `make ci-gate` (restart stack after — it downs dev).
       Commit only if changes (e.g. a tenant-leak fixture mentioned a provider column — unlikely).
  </implementation>
  <acceptance_criteria>
    - `grep -rn "preferredLlmProvider\|preferredSttProvider\|updateProviderPrefs\|LLMProviderName\|STTProviderName\|provider-prefs" packages apps` exits 1 (no matches)
    - drizzle/0045_phase10_drop_provider_prefs.sql contains `DROP COLUMN IF EXISTS preferred_llm_provider` and `preferred_stt_provider`
    - packages/identity/src/application/update-provider-prefs.ts and packages/identity/test/provider-prefs.test.ts no longer exist
    - `make test` exits 0 (identity + api)
    - `make ci-gate` reports 6 passed / 0 failed
    - `\d identity.users` (psql) lists neither preferred_llm_provider nor preferred_stt_provider
  </acceptance_criteria>
</feature>

<verification>
- grep clean for all provider symbols (exit 1)
- make migrate exits 0 after migrator rebuild; both columns gone from \d identity.users
- make test green; make ci-gate 6/6
- PUT /settings/provider-prefs → 404
</verification>

<success_criteria>

- Provider feature gone end-to-end (api + identity + web session + DB columns) — USET-08
- Migration 0045 applied + verified on the live dev DB
- ci-gate + test suites green
  </success_criteria>

<output>
After completion, create `.planning/phases/10-user-settings-redesign/10-01-SUMMARY.md`
</output>
