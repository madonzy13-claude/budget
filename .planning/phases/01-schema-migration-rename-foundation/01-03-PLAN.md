---
plan: 01-03
phase: 1
phase_name: Schema Migration & Rename Foundation
goal: Rename Hono route files `workspaces.ts` → `budgets.ts` and `accounts.ts` → `wallets.ts`, flip mount paths in `app.ts` to `/budgets/*` and `/wallets/*` with NO aliases, rename the tenant-guard request header `X-Workspace-ID` → `X-Budget-ID`, strip dropped-column references from route Zod schemas + Drizzle queries while preserving v1.0 body shape (D-07), add `/budgets/health` smoke endpoint, and cover every renamed route with at least one bun:test integration test against real Postgres.
requirements: [MIG-11]
depends_on: [01-02]
estimated_tasks: 6
---

# Plan 01-03 — Hono Route Rename, Header Rename, and Route Integration Tests

## Goal

End state: `apps/api` compiles green (`bun --cwd apps/api run typecheck` exit 0). `bun test apps/api/test/` is green. Every renamed route has at least one integration test in `apps/api/test/routes/`. Mount paths `/budgets/*` and `/wallets/*` resolve; `/workspaces/*` and `/accounts/*` return HTTP 404 (default Hono not-found — no aliases per D-09). Header read in `apps/api/src/middleware/tenant-guard.ts` accepts `X-Budget-ID` and rejects `X-Workspace-ID` (the latter becomes a soft empty-tenant case, which 403s on workspace-scoped routes per existing behavior). `/budgets/health` returns 200 — Phase 1 ROADMAP success criterion #5.

Route handler request/response BODIES otherwise preserve v1.0 shape (D-07): Phase 2 reshapes for new categorical txn schema, FX side-slider, drafts, share-link. Phase 1 ONLY strips references to columns dropped in 01-01 from Zod schemas + Drizzle queries.

## Inputs (read before starting)

- `01-CONTEXT.md` — D-05 (this is plan 01-03), D-07 (minimum compile-fix), D-08 (api-client.ts URL flip in 01-04 prevents 404 gap), D-09 (NO aliases — `/workspaces/*` returns 404), D-10 (header rename in this plan), D-12 (`balance_adjustments` retained → adjust-balance route still works).
- `01-RESEARCH.md` — middleware lines, header sites, every route file in `apps/api/src/routes/` that imports renamed domain types.
- `01-PATTERNS.md` §5 (Hono route file structure: pickTenant + safeParse + Result + serverError), §6 (`app.ts` mount block — exact pattern to follow with rename), §11B (tenant-guard.ts middleware header read block — exact rename map).
- `apps/api/src/app.ts:21-91` (mount block).
- `apps/api/src/routes/{workspaces.ts, accounts.ts, transactions.ts, categories.ts, category-limits.ts, recurring-rules.ts, recurring-drafts.ts, workspace-settings.ts}` — all the route files involved.
- `apps/api/src/middleware/tenant-guard.ts:32-78` (header read + bootstrap SQL).
- `apps/api/src/middleware/require-workspace.ts` (if exists — likely needs rename `requireWorkspace` → `requireBudget`).

## Tasks

### 1. Wave-0 failing tests — route-existence + header-acceptance integration tests

- **Files (NEW per ENGR-03 requirement: one integration test per renamed route, real Postgres):**
  - `apps/api/test/routes/budgets.test.ts` (NEW — supersedes `workspaces.test.ts` if one exists; `git mv` and rewrite)
  - `apps/api/test/routes/wallets.test.ts` (NEW — supersedes `accounts.test.ts` if exists)
  - `apps/api/test/routes/budget-settings.test.ts` (NEW — supersedes `workspace-settings.test.ts` if exists)
  - `apps/api/test/middleware/tenant-guard-header-rename.test.ts` (NEW)
- **Action:**
  - **`budgets.test.ts`** — bun:test integration suite:
    - `describe('Budgets route (renamed from workspaces)') > test('POST /budgets creates a budget and returns 201')`
    - `test('GET /budgets/health returns 200')` ← ROADMAP success criterion #5
    - `test('GET /workspaces/health returns 404')` ← D-09 no-alias check
    - `test('POST /workspaces returns 404')`
    - Use existing `apps/api/test/_helpers/` or `apps/api/test/_db.ts` for real-Postgres fixtures. No mocks per CLAUDE.md.
  - **`wallets.test.ts`** — bun:test:
    - `describe('Wallets route (renamed from accounts)') > test('POST /wallets creates a wallet with walletType=SPENDINGS and returns 201')`
    - `test('GET /wallets lists wallets')`
    - `test('POST /accounts returns 404')` ← no-alias check
    - `test('POST /wallets rejects scope field (no longer accepted)')` — confirms scope drop (D-13)
  - **`budget-settings.test.ts`** — bun:test:
    - `describe('Budget settings route') > test('PATCH /budget-settings updates budget identity')`
    - `test('PATCH /workspace-settings returns 404')`
  - **`tenant-guard-header-rename.test.ts`** — bun:test:
    - `describe('Tenant guard header rename') > test('accepts X-Budget-ID and populates tenantIds')`
    - `test('accepts x-budget-id lowercase and populates tenantIds')`
    - `test('rejects X-Workspace-ID — leaves tenantIds empty')`
    - Use a Hono test client (`hc<AppType>`) or direct `app.fetch(req)` invocation. Real Postgres for membership lookup.
  - Run tests — RED expected (routes/middleware not yet renamed).
- **Verify:** `bun test apps/api/test/routes/ apps/api/test/middleware/` runs and reports RED for the new tests. Existing tests in the directory still run (some will be deleted in subsequent tasks as their old route files are renamed).

### 2. Rename route files + factories + internal identifiers

- **Files (`git mv` to preserve history):**
  - `apps/api/src/routes/workspaces.ts` → `budgets.ts`
  - `apps/api/src/routes/accounts.ts` → `wallets.ts`
  - `apps/api/src/routes/workspace-settings.ts` → `budget-settings.ts`
- **Action:**
  - **`budgets.ts` (renamed from `workspaces.ts`)** — per PATTERNS §5 analog B:
    - Header comment: `workspaces.ts -- /workspaces route factory` → `budgets.ts -- /budgets route factory`. Update PC/T-IDs if referenced (leave numeric IDs unchanged; they're trace tags).
    - Export factory `workspacesRoutesFactory` → `budgetsRoutesFactory`.
    - Local Hono instance variable `r` (unchanged — local name).
    - Zod `createSchema`: fields `name`, `kind`, `default_currency` PRESERVED (D-07 — v1.0 shape stays). Schema variable name stays `createSchema` (local).
    - POST `/` handler: comment `// POST /workspaces -- create new workspace` → `// POST /budgets -- create new budget`. The `auth.api.createOrganization` call body fields PRESERVED (Better Auth contract; the `kind` and `default_currency` params remain — research pitfall 3). Error-message regex `/PRIVATE workspaces/` updated to `/PRIVATE budgets/` IF the application service in 01-02 changed the error message text; otherwise leave intact and verify via grep against `packages/tenancy/src/application/create-budget.ts`.
    - Any GET/PATCH/DELETE handlers: rename mounted-path comments; update `await deps.tenancy.findWorkspaceById(...)` style calls to `findBudgetById` (the rename is wired through `BootedDeps` — see task 3).
  - **`wallets.ts` (renamed from `accounts.ts`)** — per PATTERNS §5 analog A + the strip note at PATTERNS line 444:
    - Header comment: `accounts.ts -- /accounts route factory` → `wallets.ts -- /wallets route factory`.
    - Export factory `createAccountsRoute` → `createWalletsRoute`.
    - `getSchemas()` lazy import: `createAccountSchema, adjustBalanceSchema` → `createWalletSchema, adjustBalanceSchema` (the latter retained per D-12 since `balance_adjustments` survives).
    - POST `/` handler at PATTERNS §5 lines 33-79:
      - Drop the `scope` inference block at PATTERNS line 444 (the comment "Strip `parsed.data.scope` reference at line 50"). Concretely: remove any `const scope = ...` derivation and remove `scope` from the input passed to `deps.budgeting.createWallet(...)`.
      - Body parsing + Zod safeParse path unchanged.
      - Service call: `deps.budgeting.createAccount(...)` → `deps.budgeting.createWallet(...)` with input `{ ...parsed.data, tenantId, actorUserId: userId }` (no `scope`).
      - Error-shape branches preserved.
    - GET `/` handler: `deps.budgeting.listAccounts(...)` → `deps.budgeting.listWallets(...)`.
    - Any adjust-balance handler: `deps.budgeting.adjustBalance(...)` retained (D-12); column rename `account_id` → `wallet_id` flows through automatically since `BootedDeps` is type-erased and the underlying repo (Plan 01-02) already does the rename.
  - **`budget-settings.ts` (renamed from `workspace-settings.ts`)**:
    - Factory rename `createWorkspaceSettingsRoute` → `createBudgetSettingsRoute`.
    - File header + comments updated.
    - Mounted path is `/budget-settings/*` (task 3 wires this).
- **Verify:**
  - `git status` shows renames (R), not delete+add.
  - `grep -n "workspacesRoutesFactory\|createAccountsRoute\|createWorkspaceSettingsRoute" apps/api/src/routes/` returns 0 hits.
  - `bun --cwd apps/api run typecheck` — partial green; remaining errors should be limited to `app.ts` imports (next task).

### 3. Update `apps/api/src/app.ts` mount block — flip paths, drop aliases, add health endpoint

- **Files:** `apps/api/src/app.ts:21-91`
- **Action:** Per PATTERNS §6 mount-block analog, apply the following replacements (all in lockstep):
  - **Imports block (lines 21-36):**
    - `import { workspacesRoutesFactory } from "./routes/workspaces";` → `import { budgetsRoutesFactory } from "./routes/budgets";`
    - `import { createAccountsRoute } from "./routes/accounts";` → `import { createWalletsRoute } from "./routes/wallets";`
    - `import { createWorkspaceSettingsRoute } from "./routes/workspace-settings";` → `import { createBudgetSettingsRoute } from "./routes/budget-settings";`
  - **Middleware-loop block (PATTERNS lines 56-66 / app.ts:56-66):**
    - `app.use("/workspaces/*", requireAuth);` → `app.use("/budgets/*", requireAuth);`
    - `app.route("/workspaces", workspacesRoutesFactory(deps));` → `app.route("/budgets", budgetsRoutesFactory(deps));`
  - **Workspace-scoped routes loop (PATTERNS lines 70-81 / app.ts:70-81):**
    - `"/accounts/*"` → `"/wallets/*"` in the `path` array literal.
    - `"/workspace-settings/*"` → `"/budget-settings/*"` in the same array.
    - `requireWorkspace` middleware import + identifier: rename to `requireBudget`. Edit `apps/api/src/middleware/require-workspace.ts` → `git mv` to `require-budget.ts`; inside, function name `requireWorkspace` → `requireBudget`. Update the import in `app.ts`.
    - `app.route("/accounts", createAccountsRoute(deps));` → `app.route("/wallets", createWalletsRoute(deps));`
    - `app.route("/workspace-settings", createWorkspaceSettingsRoute(deps));` → `app.route("/budget-settings", createBudgetSettingsRoute(deps));`
  - **NO aliases (D-09):** do NOT add `app.route("/workspaces", ...)` or `app.route("/accounts", ...)` after the rename. Old paths fall through to default Hono 404. Confirm with: `grep -n "workspaces\|accounts" apps/api/src/app.ts` returns 0 references.
  - **Add `/budgets/health` smoke endpoint** for ROADMAP success criterion #5 — minimum implementation inside `budgetsRoutesFactory` (NOT in `app.ts`):
    ```typescript
    r.get("/health", (c) => c.json({ ok: true, phase: "1" }));
    ```
    Add this BEFORE the `requireBudget` (or `requireWorkspace`) middleware would scope it — but since `/budgets/*` is under `requireAuth` only (not `requireBudget`, per PATTERNS lines 56-66), `/budgets/health` is auth-required only and does not need workspace context. If executor judges `/budgets/health` should be public (no auth), wire it as an early `app.get("/budgets/health", ...)` BEFORE the `app.use("/budgets/*", requireAuth)` line. CLAIM: ROADMAP criterion #5 wording is silent on auth — executor's discretion (see CONTEXT Claude's Discretion section).
- **Verify:**
  - `grep -n "\\/workspaces\\|\\/accounts\\|workspace_settings\\|workspace-settings" apps/api/src/app.ts` returns 0.
  - `bun --cwd apps/api run typecheck` — green.
  - Run the dev server: `make restart-api` (or equivalent) and `curl http://localhost:4000/budgets/health` returns 200; `curl http://localhost:4000/workspaces/health` returns 404.

### 4. Rename tenant-guard header read + bootstrap SQL

- **Files:** `apps/api/src/middleware/tenant-guard.ts:32-78`
- **Action:** Per PATTERNS §11B (the header-read block + the bootstrap SQL block):
  - Variable name `requestedWsId` → `requestedBudgetId` (PATTERNS line 940).
  - Header reads: `c.req.header("x-workspace-id")` → `c.req.header("x-budget-id")`, `c.req.header("X-Workspace-ID")` → `c.req.header("X-Budget-ID")`. Both case variants preserved (Hono's header reader is case-insensitive but the explicit dual-read pattern is kept for clarity per existing code style).
  - Comment block (PATTERNS lines 916-924 / tenant-guard.ts:32-44): rewrite the inline comment block. Concretely:
    ```
    // The web client picks a budget from the URL (`/budgets/[id]/...`)
    // and sends it on every API call as `X-Budget-ID`. We MUST verify the
    // caller is a member of that budget before trusting it.
    //
    // If no header is present we leave tenantIds empty; downstream
    // requireBudget then returns 403 (or the route is auth-only and never
    // reads tenantIds). The legacy `active_workspace_ids` user-preference
    // path is intentionally NOT consulted any more -- budget context is
    // explicit-via-URL, never implicit-via-session.
    ```
  - Bootstrap SQL block (PATTERNS lines 936-937 / tenant-guard.ts:56-63): rewrite the inline SQL string:
    - `SELECT wm.workspace_id::text AS id FROM tenancy.workspace_members wm WHERE wm.user_id = ... AND wm.workspace_id = ...` →
    - `SELECT bm.budget_id::text AS id FROM tenancy.budget_members bm WHERE bm.user_id = ... AND bm.budget_id = ...`
    - Alias `wm` → `bm` to match the table rename idiomatically.
  - **GUC unchanged:** `set_config('app.tenant_ids', ...)` stays as `app.tenant_ids` — research pitfall 4 confirms the GUC name is DB-level and not HTTP-level. Do NOT rename to `app.budget_ids`.
- **Verify:**
  - `grep -n "x-workspace-id\|X-Workspace-ID\|workspace_members\|tenancy.workspaces" apps/api/src/middleware/tenant-guard.ts` returns 0.
  - `bun test apps/api/test/middleware/tenant-guard-header-rename.test.ts` — GREEN (4 tests from task 1 all pass).

### 5. Compile-fix route bodies — strip dropped-column refs while preserving v1.0 shape

- **Files (sweep across `apps/api/src/routes/`):**
  - `apps/api/src/routes/transactions.ts` — transactions table dropped `kind`, `account_id` (and possibly `to_account_id`, `direction` if they were referenced).
  - `apps/api/src/routes/categories.ts` — `scope` field drop (D-13).
  - `apps/api/src/routes/category-limits.ts` — verify no `scope` refs.
  - `apps/api/src/routes/recurring-rules.ts`, `apps/api/src/routes/recurring-drafts.ts` — `account_id` field renames to `wallet_id` in any inline Drizzle queries (most flow through repos in `packages/budgeting` and were handled in 01-02).
  - `apps/api/src/routes/budgets.ts` (renamed in task 2) — any `workspace_*` field-name references in Zod request shapes (D-07: preserve v1.0 SHAPE; if a Zod schema field is literally named `workspaceId`, rename to `budgetId` since the body shape is the same — only the field name changes).
  - `apps/api/src/routes/wallets.ts` (renamed in task 2) — `scope` field already dropped in task 2; verify no remaining refs.
- **Action:**
  - **`transactions.ts`:**
    - Locate the Zod request schema for create-transaction (likely `createTransactionSchema` imported from `@budget/budgeting/src/contracts/api`). If the schema previously listed `kind` or `account_id` fields, those fields are gone (Plan 01-02 stripped them from the contract). Confirm no inline Zod refers to them.
    - Route handler bodies: strip any `kind`/`account_id` references from the input passed to `deps.budgeting.recordTransaction(...)`. PRESERVE the rest of the v1.0 shape: `date, amount, currency, categoryId, note` etc. stay.
    - **Phase 1 boundary (D-07):** Do NOT reshape to add `amount_original_cents`, `currency_original`, `amount_converted_cents`, `fx_rate`, `fx_as_of` — those are TXN-01..06 in Phase 2.
    - If the route currently 4xxs on missing `kind`/`account_id`, weaken the validation to make the field optional and ignored (Phase 2 deletes the route entirely and rebuilds).
  - **`categories.ts`:**
    - Drop `scope` from Zod request schema (likely already done via `contracts/api.ts` in 01-02; this is a sweep).
    - Drop `scope` from any service call input. `deps.budgeting.createCategory({ name, ...rest })` no longer takes scope.
  - **`recurring-*.ts` files:**
    - In any inline Drizzle query (e.g., `sql\`SELECT ... FROM budgeting.recurring_rules WHERE account_id = ...\``), rename `account_id`→`wallet_id`. Routes mostly delegate to package services, so this should be minimal.
  - **`budgets.ts` body field renames:**
    - If the request body for `POST /budgets` used `workspaceId` JSON key, rename to `budgetId` (semantic match; D-07 says preserve SHAPE, not bytes — field-name rename is part of the rename pass, not a Phase-2 reshape). Same for any GET-by-id route accepting `workspaceId` query param → `budgetId`.
  - Sweep: `grep -rn "workspaceId\|accountId\|account_id\|workspace_id\|\\.scope\\b\|kind:.*EXPENSE\\|TRANSFER\\|INCOME" apps/api/src/routes/` — for each hit, either rename to the new identifier or strip.
- **Verify:**
  - `bun --cwd apps/api run typecheck` — green.
  - `bun test apps/api/test/routes/` — all integration tests green (the new ones from task 1 plus any existing route tests that have been kept).
  - `grep -rn "workspaceId\|account_id\|workspace_id" apps/api/src/routes/` returns 0 (or only false positives in comments; verify by inspection).

### 6. Verification + atomic commits

- **Files:** entire `apps/api/` set + Wave-0 tests.
- **Action:**
  - Run the full backend test suite: `make test` from repo root — expect green.
  - Run the auth+route smoke via Hono test client: `bun test apps/api/test/` — green.
  - Rebuild `api` image and confirm runtime behavior:
    ```bash
    docker compose build api && make restart-api
    curl -i http://localhost:4000/budgets/health     # 200
    curl -i http://localhost:4000/workspaces/health  # 404
    curl -i http://localhost:4000/wallets            # 401 (no auth) — confirms route mounted under requireAuth
    curl -i http://localhost:4000/accounts           # 404
    ```
  - Atomic commits (suggested 4):
    1.  `test(01-03): add bun:test route + header integration tests` — Wave-0 tests.
    2.  `refactor(01-03): rename workspaces/accounts routes to budgets/wallets, factory + middleware rename` — task 2 + 3.
    3.  `feat(01-03): rename X-Workspace-ID header to X-Budget-ID in tenant-guard` — task 4.
    4.  `refactor(01-03): strip dropped-column refs from route bodies (D-07 minimum compile-fix)` — task 5.
- **Verify:**
  - `make test` exit 0.
  - All 4 curl smoke checks match expectations.
  - `grep -rn "workspaces\|/accounts\|X-Workspace-ID\|x-workspace-id" apps/api/src/` returns 0.

## Tests (write FIRST per CLAUDE.md TDD rule)

- `apps/api/test/routes/budgets.test.ts` — 4 tests (POST creates, /budgets/health 200, /workspaces/health 404, POST /workspaces 404).
- `apps/api/test/routes/wallets.test.ts` — 4 tests (POST creates with walletType, GET lists, POST /accounts 404, scope rejected).
- `apps/api/test/routes/budget-settings.test.ts` — 2 tests (PATCH works, /workspace-settings 404).
- `apps/api/test/middleware/tenant-guard-header-rename.test.ts` — 4 tests (X-Budget-ID accepted, lowercase accepted, X-Workspace-ID rejected, no header empty tenantIds).
- Existing route tests under `apps/api/test/routes/{transactions,categories,recurring-*,category-limits}.test.ts` (if present) updated in lockstep with route-body strip; expected to stay green.

## Verification

- `bun --cwd apps/api run typecheck` exit 0.
- `make test` exit 0 (backend unit + integration green).
- All 4 curl smoke checks pass.
- `/budgets/health` returns 200 — ROADMAP success criterion #5.
- `/workspaces/*` returns 404 — D-09 enforced.
- `X-Budget-ID` header accepted; `X-Workspace-ID` ignored.
- `bunx dependency-cruiser --config .dependency-cruiser.cjs apps/api packages` — exit 0 (no boundary violations from the rename).
- Goal-backward check: MIG-11 truth (all Hono routes renamed, old paths removed) verified by curl smoke + greps + new route integration tests.

## Done When

- MIG-11 implemented.
- All Verification checks pass.
- 4 atomic commits land on the branch.
- Every renamed route covered by at least one integration test (ENGR-03 carried-forward discipline).
- API container rebuilds and `/budgets/health` is publicly verifiable.
