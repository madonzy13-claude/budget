---
phase: 3
plan: 7
type: execute
wave: 6
depends_on: [4, 5, 6]
files_modified:
  - apps/web/messages/pl.json
  - apps/web/messages/uk.json
  - apps/web/e2e/page-objects/TopNavPo.ts
  - apps/web/e2e/page-objects/SwitcherPo.ts
  - apps/web/e2e/page-objects/HomePo.ts
  - apps/web/e2e/page-objects/BdpPo.ts
  - apps/web/e2e/page-objects/TaskBannerPo.ts
  - apps/web/e2e/fixtures/fresh-user-per-scenario.ts
  - apps/web/e2e/features/nav-switcher.feature
  - apps/web/e2e/features/home.feature
  - apps/web/e2e/features/bdp-tab-frame.feature
  - apps/web/e2e/features/task-banner.feature
  - apps/web/e2e/steps/common-steps.ts
  - Makefile
autonomous: true
requirements:
  # Plan 03-07 is a cross-cutting closer: I18N (PL/UK) + Gherkin E2E that exercises every Phase 3 requirement.
  # Primary REQ ownership lives in plans 03-01..06. 03-07 ships the verification surfaces (E2E features +
  # PL/UK message catalogs) that complete Nyquist sampling for the 5 requirements lacking automated coverage
  # in their primary plan (NAV-04, HOME-03, BDP-01, BDP-05, and the empty-state path of HOME-01 / BDP-03).
  # These are listed here as "co-owned for verification" so the cross-cutting nature is auditable:
  - NAV-04
  - HOME-03
  - BDP-01
  - BDP-05

must_haves:
  truths:
    - "PL and UK translations exist for nav.*, home.*, bdp.*, budgets.new.* namespaces"
    - "make test-e2e runs playwright-bdd and reports green when stack is up"
    - "Feature: budget switcher — open, select, route, click + button (NAV-01..04)"
    - "Feature: home page — empty hero + card click → spendings (HOME-01, HOME-03)"
    - "Feature: BDP frame — visit /budgets/[id] redirects to spendings, switch tabs, browser back/forward (BDP-01, BDP-02, BDP-05)"
    - "Feature: task banner — banner hidden when no tasks; visible + expandable when tasks exist (BDP-03)"
    - "Existing apps/web/e2e/cross-tenant-cache.spec.ts continues to work (Phase 8 will migrate to Gherkin)"
  artifacts:
    - path: "apps/web/messages/pl.json"
      provides: "Polish translations for Phase 3 namespaces"
    - path: "apps/web/messages/uk.json"
      provides: "Ukrainian translations for Phase 3 namespaces"
    - path: "apps/web/e2e/features/nav-switcher.feature"
      provides: "Gherkin feature for NAV-01..04"
    - path: "apps/web/e2e/features/home.feature"
      provides: "Gherkin feature for HOME-01..03"
    - path: "apps/web/e2e/features/bdp-tab-frame.feature"
      provides: "Gherkin feature for BDP-01, BDP-02, BDP-04, BDP-05"
    - path: "apps/web/e2e/features/task-banner.feature"
      provides: "Gherkin feature for BDP-03"
    - path: "apps/web/e2e/page-objects/*.ts"
      provides: "Page Object encapsulation for selectors"
    - path: "apps/web/e2e/fixtures/fresh-user-per-scenario.ts"
      provides: "Filled-in fresh user fixture (replaces Plan 03-01 stub)"
  key_links:
    - from: "apps/web/e2e/features/*.feature"
      to: "apps/web/e2e/page-objects/*.ts"
      via: "playwright-bdd step bindings"
      pattern: "Given|When|Then"
    - from: "Makefile"
      to: "apps/web (bun e2e command)"
      via: "make test-e2e wrapper"
      pattern: "test-e2e"
---

<objective>
Close Phase 3 by:
1. Filling `apps/web/messages/pl.json` and `apps/web/messages/uk.json` with translations for every Phase 3 namespace (`nav.*`, `home.*`, `bdp.*`, `budgets.new.*`) — EN copy was authored in plans 03-04/05/06.
2. Writing the playwright-bdd Gherkin features + Page Objects + steps that exercise NAV-01..05, HOME-01..04, BDP-01..05 end-to-end against a running stack (PLAYWRIGHT_BASE_URL).
3. Filling out the `fresh-user-per-scenario.ts` fixture stubbed in Plan 03-01.
4. Wiring `make test-e2e` to invoke playwright-bdd.

Purpose: I18N-\* requirements technically live in Phase 8, but the Phase 3 acceptance criteria require lookup keys exist for all three languages (otherwise `t(key)` returns the key string on PL/UK pages, breaking the locale switcher in dev). E2E coverage closes the Nyquist sampling gap from 03-VALIDATION.md (5/14 requirements need E2E to verify).

Output: Two filled message catalogs, 5 Page Objects, 4 feature files, 1 fixture, Makefile target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-navigation-home-bdp-frame/03-CONTEXT.md
@.planning/phases/03-navigation-home-bdp-frame/03-UI-SPEC.md
@.planning/phases/03-navigation-home-bdp-frame/03-VALIDATION.md
@/home/claude/budget/CLAUDE.md
@apps/web/messages/en.json
@apps/web/messages/pl.json
@apps/web/messages/uk.json
@apps/web/e2e/fixtures/fresh-user-per-scenario.ts
@apps/web/playwright.config.ts

<interfaces>
<!-- playwright-bdd step + fixture pattern -->
```typescript
// page-objects example
import type { Page } from "@playwright/test";

export class HomePo {
constructor(private page: Page) {}
async goto(locale = "en") { await this.page.goto(`/${locale}`); }
cardLocator(name: string) { return this.page.getByRole("link", { name: new RegExp(name, "i") }); }
}

````
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill PL + UK message catalogs for Phase 3 namespaces (nav, home, bdp, budgets.new)</name>
  <files>apps/web/messages/pl.json, apps/web/messages/uk.json</files>
  <read_first>
    - /home/claude/budget/apps/web/messages/en.json (canonical source for shape — read full file post-Plans 03-04/05/06 to know exact key tree)
    - /home/claude/budget/apps/web/messages/pl.json (current PL state — verify existing structure to merge into)
    - /home/claude/budget/apps/web/messages/uk.json (current UK state)
    - /home/claude/budget/.planning/phases/03-navigation-home-bdp-frame/03-UI-SPEC.md §Copywriting Contract (full EN copy table — translate from these)
  </read_first>
  <action>
    Update `apps/web/messages/pl.json` and `apps/web/messages/uk.json` to add the same key tree that EN ships. The keys MUST match EN exactly (i18n falls back to key string on miss).

    PL translations (use natural Polish phrasing; if uncertain, use literal translation — user can polish later):
    ```json
    {
      "nav": {
        "budgetSwitcher": { "trigger": { "aria": "Przełącz budżet" } },
        "switcher": {
          "personal": "Osobiste",
          "shared": "Współdzielone",
          "empty": "Brak budżetów. Użyj +, aby utworzyć.",
          "emptyCta": "Utwórz budżet"
        },
        "newBudget": "Nowy budżet",
        "newBudgetTooltip": "Utwórz nowy budżet"
      },
      "home": {
        "heading": "Moje budżety",
        "card": {
          "spent": "Wydano w tym miesiącu",
          "wallets": "Łącznie portfele",
          "overspentHeading": "Przekroczono",
          "allOnBudget": "Wszystkie kategorie w budżecie",
          "error": "Nie udało się załadować podsumowania. Kliknij, aby otworzyć.",
          "openAria": "Otwórz {budgetName}"
        },
        "chart": { "placeholder": "Wkrótce: statystyki" },
        "empty": {
          "heading": "Witaj — skonfigurujmy Twój pierwszy budżet",
          "body": "Utwórz budżet, aby śledzić wydatki, portfele i rezerwy.",
          "cta": "Utwórz pierwszy budżet"
        }
      },
      "bdp": {
        "tab": {
          "aria": "Zakładki szczegółów budżetu",
          "spendings": { "label": "Wydatki", "title": "Wydatki", "placeholder": "Excel-podobna siatka kategorii pojawi się w fazie 4." },
          "reserves":  { "label": "Rezerwy", "title": "Rezerwy", "placeholder": "Salda rezerw na kategorię pojawią się w fazie 5." },
          "wallets":   { "label": "Portfele", "title": "Portfele", "placeholder": "Edytowalne wiersze portfeli pojawią się w fazie 5." },
          "settings":  { "label": "Ustawienia", "title": "Ustawienia", "placeholder": "Tożsamość budżetu, przełącznik poduszki, reguły cykliczne i członkowie pojawią się w fazie 6." }
        },
        "tasks": {
          "banner": {
            "trigger": { "aria": "Pokaż oczekujące zadania" },
            "collapse": { "aria": "Ukryj oczekujące zadania" }
          },
          "count": "{count, plural, one {# zadanie oczekujące} few {# zadania oczekujące} many {# zadań oczekujących} other {# zadań oczekujących}}",
          "actionComingSoon": "Wkrótce w fazie 7"
        }
      },
      "budgets": {
        "new": {
          "title": "Utwórz budżet",
          "placeholder": "Kreator wprowadzający pojawi się w fazie 6.",
          "backToHome": "Powrót do strony głównej"
        }
      }
    }
    ```

    UK translations (Ukrainian):
    ```json
    {
      "nav": {
        "budgetSwitcher": { "trigger": { "aria": "Перемкнути бюджет" } },
        "switcher": {
          "personal": "Особисті",
          "shared": "Спільні",
          "empty": "Бюджетів ще немає. Натисніть +, щоб створити.",
          "emptyCta": "Створити бюджет"
        },
        "newBudget": "Новий бюджет",
        "newBudgetTooltip": "Створити новий бюджет"
      },
      "home": {
        "heading": "Мої бюджети",
        "card": {
          "spent": "Витрачено цього місяця",
          "wallets": "Всього у гаманцях",
          "overspentHeading": "Перевитрачено",
          "allOnBudget": "Усі категорії в межах бюджету",
          "error": "Не вдалося завантажити підсумок. Натисніть, щоб відкрити.",
          "openAria": "Відкрити {budgetName}"
        },
        "chart": { "placeholder": "Скоро з'являться аналітичні графіки" },
        "empty": {
          "heading": "Вітаємо — налаштуймо ваш перший бюджет",
          "body": "Створіть бюджет, щоб відстежувати витрати, гаманці та резерви.",
          "cta": "Створити перший бюджет"
        }
      },
      "bdp": {
        "tab": {
          "aria": "Вкладки деталей бюджету",
          "spendings": { "label": "Витрати", "title": "Витрати", "placeholder": "Excel-подібна сітка категорій з'явиться у фазі 4." },
          "reserves":  { "label": "Резерви", "title": "Резерви", "placeholder": "Залишки резервів по категоріях з'являться у фазі 5." },
          "wallets":   { "label": "Гаманці", "title": "Гаманці", "placeholder": "Редаговані рядки гаманців з'являться у фазі 5." },
          "settings":  { "label": "Налаштування", "title": "Налаштування", "placeholder": "Ідентифікація бюджету, перемикач подушки, повторювані правила та учасники з'являться у фазі 6." }
        },
        "tasks": {
          "banner": {
            "trigger": { "aria": "Показати очікувані завдання" },
            "collapse": { "aria": "Сховати очікувані завдання" }
          },
          "count": "{count, plural, one {# завдання очікує} few {# завдання очікують} many {# завдань очікують} other {# завдань очікують}}",
          "actionComingSoon": "Скоро у фазі 7"
        }
      },
      "budgets": {
        "new": {
          "title": "Створити бюджет",
          "placeholder": "Майстер ввімкнення з'явиться у фазі 6.",
          "backToHome": "Назад на головну"
        }
      }
    }
    ```

    CRITICAL: Both files merge INTO existing structure — do not overwrite the file. If `pl.json` already has `nav` or `home` keys (Phase 1+2 carryover), merge by adding only the missing leaves. Use the Read tool first to inspect each file's full content, then surgically Edit/Write the merged result.

    Verify the EN file's actual key tree at the time of execution: the snippets in Plans 03-04/05/06 may have produced slightly different nesting (e.g. `nav.switcher.emptyCta` vs `nav.switcher.empty.cta`). The PL/UK files must MATCH the EN structure verbatim.
  </action>
  <verify>
    <automated>cd /home/claude/budget &amp;&amp; node -e "const fs = require('fs'); const en = JSON.parse(fs.readFileSync('apps/web/messages/en.json', 'utf8')); const pl = JSON.parse(fs.readFileSync('apps/web/messages/pl.json', 'utf8')); const uk = JSON.parse(fs.readFileSync('apps/web/messages/uk.json', 'utf8')); function flatten(o, p='') { const r = []; for (const [k,v] of Object.entries(o)) { const key = p ? p+'.'+k : k; if (typeof v === 'object' && v !== null) r.push(...flatten(v, key)); else r.push(key); } return r; } const enKeys = new Set(flatten(en)); const plKeys = new Set(flatten(pl)); const ukKeys = new Set(flatten(uk)); const phase3 = [...enKeys].filter(k => k.startsWith('nav.') || k.startsWith('home.') || k.startsWith('bdp.') || k.startsWith('budgets.new.')); const missingPl = phase3.filter(k => !plKeys.has(k)); const missingUk = phase3.filter(k => !ukKeys.has(k)); if (missingPl.length || missingUk.length) { console.error('MISSING PL:', missingPl, 'MISSING UK:', missingUk); process.exit(1); } console.log('OK', phase3.length, 'phase3 keys present in all 3 locales');"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/messages/pl.json` contains all phase 3 leaf keys present in `apps/web/messages/en.json` (verified by the node script in `<verify>`)
    - `apps/web/messages/uk.json` contains the same keys
    - All three files remain valid JSON (`node -e 'JSON.parse(require("fs").readFileSync("apps/web/messages/pl.json"))'` exits 0; same for uk.json)
    - Both PL and UK ICU plural strings in `bdp.tasks.count` use language-correct plural forms (Polish has `one/few/many/other`; Ukrainian likewise)
  </acceptance_criteria>
  <done>PL and UK message catalogs cover every Phase 3 key.</done>
</task>

<task type="auto">
  <name>Task 2: Page Objects + fresh-user-per-scenario fixture implementation</name>
  <files>apps/web/e2e/page-objects/TopNavPo.ts, apps/web/e2e/page-objects/SwitcherPo.ts, apps/web/e2e/page-objects/HomePo.ts, apps/web/e2e/page-objects/BdpPo.ts, apps/web/e2e/page-objects/TaskBannerPo.ts, apps/web/e2e/fixtures/fresh-user-per-scenario.ts, apps/web/e2e/steps/common-steps.ts</files>
  <read_first>
    - /home/claude/budget/.planning/STATE.md Quick Task 260507-m3x (prior Gherkin migration playbook — has the canonical fresh-user fixture pattern)
    - /home/claude/budget/apps/web/e2e/fixtures/fresh-user-per-scenario.ts (Plan 03-01 stub — replace with real implementation)
    - /home/claude/budget/apps/web/e2e/cross-tenant-cache.spec.ts (existing raw spec — see how the project does test user creation today; reuse the pattern)
    - /home/claude/budget/apps/api/test/routes/wallets.test.ts (createTestUser helper — reuse via direct PG insert against test DB)
    - /home/claude/budget/.planning/phases/03-navigation-home-bdp-frame/03-UI-SPEC.md §Component Inventory (selectors derivable from data-testid or aria-label values)
    - /home/claude/budget/CLAUDE.md memory `feedback_e2e_gherkin` (Page Object pattern is mandatory)
  </read_first>
  <action>
    Create `apps/web/e2e/page-objects/TopNavPo.ts`:
    ```ts
    import type { Page } from "@playwright/test";

    export class TopNavPo {
      constructor(private page: Page) {}
      switcherTrigger() { return this.page.getByRole("button", { name: /switch budget|przełącz budżet|перемкнути бюджет/i }); }
      newBudgetButton() { return this.page.getByTestId("new-budget-button"); }
      brandMark() { return this.page.getByRole("link", { name: /budget/i }).first(); }
    }
    ```

    Create `apps/web/e2e/page-objects/SwitcherPo.ts`:
    ```ts
    import type { Page } from "@playwright/test";

    export class SwitcherPo {
      constructor(private page: Page) {}
      personalSection() { return this.page.getByText(/^Personal$|^Osobiste$|^Особисті$/); }
      sharedSection() { return this.page.getByText(/^Shared$|^Współdzielone$|^Спільні$/); }
      budgetRow(name: string) { return this.page.getByRole("menuitemradio", { name: new RegExp(name) }); }
    }
    ```

    Create `apps/web/e2e/page-objects/HomePo.ts`:
    ```ts
    import type { Page } from "@playwright/test";

    export class HomePo {
      constructor(private page: Page) {}
      async goto(locale = "en") { await this.page.goto(`/${locale}`); }
      heading() { return this.page.getByRole("heading", { level: 1 }); }
      card(name: string) { return this.page.getByRole("link", { name: new RegExp(`Open ${name}|Otwórz ${name}|Відкрити ${name}`) }); }
      emptyCta() { return this.page.getByRole("link", { name: /Create your first budget|Utwórz pierwszy budżet|Створити перший бюджет/ }); }
      placeholderChart() { return this.page.getByText(/Insights coming soon|Wkrótce: statystyki|Скоро з'являться аналітичні графіки/); }
    }
    ```

    Create `apps/web/e2e/page-objects/BdpPo.ts`:
    ```ts
    import type { Page } from "@playwright/test";

    export class BdpPo {
      constructor(private page: Page) {}
      async goto(locale: string, budgetId: string, tab?: "spendings"|"reserves"|"wallets"|"settings") {
        await this.page.goto(`/${locale}/budgets/${budgetId}${tab ? `/${tab}` : ""}`);
      }
      pill(slug: "spendings"|"reserves"|"wallets"|"settings") {
        return this.page.getByRole("link", { name: new RegExp(slug, "i") }).first();
      }
      stickyWrapper() { return this.page.getByTestId("bdp-sticky-wrapper"); }
    }
    ```

    Create `apps/web/e2e/page-objects/TaskBannerPo.ts`:
    ```ts
    import type { Page } from "@playwright/test";

    export class TaskBannerPo {
      constructor(private page: Page) {}
      banner() { return this.page.getByTestId("task-banner"); }
      trigger() { return this.banner().getByRole("button").first(); }
      taskRow(idx: number) { return this.banner().getByRole("listitem").nth(idx); }
    }
    ```

    Rewrite `apps/web/e2e/fixtures/fresh-user-per-scenario.ts` to actually create a test user. The simplest approach replays the `apps/api/test/routes/wallets.test.ts` `createTestUser` pattern via direct Postgres inserts (using the `DATABASE_URL_APP` env). Match the project's existing convention exactly:
    ```ts
    import { test as base } from "playwright-bdd";
    import { Pool } from "pg";

    interface FreshUser { email: string; password: string; userId: string; tenantId: string; budgetId: string; }

    async function createUserAndBudget(): Promise<FreshUser> {
      const dbUrl = process.env.DATABASE_URL_APP?.replace("@db:", "@localhost:") ?? "";
      const pool = new Pool({ connectionString: dbUrl });
      try {
        const email = `phase3-e2e-${Date.now()}-${Math.random().toString(36).slice(2, 8)}@test.local`;
        const password = "Test1234!Phase3";
        const userId = crypto.randomUUID();
        const tenantId = crypto.randomUUID();
        const budgetId = crypto.randomUUID();
        // Match the exact INSERT shape used by apps/api/test/routes/wallets.test.ts createTestUser
        // (executor reads that file to confirm the exact SQL)
        await pool.query(`INSERT INTO identity.users (id, email, display_currency) VALUES ($1, $2, 'USD')`, [userId, email]);
        await pool.query(`INSERT INTO tenancy.tenants (id, name) VALUES ($1, $2)`, [tenantId, `Test ${email}`]);
        await pool.query(`INSERT INTO tenancy.budgets (id, tenant_id, name, kind, default_currency) VALUES ($1, $2, 'My E2E Budget', 'PRIVATE', 'USD')`, [budgetId, tenantId]);
        await pool.query(`INSERT INTO tenancy.tenant_membership (tenant_id, user_id, role) VALUES ($1, $2, 'OWNER')`, [tenantId, userId]);
        return { email, password, userId, tenantId, budgetId };
      } finally {
        await pool.end();
      }
    }

    export const test = base.extend<{ freshUser: FreshUser }>({
      freshUser: async ({ page: _page }, use) => {
        const user = await createUserAndBudget();
        await use(user);
        // NOTE: cleanup deferred to overall test DB nuke between runs (CLAUDE.md: integration tests use real PG, dev DB is nuked).
      },
    });

    export { expect } from "@playwright/test";
    ```
    NOTE: the exact INSERT SQL must match the project's existing convention — read `apps/api/test/routes/wallets.test.ts` for the canonical `createTestUser` shape and match table names + column types. If the executor finds the project uses Better Auth's user-creation API rather than direct INSERTs, use that path instead.

    Create `apps/web/e2e/steps/common-steps.ts` for shared Given/When/Then bindings:
    ```ts
    import { createBdd } from "playwright-bdd";
    import { test } from "../fixtures/fresh-user-per-scenario";
    import { TopNavPo } from "../page-objects/TopNavPo";
    import { HomePo } from "../page-objects/HomePo";
    import { BdpPo } from "../page-objects/BdpPo";
    import { SwitcherPo } from "../page-objects/SwitcherPo";
    import { TaskBannerPo } from "../page-objects/TaskBannerPo";

    const { Given, When, Then } = createBdd(test);

    Given("I am signed in as a fresh user", async ({ page, freshUser }) => {
      // Project-specific sign-in mechanism — likely visiting /sign-in and submitting the form,
      // OR setting Better Auth session cookies directly. Match cross-tenant-cache.spec.ts pattern.
      await page.goto(`/en/sign-in?email=${encodeURIComponent(freshUser.email)}`);
      // ... fill + submit, or seed cookie
    });

    When("I open the budget switcher", async ({ page }) => {
      const nav = new TopNavPo(page);
      await nav.switcherTrigger().click();
    });

    Then("I see the {string} budget section", async ({ page }, label: string) => {
      const sw = new SwitcherPo(page);
      if (label.toLowerCase() === "personal") await sw.personalSection().waitFor({ state: "visible" });
      else if (label.toLowerCase() === "shared") await sw.sharedSection().waitFor({ state: "visible" });
    });
    ```
    Concrete sign-in step depends on the existing test pattern — examine `apps/web/e2e/cross-tenant-cache.spec.ts` to see how it authenticates.
  </action>
  <verify>
    <automated>cd /home/claude/budget &amp;&amp; test -f apps/web/e2e/page-objects/TopNavPo.ts &amp;&amp; test -f apps/web/e2e/page-objects/HomePo.ts &amp;&amp; test -f apps/web/e2e/page-objects/BdpPo.ts &amp;&amp; test -f apps/web/e2e/page-objects/SwitcherPo.ts &amp;&amp; test -f apps/web/e2e/page-objects/TaskBannerPo.ts &amp;&amp; test -f apps/web/e2e/fixtures/fresh-user-per-scenario.ts &amp;&amp; grep -q 'createUserAndBudget\|Pool' apps/web/e2e/fixtures/fresh-user-per-scenario.ts &amp;&amp; test -f apps/web/e2e/steps/common-steps.ts &amp;&amp; grep -q 'createBdd' apps/web/e2e/steps/common-steps.ts &amp;&amp; cd apps/web &amp;&amp; bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All 5 Page Object files exist under `apps/web/e2e/page-objects/`
    - Each Page Object class exports the relevant locator methods
    - `apps/web/e2e/fixtures/fresh-user-per-scenario.ts` no longer says "pending-implementation" (`! grep -q 'pending-implementation' apps/web/e2e/fixtures/fresh-user-per-scenario.ts`)
    - Fixture file imports `Pool` from `pg` or matches the project's existing test-user-creation pattern (`grep -q 'Pool\|crypto.randomUUID' apps/web/e2e/fixtures/fresh-user-per-scenario.ts`)
    - `apps/web/e2e/steps/common-steps.ts` exists with `createBdd` import (`grep -q 'createBdd' apps/web/e2e/steps/common-steps.ts`)
    - `cd apps/web && bun run typecheck` exits 0 (Page Objects type-check against `@playwright/test`)
  </acceptance_criteria>
  <done>Page Objects, fixture, and step skeleton ready; ready for feature files.</done>
</task>

<task type="auto">
  <name>Task 3: Four Gherkin feature files + Makefile test-e2e target</name>
  <files>apps/web/e2e/features/nav-switcher.feature, apps/web/e2e/features/home.feature, apps/web/e2e/features/bdp-tab-frame.feature, apps/web/e2e/features/task-banner.feature, Makefile</files>
  <read_first>
    - /home/claude/budget/apps/web/e2e/page-objects/TopNavPo.ts (Plan 03-07 Task 2 output)
    - /home/claude/budget/apps/web/e2e/steps/common-steps.ts (step bindings — extend as needed)
    - /home/claude/budget/Makefile (existing structure — find the `test` target and add `test-e2e` near it)
    - /home/claude/budget/.planning/phases/03-navigation-home-bdp-frame/03-RESEARCH.md §Test Strategy "Playwright BDD (E2E)" feature file table (scenarios per file)
    - /home/claude/budget/.planning/phases/03-navigation-home-bdp-frame/03-VALIDATION.md (per-requirement test mapping — confirms which REQ each scenario closes)
  </read_first>
  <action>
    Create `apps/web/e2e/features/nav-switcher.feature` (NAV-01..04 + NAV-05 indirectly):
    ```gherkin
    Feature: Budget switcher in top nav
      Covers NAV-01 (trigger anatomy), NAV-02 (Personal/Shared grouping), NAV-03 (+ button), NAV-04 (route on click).

      Background:
        Given I am signed in as a fresh user

      Scenario: Switcher trigger shows current budget name + chevron
        When I open the home page
        Then the switcher trigger displays the active budget name

      Scenario: Dropdown groups budgets as Personal / Shared
        When I open the budget switcher
        Then I see the "Personal" budget section
        # SHARED section only renders when at least one SHARED budget exists; fresh user starts with PRIVATE only
        And I do not see the "Shared" budget section

      Scenario: Click + button navigates to /budgets/new
        When I click the "+" new-budget button
        Then the URL contains "/budgets/new"

      Scenario: Selecting a budget routes to its spendings tab
        When I open the budget switcher
        And I click the row for "My E2E Budget"
        Then the URL contains "/budgets/" followed by the budget id and "/spendings"
    ```

    Create `apps/web/e2e/features/home.feature` (HOME-01..04):
    ```gherkin
    Feature: Combined home page
      Covers HOME-01 (one card per budget), HOME-03 (card click → spendings), HOME-04 (placeholder chart).

      Background:
        Given I am signed in as a fresh user

      Scenario: Home renders one BudgetCard per accessible budget
        When I open the home page
        Then I see a budget card titled "My E2E Budget"

      Scenario: Card click navigates to /budgets/[id]/spendings
        When I open the home page
        And I click the card for "My E2E Budget"
        Then the URL ends with "/spendings"

      Scenario: Placeholder chart card is visible below the grid
        When I open the home page
        Then I see the "Insights coming soon" placeholder chart

      Scenario: Empty home state shows hero CTA when user has no budgets
        Given I am a signed-in user with no budgets
        When I open the home page
        Then I see the "Create your first budget" CTA
    ```

    Create `apps/web/e2e/features/bdp-tab-frame.feature` (BDP-01, BDP-02, BDP-04, BDP-05):
    ```gherkin
    Feature: BDP tab frame
      Covers BDP-01 (sticky pills), BDP-02 (tab order + default Spendings), BDP-04 (active yellow), BDP-05 (back/forward).

      Background:
        Given I am signed in as a fresh user

      Scenario: Visiting /budgets/[id] redirects to /spendings
        When I open the BDP for "My E2E Budget"
        Then the URL ends with "/spendings"

      Scenario: Clicking a pill navigates to its tab route
        When I open the BDP for "My E2E Budget"
        And I click the "Reserves" tab pill
        Then the URL ends with "/reserves"
        And the "Reserves" tab pill has the active state

      Scenario: Browser back restores the previous tab
        When I open the BDP for "My E2E Budget"
        And I click the "Wallets" tab pill
        And I press the browser Back button
        Then the URL ends with "/spendings"
        And the "Spendings" tab pill has the active state

      Scenario: Deep-link to /wallets paints Wallets as active on first render
        When I open the BDP wallets tab for "My E2E Budget"
        Then the "Wallets" tab pill has the active state on first paint
    ```

    Create `apps/web/e2e/features/task-banner.feature` (BDP-03):
    ```gherkin
    Feature: Task banner shell
      Covers BDP-03 (banner visibility + count chip + expand).

      Background:
        Given I am signed in as a fresh user

      Scenario: Banner is absent from DOM when no pending tasks
        When I open the BDP for "My E2E Budget"
        Then the task banner is not present in the DOM

      Scenario: Banner renders with count chip when pending tasks exist
        Given a "RESERVE_TOPUP" task is seeded for "My E2E Budget"
        When I open the BDP for "My E2E Budget"
        Then the task banner displays "1 task pending"

      Scenario: Clicking the banner expands the task list
        Given a "RESERVE_TOPUP" task is seeded for "My E2E Budget"
        When I open the BDP for "My E2E Budget"
        And I click the task banner
        Then the task banner is expanded
        And the expanded list shows 1 task row
        And the task row's primary action button is disabled
    ```

    The step bindings for the new `Given/When/Then` phrases must be added to `apps/web/e2e/steps/common-steps.ts` (or a sibling steps file). Specifically:
    - `Given a "{kind}" task is seeded for "{budgetName}"` — direct PG INSERT into `budgeting.tasks` with `status='PENDING'`.
    - `Given I am a signed-in user with no budgets` — variant of the fresh-user fixture that skips the budget INSERT.
    - `When I click the card for "{name}"`, `When I open the BDP for "{name}"`, etc.

    Modify the project root `Makefile` to add a `test-e2e` target:
    ```makefile
    test-e2e:
    	cd apps/web && PLAYWRIGHT_BASE_URL=$${PLAYWRIGHT_BASE_URL:-http://localhost:3000} bun run e2e
    ```
    Confirm the existing Makefile uses tab indentation (Makefiles MUST use tabs); preserve the pattern.

    DO NOT run `make test-e2e` as part of this task — it requires a running stack (web + api + db). The Plan 03-07 verifier confirms the feature files PARSE (via `bun run e2e -- --list` or equivalent) but does not require a green run. The actual green E2E run happens at phase-verification time after `make dev-up` boots the stack.
  </action>
  <verify>
    <automated>cd /home/claude/budget &amp;&amp; test -f apps/web/e2e/features/nav-switcher.feature &amp;&amp; test -f apps/web/e2e/features/home.feature &amp;&amp; test -f apps/web/e2e/features/bdp-tab-frame.feature &amp;&amp; test -f apps/web/e2e/features/task-banner.feature &amp;&amp; grep -q 'NAV-01\|NAV-02\|NAV-03\|NAV-04' apps/web/e2e/features/nav-switcher.feature &amp;&amp; grep -q 'HOME-01\|HOME-04' apps/web/e2e/features/home.feature &amp;&amp; grep -q 'BDP-01\|BDP-05' apps/web/e2e/features/bdp-tab-frame.feature &amp;&amp; grep -q 'BDP-03' apps/web/e2e/features/task-banner.feature &amp;&amp; grep -q '^test-e2e:' Makefile</automated>
  </verify>
  <acceptance_criteria>
    - All 4 `.feature` files exist under `apps/web/e2e/features/`
    - Each feature file references its target REQ-IDs in comments at the top of the `Feature:` description (`grep -q 'NAV-01' apps/web/e2e/features/nav-switcher.feature` etc.)
    - `nav-switcher.feature` has at least 4 scenarios covering NAV-01..04
    - `home.feature` has at least 3 scenarios covering HOME-01, HOME-03, HOME-04 (+ empty state)
    - `bdp-tab-frame.feature` has at least 4 scenarios covering BDP-01, BDP-02, BDP-04, BDP-05
    - `task-banner.feature` has at least 3 scenarios covering BDP-03 (empty + count + expand)
    - `Makefile` defines a `test-e2e` target (`grep -q '^test-e2e:' Makefile`)
    - `Makefile` `test-e2e` reads `PLAYWRIGHT_BASE_URL` (`grep -q 'PLAYWRIGHT_BASE_URL' Makefile`)
  </acceptance_criteria>
  <done>Four Gherkin features cover all 14 Phase 3 requirements; Makefile target added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → test DB | E2E fresh-user fixture inserts directly into `identity.users`, `tenancy.tenants`, `tenancy.budgets`, `tenancy.tenant_membership`. Test DB only — never production. |
| E2E runner → web | Playwright drives browser against `PLAYWRIGHT_BASE_URL`. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Severity | Mitigation |
|-----------|----------|-----------|-------------|----------|------------|
| T-03-07-01 | Information Disclosure | Test fixture credentials leak | accept | low | Email = `phase3-e2e-{timestamp}-{nonce}@test.local` — non-routable; password is fixed test string. Per CLAUDE.md memory `feedback_docker_always_on`, all E2E runs against local Docker — never staging or prod. |
| T-03-07-02 | Tampering | Direct PG INSERT bypasses Better Auth signup flow | accept | medium | This is integration-test convention established by `apps/api/test/routes/wallets.test.ts`. Test code never ships to production; CLAUDE.md mandates real Postgres for integration. |
| T-03-07-03 | Information Disclosure | i18n translations could contain user-supplied strings (e.g. PL/UK contributors) | accept | low | Phase 3 translations are author-controlled (in this plan); user-contributed translation flows are Phase 8 territory. |
| T-03-07-04 | Denial of Service | Long-running E2E suite | accept | low | Per 03-VALIDATION estimated runtime ~6 min full suite. Acceptable for pre-verification gates; can be parallelized later. |

No `high` severity threats.
</threat_model>

<verification>
- All Phase-3 keys present in pl.json and uk.json (Task 1 node-script verifier).
- 5 Page Objects exist and typecheck against `@playwright/test`.
- 4 feature files exist with scenarios covering all 14 REQ-IDs.
- `make test-e2e` target exists; reads `PLAYWRIGHT_BASE_URL`.
- (Out of scope for this task) actually running `make test-e2e` green — that happens at phase verification once `make dev-up` boots the stack.
</verification>

<success_criteria>
- I18N coverage: EN/PL/UK message catalogs all carry the Phase 3 namespace tree.
- E2E coverage: 14/14 Phase 3 REQ-IDs have at least one Gherkin scenario.
- playwright-bdd is fully wired (config from Plan 03-01 + steps + features from this plan).
- `make test-e2e` can be invoked once the stack is up.
- All deferred CLAUDE.md memory hooks honored: PLAYWRIGHT_BASE_URL from env, playwright-bdd Gherkin, Page Objects, fresh-user-per-scenario.
</success_criteria>

<output>
After completion, create `.planning/phases/03-navigation-home-bdp-frame/03-07-SUMMARY.md`
</output>
````
