---
phase: 08-pwa-offline-push-i18n-e2e-hardening
plan: 07
type: execute
wave: 5
depends_on: [08-02, 08-04, 08-05, 08-06]
autonomous: false
requirements: [E2EX-05, PWAX-01, PWAX-04, I18N-01]
files_modified:
  - apps/web/messages/en.json
  - apps/web/messages/pl.json
  - apps/web/messages/uk.json
  - .github/workflows/ci.yml

must_haves:
  truths:
    - "make ci-gate passes 10/10 cross-tenant files (8 prior + push_subscriptions + notification_prefs)"
    - "make test (bun:test backend) passes with 80% domain coverage preserved"
    - "cd apps/web && bun run test (Vitest component) passes"
    - "check:i18n gate runs in CI and passes (I18N-01)"
    - "make test-e2e (Playwright BDD) green against PLAYWRIGHT_BASE_URL from .env.local (E2EX-05)"
    - "dependency-cruiser still blocks domain imports of drizzle/Hono/AI SDK/adapters"
    - "Impeccable DESIGN.md sweep on the 5 new UI surfaces passes; web image rebuilt so served bundle shows new keys"
    - "Human UAT confirms install on a real browser + real push delivery + deep-link (manual-only per VALIDATION)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "check:i18n step in CI pipeline"
      contains: "check:i18n"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/check-i18n-completeness.ts"
      via: "check:i18n CI step"
      pattern: "check:i18n"
---

<objective>
Phase-8 launch gate. Wire the i18n completeness gate into CI, run the full gate sweep (ci-gate 10/10, make test + coverage, Vitest, test-e2e, dependency-cruiser), rebuild the web image so the served bundle carries the new message keys, run the impeccable DESIGN.md sweep on the 5 new UI surfaces, and gate on human UAT for the three manual-only verifications (real-device install, real web-push delivery, deep-link landing).

Purpose: This is the green-before-ship gate. All prior plans landed code + scoped tests; this plan proves the whole phase is green together and that the parts only a human can verify actually work on a device.
Output: CI i18n step + all gates green + DESIGN sweep + recorded human UAT outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-VALIDATION.md
@.planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-UI-SPEC.md

<interfaces>
Gate commands (CLAUDE.md + VALIDATION):
- `make test` (bun:test backend unit+integration), `make ci-gate` (tenant-leak), `make test-e2e` (Playwright BDD, PLAYWRIGHT_BASE_URL from .env.local APP_URL)
- `cd apps/web && bun run test` (Vitest component)
- `bun scripts/check-i18n-completeness.ts` (I18N-01 gate)
- Web image: messages bundled at build time → `make restart-web` after any message edit; verify served bundle (memory feedback_always_rebuild_web + docker_build_cache_stale).
Manual-only (VALIDATION § Manual-Only): PWA install on real mobile browser; real web-push delivery to device; PL/UK translation quality self-review.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire check:i18n into CI + full automated gate sweep</name>
  <read_first>
    - .github/workflows/ci.yml (existing job structure — where to add the check:i18n step)
    - scripts/check-i18n-completeness.ts (from 08-01)
    - .planning/STATE.md "make ci-gate coverage threshold failure is pre-existing" note (do not chase the known transitive-import coverage artifact)
    - CLAUDE.md "Local Development" (rebuild web before verifying served bundle)
  </read_first>
  <action>
    Add a `check:i18n` step to .github/workflows/ci.yml (in the lint/typecheck job or a dedicated step) running `bun scripts/check-i18n-completeness.ts` so a missing/extra locale key fails the build (I18N-01, D-17).
    Rebuild the web image so the new message keys are bundled: `make restart-web`; verify the served bundle contains a Phase-8 key (e.g. grep the built .next output or curl the Settings page) per the docker-build-cache-stale memory.
    Run the full automated sweep and capture results:
    - `make ci-gate` → expect 10/10 cross-tenant files green
    - `make test` → backend unit+integration; 80% domain coverage preserved (ignore the known pre-existing transitive-import aggregate-coverage artifact noted in STATE)
    - `cd apps/web && bun run test` → Vitest green
    - `make test-e2e` → Playwright BDD green against PLAYWRIGHT_BASE_URL from .env.local (E2EX-05)
    - dependency-cruiser still blocks domain→drizzle/Hono/AI SDK/adapters
    Fix any red surfaced by the integrated run (e.g. a scoped test that passed alone but conflicts in the full suite).
  </action>
  <verify>
    <automated>cd /home/claude/budget && bun scripts/check-i18n-completeness.ts && make ci-gate 2>&1 | tail -8 && cd apps/web && bun run test 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "check:i18n\|check-i18n-completeness" .github/workflows/ci.yml`
    - `bun scripts/check-i18n-completeness.ts` exits 0
    - `make ci-gate` exits 0 (10 cross-tenant files)
    - `cd apps/web && bun run test` exits 0
    - `make test-e2e` exits 0 (run separately; capture in SUMMARY)
    - served web bundle contains a Phase-8 key (e.g. `pwa.install` / `settings.push`) — verified post-rebuild
  </acceptance_criteria>
  <done>check:i18n in CI; all automated gates green together; web image rebuilt with new keys in the served bundle.</done>
</task>

<task type="auto">
  <name>Task 2: Impeccable DESIGN.md sweep on the 5 new UI surfaces</name>
  <read_first>
    - DESIGN.md (Binance dark canvas, single yellow accent, Inter + IBM Plex Sans — authority)
    - .planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-UI-SPEC.md (full component contracts — the sweep checklist)
    - memory feedback_design_md_authority (reuse primitives, impeccable sweep, e2e before done)
    - the 5 new components (install-banner, offline-status-badge, sync-issues-list, push-prefs-section, fallback screens) + staleness-marker + pending-row marker
  </read_first>
  <action>
    Run the impeccable DESIGN.md sweep across the Phase-8 surfaces against the UI-SPEC Checker Sign-Off dimensions: Copywriting (EN strings match the Copywriting Contract), Visuals (tokens only — no new CSS vars), Color (accent reserved for the documented affordances only; destructive never as fill), Typography (Inter/IBM Plex roles), Spacing (4-multiple scale + declared exceptions), Registry Safety (shadcn official Switch/Dialog/Sheet only).
    Verify against the served bundle (getComputedStyle / rendered DOM, not source) per the cursor-default SW-cache memory. Fix any drift (wrong token, accent overuse, missing aria-label, banner not deferring to VerifyEmailBanner). Mark the 6 Checker Sign-Off boxes in 08-UI-SPEC.md as PASS.
  </action>
  <verify>
    <automated>cd apps/web && bunx playwright test --grep @phase8 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 08-UI-SPEC.md § Checker Sign-Off all 6 dimensions marked PASS
    - new components use only existing CSS variables: `grep -rL "var(--" apps/web/src/components/common/install-banner.tsx` returns nothing unexpected; no `#`-hex literals added in the 5 new component files
    - accent `--primary` used only on the documented affordances (install CTA, push ON thumb, offline badge dot) — spot-verified
    - `bunx playwright test --grep @phase8` still green after any sweep fixes
  </acceptance_criteria>
  <done>DESIGN.md sweep passes on all Phase-8 surfaces against the served bundle; Sign-Off boxes PASS; @phase8 E2E still green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT — real-device install + push delivery + deep-link</name>
  <what-built>
    PWA install banner + profile-menu Install, VAPID web-push wired to the 3 task kinds with per-budget/per-kind prefs, deep-link to /budgets/[id]/[tab]?task=[id] with the task banner auto-expanded, offline cache + quick-entry replay, and full EN/PL/UK i18n. All automated gates (ci-gate 10/10, make test, Vitest, test-e2e, check:i18n, dependency-cruiser) are green and the DESIGN sweep passed.
  </what-built>
  <how-to-verify>
    Manual-only checks (cannot be driven headless — per 08-VALIDATION § Manual-Only):
    1. On a real mobile browser (Chrome Android or Safari iOS) open the app over HTTPS, confirm the Install banner appears, tap Install, confirm the app launches standalone from the home screen with correct manifest icons (PWAX-01).
    2. In Settings → Notifications, enable push (grant permission), enable the RESERVE_TOPUP kind. Trigger a RESERVE_TOPUP task (adjust a reserve-wallet balance to create a mismatch). Confirm a push notification arrives with generic copy (no amounts) (PWAX-04/05, D-15).
    3. Tap the push notification; confirm it opens /budgets/[id]/reserves?task=[id] with the task banner row auto-expanded (PWAX-06, D-13). Tap a push for an already-resolved task; confirm it lands on the tab silently (D-14).
    4. Optional: switch locale to PL then UK in Settings; spot-check the new push-prefs / install / offline strings read correctly (I18N-02 quality self-review, D-19 flagged machine-origin).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any install/push/deep-link/i18n issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                               |
| ---------------------------- | ----------------------------------------- |
| CI gate ↔ shippable artifact | A red gate must block the launch artifact |

## STRIDE Threat Register

| Threat ID  | Category              | Component                | Disposition | Mitigation Plan                                                                                                       |
| ---------- | --------------------- | ------------------------ | ----------- | --------------------------------------------------------------------------------------------------------------------- |
| T-08-07-01 | Repudiation / quality | i18n completeness        | mitigate    | check:i18n CI step fails the build on any missing EN/PL/UK key (D-17); runtime EN fallback is the safety net          |
| T-08-07-02 | Info Disclosure       | real push delivery (UAT) | mitigate    | Human UAT confirms the live lock-screen body carries no financials (D-15) — the one path only a real device exercises |
| T-08-07-03 | n/a                   | gate sweep               | accept      | Verification-only plan; relies on prior plans' mitigations being green together                                       |

</threat_model>

<verification>
- `bun scripts/check-i18n-completeness.ts` exits 0 and the step runs in CI.
- `make ci-gate` 10/10, `make test` (coverage preserved), `cd apps/web && bun run test`, `make test-e2e` all green.
- DESIGN sweep Sign-Off all PASS.
- Human UAT approved (install + push + deep-link on a real device).
</verification>

<success_criteria>
All Phase-8 CI gates green together with the i18n gate wired into CI; DESIGN sweep passes on the served bundle; human UAT confirms the three manual-only behaviors. Phase 8 is launch-ready. (Satisfies E2EX-05, PWAX-01/04 install+push UAT, I18N-01 CI gate.)
</success_criteria>

<output>
After completion, create `.planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-07-SUMMARY.md`
</output>
