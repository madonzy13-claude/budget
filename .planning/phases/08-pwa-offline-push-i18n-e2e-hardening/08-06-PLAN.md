---
phase: 08-pwa-offline-push-i18n-e2e-hardening
plan: 06
type: execute
wave: 4
depends_on: [08-03, 08-04, 08-05]
autonomous: true
requirements: [E2EX-01, E2EX-02, E2EX-03, E2EX-04, PWAX-03, PWAX-06]
files_modified:
  - apps/web/e2e/features/spendings.feature
  - apps/web/e2e/features/recurring-draft.feature
  - apps/web/e2e/features/cushion.feature
  - apps/web/e2e/features/share-link.feature
  - apps/web/e2e/page-objects/SpendingsPo.ts
  - apps/web/e2e/page-objects/OnboardingPo.ts
  - apps/web/e2e/page-objects/ShareLinkPo.ts
  - apps/web/e2e/steps/spendings.steps.ts
  - apps/web/e2e/steps/recurring-draft.steps.ts
  - apps/web/e2e/steps/cushion.steps.ts
  - apps/web/e2e/steps/share-link.steps.ts

must_haves:
  truths:
    - "spendings.feature covers quick-entry txn appearing in the grid + offline quick-entry queuing (pending marker) + reconnect replay + sync-issues"
    - "recurring-draft.feature covers a draft appearing in Spendings and confirm removing it"
    - "cushion.feature covers toggling cushion mode in Settings and Wallets reflecting it"
    - "share-link.feature covers generating a link in Settings and a recipient joining the budget"
    - "All new features use @phase8 tag, the fresh-user-per-scenario fixture, and the new Page Objects (no inline selectors)"
    - "Existing 5 features remain green on the new IA (E2EX-01 audit)"
  artifacts:
    - path: "apps/web/e2e/features/spendings.feature"
      provides: "quick-entry + offline replay scenarios"
      contains: "@phase8"
    - path: "apps/web/e2e/features/share-link.feature"
      provides: "share-link recipient join scenario"
      contains: "Scenario"
  key_links:
    - from: "apps/web/e2e/features/spendings.feature"
      to: "apps/web/e2e/page-objects/SpendingsPo.ts"
      via: "step bindings use the PO"
      pattern: "SpendingsPo"
    - from: "apps/web/e2e/features/share-link.feature"
      to: "apps/web/e2e/page-objects/ShareLinkPo.ts"
      via: "join-page locators"
      pattern: "ShareLinkPo"
---

<objective>
E2E audit-and-fill (D-21 — NOT a from-scratch rewrite). Fill the four missing required flows (quick-entry incl. offline replay, recurring-draft confirm, cushion toggle, share-link join — E2EX-03), implement the three new Page Objects' bodies + step bindings against the testids the 08-03/08-05 components ship, add the Phase-8 offline + push deep-link scenarios, and confirm the existing 5 features still pass on the new IA.

Purpose: The suite is already Gherkin + new-IA. The gap is coverage of 4 flows + 2 Phase-8 scenarios, plus 3 Page Objects whose bodies were scaffolded in 08-01. Leave cross-tenant-cache.spec.ts and server-down.spec.ts as raw specs (D-22).
Output: 4 .feature files + 4 step files + 3 completed Page Objects; @phase8 grep run green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-PATTERNS.md
@.planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-RESEARCH.md
@.planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-UI-SPEC.md

<interfaces>
Feature header convention (PATTERNS lines 866-874; memory feedback_e2e_gherkin):
```gherkin
@phase8
Feature: [name] — [one-line description]
  Background:
    Given I am signed in as a fresh user
```
Fresh-user fixture: apps/web/e2e/fixtures/fresh-user-per-scenario.ts (reuse unchanged; creates user+budget per scenario).
PLAYWRIGHT_BASE_URL from .env.local APP_URL (memory feedback_test_baseurl). Offline SW only active in production build (RESEARCH Pitfall 3 — follow server-down.spec.ts pattern for production-mode SW).
Page Object testids (from 08-03/08-05): quick-entry-input, txn-row-{id}, txn-pending-{id}, sync-issues-list, offline-status-badge, onboarding-push-switch, share-link join card.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete Page Objects + spendings.feature (quick-entry + offline replay)</name>
  <read_first>
    - apps/web/e2e/page-objects/ReservesPo.ts (PO method style)
    - apps/web/e2e/page-objects/SpendingsPo.ts (scaffolded in 08-01 — fill any missing methods)
    - apps/web/e2e/features/tasks.feature + reserves.feature (exact Gherkin + step-sentence style)
    - apps/web/e2e/steps/ (existing step binding files — registration pattern)
    - apps/web/e2e/server-down.spec.ts (production-build SW activation pattern for offline scenarios — RESEARCH Pitfall 3)
    - .planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-PATTERNS.md § spendings.feature (lines 662-687)
  </read_first>
  <action>
    Finalize SpendingsPo.ts (methods from 08-01 scaffold; add helpers: typeQuickEntry(amount), goOffline()/goOnline() via context.setOffline, expectPendingMarker(id), openSyncIssues()).
    Create apps/web/e2e/features/spendings.feature (@phase8) with scenarios:
    - "Quick-entry transaction appears in the grid" (E2EX-03 quick-entry)
    - "Offline quick-entry queues with a pending marker" — context offline, type+enter, assert txn-pending marker + offline-status-badge
    - "Reconnect replays the queued transaction" — go online, assert marker clears and row confirmed (PWAX-03; same-key idempotency → no duplicate)
    - "Failed replay lands in the sync-issues list" — seed an archived category, replay, assert sync-issues-list shows the item
    Create apps/web/e2e/steps/spendings.steps.ts binding the sentences to SpendingsPo. Run offline scenarios against a production-mode SW (server-down.spec.ts pattern).
  </action>
  <verify>
    <automated>cd apps/web && bunx bddgen && bunx playwright test --grep @phase8 features/spendings.feature 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "@phase8" apps/web/e2e/features/spendings.feature`
    - `grep -ci "scenario" apps/web/e2e/features/spendings.feature` >= 4
    - `grep -q "SpendingsPo" apps/web/e2e/steps/spendings.steps.ts`
    - `grep -q "txn-pending\|pendingSyncMarker" apps/web/e2e/page-objects/SpendingsPo.ts`
    - `bunx playwright test --grep @phase8 features/spendings.feature` passes (or documents the production-SW requirement if dev build skips SW)
  </acceptance_criteria>
  <done>SpendingsPo complete; spendings.feature covers quick-entry + offline queue + replay + sync-issues; steps bound; green.</done>
</task>

<task type="auto">
  <name>Task 2: recurring-draft.feature + cushion.feature</name>
  <read_first>
    - apps/web/e2e/features/reserves.feature (data-modify→assert scenario analog)
    - apps/web/e2e/page-objects/SettingsPo.ts (cushion toggle locator) + WalletsPo.ts
    - apps/web/e2e/features/tasks.feature (draft seeding Given-step style)
    - .planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-PATTERNS.md § recurring-draft.feature + cushion.feature (lines 690-708)
  </read_first>
  <action>
    Create apps/web/e2e/features/recurring-draft.feature (@phase8): "Recurring draft appears in Spendings with a confirm action" + "Confirming a draft removes it from the draft section". Seed a due recurring rule via the existing pg seeding step, open Spendings, assert the highlighted draft row + Confirm, confirm, assert it normalizes.
    Create apps/web/e2e/features/cushion.feature (@phase8): "Cushion mode can be toggled on in Settings" + "Wallets tab reflects cushion-mode state". Toggle the Settings cushion switch, assert persistence + the grid/wallets reflect cushion values.
    Create apps/web/e2e/steps/recurring-draft.steps.ts + cushion.steps.ts binding to SpendingsPo/SettingsPo/WalletsPo.
  </action>
  <verify>
    <automated>cd apps/web && bunx bddgen && bunx playwright test --grep @phase8 features/recurring-draft.feature features/cushion.feature 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "@phase8" apps/web/e2e/features/recurring-draft.feature apps/web/e2e/features/cushion.feature`
    - `grep -ci "scenario" apps/web/e2e/features/recurring-draft.feature` >= 2
    - `grep -ci "scenario" apps/web/e2e/features/cushion.feature` >= 2
    - both step files reference their Page Objects (no inline page.locator with raw CSS)
    - `bunx playwright test --grep @phase8 features/recurring-draft.feature features/cushion.feature` passes
  </acceptance_criteria>
  <done>recurring-draft + cushion features cover their E2EX-03 flows; steps bound to POs; green.</done>
</task>

<task type="auto">
  <name>Task 3: share-link.feature + ShareLinkPo/OnboardingPo + existing-suite audit</name>
  <read_first>
    - apps/web/e2e/page-objects/SettingsPo.ts (shareUrlField — generate side) + ShareLinkPo.ts/OnboardingPo.ts (08-01 scaffolds)
    - apps/web/e2e/features/tasks.feature (multi-actor scenario style)
    - the public /budgets/join/[token] page (06-07 output — recipient join card states)
    - apps/web/e2e/fixtures/fresh-user-per-scenario.ts (second-actor user creation for recipient)
    - .planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-PATTERNS.md § share-link.feature + ShareLinkPo/OnboardingPo
  </read_first>
  <action>
    Finalize ShareLinkPo.ts (joinCard/joinConfirmButton/errorState) and OnboardingPo.ts (stepTitle/pushStepSwitch/skipButton) bodies against the join-page + wizard testids.
    Create apps/web/e2e/features/share-link.feature (@phase8): "Share link can be generated in Settings" + "Recipient following the link joins the budget" (owner generates token in Settings → second fresh user opens /budgets/join/[token] → confirm → lands on /budgets/[id]/spendings as a member) + "Revoked/expired link shows an error state".
    Create apps/web/e2e/steps/share-link.steps.ts.
    Audit pass (E2EX-01/E2EX-04): run the full existing suite to confirm the 5 prior features stay green on the new IA and the fresh-user fixture is intact; fix any entity-name drift in existing Page Objects (do NOT touch cross-tenant-cache.spec.ts / server-down.spec.ts — D-22).
  </action>
  <verify>
    <automated>cd apps/web && bunx bddgen && bunx playwright test --grep @phase8 features/share-link.feature 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "@phase8" apps/web/e2e/features/share-link.feature`
    - `grep -ci "scenario" apps/web/e2e/features/share-link.feature` >= 2
    - `grep -q "joinConfirmButton\|joinCard" apps/web/e2e/page-objects/ShareLinkPo.ts`
    - cross-tenant-cache.spec.ts and server-down.spec.ts are unmodified (D-22): `git diff --name-only | grep -c "cross-tenant-cache.spec.ts\|server-down.spec.ts"` == 0
    - `bunx playwright test --grep @phase8 features/share-link.feature` passes
  </acceptance_criteria>
  <done>share-link feature + completed ShareLinkPo/OnboardingPo; existing suite audited green on new IA; raw infra specs untouched.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                            | Description                                        |
| ----------------------------------- | -------------------------------------------------- |
| recipient (actor B) → shared budget | Join flow must only grant access via a valid token |

## STRIDE Threat Register

| Threat ID  | Category               | Component       | Disposition | Mitigation Plan                                                                                                                                          |
| ---------- | ---------------------- | --------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-08-06-01 | Elevation of Privilege | share-link join | mitigate    | share-link.feature asserts a revoked/expired token shows an error state and does NOT grant membership (Better Auth orgs token-bound, validated in 06-07) |
| T-08-06-02 | n/a (test layer)       | E2E suite       | accept      | Test-only plan; no new runtime surface — exercises existing mitigations end-to-end                                                                       |

</threat_model>

<verification>
- `cd apps/web && bunx bddgen && bunx playwright test --grep @phase8` green for all 4 new features.
- Existing 5 features still green (E2EX-01 audit).
- Raw infra specs (cross-tenant-cache, server-down) unmodified.
</verification>

<success_criteria>
All 6 E2EX-03 flows covered (4 new features + offline/push scenarios); 3 Page Objects complete; fresh-user fixture retained; @phase8 suite green; existing suite green on new IA. (Satisfies E2EX-01..04 + the E2E half of PWAX-03/PWAX-06.)
</success_criteria>

<output>
After completion, create `.planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-06-SUMMARY.md`
</output>
