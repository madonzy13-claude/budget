---
phase: 08-pwa-offline-push-i18n-e2e-hardening
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [PWAX-04, I18N-01, I18N-02, E2EX-02]
files_modified:
  - apps/web/package.json
  - packages/platform/package.json
  - packages/platform/src/push/schema.ts
  - packages/platform/src/push/vapid.ts
  - packages/platform/src/push/index.ts
  - apps/migrator/drizzle/0031_phase08_push.sql
  - apps/migrator/drizzle/meta/_journal.json
  - apps/migrator/post-migration.sql
  - tests/tenant-leak/push-subscriptions-cross-tenant.test.ts
  - tests/tenant-leak/notification-prefs-cross-tenant.test.ts
  - tests/tenant-leak/USER-DATA-TABLES.txt
  - scripts/check-i18n-completeness.ts
  - apps/web/messages/en.json
  - apps/web/messages/pl.json
  - apps/web/messages/uk.json
  - apps/web/e2e/page-objects/SpendingsPo.ts
  - apps/web/e2e/page-objects/OnboardingPo.ts
  - apps/web/e2e/page-objects/ShareLinkPo.ts
  - apps/web/test/offline-cache.test.ts
  - apps/web/test/offline-queue.test.ts
  - apps/api/test/routes/push.test.ts
  - apps/worker/test/push-notification-handler.test.ts
user_setup:
  - service: web-push (VAPID)
    why: "VAPID keypair must be generated once and stored in Infisical; keys are stable forever — regenerating invalidates all push subscriptions (RESEARCH Pitfall 4)"
    env_vars:
      - name: VAPID_PUBLIC_KEY
        source: 'Generated by `bun -e "import webPush from ''web-push''; console.log(webPush.generateVAPIDKeys())"` then stored in Infisical'
      - name: VAPID_PRIVATE_KEY
        source: "Same generation command; Infisical secret — NEVER client-bundled"
      - name: VAPID_SUBJECT
        source: "Set to mailto:admin@budget.app in Infisical"
      - name: NEXT_PUBLIC_VAPID_PUBLIC_KEY
        source: "Same value as VAPID_PUBLIC_KEY; safe to expose to the browser for PushManager.subscribe()"

must_haves:
  truths:
    - "idb and web-push packages are installed and importable"
    - "push_subscriptions and notification_prefs tables exist in the live DB with tenant-isolation RLS"
    - "tenant-leak CI gate covers the two new tables (8 -> 10 cross-tenant files green via make ci-gate)"
    - "scripts/check-i18n-completeness.ts exits non-zero if any EN key is absent in PL or UK and grep-fails on workspaces.*/accounts.* namespaces"
    - "All Phase 8 i18n keys (pwa.*, settings.push.*, onboarding.push.*, offline.*, sync.*, serverDown.*) exist in en.json, pl.json, uk.json"
    - "SpendingsPo, OnboardingPo, ShareLinkPo page objects exist and export classes"
    - "Wave-0 test scaffolds exist (offline-cache, offline-queue, push route, push handler) and fail until their owning plan implements them"
  artifacts:
    - path: "packages/platform/src/push/schema.ts"
      provides: "push_subscriptions + notification_prefs Drizzle tables with pgPolicy RLS"
      contains: "pushSubscriptions"
    - path: "apps/migrator/drizzle/0031_phase08_push.sql"
      provides: "CREATE TABLE + RLS policy migration for both push tables"
      contains: "create table"
    - path: "scripts/check-i18n-completeness.ts"
      provides: "CI i18n completeness + stale-namespace gate"
      contains: "flatKeys"
  key_links:
    - from: "packages/platform/src/push/index.ts"
      to: "packages/platform/src/push/schema.ts"
      via: "barrel re-export"
      pattern: "export.*schema"
    - from: "apps/migrator/drizzle/meta/_journal.json"
      to: "0031_phase08_push"
      via: "manual journal registration"
      pattern: "0031_phase08_push"
---

<objective>
Wave 0 foundation for Phase 8. Install `idb` + `web-push`, define and migrate the `push_subscriptions` + `notification_prefs` tables (the [BLOCKING] schema task), extend the tenant-leak CI gate to 10 cross-tenant files, ship the i18n completeness gate + all new Phase 8 message keys in EN/PL/UK, and scaffold the three missing E2E Page Objects plus the Nyquist test stubs every downstream plan asserts against.

Purpose: Every other Phase 8 plan (push backend, offline cache, push UI, E2E) depends on these packages, tables, keys, POs, and test scaffolds existing first. Without the migration the phase passes build but fails verification (types come from schema config, not the live DB).
Output: Installed deps, migrated push tables, green ci-gate on new tables, complete 3-locale message catalogs, i18n gate script, 3 POs, 4 failing test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-RESEARCH.md
@.planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-PATTERNS.md
@.planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-UI-SPEC.md

<interfaces>
RLS pgPolicy pattern (from packages/platform/src/idempotency/schema.ts — copy verbatim, swap table name):
```typescript
pgPolicy("push_subscriptions_tenant_isolation", {
  as: "permissive", for: "all", to: [appRole],
  using: sql`${t.tenantId} = ANY(coalesce(nullif(current_setting('app.tenant_ids', true), ''), '{}')::uuid[])`,
  withCheck: sql`${t.tenantId} = ANY(coalesce(nullif(current_setting('app.tenant_ids', true), ''), '{}')::uuid[])`,
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install idb + web-push and define push schema barrel</name>
  <read_first>
    - packages/platform/src/outbox/schema.ts (sharedKernel.table shape — exact analog)
    - packages/platform/src/idempotency/schema.ts (pgPolicy RLS pattern, lines 28-48)
    - packages/platform/src/outbox/index.ts (barrel export shape)
    - packages/platform/src/db/roles.ts (appRole / workerRole imports)
    - CLAUDE.md (Drizzle types ONLY in adapters/persistence; sharedKernel placement)
  </read_first>
  <action>
    Run `bun add idb` in apps/web; `bun add web-push && bun add -d @types/web-push` in packages/platform.
    Create packages/platform/src/push/schema.ts with two `sharedKernel.table` definitions:
    - `pushSubscriptions` ("push_subscriptions"): id uuid pk defaultRandom, tenantId uuid not null, userId uuid not null, endpoint text not null unique, p256dh text not null, auth text not null, locale text not null default "en", createdAt timestamptz defaultNow not null. Add `pgPolicy("push_subscriptions_tenant_isolation", ...)` copying the idempotency pattern verbatim.
    - `notificationPrefs` ("notification_prefs"): id uuid pk defaultRandom, tenantId uuid not null, userId uuid not null, budgetId uuid not null, notificationType text not null (registry key — extensible per D-11; NOT an enum), enabled boolean not null default true, updatedAt timestamptz defaultNow not null. Add `pgPolicy("notification_prefs_tenant_isolation", ...)`. Add a unique index on (userId, budgetId, notificationType).
    Create packages/platform/src/push/vapid.ts: thin module that calls `webPush.setVapidDetails(process.env.VAPID_SUBJECT!, process.env.VAPID_PUBLIC_KEY!, process.env.VAPID_PRIVATE_KEY!)` once and re-exports `webPush.sendNotification`. VAPID private key reads only from env (server-only) — never imported into apps/web.
    Create packages/platform/src/push/index.ts barrel: `export * from "./schema"; export * from "./vapid";`. Wire into packages/platform's main index export so `@budget/platform` resolves the push module.
  </action>
  <verify>
    <automated>cd apps/web && bun pm ls 2>/dev/null | grep -q idb && cd /home/claude/budget && grep -q "web-push" packages/platform/package.json && bunx tsc --noEmit -p packages/platform/tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "idb" apps/web/package.json` >= 1
    - `grep -c "web-push" packages/platform/package.json` >= 1
    - `grep -q "pushSubscriptions" packages/platform/src/push/schema.ts`
    - `grep -q "notificationPrefs" packages/platform/src/push/schema.ts`
    - `grep -v '^//' packages/platform/src/push/schema.ts | grep -c "pgPolicy" ` == 2
    - notificationPrefs.enabled column type is boolean (not text)
    - `bunx tsc --noEmit -p packages/platform/tsconfig.json` exits 0
  </acceptance_criteria>
  <done>Both packages installed; push schema + vapid + index created and type-clean; barrel exported from @budget/platform.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Generate + apply push tables migration; extend tenant-leak gate to 10 files</name>
  <read_first>
    - apps/migrator/drizzle/0030*.sql (most recent migration — SQL style + RLS GRANT pattern)
    - apps/migrator/drizzle/meta/_journal.json (manual journal entry format)
    - apps/migrator/post-migration.sql (FORCE ROW LEVEL SECURITY + GRANT pattern for new tables)
    - tests/tenant-leak/cushion-summary-cross-tenant.test.ts (cross-tenant test analog — most recent gate file)
    - tests/tenant-leak/USER-DATA-TABLES.txt (the allowlist the force-rls test reads)
    - STATE.md "Plan 06-01 Decisions" (drizzle-kit BigInt serialization bug → hand-author migration + manual journal entry, established precedent)
  </read_first>
  <action>
    Hand-author apps/migrator/drizzle/0031_phase08_push.sql (drizzle-kit generate is blocked by the BigInt serialization bug — established precedent in Phases 1/5/6):
    - `CREATE TABLE shared_kernel.push_subscriptions (...)` and `shared_kernel.notification_prefs (...)` matching the Drizzle schema columns exactly.
    - `ALTER TABLE ... ENABLE ROW LEVEL SECURITY; ALTER TABLE ... FORCE ROW LEVEL SECURITY;` on both.
    - `CREATE POLICY push_subscriptions_tenant_isolation ON shared_kernel.push_subscriptions ...` and the notification_prefs equivalent, using `current_setting('app.tenant_ids', true)` ANY(...) predicate (copy from idempotency policy SQL).
    - UNIQUE index on notification_prefs(user_id, budget_id, notification_type); UNIQUE on push_subscriptions(endpoint).
    - GRANT SELECT/INSERT/UPDATE/DELETE on both to app + worker roles.
    Register the migration in apps/migrator/drizzle/meta/_journal.json manually (next idx, tag "0031_phase08_push"). Add any GRANTs the post-migration.sql expects.
    Apply with `make migrate` (wraps `infisical run -- docker compose run --rm migrator`).
    Add tests/tenant-leak/push-subscriptions-cross-tenant.test.ts and notification-prefs-cross-tenant.test.ts copying cushion-summary-cross-tenant.test.ts: seed two tenants, assert tenant A cannot SELECT tenant B's rows under app role GUC. Add both table names to tests/tenant-leak/USER-DATA-TABLES.txt so the force-rls-on-all-tables test covers them.
  </action>
  <verify>
    <automated>make migrate 2>&1 | tail -3 && make ci-gate 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "create table" apps/migrator/drizzle/0031_phase08_push.sql` >= 2
    - `grep -c "FORCE ROW LEVEL SECURITY" apps/migrator/drizzle/0031_phase08_push.sql` >= 2
    - `grep -q "0031_phase08_push" apps/migrator/drizzle/meta/_journal.json`
    - `grep -q "push_subscriptions" tests/tenant-leak/USER-DATA-TABLES.txt && grep -q "notification_prefs" tests/tenant-leak/USER-DATA-TABLES.txt`
    - `make ci-gate` exits 0 with the two new cross-tenant files green (10 cross-tenant files total)
  </acceptance_criteria>
  <done>Both tables exist in the live DB with FORCE RLS + tenant-isolation policy; tenant-leak gate green covering both; migration registered in journal.</done>
</task>

<task type="auto">
  <name>Task 3: i18n completeness gate script + all Phase 8 keys in EN/PL/UK</name>
  <read_first>
    - apps/web/messages/en.json (existing namespace structure — append, do not restructure)
    - apps/web/messages/pl.json, apps/web/messages/uk.json (parity, 1126 lines each)
    - .planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-UI-SPEC.md § "Copywriting Contract" + § "i18n Key Additions Summary" (the authoritative key list + EN strings)
    - .planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-RESEARCH.md § "Pattern 5: next-intl Completeness Gate"
    - CLAUDE.md "Local Development" (messages bundled at build time — rebuild web after edits)
  </read_first>
  <action>
    Create scripts/check-i18n-completeness.ts per RESEARCH Pattern 5: import en/pl/uk json, `flatKeys()` recursive flattener, compute EN keys missing from PL and UK (and vice versa), and ALSO grep-assert no flat key starts with `workspaces.` or `accounts.` (I18N-02). `process.exit(1)` with the missing/stale list on any gap. Add `"check:i18n": "bun scripts/check-i18n-completeness.ts"` to root package.json scripts.
    Add ALL keys from the UI-SPEC "i18n Key Additions Summary" block to en.json with the exact EN strings from the Copywriting Contract tables: namespaces `pwa.install.*`, `settings.push.*` (incl. per-kind RESERVE_TOPUP/CONFIRM_DRAFT/CUSHION_BELOW_TARGET label+description), `onboarding.push.*`, `offline.*`, `sync.*` (incl. ICU plural `sync.badge.ariaLabel` and `sync.issues.reason.*` four codes), `serverDown.*` (incl. signedOut variant).
    Add the same keys to pl.json and uk.json with LLM-translated values, flagged machine-origin per D-19 (a `_machineTranslated: true` sibling marker per new namespace block, or a leading comment). Keep existing v1.0 PL/UK strings untouched (D-18).
    Rename the single residual `no_workspaces` key (in the nav namespace) to a budget-shaped key in all three catalogs (I18N-02 cleanup; RESEARCH confirms this is the only stale string).
  </action>
  <verify>
    <automated>bun scripts/check-i18n-completeness.ts && echo "I18N_GATE_PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `bun scripts/check-i18n-completeness.ts` exits 0 and prints I18N_GATE_PASS
    - `grep -c "flatKeys" scripts/check-i18n-completeness.ts` >= 1
    - `grep -q "check:i18n" package.json`
    - en/pl/uk all contain `settings.push.kind.CUSHION_BELOW_TARGET` (gate passing proves 3-locale parity)
    - `grep -rc "no_workspaces" apps/web/messages/` == 0
  </acceptance_criteria>
  <done>i18n gate script passes; all Phase 8 keys present in 3 locales with EN canonical + PL/UK machine-flagged; stale no_workspaces removed.</done>
</task>

<task type="auto">
  <name>Task 4: Scaffold 3 Page Objects + 4 Nyquist test stubs</name>
  <read_first>
    - apps/web/e2e/page-objects/ReservesPo.ts (exact PO structure: constructor(Page), Locator methods, getByTestId)
    - apps/web/e2e/page-objects/SettingsPo.ts (shareUrlField pattern for ShareLinkPo)
    - apps/web/test/sw-offline.test.ts (Vitest + happy-dom test shape)
    - .planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-PATTERNS.md § SpendingsPo/OnboardingPo/ShareLinkPo shapes (lines 720-772)
    - .planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-VALIDATION.md § "Wave 0 Requirements"
  </read_first>
  <action>
    Create apps/web/e2e/page-objects/SpendingsPo.ts per PATTERNS shape: constructor(private page: Page); methods quickEntryInput(), quickEntrySubmit(), transactionRow(id), pendingSyncMarker(id), syncIssuesList(), offlineStatusBadge() — all returning `this.page.getByTestId(...)` with the data-testids from UI-SPEC (quick-entry-input, txn-row-{id}, txn-pending-{id}, sync-issues-list, offline-status-badge).
    Create OnboardingPo.ts: stepTitle() locator, pushStepSwitch() (`getByTestId("onboarding-push-switch")`), skipButton() locator.
    Create ShareLinkPo.ts: joinCard(), joinConfirmButton(), errorState() locators for the recipient join page.
    Create 4 failing test scaffolds, each a single test that asserts a NOT_IMPLEMENTED sentinel (e.g. `expect("SCAFFOLD").toBe("IMPLEMENTED")`) with a `// SCAFFOLD: implemented in plan 08-0X` comment so the owning plan turns it green:
    - apps/web/test/offline-cache.test.ts (PWAX-02 — implemented in 08-03)
    - apps/web/test/offline-queue.test.ts (PWAX-03 — implemented in 08-03)
    - apps/api/test/routes/push.test.ts (PWAX-04 — implemented in 08-02)
    - apps/worker/test/push-notification-handler.test.ts (PWAX-05 — implemented in 08-05)
  </action>
  <verify>
    <automated>ls apps/web/e2e/page-objects/SpendingsPo.ts apps/web/e2e/page-objects/OnboardingPo.ts apps/web/e2e/page-objects/ShareLinkPo.ts apps/web/test/offline-cache.test.ts apps/web/test/offline-queue.test.ts apps/api/test/routes/push.test.ts apps/worker/test/push-notification-handler.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 3 PO files exist and `export class SpendingsPo|OnboardingPo|ShareLinkPo`
    - `grep -q "quick-entry-input" apps/web/e2e/page-objects/SpendingsPo.ts`
    - `grep -q "onboarding-push-switch" apps/web/e2e/page-objects/OnboardingPo.ts`
    - All 4 scaffold test files exist and contain `SCAFFOLD: implemented in plan 08-`
  </acceptance_criteria>
  <done>3 Page Objects exist with PATTERNS-aligned testids; 4 scaffold tests exist and fail pending their owning plan.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                                                             |
| --------------------------------- | --------------------------------------------------------------------------------------- |
| browser → API (push subscribe)    | Browser-supplied subscription endpoint + keys cross into server storage                 |
| tenant A ↔ tenant B (push tables) | Two tenants share the push_subscriptions / notification_prefs tables — RLS must isolate |
| env → server (VAPID private key)  | VAPID private key must never cross into the client bundle                               |

## STRIDE Threat Register

| Threat ID  | Category        | Component                               | Disposition | Mitigation Plan                                                                                                                                                        |
| ---------- | --------------- | --------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-08-01-01 | Info Disclosure | push_subscriptions / notification_prefs | mitigate    | FORCE ROW LEVEL SECURITY + tenant_id ANY(app.tenant_ids) pgPolicy on both tables; two new cross-tenant CI gate tests assert isolation (ASVS V4)                        |
| T-08-01-02 | Info Disclosure | VAPID private key                       | mitigate    | VAPID_PRIVATE_KEY read only from env in packages/platform/src/push/vapid.ts; never imported by apps/web; only NEXT_PUBLIC_VAPID_PUBLIC_KEY exposed to client (ASVS V6) |
| T-08-01-03 | Tampering       | migration journal                       | accept      | Hand-authored migration + manual journal entry per established Phase 1/5/6 precedent; reviewed before make migrate                                                     |

</threat_model>

<verification>
- `make ci-gate` green (10 cross-tenant files including the two new push tables).
- `bun scripts/check-i18n-completeness.ts` exits 0.
- `bunx tsc --noEmit -p packages/platform/tsconfig.json` clean.
- 3 Page Objects + 4 scaffold tests on disk.
</verification>

<success_criteria>
idb + web-push installed; push_subscriptions + notification_prefs migrated with RLS and covered by tenant-leak gate; i18n gate script passes with all Phase 8 keys in EN/PL/UK; 3 Page Objects + 4 failing scaffolds in place. (Partially satisfies PWAX-04 storage, I18N-01/02, E2EX-02.)
</success_criteria>

<output>
After completion, create `.planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-01-SUMMARY.md`
</output>
