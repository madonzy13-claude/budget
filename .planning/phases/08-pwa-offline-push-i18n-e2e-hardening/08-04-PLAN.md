---
phase: 08-pwa-offline-push-i18n-e2e-hardening
plan: 04
type: execute
wave: 2
depends_on: [08-01]
autonomous: true
requirements: [I18N-02, I18N-03, I18N-04, I18N-05, PWAX-01]
files_modified:
  - apps/web/src/middleware.ts
  - apps/web/src/components/common/offline-fallback.tsx
  - apps/web/src/components/common/server-down-card.tsx
  - apps/web/src/components/common/staleness-marker.tsx
  - apps/web/public/offline.html
  - apps/web/public/manifest.json
  - apps/web/test/middleware-accept-language.test.ts
  - apps/web/test/staleness-marker.test.ts
  - apps/web/test/intl-format-audit.test.ts

must_haves:
  truths:
    - "First visit with Accept-Language pl/uk and no cookie/session lands on that locale; else en (D-20)"
    - "Money renders via Intl.NumberFormat with budget currency; dates via Temporal + Intl.DateTimeFormat (I18N-03/04 audited)"
    - "Never-synced offline surface shows the 'unavailable offline' empty-state with retry (D-04)"
    - "Server-unreachable auth failure shows the signed-out screen with ONLY a manual Reload button — no redirect to /login (D-07/D-08)"
    - "Cached views show a 'last synced X ago' marker while offline/reconnecting (D-05)"
    - "manifest.json passes installability (icons with correct purpose; theme-color) and the declared maskable icon file exists on disk (PWAX-01)"
  artifacts:
    - path: "apps/web/src/middleware.ts"
      provides: "Accept-Language first-visit negotiation"
      contains: "accept-language"
    - path: "apps/web/src/components/common/staleness-marker.tsx"
      provides: "last-synced relative-time marker"
      contains: "formatRelativeTime"
    - path: "apps/web/public/offline.html"
      provides: "static offline fallback served by SW"
      contains: "offline"
  key_links:
    - from: "apps/web/src/middleware.ts"
      to: "negotiated locale"
      via: "Accept-Language read before en fallback"
      pattern: "accept-language"
    - from: "apps/web/src/components/common/server-down-card.tsx"
      to: "window.location.reload"
      via: "manual reload button (no /login link)"
      pattern: "reload"
---

<objective>
i18n finalization + PWA resilience screens. Add the one missing i18n piece — Accept-Language first-visit negotiation in middleware (D-20; all other locale infra is already built per CONTEXT D-20). Audit money/date formatting to Intl.NumberFormat + Temporal/Intl.DateTimeFormat (I18N-03/04). Ship the offline + server-down + auth-failed fallback screens (D-07/08, never-blank/never-redirect-loop) and the "last synced X ago" staleness marker (D-05). Audit + fix the PWA manifest for installability (PWAX-01, RESEARCH Pitfall 8).

Purpose: I18N-05 persistence and the switchers already exist (PUT /settings/locale at settings.ts:36, locale-select.tsx, public-locale-switcher.tsx) — do NOT rebuild them. This plan closes the negotiation gap and the resilience UX that keeps the app from ever showing a blank page or redirect loop.
Output: middleware negotiation + 3 fallback components + staleness marker + offline.html + manifest fix + format audit; 3 green Vitest suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-CONTEXT.md
@.planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-PATTERNS.md
@.planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-UI-SPEC.md

<interfaces>
Accept-Language negotiation (PATTERNS lines 560-567 — add before the hardcoded "en" fallback branch):
```typescript
const acceptLang = request.headers.get("accept-language") ?? "";
const preferred = acceptLang.split(",")[0]?.split(";")[0]?.trim()?.slice(0,2)?.toLowerCase();
const negotiated = (["en","pl","uk"] as const).includes(preferred as any) ? (preferred as "en"|"pl"|"uk") : "en";
```

ALREADY BUILT — do NOT touch (CONTEXT D-20):

- apps/api/src/routes/settings.ts:36 PUT /settings/locale (writes users.locale)
- apps/web/src/components/settings/locale-select.tsx (logged-in switcher — Settings, by design)
- apps/web/src/components/common/public-locale-switcher.tsx (logged-out switcher)
- locale-cookie-sync.tsx (budget-locale cookie) + middleware logged-in redirect
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Accept-Language first-visit negotiation + Intl/Temporal format audit</name>
  <read_first>
    - apps/web/src/middleware.ts (self — the budget-locale cookie + session-locale redirect branches; the hardcoded "en" fallback is the only edit point)
    - apps/web/i18n/routing.ts (locales ["en","pl","uk"], localePrefix "always")
    - apps/web/src/lib/cents-format.ts (existing money formatter — confirm Intl.NumberFormat usage)
    - .planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-CONTEXT.md § D-20 (only Accept-Language missing; everything else built)
    - .planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-RESEARCH.md § I18N-03/I18N-04 rows
  </read_first>
  <behavior>
    - Request with no budget-locale cookie, no session, Accept-Language "pl-PL,pl;q=0.9" → negotiated "pl"
    - Same with "uk" → "uk"; with "de" or absent → "en"
    - A client-supplied budget-locale cookie or session locale still wins over Accept-Language (negotiation only fills the previously-hardcoded default)
    - Money audit: a representative cents value renders via Intl.NumberFormat with the budget currency; date audit: a Temporal date renders via Intl.DateTimeFormat for the active locale
  </behavior>
  <action>
    Modify apps/web/src/middleware.ts: in the branch where there is no budget-locale cookie AND no session locale, replace the hardcoded `"en"` with the negotiated value computed from `request.headers.get("accept-language")` per the interface snippet. Do not alter the existing cookie/session-locale precedence.
    Write apps/web/test/middleware-accept-language.test.ts asserting the pl/uk/de/absent cases and the cookie/session-wins precedence.
    Audit money + date rendering: grep for raw `.toLocaleString`/manual currency concatenation across apps/web/src; ensure money goes through cents-format.ts (Intl.NumberFormat + budget currency) and dates through a Temporal + Intl.DateTimeFormat helper. Write apps/web/test/intl-format-audit.test.ts that imports the money + date helpers and asserts locale-correct output for en/pl/uk (e.g. PL groups with spaces, UK date order). Fix any callsite using a non-Intl path.
  </action>
  <verify>
    <automated>cd apps/web && bun run test -- middleware-accept-language intl-format-audit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "accept-language" apps/web/src/middleware.ts`
    - `grep -q "Intl.NumberFormat" apps/web/src/lib/cents-format.ts`
    - `cd apps/web && bun run test -- middleware-accept-language` passes pl/uk/de/absent + precedence
    - `cd apps/web && bun run test -- intl-format-audit` passes for all 3 locales
  </acceptance_criteria>
  <done>Accept-Language negotiation lands; money/date formatting confirmed Intl/Temporal-backed and locale-correct in tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Offline + server-down + auth-failed fallback screens + staleness marker</name>
  <read_first>
    - apps/web/src/components/common/server-down-card.tsx (self — extend with signedOut variant; tokens + reload button pattern)
    - apps/web/public/offline.html (self — static fallback served by SW)
    - apps/web/messages/en.json (offline.*, serverDown.*, sync.staleness keys from 08-01)
    - .planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-UI-SPEC.md § Component 7 (offline screen + inline unavailable) + Component 8 (server-down + signedOut, D-07 invariant) + Component 9 (staleness marker)
  </read_first>
  <behavior>
    - Inline "unavailable offline" empty-state renders WifiOff/Database icon + offline.unavailable.* copy + retry button (D-04)
    - Server-down screen renders serverDown.* copy with a single Reload button calling window.location.reload(); contains NO <a href="/login"> and NO automatic redirect (D-07 invariant)
    - Auth-failed (signedOut) variant renders serverDown.signedOut.* with the same single Reload button
    - StalenessMarker renders "Last synced X ago" via next-intl formatRelativeTime; shown offline or within 30s of reconnect, hidden when fresh-online
  </behavior>
  <action>
    Create apps/web/src/components/common/offline-fallback.tsx exporting the inline "unavailable offline" empty-state per UI-SPEC Component 7 (used by D-04 cold-cache surfaces) with `data-testid="offline-unavailable"`.
    Extend apps/web/src/components/common/server-down-card.tsx with the signedOut variant per UI-SPEC Component 8: a `variant: "serverDown" | "signedOut"` prop selecting heading/body copy; the ONLY interactive element is `<button onClick={() => window.location.reload()}>` (never an `<a>` to /login). `data-testid="server-down-card"`. Ensure the auth middleware / error boundary renders THIS on a network-unreachable session check instead of redirecting (D-07/D-08).
    Update apps/web/public/offline.html copy to the offline.* EN strings, `lang="en"` static, yellow reload button (i18n unavailable in static fallback — acceptable per UI-SPEC accessibility note).
    Create apps/web/src/components/common/staleness-marker.tsx per UI-SPEC Component 9: `useTranslations` + `useFormatter().relativeTime`, visible when offline or <30s post-reconnect, `aria-live="polite"`, `data-testid="staleness-marker"`. It consumes getSyncMeta (from 08-03) for the last-synced timestamp.
    Write apps/web/test/staleness-marker.test.ts (RTL) + a server-down-card test asserting the no-/login-link + reload-only invariant.
  </action>
  <verify>
    <automated>cd apps/web && bun run test -- staleness-marker server-down-card 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "data-testid=\"offline-unavailable\"" apps/web/src/components/common/offline-fallback.tsx`
    - `grep -q "window.location.reload" apps/web/src/components/common/server-down-card.tsx`
    - `grep -ci "href=\"/login\"\|redirect(.*login)" apps/web/src/components/common/server-down-card.tsx` == 0 (D-07 invariant)
    - `grep -q "formatRelativeTime\|relativeTime" apps/web/src/components/common/staleness-marker.tsx`
    - `cd apps/web && bun run test -- staleness-marker server-down-card` passes incl. the no-redirect assertion
  </acceptance_criteria>
  <done>3 fallback surfaces + staleness marker implemented; D-07 no-redirect-loop invariant test-enforced; offline.html localized copy.</done>
</task>

<task type="auto">
  <name>Task 3: PWA manifest installability audit</name>
  <read_first>
    - apps/web/public/manifest.json (self — current icons set purpose "any maskable" on same src, incorrect per RESEARCH Pitfall 8)
    - apps/web/next.config.mjs (Serwist config; SW disabled in dev)
    - .planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-RESEARCH.md § Pitfall 8 (manifest icons)
    - DESIGN.md (theme/canvas colors for theme_color/background_color)
  </read_first>
  <action>
    Fix apps/web/public/manifest.json per RESEARCH Pitfall 8: provide a dedicated `purpose: "maskable"` icon (192 + 512) with safe-zone content AND separate `purpose: "any"` icons — do NOT set "any maskable" on the same non-maskable src. Ensure `name`, `short_name`, `start_url`, `display: "standalone"`, `theme_color` (#0b0e11 canvas), `background_color` are present so Chrome install criteria pass. If a maskable asset is absent, GENERATE a maskable PNG from the existing icon with adequate padding and write it to apps/web/public/ so the file the manifest points at actually exists on disk.
    Verify manifest validity AND that every icon src declared in the manifest resolves to a real file under apps/web/public (JSON parse + required-field assertion + on-disk existence check — a node/bun script, no test framework needed).
  </action>
  <verify>
    <automated>cd /home/claude/budget && bun -e "const fs=require('fs'); const m=require('./apps/web/public/manifest.json'); if(!m.icons.some(i=>i.purpose==='maskable')) process.exit(1); if(m.display!=='standalone') process.exit(1); if(!m.theme_color) process.exit(1); for(const i of m.icons){const p='./apps/web/public'+(i.src.startsWith('/')?i.src:'/'+i.src); if(!fs.existsSync(p)){console.error('MISSING_ICON '+p); process.exit(1);}} console.log('MANIFEST_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `bun -e` manifest check prints MANIFEST_OK
    - manifest has at least one icon with `"purpose": "maskable"` (not combined "any maskable" on same src)
    - the maskable icon file declared in manifest.json exists on disk (the verify loop exits 0 with no MISSING_ICON; e.g. `ls apps/web/public/<maskable-icon>.png` exits 0)
    - `grep -q "\"display\": \"standalone\"" apps/web/public/manifest.json`
    - `grep -q "theme_color" apps/web/public/manifest.json`
  </acceptance_criteria>
  <done>Manifest passes installability field checks with a correct dedicated maskable icon whose file actually exists on disk.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                     |
| -------------------------------------------- | ----------------------------------------------- |
| client → middleware (Accept-Language header) | Untrusted header drives first-visit locale only |
| auth check ↔ unreachable server              | Failure path must not loop redirect to /login   |

## STRIDE Threat Register

| Threat ID  | Category           | Component                    | Disposition | Mitigation Plan                                                                                                                                                                                |
| ---------- | ------------------ | ---------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-08-04-01 | Tampering          | Accept-Language header       | mitigate    | Header value is validated against the fixed locale allowlist ["en","pl","uk"]; any other value falls back to "en"; only sets a default, never overrides authenticated session locale (ASVS V5) |
| T-08-04-02 | DoS / availability | auth-failed redirect loop    | mitigate    | D-07: server-down/signedOut screens contain only a manual Reload button, no /login link, no auto-redirect — breaks any redirect loop; test asserts absence of login link                       |
| T-08-04-03 | Info Disclosure    | offline.html / fallback copy | accept      | Static fallback shows no financial data; generic copy only                                                                                                                                     |

</threat_model>

<verification>
- `cd apps/web && bun run test -- middleware-accept-language intl-format-audit staleness-marker server-down-card` green.
- manifest installability check prints MANIFEST_OK and all icon files resolve on disk.
- No `/login` link or auto-redirect in the server-down fallback.
</verification>

<success_criteria>
Accept-Language negotiation closes the only i18n gap; money/date formatting audited Intl/Temporal-correct; offline + server-down + signedOut screens never blank/never loop; staleness marker live; manifest installable with a real on-disk maskable icon. (Satisfies I18N-02/03/04/05 finalization + PWAX-01 manifest.)
</success_criteria>

<output>
After completion, create `.planning/phases/08-pwa-offline-push-i18n-e2e-hardening/08-04-SUMMARY.md`
</output>
</content>
