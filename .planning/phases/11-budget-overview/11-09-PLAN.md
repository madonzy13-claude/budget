---
phase: 11-budget-overview
plan: 09
type: execute
wave: 3
depends_on: ["11-02", "11-04", "11-05", "11-06", "11-08"]
autonomous: true
requirements: [SC3, SC4, SC5, SC7, D-18, D-20, D-21]
files_modified:
  - apps/web/src/components/budgeting/overview/overview-sections.tsx
  - apps/web/src/components/budgeting/overview/range-selector.tsx
  - apps/web/src/components/budgeting/overview/planned-section.tsx
  - apps/web/src/components/budgeting/overview/overspent-reserves-section.tsx
  - apps/web/src/components/budgeting/overview/wealth-section.tsx
  - apps/web/src/hooks/use-overview-planned.ts
  - apps/web/src/hooks/use-overview-overspent.ts
  - apps/web/src/hooks/use-overview-wealth.ts
  - apps/web/src/components/budgeting/overview/overview-tab.tsx
  - apps/web/messages/en.json
  - apps/web/test/components/budgeting/overview/overview-sections.test.tsx
must_haves:
  truths:
    - "Four sections render collapsed by default (Planned · Overspent · Reserves · Financial Wealth) and expand on tap (D-21, SC3)"
    - "A shared range selector (this month · last 3 months · this year · all · custom from→to) drives the range-scoped sections (Planned timeline/avg, Overspent, Wealth value) but NOT the two recurring charts (D-14) nor the reserves-by-category bar (SC3, SC4)"
    - "Planned section: a category selector (default = none → budget-wide) toggles per-category; renders planned-vs-real timeline, planned-avg-vs-real bar (Y=category), recurring-per-month + recurring-per-category (SC4)"
    - "Overspent section: range total + overspent-by-category bar; Reserves section: reserves-by-category bar (SC5)"
    - "Financial-Wealth section: capitalization(default)/investments toggle; grow/loss (amount+%), monthly-avg grow %, value time-series, month-over-month dynamics %; investments view adds a per-type pie using Phase-9 UI_TYPE_COLOR with tap/hover → share % + type label (SC7, D-18)"
    - "Each section fetches lazily (its RQ hook only fires when the section is open / range changes) and the charts use the 11-02 wrappers (never raw recharts) so they are responsive + themed + 375px-safe"
    - "Section data hooks pass the resolved from/to (and view/categoryId) as part of the query key so RQ caches per range/selection (D-03)"
  artifacts:
    - path: "apps/web/src/components/budgeting/overview/wealth-section.tsx"
      provides: "Wealth section with toggle + stats + series + pie"
      contains: "investments"
    - path: "apps/web/src/components/budgeting/overview/range-selector.tsx"
      provides: "shared range selector"
      contains: "last 3 months"
  key_links:
    - from: "apps/web/src/components/budgeting/overview/planned-section.tsx"
      to: "apps/web/src/components/budgeting/charts/line-chart.tsx (11-02)"
      via: "timeline rendered via the themed wrapper"
      pattern: "charts/line-chart"
    - from: "apps/web/src/components/budgeting/overview/wealth-section.tsx"
      to: "apps/web/src/lib/investment-icons.ts (UI_TYPE_COLOR)"
      via: "pie colorFor maps holding_type → Phase-9 color"
      pattern: "UI_TYPE_COLOR"
threat_model: []
---

<objective>
Build the four collapsible Overview sections + the shared range selector, wiring each section's data hook to the 11-02 chart wrappers (SC3-7). Planned (timeline + category selector + planned-avg + 2 recurring), Overspent + Reserves (bars), Financial Wealth (toggle + stats + series + MoM + per-type pie with tap/hover reveal).

Purpose: This is the chart-composition slice — it consumes the section services (11-04/05/06) and the chart primitives (11-02). All the math is server-side and tested; here the work is collapse/expand, the shared range state, the category + wealth toggles, lazy fetching per section, and mapping DTOs onto wrappers. recharts needs Numbers, so the hooks convert string cents → Number in selectors (not the API).
Output: four sections rendering real charts, shared range selector, lazy per-section fetch, pie interaction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/ui-brand.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-budget-overview/11-CONTEXT.md
@.planning/phases/11-budget-overview/11-RESEARCH.md
@.planning/phases/11-budget-overview/11-02-PLAN.md
@.planning/phases/11-budget-overview/11-04-PLAN.md
@.planning/phases/11-budget-overview/11-05-PLAN.md
@.planning/phases/11-budget-overview/11-06-PLAN.md
@.planning/phases/11-budget-overview/11-UI-SPEC.md
@DESIGN.md

INTERFACES — exact analog signatures; use directly:

CHART wrappers (11-02): OverviewAreaChart/OverviewLineChart/OverviewBarChart({data,xKey,series,height,layout,formatY}), OverviewPieChart({data,nameKey,valueKey,colorFor,height}). Import from apps/web/src/components/budgeting/charts/\*. Never import raw recharts here.

SECTION endpoints (11-04/05/06) + their DTOs:
GET /budgets/:id/overview/planned?from&to&categoryId → OverviewPlannedDTO {currency,bucket,timeline[],plannedAvgVsReal[],recurringPerMonth[],recurringPerCategory[]}
GET /budgets/:id/overview/overspent-reserves?from&to → OverviewOverspentDTO {currency,overspent_total_cents,overspent_by_category[],reserves_by_category[]}
GET /budgets/:id/overview/wealth?from&to&view → OverviewWealthDTO {currency,view,bucket,series[],grow{delta_cents,delta_pct},monthly_avg_grow_pct,dynamics[],pie[]|null}

HOOK pattern (mirror use-overview-cards.ts from 11-08): useOverviewPlanned(budgetId,{from,to,categoryId}) with queryKey ["budget",budgetId,"overview","planned",from,to,categoryId], enabled: sectionOpen (lazy). Same for overspent (no view) + wealth (view). Convert \*\_cents strings → Number in a select/transform for recharts. networkMode "online".

RANGE preset → {from,to} (D-20 / SC3): thisMonth, last3Months, thisYear, all (from = budget go-live or a sentinel min date; to = today), custom(from,to). Resolve presets in a small lib (overview-range.ts or inline). The recurring charts + reserves bar ignore range.

CATEGORY selector default none → budget-wide (D-21). Category list comes from the existing categories hook (["budget",budgetId,"categories"]) already prefetched.

WEALTH toggle (D-18): capitalization (default) / investments; switches the ?view param (new query key → new fetch). Investments view shows the pie; OverviewPieChart colorFor = (holdingType)=>UI_TYPE_COLOR[mapHoldingTypeToUiType(holdingType)] (import UI_TYPE_COLOR + the mapping from apps/web/src/lib/investment-icons.ts). Pie tap(mobile)/hover(desktop) reveals share % + type label (handled by the 11-02 wrapper's activeIndex + Tooltip).

COLLAPSE: use the app's existing disclosure/accordion primitive if one exists (search components/ui); else a simple useState open + button. Sections collapsed by default (D-21).

i18n: add bdp.tab.overview.sections.{planned,overspent,reserves,wealth}._ + range._ + wealth toggle/stat labels to en.json (PL/UK in 11-10).
</context>

<tasks>

<task type="auto">
  <name>Task 1: shared range selector + section data hooks (lazy)</name>
  <read_first>
    - apps/web/src/hooks/use-overview-cards.ts (hook pattern from 11-08)
    - apps/web/src/components/budgeting/spendings/* (an existing month/range control for visual consistency, if present)
    - apps/web/src/hooks/ (categories hook key for the category selector)
  </read_first>
  <action>
    1. range-selector.tsx ("use client") — RangeSelector({value,onChange}) with presets thisMonth/last3Months/thisYear/all/custom (custom opens two date inputs). Emits {preset, from, to}. Resolve preset→{from,to} (today-relative) in this file or a tiny overview-range.ts helper.
    2. use-overview-planned.ts / use-overview-overspent.ts / use-overview-wealth.ts — RQ hooks per 11-08 pattern, query keys including the range (+ categoryId / view), `enabled` gated on the section being open (lazy). Each exposes a selector turning string cents → Number for recharts.
  </action>
  <verify>
    <automated>cd /home/claude/budget && grep -q "last3Months\|last 3 months" apps/web/src/components/budgeting/overview/range-selector.tsx && grep -q "overview/planned" apps/web/src/hooks/use-overview-planned.ts && grep -q "enabled" apps/web/src/hooks/use-overview-wealth.ts && cd apps/web && bunx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - range-selector.tsx offers the 5 presets incl. custom from→to
    - three section hooks fetch their endpoints with range in the query key and are lazy (enabled gated)
    - typecheck passes
  </acceptance_criteria>
  <done>Range selector + 3 lazy section hooks ready.</done>
</task>

<task type="auto">
  <name>Task 2: Planned + Overspent/Reserves sections (charts wired)</name>
  <read_first>
    - apps/web/src/components/budgeting/charts/* (wrapper prop shapes from 11-02)
    - apps/web/src/hooks/use-overview-planned.ts + use-overview-overspent.ts (Task 1)
    - 11-04-PLAN.md + 11-05-PLAN.md (DTO field names)
  </read_first>
  <action>
    1. planned-section.tsx ("use client") — collapsible; shared range (from parent) + a category selector (default none). When open: OverviewLineChart for planned-vs-real timeline (series planned+real), OverviewBarChart (layout vertical, Y=category) for plannedAvgVsReal, OverviewBarChart for recurringPerMonth (X=month 1-12, NOT range-scoped) + recurringPerCategory. Empty states per section.
    2. overspent-reserves-section.tsx ("use client") — two collapsibles (Overspent: range total figure + OverviewBarChart overspent-by-category; Reserves: OverviewBarChart reserves-by-category, NOT range-scoped). 
    Convert string cents → Number via the hook selectors. Charts via 11-02 wrappers only.
  </action>
  <verify>
    <automated>cd /home/claude/budget && grep -q "charts/line-chart\|OverviewLineChart" apps/web/src/components/budgeting/overview/planned-section.tsx && grep -q "OverviewBarChart\|charts/bar-chart" apps/web/src/components/budgeting/overview/overspent-reserves-section.tsx && cd apps/web && bunx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - planned-section renders timeline (line) + planned-avg (bar) + 2 recurring (bar) via 11-02 wrappers; has a category selector defaulting to none
    - overspent-reserves-section renders the overspent total + by-category bar + reserves-by-category bar
    - recurring + reserves bars are not wired to the range param
    - typecheck passes
  </acceptance_criteria>
  <done>Planned + Overspent/Reserves sections render real charts.</done>
</task>

<task type="auto">
  <name>Task 3: Financial-Wealth section (toggle + stats + series + dynamics + pie)</name>
  <read_first>
    - apps/web/src/components/budgeting/charts/area-chart.tsx + pie-chart.tsx (11-02 wrappers — Area for series, Pie for type breakdown)
    - apps/web/src/lib/investment-icons.ts:42 (UI_TYPE_COLOR + the holding_type→Ui type mapping for colorFor)
    - apps/web/src/hooks/use-overview-wealth.ts (Task 1 hook; view param)
    - 11-06-PLAN.md (OverviewWealthDTO fields)
  </read_first>
  <action>
    wealth-section.tsx ("use client") — collapsible; capitalization(default)/investments toggle (sets ?view → new fetch). When open render: stat row grow/loss (delta_cents formatted + delta_pct), monthly_avg_grow_pct, the value time-series (OverviewAreaChart over series, range-scoped), a month-over-month dynamics bar/line (dynamics pct), and — only in investments view — OverviewPieChart(data=pie, nameKey="holding_type", valueKey="value_cents"→Number, colorFor=ht=>UI_TYPE_COLOR[mapHoldingTypeToUiType(ht)]) with tap/hover share% + label (D-18). Handle null delta_pct (show "—"). Empty state when no snapshots yet.
  </action>
  <verify>
    <automated>cd /home/claude/budget && grep -q "investments" apps/web/src/components/budgeting/overview/wealth-section.tsx && grep -q "UI_TYPE_COLOR" apps/web/src/components/budgeting/overview/wealth-section.tsx && grep -q "OverviewPieChart\|charts/pie-chart" apps/web/src/components/budgeting/overview/wealth-section.tsx && cd apps/web && bunx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - wealth-section has a capitalization/investments toggle switching the view param
    - renders grow/loss, monthly-avg grow, value series (area), MoM dynamics; pie only in investments view with UI_TYPE_COLOR
    - null delta_pct handled
    - typecheck passes
  </acceptance_criteria>
  <done>Wealth section: toggle + stats + series + dynamics + per-type pie with interaction.</done>
</task>

<task type="auto">
  <name>Task 4: compose sections into OverviewTab + component test</name>
  <read_first>
    - apps/web/src/components/budgeting/overview/overview-tab.tsx (the sections slot from 11-08)
    - apps/web/test/components/budgeting/overview/overview-cards.test.tsx (test harness from 11-08)
  </read_first>
  <action>
    1. overview-sections.tsx ("use client") — OverviewSections({budgetId}): own the shared range state; render RangeSelector + the four section components (all collapsed initially), passing range down to range-scoped ones. Insert into overview-tab.tsx below the cards.
    2. overview-sections.test.tsx — Vitest+RTL: mock the three section hooks; assert all four sections start collapsed; expanding Planned fires its hook + renders a chart container (svg); changing the range updates the query key (assert the mocked hook re-called with new from/to); toggling Wealth to investments shows a pie. BDD names. (happy-dom: assert chart container present, not pixels.)
  </action>
  <verify>
    <automated>cd /home/claude/budget/apps/web && bun run test -- overview-sections.test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - overview-tab.tsx renders OverviewSections under the cards
    - overview-sections.test.tsx passes (collapsed-by-default, expand-fetches, range-changes-key, wealth-toggle-pie)
  </acceptance_criteria>
  <done>Sections composed under the tab; component test green.</done>
</task>

</tasks>

<verification>
- cd apps/web && bunx tsc --noEmit exits 0
- overview-sections.test.tsx passes
- Manual/E2E (in 11-10): collapse/expand, range switch, category selector, wealth toggle, pie tap
</verification>

<success_criteria>

- Four sections collapsed by default + shared range selector (SC3, D-21); recurring + reserves bars not range-scoped (D-14)
- Planned (timeline + category selector + planned-avg + 2 recurring) (SC4); Overspent + Reserves bars (SC5)
- Wealth toggle + grow/loss + monthly-avg + series + MoM + per-type pie w/ tap-hover (SC7, D-18)
- Charts via 11-02 wrappers (responsive/themed); lazy per-section fetch (D-03)
  </success_criteria>

<output>
After completion, create `.planning/phases/11-budget-overview/11-09-SUMMARY.md`
</output>
