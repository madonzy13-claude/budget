---
phase: 11-budget-overview
plan: 07
type: execute
wave: 2
depends_on: ["11-01", "11-03"]
autonomous: true
requirements: [SC8, D-04]
files_modified:
  - apps/worker/src/handlers/budget-wealth-snapshot-3h.ts
  - apps/worker/src/worker.ts
  - apps/worker/test/handlers/budget-wealth-snapshot-3h.test.ts
must_haves:
  truths:
    - "A pg-boss queue 'budget-wealth-snapshot-3h' is created and scheduled with cron '0 */3 * * *' (every 3 hours) and the handler is registered, mirroring investment-snapshot-daily (D-04, SC8)"
    - "The handler scans DISTINCT budgets via withInfraTx, then for EACH budget runs withTenantTx(TenantId(budgetId), UserId(SYSTEM_USER_ID), cb) so the RLS GUC app.tenant_ids is scoped to that one tenant before the insert (T-11-02)"
    - "Per budget it computes {capitalization_cents, investment_value_cents, currency} via computeBudgetWealthNow (11-03) — identical numbers to the capitalization card and the wealth live point"
    - "It inserts ONE row per budget into budget_wealth_snapshots with ON CONFLICT (budget_id, date_trunc('hour',captured_at)) DO NOTHING so a same-bucket re-run is idempotent"
    - "Snapshots cover ALL budgets (capitalization meaningful even with zero investments; investment field = 0 when nothing held) (D-04)"
    - "The handler is exercised by an integration test that asserts one row/budget in default_currency and idempotency on re-run"
  artifacts:
    - path: "apps/worker/src/handlers/budget-wealth-snapshot-3h.ts"
      provides: "3h per-budget wealth snapshot handler + register fn"
      contains: "budget-wealth-snapshot-3h"
    - path: "apps/worker/src/worker.ts"
      provides: "queue create + schedule(0 */3 * * *) + register"
      contains: "0 */3 * * *"
  key_links:
    - from: "apps/worker/src/handlers/budget-wealth-snapshot-3h.ts"
      to: "packages/budgeting/src/application/compute-budget-wealth-now.ts (11-03)"
      via: "the snapshot value IS this primitive's output per budget"
      pattern: "computeBudgetWealthNow"
    - from: "apps/worker/src/handlers/budget-wealth-snapshot-3h.ts"
      to: "drizzle/0049 budget_wealth_snapshots_bucket_uidx (11-01)"
      via: "ON CONFLICT target for idempotent insert"
      pattern: "ON CONFLICT"
threat_model:
  - id: T-11-02
    stride: Information Disclosure (cross-tenant write)
    surface: a single cron writing rows for every budget
    mitigation: "withInfraTx is used ONLY to scan DISTINCT budget ids (no row data). Each snapshot write happens inside withTenantTx(TenantId(budgetId), UserId(SYSTEM_USER_ID), cb) which sets app.tenant_ids to that one tenant, so RLS forbids writing another tenant's row; never a bulk cross-tenant INSERT. Mirrors budgeting-reconciliation.ts."
---

<objective>
Add the pg-boss handler that snapshots every budget's wealth every ≤3h (D-04, SC8): for each budget it computes `{capitalization_cents, investment_value_cents}` in `default_currency` via the shared `computeBudgetWealthNow` primitive (11-03) and writes one idempotent row into `budget_wealth_snapshots` (11-01). Register + schedule it in the worker.

Purpose: This is the only background machinery in the phase — everything else is compute-on-read. It mirrors `investment-snapshot-daily` exactly (queue → schedule → register; `withInfraTx` scan → `withTenantTx` per budget). Standard plan (no business logic to TDD — the math lives in the already-tested 11-03 primitive), but it gets a real-DB integration test for the per-tenant + idempotency contract.
Output: `budget-wealth-snapshot-3h.ts` handler + worker registration on `0 */3 * * *` + integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-budget-overview/11-CONTEXT.md
@.planning/phases/11-budget-overview/11-RESEARCH.md
@.planning/phases/11-budget-overview/11-03-PLAN.md

INTERFACES — exact analog signatures (11-RESEARCH Code Examples); use directly:

EXISTING handler template to mirror (apps/worker/src/handlers/investment-snapshot-daily.ts): a run* function + a register* function (boss.work(queue, async () => run(...))).

EXISTING per-tenant iteration (apps/worker/src/handlers/budgeting-reconciliation.ts):
const { value: budgets } = await withInfraTx(async (tx) => { const r = await (tx as any).execute(sql`SELECT DISTINCT id AS budget_id, tenant_id FROM tenancy.budgets`); return r.rows as {budget_id:string;tenant_id:string}[]; });
for (const b of budgets) { await withTenantTx(TenantId(b.tenant_id), UserId(SYSTEM_USER_ID), async (tx) => { /_ GUC app.tenant_ids set to [tenant_id] here _/ }); }
imports: { withInfraTx, withTenantTx } from "@budget/platform"; { TenantId, UserId } from "@budget/shared-kernel"; SYSTEM_USER_ID = "00000000-0000-0000-0000-000000000001".

EXISTING schedule lines (apps/worker/src/worker.ts):
await boss.createQueue("budget-wealth-snapshot-3h");
await boss.schedule("budget-wealth-snapshot-3h", "0 _/3 _ \* _", null, { tz: "Europe/Berlin" });
registerBudgetWealthSnapshot3h(boss, /_ deps for computeBudgetWealthNow: fxProvider, repos \*/);
(Place AFTER the fx/investment refresh schedules so prices are fresh — CONTEXT D-04 "runs after fx/investment refresh".)

INSERT (idempotent, 11-RESEARCH Code Examples):
INSERT INTO budgeting.budget_wealth_snapshots (tenant_id, budget_id, capitalization_cents, investment_value_cents, currency)
VALUES (${b.tenant_id}, ${b.budget_id}, ${now.capitalization_cents}, ${now.investment_value_cents}, ${now.currency})
ON CONFLICT (budget_id, date_trunc('hour', captured_at)) DO NOTHING;

computeBudgetWealthNow (11-03): computeBudgetWealthNow(deps)({budgetId, defaultCurrency, now}) → {capitalization_cents:bigint, investment_value_cents:bigint, currency}. The handler needs the budget's default_currency — read it alongside the budget scan (SELECT id, tenant_id, default_currency FROM tenancy.budgets).

Test infra: apps/worker/test/ integration with real Postgres (seed budgets + wallets + holdings; run the handler; assert rows).
</context>

<tasks>

<task type="auto">
  <name>Task 1: budget-wealth-snapshot-3h handler (scan → per-tenant compute → idempotent insert)</name>
  <read_first>
    - apps/worker/src/handlers/investment-snapshot-daily.ts (run/register shape)
    - apps/worker/src/handlers/budgeting-reconciliation.ts (withInfraTx scan → withTenantTx per budget + GUC)
    - packages/budgeting/src/application/compute-budget-wealth-now.ts (11-03 primitive signature)
    - packages/budgeting/src/adapters/persistence/budget-wealth-snapshots-schema.ts (11-01 columns)
  </read_first>
  <action>
    Create apps/worker/src/handlers/budget-wealth-snapshot-3h.ts:
    - export async function runBudgetWealthSnapshot3h(deps): withInfraTx scan SELECT id AS budget_id, tenant_id, default_currency FROM tenancy.budgets. For each budget: withTenantTx(TenantId(tenant_id), UserId(SYSTEM_USER_ID), async (tx) => { const now = await computeBudgetWealthNow(deps)({budgetId, defaultCurrency:default_currency, now:new Date()}); INSERT ... ON CONFLICT (budget_id, date_trunc('hour',captured_at)) DO NOTHING; }). Wrap per-budget in try/catch so one budget's failure doesn't abort the batch (log + continue), mirroring the daily handler.
    - export function registerBudgetWealthSnapshot3h(boss, deps) { boss.work("budget-wealth-snapshot-3h", async () => runBudgetWealthSnapshot3h(deps)); }
    - deps = whatever computeBudgetWealthNow needs (walletRepo, holdingsValuation, fxProvider) — construct in worker.ts the same way the API boot does.
  </action>
  <verify>
    <automated>cd /home/claude/budget && grep -q "withInfraTx" apps/worker/src/handlers/budget-wealth-snapshot-3h.ts && grep -q "withTenantTx" apps/worker/src/handlers/budget-wealth-snapshot-3h.ts && grep -q "computeBudgetWealthNow" apps/worker/src/handlers/budget-wealth-snapshot-3h.ts && grep -q "ON CONFLICT" apps/worker/src/handlers/budget-wealth-snapshot-3h.ts && bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - handler uses withInfraTx (scan) + withTenantTx (per-budget GUC) + computeBudgetWealthNow + ON CONFLICT DO NOTHING
    - exports runBudgetWealthSnapshot3h and registerBudgetWealthSnapshot3h
    - typecheck exits 0
  </acceptance_criteria>
  <done>Handler computes + inserts per budget under the right tenant GUC, idempotently.</done>
</task>

<task type="auto">
  <name>Task 2: register + schedule the queue in worker.ts (0 */3 * * *)</name>
  <read_first>
    - apps/worker/src/worker.ts (the investment-snapshot-daily createQueue+schedule+register block + where fxProvider/deps are built)
  </read_first>
  <action>
    In apps/worker/src/worker.ts, AFTER the fx + investment refresh schedules, add: await boss.createQueue("budget-wealth-snapshot-3h"); await boss.schedule("budget-wealth-snapshot-3h", "0 */3 * * *", null, { tz: "Europe/Berlin" }); registerBudgetWealthSnapshot3h(boss, deps); — building deps (walletRepo/holdingsValuation/fxProvider) the same way the existing handlers do. Import the register fn.
  </action>
  <verify>
    <automated>cd /home/claude/budget && grep -q '"budget-wealth-snapshot-3h", "0 \*/3 \* \* \*"' apps/worker/src/worker.ts && grep -q "registerBudgetWealthSnapshot3h" apps/worker/src/worker.ts && bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - worker.ts contains schedule("budget-wealth-snapshot-3h", "0 */3 * * *", ...)
    - worker.ts calls registerBudgetWealthSnapshot3h
    - typecheck exits 0
  </acceptance_criteria>
  <done>Queue created, scheduled every 3h, handler registered after price refresh.</done>
</task>

<task type="auto">
  <name>Task 3: real-DB integration test (one row/budget, default_ccy, idempotent, per-tenant)</name>
  <read_first>
    - apps/worker/test/ (an existing handler integration test with real Postgres + withTenantTx)
    - apps/worker/src/handlers/budget-wealth-snapshot-3h.ts (the run fn to invoke directly)
  </read_first>
  <action>
    Create apps/worker/test/handlers/budget-wealth-snapshot-3h.test.ts: seed 2 budgets (different default_currency) with wallets + holdings; call runBudgetWealthSnapshot3h(deps) directly (not via pg-boss); assert exactly one row per budget in its default_currency with capitalization_cents = Σ wallets+investments and investment_value_cents correct; call run again and assert NO duplicate row (ON CONFLICT idempotency within the same hour bucket); assert budget B's row is not visible/writable under budget A's tenant context (RLS). NO DB mocking.
  </action>
  <verify>
    <automated>cd /home/claude/budget && bun test apps/worker/test/handlers/budget-wealth-snapshot-3h.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test seeds ≥2 budgets, invokes the handler, asserts one row/budget in default_ccy
    - re-running the handler produces no duplicate (idempotency)
    - test passes against real Postgres
  </acceptance_criteria>
  <done>Integration test green: per-budget default-ccy snapshot, idempotent, tenant-scoped.</done>
</task>

</tasks>

<verification>
- bun run typecheck exits 0
- bun test apps/worker/test/handlers/budget-wealth-snapshot-3h.test.ts green (real DB)
- worker boots with the new schedule (manual/log check noted in VALIDATION manual section)
</verification>

<success_criteria>

- 3h cron snapshots every budget in default_ccy (D-04, SC8)
- Per-budget withTenantTx GUC (T-11-02); ON CONFLICT idempotent
- Values come from computeBudgetWealthNow (consistent with card + wealth live point)
  </success_criteria>

<output>
After completion, create `.planning/phases/11-budget-overview/11-07-SUMMARY.md`
</output>
