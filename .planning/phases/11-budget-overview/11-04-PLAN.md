---
phase: 11-budget-overview
plan: 04
type: tdd
wave: 1
depends_on: []
autonomous: true
requirements: [SC4, SC6, D-12, D-13, D-14, D-20, D-06]
files_modified:
  - packages/budgeting/src/application/get-overview-planned.ts
  - packages/budgeting/src/application/recurring-monthly-normalize.ts
  - packages/budgeting/src/adapters/persistence/overview-repo.ts
  - packages/budgeting/test/overview/get-overview-planned.test.ts
  - packages/budgeting/test/overview/recurring-monthly-normalize.test.ts
  - apps/api/src/routes/overview-planned.ts
  - apps/api/src/boot.ts
  - apps/api/test/routes/overview-planned.test.ts
must_haves:
  truths:
    - "Planned-vs-Real timeline returns one bucket per month in range, budget-wide OR per selected category (D-21 category selector); Real = confirmed transactions only (confirmed_at set), Planned = the SCD-2 limit active that month per mode (cushion_amount in CUSHION months via budget_mode_history, else normal_amount) (D-12)"
    - "Bucket granularity is monthly by default; for 'this month' or a short custom span (≤~62 days) the timeline returns a daily cumulative series instead (D-20)"
    - "Planned-average-vs-Real-average (Y=category) averages each metric over ONLY the months the category existed within the range (after creation, before archive) — not diluted by inactive months (D-13)"
    - "Archived categories still appear in past-period buckets where they had activity, but not after their archive month (D-06)"
    - "Recurring-per-month projects the CURRENT recurring_rules config (NOT range-scoped) as a 12-month distribution; recurring-per-category sums current planned recurring per category (D-14)"
    - "Recurring normalization handles ALL four cadences DAILY/WEEKLY/MONTHLY/YEARLY → a monthly figure (DAILY×30.44, WEEKLY×4.345, MONTHLY×1, YEARLY÷12) — the recurring_rules CHECK allows all four (Pitfall 2), not just MONTHLY/WEEKLY"
    - "The multi-month timeline is ONE aggregation query (confirmed spend grouped by category+month joined to the month-active limit), NOT N single-month service calls (Pitfall 5)"
    - "Range inputs are Zod-validated (from<=to, span cap) before SQL (T-11-03)"
  artifacts:
    - path: "packages/budgeting/src/application/get-overview-planned.ts"
      provides: "Planned section service (timeline + planned-avg + 2 recurring)"
      contains: "plannedVsReal"
    - path: "packages/budgeting/src/application/recurring-monthly-normalize.ts"
      provides: "4-cadence → monthly amount pure function"
      contains: "YEARLY"
    - path: "apps/api/src/routes/overview-planned.ts"
      provides: "GET /budgets/:id/overview/planned?from&to&categoryId"
      contains: "overview/planned"
  key_links:
    - from: "packages/budgeting/src/adapters/persistence/overview-repo.ts"
      to: "drizzle/0049 transactions_budget_cat_confirmed_idx (11-01)"
      via: "the monthly-bucket confirmed-spend rollup uses this index"
      pattern: "confirmed_at"
threat_model:
  - id: T-11-03
    stride: Tampering / SQL Injection
    surface: from/to/categoryId query params on /overview/planned
    mitigation: "Zod schema validates from<=to, caps the span, and (if categoryId) verifies it belongs to the budget; all values bound as parameters in Drizzle sql`` — no string interpolation into SQL."
  - id: T-11-04
    stride: Denial of Service
    surface: unbounded 'all' / wide custom range monthly aggregation
    mitigation: "monthly-bucket aggregation backed by transactions_budget_cat_confirmed_idx (budget_id,category_id,confirmed_at); span cap on custom ranges; the section is collapsed by default (D-21) so the query fires only on open."
---

<objective>
Build the Planned section data service (D-12/13/14/20): a multi-month Planned-vs-Real timeline (budget-wide or per-category), planned-average-vs-real-average per category over active months, and the two current-config recurring charts (per-month distribution + per-category totals). All in `default_currency`, range-scoped except the recurring charts.

Purpose: TDD fits — this is the densest money/temporal logic in the phase (SCD-2 limit-active-per-month, confirmed-only real, average-over-active-months, 4-cadence normalization, adaptive bucketing). Each rule has an exact expected output; writing them first prevents silent off-by-one-month and cadence-drop bugs. The recurring normalizer is a pure function tested in isolation.
Output: `get-overview-planned.ts` + `recurring-monthly-normalize.ts` + `overview-repo.ts` multi-month query + `overview/planned` route + integration test, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-budget-overview/11-CONTEXT.md
@.planning/phases/11-budget-overview/11-RESEARCH.md

INTERFACES — exact analog signatures (11-RESEARCH); use directly:

EXISTING per-month planned-vs-real math to replicate across months (get-spendings-summary.ts):
per category per month: activeBudget = cushionModeEnabled(that month) ? cushion_amount : normal_amount; spent = Σ confirmed transactions in that month; the SCD-2 limit "active that month" = the category_limits row whose [effective_from,effective_to) contains the month. Mode per month from budget_mode_history (NORMAL|CUSHION effective_from/to).
⚠ get-spendings-summary is SINGLE month — do NOT loop it N times. Build a multi-month SQL aggregation in overview-repo.ts.

SCD-2 sources (11-RESEARCH): category_limits (normal_amount/cushion_amount bigint, effective_from/to), budget_mode_history (mode, effective_from/to), categories.archived_at + created_at (D-06/D-13 active-window).

recurring_rules (11-RESEARCH Pitfall 2): columns amount numeric, currency char(3), category_id, cadence CHECK IN ('DAILY','WEEKLY','MONTHLY','YEARLY'), weekly_dow, yearly_month, active. Only active=true rules count. Convert amount→cents (×100) and FX→default_ccy.

recurring-monthly-normalize (pure): monthlyCents(amountCents:bigint, cadence) = DAILY→round(amountCents×30.44), WEEKLY→round(amountCents×4.345), MONTHLY→amountCents, YEARLY→round(amountCents÷12). Use a fixed DAYS_PER_MONTH=30.44, WEEKS_PER_MONTH=4.345 (document constants). // ponytail: average-month constants; exact calendar projection not needed for a distribution chart.

Adaptive bucketing (D-20): rangeBuckets(from,to): if same calendar month OR (to−from) ≤ 62 days → 'daily' cumulative; else 'monthly'. The timeline returns {bucket:'monthly'|'daily', points:[{label, planned_cents, real_cents}]}.

FX: FxProvider.rateAsOf(from,to,date) per 11-RESEARCH; convert limits + spend + recurring amounts to default_ccy.

DTO (route response; *\_cents strings at boundary):
OverviewPlannedDTO = {
currency:string, bucket:'monthly'|'daily',
timeline:{ label:string, planned_cents:string, real_cents:string }[],
plannedAvgVsReal:{ category_id:string, name:string, planned_avg_cents:string, real_avg_cents:string }[],
recurringPerMonth:{ month:number /*1-12\*/, planned_cents:string }[],
recurringPerCategory:{ category_id:string, name:string, planned_cents:string }[] }

Tx + DI + route: withTenantTxRead; boot.ts closure DI; X-Budget-ID; bigint→string at route. Range parsed from ?from=YYYY-MM-DD&to=YYYY-MM-DD&categoryId=uuid? (preset→from/to resolved client-side or accept ?preset=). Zod-validate.

Test infra: bun:test for the service + the pure normalizer; real Postgres for the route integration test. BDD names.
</context>

<feature>
  <name>Planned section service (timeline + planned-avg + recurring×2)</name>
  <files>get-overview-planned.ts, recurring-monthly-normalize.ts, overview-repo.ts (+ tests), overview-planned.ts route</files>
  <behavior>
    Cases:
    - recurring-monthly-normalize: 100_00 DAILY→304_400; 100_00 WEEKLY→43_450; 100_00 MONTHLY→100_00; 1200_00 YEARLY→100_00.
    - timeline monthly: 3-month range, category limit 200_00 normal all months, one CUSHION month with cushion 150_00 → planned=[200,200,150]; confirmed spend [180,210,140] → real=[180,210,140]; a PENDING draft is excluded from real (D-12).
    - timeline daily: range = this month → bucket='daily', cumulative real per day.
    - planned-avg: category created mid-range (exists 2 of 3 months) → averages over the 2 active months only (D-13), not /3.
    - archived: category archived in month 2 → present in months 1-2 buckets, absent in month 3 (D-06).
    - recurringPerMonth: a YEARLY rule with yearly_month=6 contributes to month 6; MONTHLY rule contributes 1/12 of the year evenly OR per-month — define: per-month distribution = each rule's monthly-normalized amount placed in the months it actually fires (MONTHLY→all 12; YEARLY→its yearly_month; WEEKLY/DAILY→all 12). Document the chosen distribution in code.
  </behavior>
  <implementation>overview-repo.monthlySpendByCategory(budgetId,from,to) (confirmed-only, grouped category+month) + monthlyActiveLimit(...) join over SCD-2 + mode; service assembles timeline/avg; recurring services read recurring_rules (active) + normalize.</implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1 — RED: failing tests for recurring-monthly-normalize (pure) + get-overview-planned</name>
  <read_first>
    - packages/budgeting/test/ (bun:test harness for a service with fake repos)
    - packages/budgeting/src/application/get-spendings-summary.ts (the per-month formula to generalize)
    - 11-RESEARCH.md (Pitfall 2 cadences, D-12/13/14/20) + this plan's <feature> cases
  </read_first>
  <action>
    Create packages/budgeting/test/overview/recurring-monthly-normalize.test.ts (pure-function cases for all 4 cadences + rounding) and get-overview-planned.test.ts (timeline monthly+daily, planned-avg over active months, archived-in-history, recurring distribution) with fake overview-repo + fake recurring repo + fake fxProvider. Import from the not-yet-existing modules → RED.
  </action>
  <verify>
    <automated>cd /home/claude/budget && bun test packages/budgeting/test/overview/recurring-monthly-normalize.test.ts packages/budgeting/test/overview/get-overview-planned.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - both test files exist; reference recurringMonthlyNormalize and getOverviewPlanned
    - the normalize test covers DAILY, WEEKLY, MONTHLY, YEARLY
    - test run FAILS (RED)
  </acceptance_criteria>
  <done>Failing tests committed (test(11-04): planned section + recurring normalize).</done>
</task>

<task type="auto">
  <name>Task 2 — GREEN: implement normalizer + overview-repo multi-month query + planned service</name>
  <read_first>
    - packages/budgeting/src/adapters/persistence/budget-home-summary-repo.ts + spendings-summary-repo (Drizzle sql`` aggregation + tenant tx shape)
    - packages/budgeting/src/adapters/persistence/category-limits-schema.ts + budget-mode-history-schema.ts + recurring-rules-schema.ts (column names for the joins)
    - 11-RESEARCH.md INTERFACES (monthlySpendByCategory + monthlyActiveLimit + normalize constants)
  </read_first>
  <action>
    1. recurring-monthly-normalize.ts — export function recurringMonthlyNormalize(amountCents:bigint, cadence:'DAILY'|'WEEKLY'|'MONTHLY'|'YEARLY'):bigint per the constants; export DAYS_PER_MONTH/WEEKS_PER_MONTH.
    2. overview-repo.ts — add:
       - monthlySpendByCategory(budgetId,from,to): {category_id,month:string,spent_cents:bigint}[] — SUM of CONFIRMED transactions (confirmed_at IS NOT NULL) grouped by category_id, date_trunc('month',confirmed_at); uses transactions_budget_cat_confirmed_idx.
       - monthlyActiveLimit(budgetId,from,to): for each (category,month) the SCD-2 category_limits row active that month + the mode (budget_mode_history) → planned_cents (cushion vs normal). 
       - categoryActiveWindows(budgetId): {category_id,name,created_at,archived_at}[] for D-06/D-13.
       - activeRecurringRules(budgetId): {category_id,name,amount_cents:bigint,currency,cadence,yearly_month}[] WHERE active.
    3. get-overview-planned.ts — getOverviewPlanned(deps)({tenantId,budgetId,from,to,categoryId?,now}) → Result<OverviewPlannedDTO>: choose bucket (D-20); assemble timeline (planned=active limit FX→default, real=confirmed spend FX→default; if categoryId filter to it, else sum across non-archived-in-month categories); plannedAvgVsReal averaging over active months only (D-13); recurringPerMonth + recurringPerCategory via normalize + distribution. Keep cents bigint.
    Run tests — MUST pass.
  </action>
  <verify>
    <automated>cd /home/claude/budget && bun test packages/budgeting/test/overview/recurring-monthly-normalize.test.ts packages/budgeting/test/overview/get-overview-planned.test.ts 2>&1 | tail -15 && bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - recurring-monthly-normalize.ts handles all four cadences (grep DAILY,WEEKLY,MONTHLY,YEARLY)
    - overview-repo.ts monthlySpendByCategory filters confirmed_at IS NOT NULL (D-12)
    - get-overview-planned.ts averages planned/real over active months only (D-13) and selects bucket monthly/daily (D-20)
    - all planned + normalize unit tests pass; typecheck exits 0
  </acceptance_criteria>
  <done>Normalizer + multi-month query + planned service implemented; unit tests green (feat(11-04): implement planned section).</done>
</task>

<task type="auto">
  <name>Task 3: route GET /budgets/:id/overview/planned (Zod range) + boot wiring + real-DB integration test</name>
  <read_first>
    - apps/api/src/routes/spendings-summary.ts (route + month parse) and an existing Zod-validated query route
    - apps/api/src/boot.ts (closure DI)
    - apps/api/test/routes/ (real-Postgres integration harness)
  </read_first>
  <action>
    1. overview-planned.ts route — GET; Zod-parse ?from,?to (YYYY-MM-DD, from<=to, span cap e.g. ≤ 5 years), ?categoryId optional uuid (verify belongs to budget); call deps.budgeting.getOverviewPlanned; stringify *_cents; c.json. On bad range → 400.
    2. boot.ts — wire getOverviewPlanned({ overviewRepo, fxProvider }) + register route.
    3. apps/api/test/routes/overview-planned.test.ts — real Postgres: seed budget + 2 categories (one archived mid-range) + SCD-2 limits + a CUSHION mode window + confirmed + pending transactions across 3 months + an active recurring rule of each cadence; GET with a 3-month range; assert bucket='monthly', timeline planned/real, planned-avg over active months, recurringPerMonth includes the YEARLY rule in its month, pending excluded. Also GET this-month → bucket='daily'. NO DB mocking.
  </action>
  <verify>
    <automated>cd /home/claude/budget && bun test apps/api/test/routes/overview-planned.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - route Zod-validates the range and rejects from>to with 400
    - boot.ts wires getOverviewPlanned + registers the route
    - integration test passes (monthly + daily bucket, confirmed-only real, planned-avg active-months, recurring distribution) against real Postgres
  </acceptance_criteria>
  <done>Route + DI + integration test green; planned endpoint serves the full section DTO.</done>
</task>

</tasks>

<verification>
- bun test packages/budgeting/test/overview/ (planned + normalize) green
- bun test apps/api/test/routes/overview-planned.test.ts green (real DB, both bucket modes)
- bun run typecheck exits 0
</verification>

<success_criteria>

- Multi-month planned-vs-real (confirmed-only, mode-active limit, D-12) budget-wide + per-category; adaptive bucket (D-20)
- Planned-avg over active months only (D-13); archived in-history not current (D-06)
- Recurring per-month + per-category from current config (D-14), all 4 cadences normalized (Pitfall 2)
- ONE aggregation query, not N calls (Pitfall 5); range Zod-validated (T-11-03)
- RED→GREEN commits present
  </success_criteria>

<output>
After completion, create `.planning/phases/11-budget-overview/11-04-SUMMARY.md` with RED/GREEN notes + commits.
</output>
