---
phase: 11-budget-overview
plan: 10
type: execute
wave: 4
depends_on: ["11-08", "11-09"]
autonomous: true
requirements: [SC9]
files_modified:
  - apps/web/messages/pl.json
  - apps/web/messages/uk.json
  - apps/web/messages/en.json
  - apps/web/e2e/features/overview.feature
  - apps/web/e2e/steps/overview.steps.ts
  - apps/web/e2e/pages/overview.page.ts
  - apps/web/test/i18n/overview-keys.test.ts
must_haves:
  truths:
    - "Every overview i18n key present in en.json also exists in pl.json and uk.json (no missing-translation fallback) — verified by a key-parity test (SC9)"
    - "All overview UI strings are translated EN/PL/UK (cards, section titles, range presets, wealth toggle, stat labels, empty states) (SC9)"
    - "The overview.feature Gherkin suite (@overview) covers the golden render + range switch + category selector + wealth toggle + pie interaction, using Page Objects + the fresh-user-per-scenario fixture (SC9, CLAUDE.md E2E rule)"
    - "The E2E suite runs against the canonical test host and is wired into the blocking E2E job (never continue-on-error)"
  artifacts:
    - path: "apps/web/messages/pl.json"
      provides: "PL overview translations"
      contains: "overview"
    - path: "apps/web/e2e/features/overview.feature"
      provides: "full Gherkin scenarios for the overview tab"
      contains: "Wealth"
  key_links:
    - from: "apps/web/e2e/steps/overview.steps.ts"
      to: "apps/web/e2e/pages/overview.page.ts"
      via: "step definitions drive the page object"
      pattern: "OverviewPage"
threat_model: []
---

<objective>
Finish localization (PL + UK to match EN) and the E2E Gherkin coverage for the Overview tab (SC9): golden render + range switch + category selector + wealth toggle + pie interaction, via playwright-bdd Page Objects and the fresh-user fixture. Add a key-parity test so a missing translation fails CI.

Purpose: SC9's localization + E2E contract. The strings were added EN-first across 11-08/09; this plan mirrors them to PL/UK, proves parity with a test, and completes the .feature suite (the golden scenario was stubbed in 11-08). E2E must block CI (project rule — no continue-on-error).
Output: PL/UK translations at parity, key-parity test, complete @overview feature + steps + page object, green E2E run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-budget-overview/11-CONTEXT.md
@.planning/phases/11-budget-overview/11-RESEARCH.md
@.planning/phases/11-budget-overview/11-08-PLAN.md
@.planning/phases/11-budget-overview/11-09-PLAN.md
@.planning/phases/11-budget-overview/11-UI-SPEC.md

INTERFACES — exact analog signatures; use directly:

i18n files (apps/web/messages/{en,pl,uk}.json): EN already has bdp.tab.overview._ (cards + sections + range + wealth) from 11-08/09. Mirror EVERY key into pl.json and uk.json with translated copy, same nested shape. Use the existing translations in these files as the tone/terminology reference (e.g. existing bdp.tab.reserves._ PL/UK wording for financial terms).

Key-parity test (apps/web/test/i18n/ — see icu-placeholder-escaping.test.ts for the load pattern): load en/pl/uk JSON, collect all dotted key paths under bdp.tab.overview, assert pl and uk contain the exact same set (no missing, no extra). Vitest.

E2E (playwright-bdd; apps/web/e2e/{features,steps,pages}; see bdp-tab-frame.feature + its page object + the fresh-user Background "Given I am signed in as a fresh user"; tag convention @<feature> e.g. @tasks-redesign). Canonical base URL = https://budget-dev.madonzy.com (PLAYWRIGHT_BASE_URL). Config source of truth = apps/web/playwright.config.ts. Fresh-user-per-scenario fixture seeds a budget; seed steps that need DATABASE_URL_APP run in the CI step env.

Scenarios to cover (SC9): golden render (5 cards + first pill), range switch (this month → last 3 months updates the range-scoped charts), category selector (none → a category re-scopes the planned timeline), wealth toggle (capitalization → investments reveals the pie), pie interaction (tap a slice → share % + type label visible).
</context>

<tasks>

<task type="auto">
  <name>Task 1: PL + UK translations at parity + key-parity test</name>
  <read_first>
    - apps/web/messages/en.json (the bdp.tab.overview.* subtree added in 11-08/09 — the source of truth for keys)
    - apps/web/messages/pl.json + uk.json (existing bdp.tab.* wording for terminology consistency)
    - apps/web/test/i18n/icu-placeholder-escaping.test.ts (JSON load + assert pattern)
  </read_first>
  <action>
    1. Mirror the entire bdp.tab.overview.* subtree from en.json into pl.json and uk.json with proper Polish + Ukrainian translations (cards labels, section titles, range presets, wealth toggle/stat labels, empty states, real-months/percent labels). Match existing financial terminology in those files.
    2. apps/web/test/i18n/overview-keys.test.ts — Vitest: import the three JSON files; build the set of dotted paths under bdp.tab.overview for each; assert pl set === en set and uk set === en set (symmetric difference empty). Fail with the missing keys listed.
  </action>
  <verify>
    <automated>cd /home/claude/budget/apps/web && bun run test -- overview-keys.test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - pl.json and uk.json contain a bdp.tab.overview subtree
    - overview-keys.test.ts passes (EN/PL/UK key sets identical under bdp.tab.overview)
  </acceptance_criteria>
  <done>PL/UK at full key parity with EN; parity test green.</done>
</task>

<task type="auto">
  <name>Task 2: complete overview.feature scenarios + page object + steps</name>
  <read_first>
    - apps/web/e2e/features/overview.feature (the @overview golden stub from 11-08)
    - apps/web/e2e/features/bdp-tab-frame.feature + its page object + steps (pill navigation, fresh-user Background, selectors)
    - apps/web/src/components/budgeting/overview/* (data-testid / roles to target: cards, section toggles, range selector, category selector, wealth toggle, pie slices)
  </read_first>
  <action>
    1. overview.page.ts — OverviewPage page object: openOverview(budgetName), pill/first-pill getters, card getters (5), expandSection(name), selectRange(preset), selectCategory(name), toggleWealthView(view), tapPieSlice(index), and assertion helpers (cardsVisible, chartVisibleIn(section), pieShareLabelVisible). Add stable data-testid attributes to the overview components if missing (small edits to 11-08/09 components are allowed here for testability).
    2. overview.feature — extend the @overview Feature with scenarios: "shows five summary cards" (golden), "switching range updates range-scoped charts", "selecting a category re-scopes the planned timeline", "wealth toggle to investments reveals the per-type pie", "tapping a pie slice reveals share % and type label". Each uses the fresh-user Background.
    3. overview.steps.ts — step definitions binding the Gherkin to OverviewPage.
  </action>
  <verify>
    <automated>cd /home/claude/budget && grep -q "Wealth\|wealth" apps/web/e2e/features/overview.feature && grep -c "Scenario" apps/web/e2e/features/overview.feature && test -f apps/web/e2e/pages/overview.page.ts && test -f apps/web/e2e/steps/overview.steps.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - overview.feature has ≥5 scenarios (golden, range, category, wealth toggle, pie) all under @overview with the fresh-user Background
    - overview.page.ts + overview.steps.ts exist and bind the scenarios
  </acceptance_criteria>
  <done>Full Gherkin suite + page object + steps authored.</done>
</task>

<task type="auto">
  <name>Task 3: run the E2E suite green against the dev host (blocking)</name>
  <read_first>
    - apps/web/playwright.config.ts (base URL, projects, BDD config — source of truth)
    - project memory: canonical PLAYWRIGHT_BASE_URL=https://budget-dev.madonzy.com; rebuild web after FE edits before E2E; CI E2E must block (no continue-on-error)
    - Makefile (test-e2e target)
  </read_first>
  <action>
    Rebuild the web image so the new overview UI + strings are served (FE bundles at build time): `docker compose build web && make restart-web`. Then run the overview suite: `PLAYWRIGHT_BASE_URL=https://budget-dev.madonzy.com make test-e2e` filtered to @overview (or the bdd grep equivalent). Fix selectors/timing until green. Confirm the E2E job is NOT continue-on-error (if a CI workflow references overview, ensure it stays blocking). Do NOT mark done on red E2E.
  </action>
  <verify>
    <automated>cd /home/claude/budget && docker compose build web >/dev/null 2>&1 && make restart-web >/dev/null 2>&1; cd apps/web && PLAYWRIGHT_BASE_URL=https://budget-dev.madonzy.com bunx playwright test --grep @overview 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - the @overview Playwright-BDD scenarios pass against budget-dev.madonzy.com
    - no continue-on-error introduced for the overview E2E
  </acceptance_criteria>
  <done>Overview E2E suite green on the dev host; localization + flows verified end to end.</done>
</task>

</tasks>

<verification>
- overview-keys.test.ts passes (EN/PL/UK parity)
- overview.feature ≥5 @overview scenarios green against budget-dev.madonzy.com
- web image rebuilt + restarted so served bundle includes the new tab
</verification>

<success_criteria>

- All overview strings EN/PL/UK at key parity (SC9), enforced by a test
- E2E Gherkin covers golden render + range switch + category selector + wealth toggle + pie interaction (SC9)
- E2E blocking (project rule); verified on the canonical dev host
  </success_criteria>

<output>
After completion, create `.planning/phases/11-budget-overview/11-10-SUMMARY.md`
</output>
