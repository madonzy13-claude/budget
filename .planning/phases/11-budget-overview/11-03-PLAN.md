---
phase: 11-budget-overview
plan: 03
type: tdd
wave: 1
depends_on: []
autonomous: true
requirements: [SC2, SC6, D-07, D-08, D-09, D-10, D-11]
files_modified:
  - packages/budgeting/src/application/compute-budget-wealth-now.ts
  - packages/budgeting/src/application/get-overview-cards.ts
  - packages/budgeting/src/adapters/persistence/overview-cards-repo.ts
  - packages/budgeting/test/overview/compute-budget-wealth-now.test.ts
  - packages/budgeting/test/overview/get-overview-cards.test.ts
  - apps/api/src/routes/overview-cards.ts
  - apps/api/src/boot.ts
  - apps/api/test/routes/overview-cards.test.ts
must_haves:
  truths:
    - "computeBudgetWealthNow(budgetId) returns {capitalization_cents, investment_value_cents, currency} in the budget default_currency (D-07, D-11), reused by the wealth live-point (11-06) and the 3h cron (11-07)"
    - "capitalization_cents = Σ ALL wallet balances (FX→default_ccy) + investment_value_cents (D-07)"
    - "investment_value_cents = Σ(quantity × current_price, FX→default_ccy) over non-archived holdings, reusing portfolio-metrics (D-07)"
    - "available_to_spend = Σ SPENDINGS wallet balances; available_reserves = Σ RESERVE wallet balances, both default_ccy (D-09)"
    - "cushion card = {total_cents = actual_cents, real_months = actual_cents ÷ (required_cents ÷ target_months)} from get-cushion-summary, no new cushion math (D-08)"
    - "this-month overspent card = after-reserves max(0, spent − active_limit − reserve_used) top-N + count, matching the spendings grid (D-10)"
    - "all card amounts are in default_currency, NOT display_currency — Overview intentionally differs from home-summary (D-11)"
    - "archived categories are excluded from the current-month overspent card (D-06)"
    - "every amount is bigint through the service; .toString() only at the route boundary"
  artifacts:
    - path: "packages/budgeting/src/application/compute-budget-wealth-now.ts"
      provides: "shared wealth-now primitive (capitalization + investment value, default_ccy)"
      contains: "investment_value_cents"
    - path: "packages/budgeting/src/application/get-overview-cards.ts"
      provides: "5-card overview summary service (default_ccy)"
      contains: "available_to_spend"
    - path: "apps/api/src/routes/overview-cards.ts"
      provides: "GET /budgets/:id/overview/cards"
      contains: "overview/cards"
  key_links:
    - from: "packages/budgeting/src/application/compute-budget-wealth-now.ts"
      to: "apps/worker/src/handlers/budget-wealth-snapshot-3h.ts (11-07)"
      via: "the cron snapshots exactly this primitive's output per budget"
      pattern: "computeBudgetWealthNow"
    - from: "packages/budgeting/src/application/compute-budget-wealth-now.ts"
      to: "packages/budgeting/src/application/get-overview-wealth.ts (11-06)"
      via: "live current point = computeBudgetWealthNow appended to the snapshot series"
      pattern: "computeBudgetWealthNow"
threat_model:
  - id: T-11-05
    stride: Elevation of Privilege / IDOR
    surface: GET /budgets/:id/overview/cards
    mitigation: "Route enforces X-Budget-ID + existing membership middleware; service runs under withTenantTxRead(TenantId(budgetId)) so RLS scopes every wallet/holding/category read to the tenant. tenantId === budgetId invariant. No cross-budget read path."
---

<objective>
Build the five-card Overview summary as a compute-on-read service in `default_currency` (D-11), plus the shared `computeBudgetWealthNow` primitive that the wealth series (11-06) and the 3h cron (11-07) reuse. Cards: available-to-spend (Σ SPENDINGS), capitalization (Σ all wallets + investments), this-month overspent (after-reserves top-N), cushion (real months + total), available reserves (Σ RESERVE).

Purpose: TDD fits — these are pure functions over wallets + holdings + limits with exact, testable money outputs (D-07/08/09/10). The hard part is correctness: default_ccy (NOT display_ccy), wallet_type partitioning, investment valuation reuse, and the cushion real-months formula. Writing the expectations first locks the math before wiring. `computeBudgetWealthNow` is authored ONCE here because three consumers need identical numbers.
Output: `compute-budget-wealth-now.ts` + `get-overview-cards.ts` + a thin `overview/cards` route + integration test, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-budget-overview/11-CONTEXT.md
@.planning/phases/11-budget-overview/11-RESEARCH.md

INTERFACES — exact analog signatures (11-RESEARCH "Concrete Interface Signatures"); use directly:

EXISTING get-budget-home-summary.ts (the cards analog — but converts to display_currency; DO NOT reuse its conversion target):
summaryRepo.getBudgetMeta(budgetId) → { default_currency, ... }; sumCurrentMonthSpend(budgetId,monthStart,monthEnd):bigint; listWalletsForBudget(budgetId): {amount_cents:bigint,currency}[] ⚠ NO wallet_type; topOverspentCategories(budgetId,monthStart,monthEnd,useCushion,limit): {category_id,category_name,over_amount_cents:bigint}[]
FX pattern: collect distinct (from→default_ccy) pairs → Promise.all rateAsOf → Money.mul(rate) → sum bigint.

EXTEND the wallet read: add listWalletsWithType(budgetId): {amount_cents:bigint,currency,wallet_type:'SPENDINGS'|'CUSHION'|'RESERVE'}[] in overview-cards-repo.ts (one extra column over listWalletsForBudget). Needed to partition Σ SPENDINGS / Σ RESERVE / Σ ALL.

EXISTING get-cushion-summary.ts (D-08): getCushionSummary({fxProvider})({tenantId,budgetId}) → {required_cents,actual_cents,shortfall_cents,currency,enabled,target_months:number}. real_months = target_months<=0||required_cents===0n ? 0 : Number(actual_cents) ÷ (Number(required_cents) ÷ target_months). total_cents = actual_cents.

EXISTING investment valuation reuse (D-07): packages/investments/src/application/list-holdings.ts + domain/portfolio-metrics.ts. Holdings: {quantity:numeric(28,8), current_price_cents:bigint, current_price_currency:char(3), holding_type, archived_at}. investment_value_cents = Σ over archived_at IS NULL of (quantity × current_price_cents) FX-converted current_price_currency→default_ccy. Confirm the exact portfolio-metrics export in read_first; if it already yields a per-holding cents value, reuse it; else compute inline with Money + fxProvider.

EXISTING FxProvider port: rateAsOf(from,to,date) → {rate:string,...}; rateAsOf(X,X,\_)→"1". Parallelise distinct pairs.

EXISTING tx + DI: withTenantTxRead(TenantId(budgetId), UserId(SYSTEM_USER_ID), cb) for reads; services wired in apps/api/src/boot.ts via closure factory; route reads X-Budget-ID; bigint→string via .toString() at the route only.

DTO contract (route response, all \*\_cents are strings at the boundary):
OverviewCardsDTO = { default_currency:string, available_to_spend_cents:string, capitalization_cents:string, investment_value_cents:string, available_reserves_cents:string, cushion:{ enabled:boolean, real_months:number, total_cents:string }, overspent:{ count:number, currency:string, top:{category_id:string,name:string,over_amount_cents:string}[] } }

Test infra: bun:test (packages/budgeting/test/), real Postgres for the route integration test (apps/api/test/routes/, NO DB mocking — project rule). BDD names: describe('Overview cards') > test('available-to-spend sums only SPENDINGS wallets in default_currency').
</context>

<feature>
  <name>Overview cards service + computeBudgetWealthNow primitive (default_currency)</name>
  <files>compute-budget-wealth-now.ts, get-overview-cards.ts, overview-cards-repo.ts (+ tests), overview-cards.ts route</files>
  <behavior>
    Cases (input → expected):
    - Wallets [SPENDINGS 100 USD, RESERVE 50 USD, CUSHION 30 USD] budget default USD, no holdings → available_to_spend=100_00, available_reserves=50_00, capitalization=180_00, investment_value=0.
    - Mixed currency: SPENDINGS 100 EUR with rateAsOf(EUR,USD)=1.1 → available_to_spend=110_00 (FX→default_ccy, D-11).
    - Holdings [2 × 50_00 USD] → investment_value=100_00; capitalization += 100_00.
    - Cushion required=600_00, actual=300_00, target_months=6 → real_months = 300/(600/6)=300/100=3.0; total_cents=300_00.
    - Overspent: category over by 20_00 after reserve_used, another exactly at limit → count=1, top=[that category]; an ARCHIVED category that overspent this month is EXCLUDED (D-06).
    - default_currency reflects budget meta, not user display_currency (D-11).
  </behavior>
  <implementation>computeBudgetWealthNow = partition listWalletsWithType, FX each to default_ccy, sum ALL → cap base; add investment_value from holdings valuation. getOverviewCards composes it with cushion-summary + topOverspentCategories(useCushion=mode) + SPENDINGS/RESERVE partial sums.</implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1 — RED: failing unit tests for computeBudgetWealthNow + getOverviewCards</name>
  <read_first>
    - packages/budgeting/test/ (an existing bun:test for an application service — copy harness: in-memory/fake repos, Result assertions)
    - packages/budgeting/src/application/get-budget-home-summary.ts (deps + FX-sum pattern to mirror)
    - packages/budgeting/src/application/get-cushion-summary.ts (cushion DTO fields for the real_months case)
    - 11-RESEARCH.md Concrete Interface Signatures + this plan's <feature> cases
  </read_first>
  <action>
    Create packages/budgeting/test/overview/compute-budget-wealth-now.test.ts and get-overview-cards.test.ts. Define the function signatures by importing from the (not-yet-existing) modules so the tests fail to compile/run (RED). Write the <feature> cases with fake repos: a fake walletRepo.listWalletsWithType, fake holdings valuation, fake fxProvider (rateAsOf returns a fixture map incl. X→X="1"), fake cushion + overspent repos. Assert exact bigint/string outputs and real_months number. Use BDD describe/test names. Run — MUST fail (modules absent).
  </action>
  <verify>
    <automated>cd /home/claude/budget && bun test packages/budgeting/test/overview/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - both test files exist under packages/budgeting/test/overview/
    - tests reference computeBudgetWealthNow and getOverviewCards
    - test run FAILS (RED — implementation modules do not exist yet)
  </acceptance_criteria>
  <done>Failing unit tests committed (test(11-03): add failing overview-cards tests).</done>
</task>

<task type="auto">
  <name>Task 2 — GREEN: implement repo + primitive + cards service to pass</name>
  <read_first>
    - packages/budgeting/src/adapters/persistence/budget-home-summary-repo.ts (listWalletsForBudget query — copy and add wallet_type column for listWalletsWithType)
    - packages/investments/src/application/list-holdings.ts + packages/investments/src/domain/portfolio-metrics.ts (valuation reuse — exact export)
    - packages/budgeting/src/application/get-cushion-summary.ts + get-budget-home-summary.ts (compose)
    - packages/shared-kernel/src/ports/fx-provider.ts (rateAsOf)
  </read_first>
  <action>
    1. overview-cards-repo.ts — add listWalletsWithType(budgetId): {amount_cents:bigint,currency,wallet_type}[] (SELECT current_balance→cents, currency, wallet_type FROM budgeting.wallets WHERE budget_id=$1 AND archived_at IS NULL). Reuse the existing wallet→cents conversion from budget-home-summary-repo.
    2. compute-budget-wealth-now.ts — export function computeBudgetWealthNow(deps:{ walletRepo, holdingsValuation, fxProvider }) returning async (input:{budgetId, defaultCurrency, now:Date}) => {capitalization_cents:bigint, investment_value_cents:bigint, currency:string}. FX each wallet + each holding value to defaultCurrency (parallel distinct pairs), sum bigint. investment_value from holdings (archived_at IS NULL).
    3. get-overview-cards.ts — export function getOverviewCards(deps) returning async ({tenantId,budgetId,now}) => Result<OverviewCardsDTO,Error>: meta.default_currency; partition listWalletsWithType → Σ SPENDINGS, Σ RESERVE; capitalization+investment from computeBudgetWealthNow; cushion via getCushionSummary → real_months formula; overspent via topOverspentCategories(useCushion=mode-active) filtered to non-archived + count. Keep cents bigint; DTO uses bigint internally, route stringifies.
    Run tests — MUST pass.
  </action>
  <verify>
    <automated>cd /home/claude/budget && bun test packages/budgeting/test/overview/ 2>&1 | tail -15 && bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - compute-budget-wealth-now.ts exports computeBudgetWealthNow and computes investment_value_cents from non-archived holdings
    - get-overview-cards.ts converts to default_currency (grep shows default_currency, NOT display_currency)
    - all packages/budgeting/test/overview/ tests pass
    - typecheck exits 0
  </acceptance_criteria>
  <done>Primitive + cards service implemented; unit tests green (feat(11-03): implement overview cards + wealth-now).</done>
</task>

<task type="auto">
  <name>Task 3: route GET /budgets/:id/overview/cards + boot wiring + real-DB integration test</name>
  <read_first>
    - apps/api/src/routes/spendings-summary.ts (route shape: X-Budget-ID, c.json, bigint→string)
    - apps/api/src/boot.ts (closure DI — where getSpendingsSummary etc. are wired; add getOverviewCards)
    - apps/api/test/routes/ (an existing real-Postgres integration test — seed budget+wallets+holdings, assert response)
  </read_first>
  <action>
    1. overview-cards.ts route — GET under the budgets router: read budgetId (X-Budget-ID), call deps.budgeting.getOverviewCards({tenantId:budgetId,budgetId,now:new Date()}); on ok, c.json(stringifyCents(dto)) where every *_cents bigint → .toString(); on err, map to the standard error response.
    2. boot.ts — instantiate getOverviewCards({ walletRepo: overviewCardsRepo, holdingsValuation, fxProvider, cushion: getCushionSummary(...), overspentRepo: summaryRepo }) and expose on deps.budgeting; register the route.
    3. apps/api/test/routes/overview-cards.test.ts — real Postgres: seed a budget (default USD), SPENDINGS+RESERVE+CUSHION wallets, one holding, cushion config; GET; assert default_currency=USD, available_to_spend/available_reserves/capitalization/investment_value strings, cushion.real_months number, overspent shape. NO DB mocking.
  </action>
  <verify>
    <automated>cd /home/claude/budget && bun test apps/api/test/routes/overview-cards.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - overview-cards.ts route returns *_cents as strings (grep .toString in the route)
    - boot.ts wires getOverviewCards and registers the route
    - apps/api/test/routes/overview-cards.test.ts passes against real Postgres
  </acceptance_criteria>
  <done>Route + DI + integration test green; cards endpoint serves default_ccy DTO.</done>
</task>

<task type="auto">
  <name>Task 4 — REFACTOR (if needed): extract shared FX-sum helper</name>
  <read_first>
    - packages/budgeting/src/application/compute-budget-wealth-now.ts + get-overview-cards.ts (duplication check vs get-budget-home-summary FX-sum)
  </read_first>
  <action>
    Only if the FX "sum a list of {amount_cents,currency} into a target currency" logic is duplicated ≥3×, extract a small sumToCurrency(items, target, fxProvider, asOf) helper and reuse. Otherwise skip (no commit). Do NOT change behavior — tests must stay green.
  </action>
  <verify>
    <automated>cd /home/claude/budget && bun test packages/budgeting/test/overview/ 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - if a helper was extracted, all overview tests still pass; if skipped, note "no refactor needed" in SUMMARY
  </acceptance_criteria>
  <done>Refactor applied or consciously skipped; tests green.</done>
</task>

</tasks>

<verification>
- bun test packages/budgeting/test/overview/ green (unit math)
- bun test apps/api/test/routes/overview-cards.test.ts green (real DB)
- bun run typecheck exits 0
</verification>

<success_criteria>

- 5 cards correct in default_currency (D-07/08/09/10/11); archived excluded from current (D-06)
- computeBudgetWealthNow authored once, reused by 11-06 + 11-07
- cushion real-months + total via get-cushion-summary (no new cushion math, D-08)
- RED→GREEN→(REFACTOR) commits present; route serves string cents (T-11-05 isolation)
  </success_criteria>

<output>
After completion, create `.planning/phases/11-budget-overview/11-03-SUMMARY.md` with RED/GREEN/REFACTOR notes + commits.
</output>
