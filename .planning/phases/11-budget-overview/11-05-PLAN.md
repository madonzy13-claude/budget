---
phase: 11-budget-overview
plan: 05
type: tdd
wave: 1
depends_on: []
autonomous: true
requirements: [SC5, SC6, D-10, D-06]
files_modified:
  - packages/budgeting/src/application/get-overview-overspent.ts
  - packages/budgeting/src/adapters/persistence/overview-repo.ts
  - packages/budgeting/test/overview/get-overview-overspent.test.ts
  - apps/api/src/routes/overview-overspent.ts
  - apps/api/src/boot.ts
  - apps/api/test/routes/overview-overspent.test.ts
must_haves:
  truths:
    - "Overspent (range) per category = Σ over months in range of max(0, spent − active_limit − reserve_used), the after-reserves formula matching the Spendings grid exactly (D-10)"
    - "The section returns the range total overspent + an overspent-by-category bar (descending), in default_currency (D-10, D-11)"
    - "active_limit per month = cushion_amount in CUSHION-mode months (via budget_mode_history) else normal_amount, from the SCD-2 category_limits row active that month (D-10)"
    - "Reserves-by-category bar reuses get-reserves-summary rows[].reserveCents — the raw category_reserve_balance view is NEVER queried directly (it changed across 6 migrations; latest 0030)"
    - "Archived categories contribute overspent for past months where they had activity, but not after their archive month (D-06)"
    - "Range inputs Zod-validated; aggregation uses the confirmed-only ledger index (T-11-04)"
  artifacts:
    - path: "packages/budgeting/src/application/get-overview-overspent.ts"
      provides: "Overspent section service (range total + by-category) + reserves-by-category passthrough"
      contains: "overspent_by_category"
    - path: "apps/api/src/routes/overview-overspent.ts"
      provides: "GET /budgets/:id/overview/overspent-reserves?from&to"
      contains: "overview/overspent"
  key_links:
    - from: "packages/budgeting/src/application/get-overview-overspent.ts"
      to: "packages/budgeting/src/application/get-reserves-summary.ts"
      via: "reserves-by-category reuses the reserves-summary service (not the raw view)"
      pattern: "getReservesSummary"
threat_model:
  - id: T-11-04
    stride: Denial of Service
    surface: range overspent aggregation over wide ranges
    mitigation: "monthly-bucket aggregation on transactions_budget_cat_confirmed_idx; span cap on custom ranges; section collapsed by default (D-21) so it fires only on open."
---

<objective>
Build the Overspent + Reserves section service (D-10/D-06): the range overspent total + overspent-by-category bar using the exact after-reserves formula from the Spendings grid, plus the reserves-by-category bar (reusing get-reserves-summary). All in default_currency.

Purpose: TDD fits — the after-reserves overspent formula (`max(0, spent − active_limit − reserve_used)` summed across months) must match the grid bit-for-bit, and archived-in-history (D-06) is a classic off-by-one-month trap. Reserves reuse means almost no new reserve math — the risk is the overspent aggregation, so test it first.
Output: `get-overview-overspent.ts` + an `overview-repo` overspent rollup + the `overview/overspent-reserves` route + integration test, green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-budget-overview/11-CONTEXT.md
@.planning/phases/11-budget-overview/11-RESEARCH.md

INTERFACES — exact analog signatures (11-RESEARCH); use directly:

EXISTING after-reserves overspent formula (get-spendings-summary.ts, D-10): per category per month overspentCents = max(0, spentCents − activeBudgetCents − reserveUsedCents) where activeBudgetCents = cushion vs normal by month mode, reserveUsedCents = reserve drawn that month. Range overspent per category = Σ over the months in range. This MUST match the grid.

EXISTING reserves-by-category source (get-reserves-summary.ts): getReservesSummary({tenantId,budgetId}) → rows:{categoryId,name,colorKey,reserveCents,usedCents,...}[], totals:{...,budgetCurrency}. Use rows[].reserveCents for the bar (already default_ccy). DO NOT query budgeting.category_reserve_balance directly (view changed 0013→0030).

REUSE overview-repo from 11-04: monthlySpendByCategory + monthlyActiveLimit. ADD monthlyReserveUsedByCategory(budgetId,from,to): {category_id,month,reserve_used_cents:bigint}[] (reserve drawn per category per month) so the overspent formula has the reserve_used term per month. categoryActiveWindows for D-06.

FX: FxProvider.rateAsOf → default_ccy.

DTO (route; \*\_cents strings):
OverviewOverspentDTO = {
currency:string,
overspent_total_cents:string,
overspent_by_category:{ category_id:string, name:string, overspent_cents:string }[], // desc, > 0 only
reserves_by_category:{ category_id:string, name:string, reserve_cents:string }[] }

Tx + DI + route: withTenantTxRead; boot.ts closure DI; X-Budget-ID; Zod range; bigint→string at route.

Test infra: bun:test (service) + real Postgres (route). BDD names.
</context>

<feature>
  <name>Overspent + Reserves section service</name>
  <files>get-overview-overspent.ts, overview-repo.ts (+ test), overview-overspent.ts route</files>
  <behavior>
    Cases:
    - category A: month1 spent 250 limit 200 reserve_used 0 → over 50; month2 spent 180 limit 200 → over 0; month3 spent 240 limit 200 reserve_used 20 → over 20. Range total for A = 70; by-category A=70.
    - category B never overspends → excluded from by-category (>0 only).
    - reserves_by_category mirrors get-reserves-summary rows[].reserveCents.
    - archived: category archived after month2 → contributes months1-2 only (D-06); not month3.
    - currency = default_ccy.
  </behavior>
  <implementation>service sums monthlySpend − monthlyActiveLimit − monthlyReserveUsed (floored at 0) per category over range; sorts desc; reserves passthrough from getReservesSummary.</implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1 — RED: failing tests for get-overview-overspent</name>
  <read_first>
    - packages/budgeting/test/overview/ (the 11-03/11-04 harness with fake repos)
    - packages/budgeting/src/application/get-spendings-summary.ts (the overspent formula to match) + get-reserves-summary.ts (reserves rows)
    - 11-RESEARCH.md (D-10/D-06) + this plan's <feature> cases
  </read_first>
  <action>
    Create packages/budgeting/test/overview/get-overview-overspent.test.ts: fake overview-repo (monthlySpendByCategory, monthlyActiveLimit, monthlyReserveUsedByCategory, categoryActiveWindows), fake getReservesSummary, fake fxProvider. Assert overspent_total, overspent_by_category (desc, >0 only), reserves_by_category passthrough, archived-in-history. Import from not-yet-existing module → RED.
  </action>
  <verify>
    <automated>cd /home/claude/budget && bun test packages/budgeting/test/overview/get-overview-overspent.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - test file exists; references getOverviewOverspent
    - covers after-reserves formula, >0-only filter, archived-in-history
    - test run FAILS (RED)
  </acceptance_criteria>
  <done>Failing tests committed (test(11-05): overspent + reserves section).</done>
</task>

<task type="auto">
  <name>Task 2 — GREEN: implement overspent rollup + service</name>
  <read_first>
    - packages/budgeting/src/adapters/persistence/overview-repo.ts (from 11-04 — add monthlyReserveUsedByCategory)
    - packages/budgeting/src/application/get-reserves-summary.ts (reserves rows reuse)
    - reserve replay/cells source the grid uses for reserve_used per month (follow get-spendings-summary's reserve term)
  </read_first>
  <action>
    1. overview-repo.ts — add monthlyReserveUsedByCategory(budgetId,from,to): {category_id,month,reserve_used_cents:bigint}[] consistent with the grid's reserveUsed term (per-month reserve drawn). Reuse the existing reserve replay/positions source rather than re-deriving.
    2. get-overview-overspent.ts — getOverviewOverspent(deps:{overviewRepo, reservesSummary, fxProvider})({tenantId,budgetId,from,to,now}) → Result<OverviewOverspentDTO>: per category per month over = max(0n, spent − activeLimit − reserveUsed), FX→default_ccy, Σ over range; build by-category desc (>0), total; reserves_by_category from reservesSummary rows[].reserveCents. Respect categoryActiveWindows (D-06).
    Run tests — MUST pass.
  </action>
  <verify>
    <automated>cd /home/claude/budget && bun test packages/budgeting/test/overview/get-overview-overspent.test.ts 2>&1 | tail -12 && bun run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - get-overview-overspent.ts uses max(0, spent − active_limit − reserve_used) (grep the floor + reserve term)
    - reserves_by_category sourced from getReservesSummary (no direct category_reserve_balance query in this plan's files)
    - unit tests pass; typecheck exits 0
  </acceptance_criteria>
  <done>Overspent rollup + service implemented; unit tests green (feat(11-05): implement overspent+reserves).</done>
</task>

<task type="auto">
  <name>Task 3: route GET /budgets/:id/overview/overspent-reserves + boot wiring + real-DB integration test</name>
  <read_first>
    - apps/api/src/routes/overview-planned.ts (sibling route + Zod range from 11-04)
    - apps/api/src/boot.ts (closure DI)
    - apps/api/test/routes/ (real-Postgres harness; seed overspend + reserve usage)
  </read_first>
  <action>
    1. overview-overspent.ts route — GET; Zod range; call deps.budgeting.getOverviewOverspent; stringify cents; c.json.
    2. boot.ts — wire getOverviewOverspent({ overviewRepo, reservesSummary: getReservesSummary(...), fxProvider }) + register route.
    3. apps/api/test/routes/overview-overspent.test.ts — real Postgres: seed a budget with a category that overspends in 2 of 3 months (with a reserve draw in one), a never-overspending category, and reserve balances; GET 3-month range; assert overspent_total, by-category desc/>0, reserves_by_category. NO DB mocking.
  </action>
  <verify>
    <automated>cd /home/claude/budget && bun test apps/api/test/routes/overview-overspent.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - route Zod-validates range; wired in boot.ts; registered
    - integration test passes (after-reserves overspent + reserves bar) against real Postgres
  </acceptance_criteria>
  <done>Route + DI + integration test green; overspent-reserves endpoint serves the section DTO.</done>
</task>

</tasks>

<verification>
- bun test packages/budgeting/test/overview/get-overview-overspent.test.ts green
- bun test apps/api/test/routes/overview-overspent.test.ts green (real DB)
- bun run typecheck exits 0
</verification>

<success_criteria>

- After-reserves overspent matching the grid (D-10); range total + by-category bar; default_ccy
- Reserves-by-category via get-reserves-summary (no raw view); archived in-history (D-06)
- Range Zod-validated; confirmed-only index used (T-11-04)
- RED→GREEN commits present
  </success_criteria>

<output>
After completion, create `.planning/phases/11-budget-overview/11-05-SUMMARY.md` with RED/GREEN notes + commits.
</output>
