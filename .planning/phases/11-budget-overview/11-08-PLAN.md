---
phase: 11-budget-overview
plan: 08
type: execute
wave: 2
depends_on: ["11-03"]
autonomous: true
requirements: [SC1, SC2]
files_modified:
  - apps/web/src/lib/bdp-tabs.ts
  - apps/web/src/components/budgeting/budget-detail.tsx
  - apps/web/src/components/budgeting/overview/overview-tab.tsx
  - apps/web/src/components/budgeting/overview/overview-cards.tsx
  - apps/web/src/hooks/use-overview-cards.ts
  - apps/web/src/hooks/use-prefetch-budget-tabs.ts
  - apps/web/messages/en.json
  - apps/web/test/components/budgeting/overview/overview-cards.test.tsx
  - apps/web/e2e/features/overview.feature
must_haves:
  truths:
    - "'overview' is the FIRST entry in TAB_ORDER and isBdpTab accepts it; the server page validates it like every other tab (SC1)"
    - "Selecting the overview pill uses the SAME pushState/popstate/no-RSC carousel mechanism as the other tabs (a new case in the TabPane switch) — zero per-nav RSC (SC1)"
    - "The overview tab renders without horizontal scroll at 375px (SC1)"
    - "Five summary cards render, each in the budget default_currency, from GET /budgets/:id/overview/cards via a persisted React-Query hook (SC2): available-to-spend, capitalization, this-month overspent, cushion (real months + total), available reserves"
    - "use-prefetch-budget-tabs warms the overview cards query in the PRIORITY tier so the tab is warm before tap (D-05); section queries stay lazy"
    - "A golden E2E scenario (overview.feature, @overview) renders the tab and asserts the five cards + no h-scroll at 375px"
  artifacts:
    - path: "apps/web/src/lib/bdp-tabs.ts"
      provides: "TAB_ORDER with overview first"
      contains: "overview"
    - path: "apps/web/src/components/budgeting/overview/overview-cards.tsx"
      provides: "5 summary cards (default_ccy)"
      contains: "capitalization"
    - path: "apps/web/src/hooks/use-overview-cards.ts"
      provides: "persisted RQ hook for the cards endpoint"
      contains: "overview"
  key_links:
    - from: "apps/web/src/hooks/use-overview-cards.ts"
      to: "apps/api/src/routes/overview-cards.ts (11-03)"
      via: "clientApiFetch GET /budgets/:id/overview/cards"
      pattern: "overview/cards"
    - from: "apps/web/src/components/budgeting/budget-detail.tsx"
      to: "apps/web/src/components/budgeting/overview/overview-tab.tsx"
      via: "TabPane switch case 'overview'"
      pattern: "OverviewTab"
threat_model:
  - id: T-11-05
    stride: Elevation of Privilege / IDOR
    surface: client cards fetch
    mitigation: "Hook sends X-Budget-ID; server route enforces membership + RLS (11-03). No client-trusted tenant scoping."
---

<objective>
Wire the new `overview` pill as the first BDP tab and render the five summary cards in `default_currency` (SC1, SC2). Prepend `"overview"` to `TAB_ORDER`, add the carousel case, add the persisted RQ cards hook + prefetch warming, build the cards component, add EN strings, and stub the golden E2E scenario.

Purpose: This is the tab frame + cards slice. It reuses the existing pushState carousel (zero RSC) and persisted React-Query SWR (D-03) verbatim — the only new surface is the overview tab component + cards hook. Sections come in 11-09; this plan ships a usable Overview (cards visible, sections as collapsed placeholders wired in 11-09).
Output: overview pill first, 5 cards rendering from the cards endpoint, prefetch warm, golden E2E stub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/ui-brand.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-budget-overview/11-CONTEXT.md
@.planning/phases/11-budget-overview/11-RESEARCH.md
@.planning/phases/11-budget-overview/11-UI-SPEC.md
@DESIGN.md

INTERFACES — exact analog signatures (11-RESEARCH Frontend frame); use directly:

EXISTING tab frame (apps/web/src/lib/bdp-tabs.ts): TAB_ORDER = ["wallets","spendings","reserves","settings"] as const; isBdpTab(s). Prepend "overview" → ["overview","wallets","spendings","reserves","settings"]. bdp-tabs.ts is a NON-client lib (server page calls isBdpTab) — do not add "use client".

EXISTING carousel (apps/web/src/components/budgeting/budget-detail.tsx ~L43-70 TabPane switch + L133-162 pushState/popstate + L187-216 AnimatePresence). Add case "overview": return <OverviewTab budgetId={budgetId} />. The slide direction uses TAB_ORDER index — overview being index 0 makes it slide correctly automatically.

EXISTING server page (apps/web/src/app/[locale]/(app)/budgets/[id]/[[...tab]]/page.tsx): validates via isBdpTab + seeds initialTab — no change needed beyond TAB_ORDER (it imports isBdpTab). Keep the existing bare-/budgets/[id] redirect target unless DESIGN says otherwise. // ponytail: don't change the redirect unless asked.

EXISTING persisted RQ hook pattern (apps/web/src/hooks/use-reserves-summary.ts): useQuery({ queryKey:["budget",budgetId,"overview","cards"], queryFn: async () => { const res = await clientApiFetch(`/budgets/${budgetId}/overview/cards`, { headers:{ "X-Budget-ID": budgetId } }); if(!res.ok) throw new Error(...); return res.json() as Promise<OverviewCardsDTO>; }, networkMode:"online" }). Persisted client + invalidation already app-wide (D-03).

EXISTING prefetch (apps/web/src/hooks/use-prefetch-budget-tabs.ts): add ["budget",budgetId,"overview","cards"] to the PRIORITY tier (warm before tap, D-05). Section queries (planned/overspent/wealth) are NOT prefetched (collapsed by default).

EXISTING card frame (apps/web/src/components/budgeting/placeholder-chart.tsx): rounded-[var(--radius-xl)] bg-[var(--surface-card-dark)] border-y border-[var(--hairline-dark)]. Cards use the same tokens; grid must be width-flexible (no fixed px) for 375px (SC1).

OverviewCardsDTO (from 11-03): { default_currency, available_to_spend_cents, capitalization_cents, investment_value_cents, available_reserves_cents, cushion:{enabled,real_months,total_cents}, overspent:{count,currency,top:[{category_id,name,over_amount_cents}]} }. Money formatting: reuse the app's existing cents→display formatter (the same one wallets/spendings use; find it in lib) with default_currency.

i18n (apps/web/messages/en.json): add bdp.tab.overview.{label,title} + bdp.tab.overview.cards.{availableToSpend,capitalization,overspent,cushion,availableReserves,realMonths} following the bdp.tab.\* nesting. (PL/UK added in 11-10.)

E2E (apps/web/e2e/features/\*.feature + Page Objects, playwright-bdd, fresh-user Background, @tag — see e2e/features/bdp-tab-frame.feature): create overview.feature with a @overview golden scenario.
</context>

<tasks>

<task type="auto">
  <name>Task 1: prepend "overview" to TAB_ORDER + carousel case + EN strings</name>
  <read_first>
    - apps/web/src/lib/bdp-tabs.ts (TAB_ORDER + isBdpTab + BdpTab type)
    - apps/web/src/components/budgeting/budget-detail.tsx (TabPane switch + slide variants + pill list source)
    - apps/web/messages/en.json (bdp.tab.* nesting)
  </read_first>
  <action>
    1. bdp-tabs.ts — TAB_ORDER = ["overview","wallets","spendings","reserves","settings"] as const (overview FIRST). BdpTab union + isBdpTab derive from it automatically.
    2. budget-detail.tsx — import OverviewTab; add `case "overview": return <OverviewTab budgetId={budgetId} />;` to the TabPane switch. If the pill list is rendered from TAB_ORDER, no further change; if pills are hard-coded, add the overview pill first.
    3. en.json — add bdp.tab.overview.{label:"Overview",title:"Overview"} + the cards.* keys.
  </action>
  <verify>
    <automated>cd /home/claude/budget && head -8 apps/web/src/lib/bdp-tabs.ts | grep -q '"overview"' && grep -q 'case "overview"' apps/web/src/components/budgeting/budget-detail.tsx && node -e "const m=require('./apps/web/messages/en.json'); if(!m.bdp.tab.overview) process.exit(1)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - TAB_ORDER first element is "overview"
    - budget-detail.tsx has a case "overview" returning OverviewTab
    - en.json contains bdp.tab.overview
  </acceptance_criteria>
  <done>Overview pill is first; carousel routes to OverviewTab; EN strings present.</done>
</task>

<task type="auto">
  <name>Task 2: use-overview-cards hook + prefetch warming</name>
  <read_first>
    - apps/web/src/hooks/use-reserves-summary.ts (persisted RQ hook pattern: queryKey, clientApiFetch, X-Budget-ID, networkMode)
    - apps/web/src/hooks/use-prefetch-budget-tabs.ts (priority tier — where to add the cards key)
    - apps/api/src/routes/overview-cards.ts (the endpoint shape from 11-03)
  </read_first>
  <action>
    1. use-overview-cards.ts — export useOverviewCards(budgetId): useQuery over ["budget",budgetId,"overview","cards"] calling clientApiFetch("/budgets/${budgetId}/overview/cards",{headers:{"X-Budget-ID":budgetId}}), returning OverviewCardsDTO; networkMode "online". Export the OverviewCardsDTO type (or import from a shared contracts module).
    2. use-prefetch-budget-tabs.ts — add the same query (key + queryFn) to the PRIORITY prefetch tier so overview cards are warm before the pill is tapped (D-05). Do NOT prefetch section endpoints.
  </action>
  <verify>
    <automated>cd /home/claude/budget && grep -q "overview/cards" apps/web/src/hooks/use-overview-cards.ts && grep -q '"overview"' apps/web/src/hooks/use-prefetch-budget-tabs.ts && cd apps/web && bunx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - use-overview-cards.ts fetches /overview/cards with X-Budget-ID and a ["budget",budgetId,"overview","cards"] key
    - prefetch hook includes the overview cards key in the priority tier
    - typecheck passes
  </acceptance_criteria>
  <done>Cards hook + priority prefetch wired.</done>
</task>

<task type="auto">
  <name>Task 3: OverviewTab + OverviewCards components (5 cards, default_ccy, 375px-safe)</name>
  <read_first>
    - apps/web/src/components/budgeting/placeholder-chart.tsx (card frame tokens)
    - apps/web/src/components/budgeting/reserves-table-client.tsx (an existing tab client component: loading/empty/data states + money formatter import)
    - DESIGN.md (card layout, accent, typography)
    - apps/web/src/hooks/use-overview-cards.ts (the hook from Task 2)
  </read_first>
  <action>
    1. overview-cards.tsx ("use client") — OverviewCards({budgetId}): use useOverviewCards; render a responsive grid (e.g. grid-cols-2 at narrow, no fixed px → 375px-safe) of 5 cards using the placeholder-chart frame tokens: available-to-spend, capitalization (with investments sub-figure optional), this-month overspent (count + top list per Discretion), cushion (real_months formatted to 1 decimal + total_cents), available reserves. Format cents with the app money formatter + default_currency. Loading skeleton + error states.
    2. overview-tab.tsx ("use client") — OverviewTab({budgetId}): render <OverviewCards budgetId/> then a slot for the four sections (placeholders/collapsed; 11-09 fills them). Ensure the container has no min-width forcing h-scroll at 375px.
  </action>
  <verify>
    <automated>cd /home/claude/budget && head -1 apps/web/src/components/budgeting/overview/overview-cards.tsx | grep -q '"use client"' && grep -q "capitalization" apps/web/src/components/budgeting/overview/overview-cards.tsx && cd apps/web && bunx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - overview-cards.tsx is a client component rendering 5 cards keyed to the DTO fields (available_to_spend, capitalization, overspent, cushion, available_reserves)
    - amounts formatted with default_currency
    - typecheck passes
  </acceptance_criteria>
  <done>Overview tab renders 5 cards from the cards endpoint; sections slot present.</done>
</task>

<task type="auto">
  <name>Task 4: component test + golden E2E scenario stub</name>
  <read_first>
    - apps/web/test/components/budgeting/task-banner-row.test.tsx (Vitest+RTL pattern; next-intl + RQ mocking)
    - apps/web/e2e/features/bdp-tab-frame.feature + its page object (Gherkin + fresh-user Background + @tag + pill navigation steps)
  </read_first>
  <action>
    1. overview-cards.test.tsx — Vitest+RTL: mock useOverviewCards to return a fixture DTO; assert the 5 card labels + formatted default_ccy amounts render; assert cushion real_months shows one decimal. BDD names.
    2. overview.feature — @overview Feature "Budget Overview tab" with Background "signed in as a fresh user"; Scenario "overview is the first pill and shows five cards": open BDP, assert the first pill is "Overview", click it, assert URL ends "/overview", assert the five cards are visible, and assert no horizontal scroll at 375px (page object checks document.scrollingElement.scrollWidth <= clientWidth at iPhone-SE viewport). Reuse the existing pill page object; add overview steps/selectors. (Full section scenarios added in 11-10.)
  </action>
  <verify>
    <automated>cd /home/claude/budget/apps/web && bun run test -- overview-cards.test 2>&1 | tail -12 && test -f e2e/features/overview.feature && grep -q "@overview" e2e/features/overview.feature && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - overview-cards.test.tsx passes (5 cards + default_ccy + cushion decimal)
    - overview.feature exists, tagged @overview, with the first-pill + five-cards + no-h-scroll golden scenario
  </acceptance_criteria>
  <done>Component test green; golden E2E scenario stubbed (run in 11-10 suite).</done>
</task>

</tasks>

<verification>
- cd apps/web && bunx tsc --noEmit exits 0
- overview-cards.test.tsx passes
- overview.feature @overview scenario present (executed with the full suite in 11-10)
- Manual/E2E: overview pill first; 5 cards render; no h-scroll at 375px
</verification>

<success_criteria>

- overview first pill, same pushState/prefetch/no-RSC mechanism (SC1); 375px no h-scroll (SC1)
- 5 cards in default_currency from the cards endpoint via persisted RQ (SC2); prefetch warm (D-05)
- Golden E2E scenario stubbed @overview
  </success_criteria>

<output>
After completion, create `.planning/phases/11-budget-overview/11-08-SUMMARY.md`
</output>
