---
phase: 11-budget-overview
plan: 02
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [SC9, D-19]
files_modified:
  - apps/web/package.json
  - apps/web/src/components/budgeting/charts/chart-theme.ts
  - apps/web/src/components/budgeting/charts/area-chart.tsx
  - apps/web/src/components/budgeting/charts/line-chart.tsx
  - apps/web/src/components/budgeting/charts/bar-chart.tsx
  - apps/web/src/components/budgeting/charts/pie-chart.tsx
  - apps/web/test/components/budgeting/charts/charts.test.tsx
must_haves:
  truths:
    - "recharts@3.9.0 (latest stable) is a dependency of apps/web (D-19) — verified by rebuilding the web image, not just the lockfile (Docker ignores bun.lock)"
    - "Four themed, responsive chart wrappers exist (Area/Line/Bar/Pie), each a 'use client' component wrapping recharts ResponsiveContainer (D-19)"
    - "Wrappers theme to DESIGN.md tokens (Binance dark canvas, single yellow accent, hairline borders) via props/CSS vars — colors are NOT hard-coded hex except the Phase-9 UI_TYPE_COLOR pie map"
    - "Charts render at width=100% (no fixed pixel width) so they fit 375px without horizontal scroll (SC1 prerequisite)"
    - "The Pie wrapper supports tap(mobile)/hover(desktop) active-slice highlight via a controlled activeIndex + Tooltip defaultIndex + Sector shape (D-18)"
    - "Each wrapper renders in happy-dom under Vitest without throwing"
  artifacts:
    - path: "apps/web/src/components/budgeting/charts/pie-chart.tsx"
      provides: "Themed Pie with tap/hover active-slice highlight"
      contains: "activeIndex"
    - path: "apps/web/src/components/budgeting/charts/chart-theme.ts"
      provides: "DESIGN-token chart palette + shared axis/grid/tooltip styles"
      contains: "surface-card-dark"
  key_links:
    - from: "apps/web/src/components/budgeting/charts/area-chart.tsx"
      to: "apps/web/src/components/budgeting/overview/ (11-09)"
      via: "section charts import these wrappers (never raw recharts)"
      pattern: "charts/area-chart"
threat_model: []
---

<objective>
Introduce the charting foundation: add `recharts@3.9.0` (latest stable, the CLAUDE.md-named pick) to `apps/web` and build four thin, themed, responsive wrappers — Area, Line, Bar, Pie — each a `"use client"` component over `ResponsiveContainer`, styled to DESIGN.md (Binance dark, single yellow accent, IBM Plex/Inter). The Pie supports the D-18 tap/hover active-slice reveal.

Purpose: All Phase-11 section charts (11-09) consume these wrappers, never raw recharts — one place owns the theme + responsive + accessibility contract, so 11-09 is pure data-wiring. recharts' `ResponsiveContainer` uses `ResizeObserver`, so the wrappers MUST be client components (RSC would crash). No data logic here — wrappers take already-shaped data + color props.
Output: recharts installed; 4 reusable chart primitives + a shared theme module; Vitest render smoke tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-budget-overview/11-CONTEXT.md
@.planning/phases/11-budget-overview/11-RESEARCH.md
@DESIGN.md

INTERFACES — exact analog signatures; use directly, no codebase exploration needed:

recharts version (11-RESEARCH Standard Stack): "recharts": "3.9.0" — latest stable, React-19 peer OK. NOT installed yet (only placeholder-chart.tsx). Add to apps/web/package.json dependencies. ⚠ Docker ignores bun.lock and runs `bun install --no-save` (project memory) — to verify the dep actually lands, rebuild the web image (`docker compose build web && make restart-web`) and confirm the served bundle, not just the lockfile.

recharts × Next 16 (11-RESEARCH "recharts × Next 16 App Router"):

- Every wrapper file starts with "use client" (ResponsiveContainer → ResizeObserver, client-only).
- Shape: <ResponsiveContainer width="100%" height={height}>{<AreaChart data={data}>…</AreaChart>}</ResponsiveContainer>
- v3 accessibilityLayer is ON by default — do not add it.
- Pie active highlight (D-18): const [activeIndex,setActiveIndex]=useState<number|undefined>(); <Pie data shape={({isActive,...p})=> <Sector {...p} fill={isActive?accent:p.fill}/>} onClick={(\_,i)=>setActiveIndex(i)} /> + <Tooltip defaultIndex={activeIndex}/>. Hover is native; tap sets activeIndex (mobile).

DESIGN tokens (DESIGN.md / global.css CSS vars — already in the app): --surface-card-dark, --hairline-dark, --radius-xl; the single yellow accent var (confirm exact name in DESIGN.md, e.g. --accent or --brand-yellow). Reuse, never hard-code hex.

EXISTING card frame to nest charts in (apps/web/src/components/budgeting/placeholder-chart.tsx): rounded-[var(--radius-xl)] bg-[var(--surface-card-dark)] border-y border-[var(--hairline-dark)], minHeight 240px. The chart wrappers render INSIDE this frame (11-09 composes them); wrappers themselves are frame-agnostic (just the ResponsiveContainer + chart).

Phase-9 type pie colors (apps/web/src/lib/investment-icons.ts:42 — UI_TYPE_COLOR map + holdingIcon()): import for the investments pie (D-18). The Pie wrapper accepts a colorFor(name)=>string prop so 11-09 can pass UI_TYPE_COLOR-based mapping; the wrapper does not import the map itself.

Vitest component test pattern (apps/web/test/components/budgeting/\*.test.tsx): import { describe,it,expect } from "vitest"; render from @testing-library/react; happy-dom env. recharts needs a sized container in happy-dom — ResponsiveContainer may report 0×0; pass an explicit width/height in tests OR assert the wrapper mounts without throwing (smoke), not pixel output.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add recharts@3.9.0 to apps/web</name>
  <read_first>
    - apps/web/package.json (dependencies block — add recharts next to other UI deps)
    - 11-RESEARCH.md Standard Stack (version 3.9.0, React-19 peer)
  </read_first>
  <action>
    Add "recharts": "3.9.0" to apps/web/package.json dependencies (exact pin, not ^ — project wants reproducible builds and Docker resolves fresh). Run `cd apps/web && bun install` to update the local lockfile. Because Docker runs `bun install --no-save` ignoring bun.lock, the authoritative verification is a web image rebuild (next task chain / 11-08), but install locally now so typecheck + Vitest resolve the import.
  </action>
  <verify>
    <automated>cd /home/claude/budget && grep -q '"recharts": "3.9.0"' apps/web/package.json && cd apps/web && bun pm ls 2>/dev/null | grep -qi recharts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/package.json dependencies contains "recharts": "3.9.0"
    - recharts resolves locally (bun import works)
  </acceptance_criteria>
  <done>recharts 3.9.0 added and resolvable locally.</done>
</task>

<task type="auto">
  <name>Task 2: chart-theme.ts shared palette + 4 themed wrappers (Area/Line/Bar/Pie)</name>
  <read_first>
    - DESIGN.md (canvas/accent/typography tokens; exact accent CSS var name)
    - apps/web/src/components/budgeting/placeholder-chart.tsx (frame tokens to align with)
    - apps/web/src/lib/investment-icons.ts:42 (UI_TYPE_COLOR — confirm the colorFor prop contract for the pie)
    - 11-RESEARCH.md "recharts × Next 16 App Router" (use-client + ResponsiveContainer + Pie highlight)
  </read_first>
  <action>
    1. apps/web/src/components/budgeting/charts/chart-theme.ts — export a CHART_THEME object: axis tick color/hairline (--hairline-dark), grid stroke, tooltip bg (--surface-card-dark), the single accent (read the DESIGN var) as the default series fill/stroke, font-family Inter/IBM Plex. No "use client" (plain TS).
    2. area-chart.tsx ("use client") — export function OverviewAreaChart({ data, xKey, series, height=240, formatY }: { data:any[]; xKey:string; series:{key:string;label:string;color?:string}[]; height?:number; formatY?:(n:number)=>string }) → <ResponsiveContainer width="100%" height={height}><AreaChart data={data}><CartesianGrid/><XAxis dataKey={xKey}/><YAxis tickFormatter={formatY}/><Tooltip/>{series.map(Area)}</AreaChart></ResponsiveContainer>, themed via CHART_THEME, default color = accent.
    3. line-chart.tsx ("use client") — OverviewLineChart, same prop shape, <LineChart>/<Line type="monotone">. Used for the daily-cumulative + planned-vs-real timelines.
    4. bar-chart.tsx ("use client") — OverviewBarChart({ data, xKey, series, height, layout }) supporting vertical layout (Y=category) for planned-avg-vs-real + overspent-by-category + reserves-by-category bars.
    5. pie-chart.tsx ("use client") — OverviewPieChart({ data, nameKey, valueKey, colorFor, height }) with controlled activeIndex (useState), Pie shape={({isActive,...p})=><Sector {...p} fill={isActive?accent:colorFor(p.name)}/>}, onClick sets activeIndex (mobile tap), <Tooltip defaultIndex={activeIndex}/> showing share % + name (D-18). colorFor passed by caller (UI_TYPE_COLOR-based).
    All wrappers: width="100%" (no fixed px width → 375px-safe). Keep them dumb — no fetching, no money math; data + formatters come from props.
  </action>
  <verify>
    <automated>cd /home/claude/budget && for f in area-chart line-chart bar-chart pie-chart; do head -1 apps/web/src/components/budgeting/charts/$f.tsx | grep -q '"use client"' || { echo "MISSING use client: $f"; exit 1; }; done && grep -q "ResponsiveContainer" apps/web/src/components/budgeting/charts/area-chart.tsx && grep -q "activeIndex" apps/web/src/components/budgeting/charts/pie-chart.tsx && grep -q "surface-card-dark" apps/web/src/components/budgeting/charts/chart-theme.ts && cd apps/web && bunx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - all four wrapper files begin with "use client"
    - each wrapper uses ResponsiveContainer with width="100%" (no hard-coded pixel width)
    - pie-chart.tsx contains activeIndex (controlled highlight) and a colorFor prop
    - chart-theme.ts references DESIGN tokens (surface-card-dark / hairline-dark / accent) and hard-codes no chart-series hex
    - typecheck passes
  </acceptance_criteria>
  <done>Four themed responsive client wrappers + shared theme; DESIGN-tokened; Pie supports D-18 highlight.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest render smoke tests for the wrappers</name>
  <read_first>
    - apps/web/test/components/budgeting/task-banner-row.test.tsx (Vitest+RTL+happy-dom pattern, next-intl mock if needed)
    - the 4 wrapper files from Task 2
  </read_first>
  <action>
    Create apps/web/test/components/budgeting/charts/charts.test.tsx: render each wrapper with a tiny fixture dataset inside a fixed-size wrapper div (e.g. style width:400 height:240 so ResponsiveContainer has a box in happy-dom). Assert it mounts without throwing and renders an SVG (screen.container.querySelector('svg') truthy) — smoke level, NOT pixel geometry (recharts + happy-dom can't measure). For the Pie, fire a click on a slice and assert no throw (activeIndex path). describe('Overview charts') with one it() per wrapper, BDD names ("renders an area chart without throwing").
  </action>
  <verify>
    <automated>cd /home/claude/budget/apps/web && bun run test -- charts.test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - charts.test.tsx exists with one test per wrapper (4+) plus a Pie click test
    - all chart tests pass (exit 0)
  </acceptance_criteria>
  <done>Smoke tests green: all wrappers mount + render SVG; Pie click path runs.</done>
</task>

</tasks>

<verification>
- recharts 3.9.0 in apps/web/package.json and resolvable
- 4 client wrappers + theme module compile (tsc --noEmit)
- charts.test.tsx passes in Vitest/happy-dom
</verification>

<success_criteria>

- recharts (latest stable) added (D-19); verify via web image rebuild, not lockfile
- Area/Line/Bar/Pie wrappers: client, responsive (width 100%), DESIGN-themed, accessibility default
- Pie tap/hover active highlight ready (D-18)
- Wrappers are data-agnostic — 11-09 only wires data
  </success_criteria>

<output>
After completion, create `.planning/phases/11-budget-overview/11-02-SUMMARY.md`
</output>
