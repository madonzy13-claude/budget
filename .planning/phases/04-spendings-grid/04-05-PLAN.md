---
phase: 04-spendings-grid
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - tests/e2e/features/spendings/quick-entry.feature
  - tests/e2e/features/spendings/quick-entry-retry.feature
  - tests/e2e/features/spendings/drag-reorder.feature
  - tests/e2e/features/spendings/category-create.feature
  - tests/e2e/features/spendings/category-edit.feature
  - tests/e2e/features/spendings/month-nav.feature
  - tests/e2e/features/spendings/past-month-edit.feature
  - tests/e2e/features/spendings/draft-confirm.feature
  - tests/e2e/features/spendings/draft-edit-promote.feature
  - tests/e2e/features/spendings/draft-dismiss.feature
  - tests/e2e/features/spendings/no-hover-reveal.feature
  - tests/e2e/features/spendings/category-cell-no-inline-edit.feature
  - tests/e2e/features/spendings/mobile-scroll.feature
  - tests/e2e/features/spendings/reserve-deduct.feature
  - tests/e2e/features/spendings/overflow-cascade.feature
  - tests/e2e/steps/spendings.steps.ts
  - tests/e2e/pages/SpendingsPage.ts
  - tests/e2e/features/spendings/placeholder.feature
  - apps/api/src/app.ts
autonomous: false
requirements:
  - GRID-01
  - GRID-02
  - GRID-03
  - GRID-05
  - GRID-06
  - GRID-07
  - GRID-08
  - GRID-09
  - GRID-10
  - GRID-11
  - GRID-13
  - GRID-14
  - RECR-03
  - RECR-04
  - RECR-05
  - RECR-06
  - RECR-07
  - RSCM-03
  - RSCM-04
must_haves:
  truths:
    - "13+ Gherkin scenarios cover every D-PH4-E1 minimum scenario list item (quick-entry, optimistic retry, drag-reorder, dashed-+ create, month-nav button + Cmd/Ctrl, draft confirm-unchanged, draft double-click-edit-promote, draft dismiss, past-month last-day-of-month, mobile horizontal-scroll, single-click-reveals-options on each row type, double-click-inline-edits amount on txn rows, double-click on category cells does NOT inline-edit, hover does NOT reveal options)"
    - "Two dedicated regression-guard scenarios: no-hover-reveal.feature + category-cell-no-inline-edit.feature"
    - "RSCM-03 timing assertion: quick-entry over budget triggers row 4 update visible within ~200ms (D-PH4-R4 + RSCM-03)"
    - "RSCM-04 overflow cascade scenario: spent > active_budget + reserve_available; assert row 4 = reserve_available, row 3 = remainder"
    - "Playwright base URL pulled from .env.local APP_URL (feedback_test_baseurl rule); fresh-user-per-scenario fixture reused"
    - "All scenarios green when run via `make test-e2e`"
    - "`make test && make ci-gate && make test-e2e` all green before checkpoint"
    - "impeccable sweep passes on grid + sliders (DESIGN.md visual contract — D-PH4-E5)"
    - "Legacy root mounts removed from apps/api/src/app.ts (app.route('/categories', …), app.route('/recurring-rules', …), and any other root-level Phase 2/3 mount that Plan 04-02 replaced with a /budgets/:budgetId-prefixed equivalent) — confirmed by grep + ci-gate still passing"
  artifacts:
    - path: "tests/e2e/features/spendings/quick-entry.feature"
      provides: "Golden-path quick-entry scenario"
      contains: "@phase4"
    - path: "tests/e2e/features/spendings/no-hover-reveal.feature"
      provides: "Regression guard for D-PH4-INT1"
      contains: "Hover"
    - path: "tests/e2e/features/spendings/category-cell-no-inline-edit.feature"
      provides: "Regression guard for D-PH4-INT4"
      contains: "double-click"
    - path: "tests/e2e/features/spendings/drag-reorder.feature"
      provides: "Drag persists to categories.sort_index"
      contains: "drag"
    - path: "tests/e2e/features/spendings/reserve-deduct.feature"
      provides: "RSCM-03 real-time reserve deduction"
      contains: "reserve"
    - path: "tests/e2e/features/spendings/overflow-cascade.feature"
      provides: "RSCM-04 overflow remainder shown as overspent"
      contains: "overflow"
    - path: "tests/e2e/steps/spendings.steps.ts"
      provides: "Full step bindings (replaces Plan 04-01 stub)"
      contains: "createBdd"
    - path: "apps/api/src/app.ts"
      provides: "Cleaned-up route table — only /budgets/:budgetId-prefixed mounts remain"
      contains: "/budgets/:budgetId"
  key_links:
    - from: "tests/e2e/features/spendings/*.feature"
      to: "tests/e2e/pages/SpendingsPage.ts"
      via: "Page Object getByTestId locators"
      pattern: "SpendingsPage"
    - from: "Makefile target test-e2e"
      to: ".env.local APP_URL"
      via: "PLAYWRIGHT_BASE_URL"
      pattern: "PLAYWRIGHT_BASE_URL"
    - from: "apps/api/src/app.ts (post-cleanup)"
      to: "apps/api/src/routes/{categories,recurring-rules,spendings-summary}.ts"
      via: "ONLY /budgets/:budgetId/{categories,recurring-rules,spendings-summary} mount paths"
      pattern: "app.route"
---

<objective>
Ship the production-grade E2E Gherkin coverage for Phase 4 + run the impeccable visual sweep + green every gate (make test, make ci-gate, make test-e2e) + clean up the legacy root mounts that Plan 04-02 left in place. This is the final phase gate before `/gsd-verify-work`.

Purpose: D-PH4-E1 mandates 13 minimum scenarios. Two are explicit regression-guards (no-hover-reveal, category-cell-no-inline-edit) that would otherwise be near-impossible to retro-fit after release. The impeccable sweep verifies the DESIGN.md visual contract end-to-end on the new surface. The legacy mount cleanup closes the phasing loop opened by Plan 04-02 — once Plan 04-04 rewires the client to the new prefix, the duplicate root mounts can come out safely.

Output: 15 `.feature` files (13 minimum + RSCM-03 timing + RSCM-04 cascade scenarios), expanded `spendings.steps.ts` step bindings, impeccable-sweep-clean grid + sliders, legacy root mounts removed from app.ts, all gates green, ready for user manual UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-spendings-grid/04-CONTEXT.md
@.planning/phases/04-spendings-grid/04-RESEARCH.md
@.planning/phases/04-spendings-grid/04-PATTERNS.md
@.planning/phases/04-spendings-grid/04-UI-SPEC.md
@.planning/phases/04-spendings-grid/04-VALIDATION.md
@.planning/phases/04-spendings-grid/04-01-SUMMARY.md
@.planning/phases/04-spendings-grid/04-02-SUMMARY.md
@.planning/phases/04-spendings-grid/04-03-SUMMARY.md
@.planning/phases/04-spendings-grid/04-04-SUMMARY.md
@tests/e2e/features/budget/create-transaction.feature
@tests/e2e/steps/budget.steps.ts
@tests/e2e/pages/TransactionsPage.ts
@tests/e2e/pages/SpendingsPage.ts
@tests/e2e/fixtures/freshUser.ts
@apps/web/playwright.config.ts
@apps/api/src/app.ts
@CLAUDE.md

<interfaces>
<!-- Steps used across all 15 features (additive to Plan 04-01 stub) -->

Given:

- "I am signed in as a fresh user with workspace {string}" (existing — reuse from budget.steps.ts)
- "the budget {string} has a category {string} with planned {string} {string}"
- "the budget {string} has a category {string} with planned {string} {string} and cushion {string} {string}"
- "the budget {string} has cushion_mode enabled"
- "the category {string} has a reserve balance of {string} {string}" (seed via DB direct insert into past-month transactions to create reserve)
- "the budget {string} has a recurring rule {string} for category {string} of {string} {string} due this month" (seed via DB insert of pending draft row)
- "I am viewing month {string}" (sets ?month query param)

When:

- "I open the Spendings tab on a budget {string}" (already stubbed in Plan 04-01)
- "I type {string} into the quick-entry input for category {string}"
- "I press Enter in the quick-entry input"
- "I single-click the transaction row {string}"
- "I double-click the amount cell on transaction {string}"
- "I single-click the draft row {string}"
- "I click the Confirm action on draft {string}"
- "I click the Dismiss action on draft {string} and confirm in the dialog"
- "I single-click the column header {string}"
- "I click the pen action on column header {string}"
- "I drag column {string} before column {string}"
- "I click the Add category column"
- "I press {string}" (keyboard shortcut, e.g. "Cmd+ArrowLeft")
- "I click the next month button"
- "I click the previous month button"
- "I move the pointer over {string} without clicking"

Then:

- "I see a transaction row {string} in the {string} column"
- "I see the column {string} header overspent shows {string}"
- "I see the column {string} header reserves used shows {string}"
- "I see the column {string} header balance shows {string}"
- "I see the column order is {string}" (comma-separated category names)
- "I do not see floating action chips on {string}"
- "I do not see the inline-edit input on {string}"
- "the quick-entry input is in retry state"
- "the draft row {string} is no longer visible"
- "the transaction {string} is no longer visible"
- "the recurring rule {string} is still active" (DB query helper)
- "I see the month label {string}"
- "the URL has search param month equal to {string}"
- "I see the spendings grid container" (already stubbed)
- "I see the dashed `+` column at the rightmost position"

Mobile viewport:

- Tag `@mobile` on feature; Playwright project applies `viewport: { width: 390, height: 844 }` (iPhone 14)
  </interfaces>
  </context>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                                                                                                                  |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| E2E seed → DB                   | Test seeds via `pg` direct insert (dynamic import to keep pg out of web bundle); must stay inside fresh-user tenant_id                                       |
| Playwright base URL             | from .env.local APP_URL, not hardcoded                                                                                                                       |
| legacy mount cleanup → live API | Removing root mounts has zero behavioural effect once Plan 04-04 has migrated the client to /budgets/:budgetId prefix; verified by ci-gate + full test sweep |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                             | Disposition | Mitigation Plan                                                                                                                                                                                                                     |
| ---------- | ---------------------- | --------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-05-01 | Information Disclosure | Test fixtures leak credentials in CI logs                             | mitigate    | Use existing freshUser fixture (Phase 3 proven); no plaintext credentials in feature files                                                                                                                                          |
| T-04-05-02 | Tampering              | Test seed writes to production DB                                     | mitigate    | DATABASE_URL_APP env points to docker test DB; CI gate verifies before make test-e2e                                                                                                                                                |
| T-04-05-03 | Denial of Service      | Flaky timing assertions on RSCM-03 200ms target                       | mitigate    | Use `expect(...).toHaveText(...)` with default Playwright timeout (5s) rather than asserting strict 200ms; RSCM-03 "real-time" is interpreted as "within UI test timeout" — exact ms target requires perf test, deferred to Phase 8 |
| T-04-05-04 | Tampering              | Hover-reveal regression test relies on framework-level pointer events | accept      | Playwright's `page.hover()` faithfully fires pointermove without click; matches user behavior                                                                                                                                       |
| T-04-05-05 | Tampering              | Legacy mount removal breaks an undiscovered consumer                  | mitigate    | Grep first for ALL importers / callers of the legacy root paths; tenant-leak ci-gate + make test + make test-e2e MUST all stay green after removal; commit cleanup as separate diff so it can be reverted in isolation              |

</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Expand spendings.steps.ts step bindings + extend SpendingsPage page object</name>
  <read_first>
    - tests/e2e/steps/budget.steps.ts (full step binding pattern, including DB seed helper via dynamic pg import — STATE.md Phase 3 decision "Dynamic import('pg') in E2E task-seeding step keeps pg out of the web bundle")
    - tests/e2e/pages/SpendingsPage.ts (Plan 04-01 stub — extend with new locators)
    - tests/e2e/fixtures/freshUser.ts (do NOT modify)
    - apps/web/playwright.config.ts (verify base URL + project + project-mobile if needed)
    - .planning/STATE.md (Phase 3 decision "DATABASE_URL_APP rewriter @db: -> @localhost: lets E2E steps run from host AND inside compose net")
  </read_first>
  <files>
    tests/e2e/steps/spendings.steps.ts (EXTEND — replace Plan 04-01 stub)
    tests/e2e/pages/SpendingsPage.ts (EXTEND — add locators for revealed actions, draft rows, retry icon, etc.)
    apps/web/playwright.config.ts (MODIFY only to add `@mobile` project if absent)
  </files>
  <action>
1. **Extend `SpendingsPage.ts`** — add locators for every primitive Plan 04-03/04-04 components emit. Final locator surface:
   ```typescript
   gridContainer()                              // [data-testid="spendings-grid"]
   monthLabel(), monthPrevBtn(), monthNextBtn() // [data-testid="month-navigator-{label,prev,next}"]
   columnHeader(name)                           // [data-testid="column-header-{name.toLowerCase()}"]
   columnHeaderRow(name, row)                   // row in {planned, cushion, overspent, reservesUsed, balance}
   quickEntryInput(name)                        // [data-testid="quick-entry-{name.toLowerCase()}"]
   quickEntryRetryIcon(name)                    // child of quickEntryInput when most recent txn is unsent
   addCategoryColumn()                          // [data-testid="add-category-column"]
   transactionRow(amount, categoryName)         // scopes inside category column
   draftRow(ruleName)                           // [data-testid="draft-row-{ruleName.toLowerCase()}"]
   revealedActionPen(rowTestId)                 // child of row when revealed
   revealedActionTrash(rowTestId)
   revealedActionConfirm(draftTestId)
   revealedActionDismiss(draftTestId)
   inlineEditInput(rowTestId)                   // child of row when in inline-edit state
   transactionSlider()                          // [role=dialog] inside the right-side Sheet — distinguish from categorySlider via title text
   categorySlider()                             // same
   dragGrip(name)                               // child of columnHeader(name) — used for page.dragAndDrop()
   ```
   For mobile viewport, add `@mobile` project in playwright.config.ts (e.g. `{ name: 'mobile', use: { viewport: { width: 390, height: 844 }, ...baseUse } }`) if not already present.

2. **Extend `spendings.steps.ts`** — implement every step from `<interfaces>` block. Use dynamic `pg` import for DB seed (Phase 3 idiom):

   ```typescript
   import { createBdd } from "playwright-bdd";
   import { expect } from "@playwright/test";
   import { test } from "../fixtures/index.js";
   import { SpendingsPage } from "../pages/SpendingsPage.js";
   import { createFreshUser } from "../fixtures/freshUser.js";

   const { Given, When, Then } = createBdd(test);

   async function withPg<T>(fn: (client: any) => Promise<T>): Promise<T> {
     const { Client } = await import("pg");
     const raw = process.env.DATABASE_URL_APP!;
     const url = raw.replace("@db:", "@localhost:");
     const c = new Client({ connectionString: url });
     await c.connect();
     try {
       return await fn(c);
     } finally {
       await c.end();
     }
   }

   Given(
     "the budget {string} has a category {string} with planned {string} {string}",
     async ({}, budgetName, catName, plannedStr, currency) => {
       await withPg(async (c) => {
         // SELECT budget by name in current tenant, INSERT into budgeting.categories + category_limits
       });
     },
   );

   // ... implement each step from <interfaces>.
   ```

3. **Rebuild API+web image if necessary** (no app code changed; might skip).

4. **Smoke-run the placeholder feature** to confirm step bindings register correctly:
   ```bash
   PLAYWRIGHT_BASE_URL="$(grep ^APP_URL .env.local | cut -d= -f2)" make test-e2e -- spendings/placeholder
   ```
   MUST pass.
   </action>
   <verify>
   <automated>cd /home/claude/budget && grep -c "Given\|When\|Then" tests/e2e/steps/spendings.steps.ts | awk '$1 >= 25 {print "OK"; exit} {print "FAIL: only " $1 " bindings"; exit 1}'</automated>
   </verify>
   <done> - SpendingsPage page object has locators for every primitive (≥15 locators) - spendings.steps.ts has ≥25 Given/When/Then bindings covering the interface list - Placeholder feature still green after expansion
   </done>
   </task>

<task type="auto" tdd="false">
  <name>Task 2: Write 15 .feature files covering every D-PH4-E1 scenario + RSCM-03/04 + regression guards</name>
  <read_first>
    - .planning/phases/04-spendings-grid/04-CONTEXT.md §Engineering discipline D-PH4-E1 (minimum scenario list)
    - .planning/phases/04-spendings-grid/04-UI-SPEC.md §Regression-guard rules
    - tests/e2e/features/budget/create-transaction.feature (Gherkin format reference)
    - tests/e2e/steps/spendings.steps.ts (step library from Task 1)
  </read_first>
  <files>
    tests/e2e/features/spendings/quick-entry.feature
    tests/e2e/features/spendings/quick-entry-retry.feature
    tests/e2e/features/spendings/drag-reorder.feature
    tests/e2e/features/spendings/category-create.feature
    tests/e2e/features/spendings/category-edit.feature
    tests/e2e/features/spendings/month-nav.feature
    tests/e2e/features/spendings/past-month-edit.feature
    tests/e2e/features/spendings/draft-confirm.feature
    tests/e2e/features/spendings/draft-edit-promote.feature
    tests/e2e/features/spendings/draft-dismiss.feature
    tests/e2e/features/spendings/no-hover-reveal.feature
    tests/e2e/features/spendings/category-cell-no-inline-edit.feature
    tests/e2e/features/spendings/mobile-scroll.feature
    tests/e2e/features/spendings/reserve-deduct.feature
    tests/e2e/features/spendings/overflow-cascade.feature
    tests/e2e/features/spendings/placeholder.feature (DELETE after replacement files exist)
  </files>
  <action>
For each feature below, use `@phase4` tag and apply `@mobile` ONLY to the mobile-scroll feature (the rest run desktop by default).

1. **`quick-entry.feature`** (GRID-05 happy path):

   ```gherkin
   @phase4
   Feature: Quick-entry expense from Spendings grid

     Scenario: User adds a PLN expense by typing into the Groceries column
       Given I am signed in as a fresh user with workspace "Family"
       And the budget "Family" has a category "Groceries" with planned "200.00" "PLN"
       When I open the Spendings tab on a budget "Family"
       And I type "12.50" into the quick-entry input for category "Groceries"
       And I press Enter in the quick-entry input
       Then I see a transaction row "12.50" in the "Groceries" column
       And I see the column "Groceries" header balance shows "187.50"
   ```

2. **`quick-entry-retry.feature`** (D-PH4-Q1 unsent + retry):
   Scenario: simulate API failure (network-route off via Playwright). Assert quick-entry input shows retry icon. Click retry; succeed. Final txn visible.

3. **`drag-reorder.feature`** (GRID-09):
   Scenario: 3 columns A, B, C; drag A after C; reload page; assert order is B, C, A. Use `page.dragAndDrop(SpendingsPage.dragGrip("A"), SpendingsPage.dragGrip("C"))`.

4. **`category-create.feature`** (GRID-08 + D-PH4-S4):
   Scenario: click `+` column → CategorySlider opens → fill name "Food" + planned 100 + cushion 150 → Save → new "Food" column appears at rightmost (left of `+`).

5. **`category-edit.feature`** (GRID-03, GRID-04):
   Scenario: single-click column header "Groceries" → pen revealed → click pen → CategorySlider opens with planned/cushion values prefilled → change planned to 250 → Save → header row 2 shows 250.00.

6. **`month-nav.feature`** (GRID-10 softened to Cmd/Ctrl):
   Scenarios:
   - "click next month: URL ?month increments"
   - "Cmd+ArrowLeft navigates prev month"
   - "Plain ArrowLeft does NOT navigate"
   - "Cmd+ArrowLeft INSIDE quick-entry input does NOT navigate" (regression: keyboard guard in MonthNavigator)

7. **`past-month-edit.feature`** (GRID-11, D-PH4-Q5):
   Scenario: navigate to previous month; quick-entry helper shows "Saves as YYYY-MM-{lastDay}"; type 5.96 + Enter; assert txn created with that date.

8. **`draft-confirm.feature`** (RECR-03 + RECR-04):
   Scenario: seed a pending draft for "Rent"; open spendings; assert draft row has dashed yellow border (`getComputedStyle().borderLeft.includes('dashed')`); single-click draft row reveals Confirm/Pen/Dismiss; click Confirm; draft transitions to normal txn styling (no dashed border).

9. **`draft-edit-promote.feature`** (RECR-05 + D-PH4-INT5 power-user shortcut):
   Scenario: seed pending draft; double-click amount cell; input mode active; type new amount "75.00" + Enter; row promotes to real txn with the edited amount.

10. **`draft-dismiss.feature`** (RECR-06, D-PH4-R3):
    Scenario: seed pending draft; single-click; click Dismiss; dialog title interpolates rule name + month; click confirm; row disappears; DB query asserts recurring_rule.status = active.

11. **`no-hover-reveal.feature`** (D-PH4-INT1 regression-guard):

    ```gherkin
    @phase4
    Feature: Hover does not reveal action chips (D-PH4-INT1 regression-guard)

      Scenario: Pointermove over a transaction row leaves DOM in resting state
        Given I am signed in as a fresh user with workspace "Family"
        And the budget "Family" has a category "Groceries" with planned "200.00" "PLN"
        And the budget "Family" has a transaction "10.00" "PLN" in category "Groceries"
        When I open the Spendings tab on a budget "Family"
        And I move the pointer over the transaction row "10.00" without clicking
        Then I do not see floating action chips on the transaction row "10.00"

      Scenario: Pointermove over a draft row leaves DOM in resting state
        Given I am signed in as a fresh user with workspace "Family"
        And the budget "Family" has a recurring rule "Rent" for category "Groceries" of "50.00" "PLN" due this month
        When I open the Spendings tab on a budget "Family"
        And I move the pointer over the draft row "Rent" without clicking
        Then I do not see floating action chips on the draft row "Rent"

      Scenario: Pointermove over a column header leaves pen icon hidden
        Given I am signed in as a fresh user with workspace "Family"
        And the budget "Family" has a category "Groceries" with planned "200.00" "PLN"
        When I open the Spendings tab on a budget "Family"
        And I move the pointer over the column header "Groceries" without clicking
        Then I do not see the pen action on column header "Groceries"
    ```

12. **`category-cell-no-inline-edit.feature`** (D-PH4-INT4 regression-guard):
    Scenarios:
    - "double-click on category name cell does NOT enter inline-edit"
    - "double-click on planned value cell does NOT enter inline-edit"
    - "double-click on cushion value cell does NOT enter inline-edit"

13. **`mobile-scroll.feature`** (GRID-13, D-PH4-Q6) — tag `@mobile`:
    Scenario: seed 8 categories; viewport 390px; horizontal-scroll right; verify last column becomes visible; verify drag-reorder still works on touch.

14. **`reserve-deduct.feature`** (RSCM-03):
    Scenario: seed category with planned 100, reserve balance 50; current month already has 95 spent; type "20.00" + Enter; assert column header row 4 (reserves used) updates to "15.00"; row 3 (overspent) shows "0.00".

15. **`overflow-cascade.feature`** (RSCM-04):
    Scenario: seed category with planned 100, reserve balance 50; current month 90 spent; type "100.00"; assert row 4 = "50.00" (full reserve consumed), row 3 = "40.00" (remainder), row 5 (balance) = "-40.00".

16. **DELETE** `tests/e2e/features/spendings/placeholder.feature` — replaced by the 15 production-grade scenarios.

17. **Run** the full Phase 4 feature suite:
    `bash
    PLAYWRIGHT_BASE_URL="$(grep ^APP_URL .env.local | cut -d= -f2)" make test-e2e -- spendings
    `
    All MUST pass.
    </action>
    <verify>
    <automated>cd /home/claude/budget && ls tests/e2e/features/spendings/\*.feature | wc -l | awk '$1 >= 15 {print "OK: " $1 " feature files"; exit} {print "FAIL: only " $1 " features"; exit 1}'</automated>
    </verify>
    <done> - 15 production feature files exist - Placeholder feature deleted - All scenarios green when run via make test-e2e -- spendings - Two regression-guard features (no-hover-reveal, category-cell-no-inline-edit) explicitly cover D-PH4-INT1 + D-PH4-INT4
    </done>
    </task>

<task type="auto" tdd="false">
  <name>Task 3: Impeccable visual sweep on grid + sliders + full gates green</name>
  <read_first>
    - DESIGN.md (visual contract — yellow scarcity, Inter + IBM Plex Sans, 60/30/10 color split)
    - .planning/phases/04-spendings-grid/04-UI-SPEC.md (color reserved-for lists, spacing, typography)
    - apps/web/src/app/global.css (token definitions)
    - Existing impeccable sweep tooling — discover via `ls scripts/` or `grep impeccable Makefile`
  </read_first>
  <files>
    (No code edits — verification only; if visual issues found, fix in spendings-grid components or sliders)
  </files>
  <action>
1. **Run impeccable sweep** on the grid + sliders.

2. **Full test gates green**:

   ```bash
   make test
   make ci-gate
   cd apps/web && bun run test --run
   cd apps/web && bun run typecheck
   PLAYWRIGHT_BASE_URL="$(grep ^APP_URL .env.local | cut -d= -f2)" make test-e2e
   ```

3. **Domain coverage check**:

   ```bash
   bun test --coverage packages/budgeting
   ```

4. **dependency-cruiser sanity** (ENGR-02):

   ```bash
   bunx depcruise apps/api/src packages/budgeting/src --include-only "^(packages/budgeting/src/domain|packages/budgeting/src/application)" 2>&1 | tail -10
   ```

5. Commit the entire Plan 04-05 diff: `feat(04): e2e features + impeccable sweep + gates green`
   </action>
   <verify>
   <automated>cd /home/claude/budget && make test 2>&1 | tail -3 && make ci-gate 2>&1 | tail -3 && cd apps/web && bun run test --run 2>&1 | tail -3 && bun run typecheck 2>&1 | tail -3</automated>
   </verify>
   <done> - Impeccable sweep clean OR all visual deviations fixed - `make test` green - `make ci-gate` green - `bun run test --run` green - `bun run typecheck` clean - `make test-e2e` green incl. all 15 Phase 4 scenarios - dependency-cruiser confirms domain isolation - Domain coverage ≥80%
   </done>
   </task>

<task type="auto" tdd="false">
  <name>Task 4: Remove legacy root mounts in app.ts (cleanup deferred from Plan 04-02)</name>
  <read_first>
    - apps/api/src/app.ts (current state — should still contain both legacy root mounts AND new /budgets/:budgetId-prefixed mounts after Plan 04-04 lands)
    - .planning/phases/04-spendings-grid/04-02-SUMMARY.md (records which legacy mounts existed pre-rewire)
    - .planning/phases/04-spendings-grid/04-04-SUMMARY.md (confirms client now uses /budgets/:budgetId prefix exclusively)
    - apps/web/src/lib/budget-fetch.ts + budget-fetch.server.ts (verify NO client/server fetch hits a legacy root path)
    - scripts/ci/tenant-leak-gate.ts (or equivalent — confirm fixture list points at /budgets/:budgetId paths only)
  </read_first>
  <files>
    apps/api/src/app.ts (MODIFY — delete legacy root mount lines)
  </files>
  <action>
1. **Inventory the legacy root mounts** that exist alongside the new `/budgets/:budgetId/...` mounts in `apps/api/src/app.ts`:
   ```bash
   grep -nE "app\.route\(['\"]/(categories|recurring-rules|transactions|spendings-summary)" apps/api/src/app.ts
   ```
   Expected pre-cleanup state: both forms present —
   - `app.route("/categories", createCategoriesRoute(deps))` (legacy)
   - `app.route("/budgets/:budgetId/categories", createCategoriesRoute(deps))` (Plan 04-02 added)
   - similar for recurring-rules and any other Phase 2/3 root mount that Plan 04-02 duplicated under `/budgets/:budgetId/`

2. **Cross-check NO consumer still hits the legacy paths**:

   ```bash
   # Client side (web)
   grep -rnE "(clientApiFetch|serverApiFetch)\([^,]*['\"]/(categories|recurring-rules|transactions|spendings-summary)" apps/web/src/ 2>/dev/null
   # E2E (must use /budgets/:budgetId/... after Plan 04-04)
   grep -rnE "['\"]/(categories|recurring-rules|spendings-summary)" tests/e2e/ 2>/dev/null
   ```

   Both grep results should be EMPTY (no consumer at legacy paths). If anything appears: do NOT remove the legacy mount; instead rewire that consumer first, then return here.

3. **Remove the legacy mount lines** in `apps/api/src/app.ts`:
   - DELETE `app.route("/categories", ...)`
   - DELETE `app.route("/recurring-rules", ...)`
   - DELETE any other root-level mount that Plan 04-02 replaced with a `/budgets/:budgetId/...` equivalent
   - DO NOT touch `app.route("/budgets/:budgetId/categories", ...)` etc.
   - DO NOT touch any unrelated root mount (auth, health, fx, etc. — those have no `/budgets/:budgetId/` counterpart and are NOT in scope)

4. **Sanity-check the mount table after edits**:

   ```bash
   grep -cE "app\.route\(['\"]/(categories|recurring-rules|spendings-summary)['\"]" apps/api/src/app.ts
   ```

   MUST return 0 (no remaining bare-root mount for those 3 paths). Conversely:

   ```bash
   grep -cE "app\.route\(['\"]/budgets/:budgetId/(categories|recurring-rules|spendings-summary)" apps/api/src/app.ts
   ```

   MUST return 3 (one mount per route under the budget-id prefix).

5. **Run the full gate sweep AGAIN** post-cleanup:

   ```bash
   make test           # backend integration must stay green
   make ci-gate        # tenant-leak gate must stay green
   PLAYWRIGHT_BASE_URL="$(grep ^APP_URL .env.local | cut -d= -f2)" make test-e2e   # E2E must stay green
   cd apps/web && bun run typecheck   # client typecheck unchanged
   ```

   ALL must exit 0. If any test fails, the legacy mount removal exposed a hidden consumer — restore the legacy mount, locate the consumer, fix it, then retry.

6. **Rebuild API image**:

   ```bash
   docker compose build api && make restart-api
   ```

7. **Commit cleanup as a SEPARATE diff** (per threat T-04-05-05 — easier to revert in isolation):
   `chore(04): remove legacy root mounts after client migrated to /budgets/:budgetId prefix`
   </action>
   <verify>
   <automated>cd /home/claude/budget && [ "$(grep -cE 'app\.route\(.\\/(categories|recurring-rules|spendings-summary).' apps/api/src/app.ts)" = "0" ] && [ "$(grep -cE 'app\.route\(.\\/budgets/:budgetId/(categories|recurring-rules|spendings-summary)' apps/api/src/app.ts)" = "3" ] && echo OK</automated>
   </verify>
   <done> - Legacy root mounts for `/categories`, `/recurring-rules`, `/spendings-summary` removed from apps/api/src/app.ts - Only `/budgets/:budgetId/...` prefixed mounts remain for those 3 routes - `make test`, `make ci-gate`, `make test-e2e`, `bun run typecheck` all still green - API image rebuilt - Cleanup committed as separate diff for revertability
   </done>
   </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: User manual UAT on the Spendings grid (sanity sweep before phase verified)</name>
  <what-built>
    - Full Spendings tab: column-per-category grid, 5-row headers, drag-reorder, quick-entry, sliders, recurring drafts, month nav
    - 3-4 new backend routes (sort-order, spendings-summary, drafts/dismiss, [drafts/confirm])
    - 7 client primitives + 2 sliders + RSC shell
    - 15 E2E feature files green
    - Legacy root mounts removed; only /budgets/:budgetId/... remain
    - All gates green; impeccable sweep clean; domain coverage ≥80%
  </what-built>
  <how-to-verify>
1. Open `http://<APP_URL from .env.local>/en/budgets/<existing-budget-id>/spendings` in a browser
2. Visual sanity (DESIGN.md):
   - Binance dark canvas (no light surfaces leak)
   - Yellow only in: sticky Spendings pill, Confirm button (if drafts exist), dashed `+` column border on focus, dashed left border of draft rows
   - Inter for labels/body; IBM Plex Sans for all currency numbers
3. Interaction sanity:
   - Single-click a transaction row → [Pen][Trash] reveal
   - Double-click amount on a txn row → input mode → type new amount + Enter → saves
   - Drag a column header by the grip icon → column reorders; refresh page; order persists
   - Click `‹` and `›` buttons → month changes; URL updates with `?month=YYYY-MM`
   - Cmd/Ctrl + ←/→ outside an input → month changes; same key inside a quick-entry input → cursor moves (no nav)
   - Click pen icon on column header → CategorySlider opens; edit planned + Save → header row 2 updates
   - Click dashed `+` column → CategorySlider opens in create mode
   - On mobile viewport: grid scrolls horizontally; drag-reorder still works
4. Real-time reserve deduction (RSCM-03):
   - Create a category with planned 100; previous month spent 80 (creates reserve 20)
   - Current month spend 110: header row 4 should show 10 (reserve drawn); row 3 (overspent) shows 0
   - Spend another 50: row 4 shows 20 (full reserve), row 3 shows 40 (overflow remainder), row 5 (balance) shows -50 in red
5. Drafts (RECR-03..07):
   - Create a recurring rule due this month
   - Verify draft appears as highlighted row with dashed yellow left border
   - Click Confirm → row transitions to normal txn styling
   - Repeat: dismiss → row disappears, rule keeps running
   - Power-user shortcut: double-click amount + Enter on a draft → promotes with edited amount in one keystroke

Approve if all 5 sections pass without surprising behavior. Document any visual/UX issues for follow-up.
</how-to-verify>
<resume-signal>Type "approved" to mark Phase 4 complete or describe specific issues</resume-signal>
</task>

</tasks>

<verification>
- 15 .feature files cover D-PH4-E1 minimum + RSCM-03 + RSCM-04 + 2 regression-guard scenarios
- Step bindings complete; SpendingsPage page object has all locators
- `make test`, `make ci-gate`, `make test-e2e`, `bun run test --run`, `bun run typecheck` all green
- Impeccable visual sweep clean
- Legacy root mounts cleaned up; only /budgets/:budgetId/{categories,recurring-rules,spendings-summary} remain
- Domain coverage ≥80%
- User manual UAT signed off
</verification>

<success_criteria>

- `ls tests/e2e/features/spendings/*.feature | wc -l` ≥ 15
- `make test-e2e -- spendings` exits 0
- `make ci-gate` exits 0
- `make test` exits 0
- `cd apps/web && bun run test --run` exits 0
- `cd apps/web && bun run typecheck` exits 0
- `[ ! -e tests/e2e/features/spendings/placeholder.feature ]`
- `grep -cE "app\.route\(.\\/(categories|recurring-rules|spendings-summary).$" apps/api/src/app.ts` returns 0 (no bare-root mounts for those 3)
- Manual UAT approved
  </success_criteria>

<output>
After completion, create `.planning/phases/04-spendings-grid/04-05-SUMMARY.md` with:
- Scenarios shipped (count + D-PH4-E1 coverage matrix)
- Step bindings count
- Gate results (test, ci-gate, test-e2e, typecheck)
- Legacy mount cleanup: confirmed removed (yes/no) + grep proof
- Impeccable sweep findings (cleanups made if any)
- Domain coverage percentage
- User UAT outcome
</output>
</context>
</content>
</invoke>
